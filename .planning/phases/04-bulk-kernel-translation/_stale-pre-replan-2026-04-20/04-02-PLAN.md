---
phase: 04-bulk-kernel-translation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/kernel/lda/*.rs
  - src/kernel/lda/mod.rs
  - src/eval/dispatch.rs
  - verify/tests/lda_oracle.rs
autonomous: true
requirements: [KERN-03, KERN-06, KERN-07, KERN-08, KERN-09, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06, VERIFY-07]
must_haves:
  truths:
    - "All 42 LDA functional kernels are translated from maple2c C to Rust #[cube] functions"
    - "Each LDA functional has a launch wrapper file matching the launch_lda_x.rs pattern"
    - "Each LDA functional is wired into dispatch_lda via a two-level dispatch"
    - "Every LDA functional passes oracle verification at all applicable derivative orders and both spin modes"
    - "The special lda_xc_tih _vxc functional is included in the LDA batch"
  artifacts:
    - path: "src/kernel/lda/lda_c_vwn.rs"
      provides: "Representative LDA kernel translation"
      contains: "#[cube(launch_unchecked)]"
    - path: "src/kernel/lda/launch_lda_c_vwn.rs"
      provides: "Representative LDA launch wrapper"
      contains: "pub fn launch_lda_c_vwn"
    - path: "src/eval/dispatch.rs"
      provides: "LDA dispatch routing for all 42 functionals"
      contains: "dispatch_lda"
  key_links:
    - from: "src/eval/dispatch.rs"
      to: "src/kernel/lda/launch_*.rs"
      via: "match on functional ID then (order, spin)"
      pattern: "launch_lda_c_vwn"
    - from: "verify/tests/lda_oracle.rs"
      to: "src/eval/dispatch.rs"
      via: "dispatch_lda called for each functional"
      pattern: "dispatch_lda"
---

<objective>
Translate all 42 LDA maple2c kernel files (41 remaining + 1 special _vxc) to Rust #[cube] functions, wire each into the dispatch layer, and verify every functional against the C libxc oracle at all derivative orders and spin modes.

Purpose: Complete the LDA family translation, establishing the batch workflow pattern for the larger GGA and MGGA families.
Output: 42 kernel files, 42 launch wrappers, extended dispatch, passing oracle tests.
</objective>

<execution_context>
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-bulk-kernel-translation/04-CONTEXT.md
@.planning/phases/04-bulk-kernel-translation/04-RESEARCH.md
@.planning/phases/04-bulk-kernel-translation/04-01-SUMMARY.md

<interfaces>
From src/kernel/lda/lda_x.rs (CANONICAL PATTERN -- follow exactly):
```rust
use cubecl::prelude::*;
use crate::math::constants::{M_CBRT2, M_CBRT3, M_CBRTPI};
use crate::math::piecewise::piecewise3;
use crate::math::powers::pow_1_3;

#[cube(launch_unchecked)]
pub fn lda_x_exc_unpol(
    rho: &Array<f64>,
    zk: &mut Array<f64>,
    alpha: f64,
    dens_threshold: f64,
    zeta_threshold: f64,
) {
    let ip = ABSOLUTE_POS;
    if ip < zk.len() {
        // ... exact maple2c variable translation ...
        zk[ip] += tzk0;
    }
}
```

From src/kernel/lda/launch_lda_x.rs (LAUNCH WRAPPER PATTERN):
```rust
pub struct BufArg<'a> {
    pub handle: &'a cubecl::server::Handle,
    pub len: usize,
}
pub fn launch_lda_x_exc_unpol(
    client: &ComputeClient<CpuRuntime>, cube_count: CubeCount, cube_dim: CubeDim,
    rho: &BufArg<'_>, zk: &BufArg<'_>,
    alpha: f64, dens_threshold: f64, zeta_threshold: f64,
) -> Result<(), Box<dyn std::error::Error>>;
```

From src/eval/dispatch.rs (DISPATCH PATTERN):
```rust
pub fn dispatch_lda(input: &LdaInput, order: DerivativeOrder, output: &mut LdaOutput,
    alpha: f64, thresholds: &Thresholds) -> Result<(), LibxcRsError>;
// Currently hardcoded to LDA_X only. Must be extended to route by functional ID.
```

C macro to Rust mapping (from libxc-master/src/util.h):
- POW_1_3(x) -> pow_1_3(x)
- POW_2_3(x) -> pow_2_3(x)
- POW_4_3(x) -> pow_4_3(x)
- POW_5_3(x) -> pow_5_3(x)
- POW_3_2(x) -> pow_3_2(x)
- POW_2(x) -> pow_2(x) or x*x
- my_piecewise3(c, x1, x2) -> piecewise3(c, x1, x2)
- my_piecewise5(c1, x1, c2, x2, x3) -> piecewise5(c1, x1, c2, x2, x3)
- M_CBRT3, M_CBRT2, M_CBRTPI -> crate::math::constants::*
- Numeric literals: 0.2e1 -> 2.0, 0.3e1 -> 3.0, 0.XYZeN -> 0.XYZ * 10^N
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor dispatch_lda to accept functional ID and translate all 42 LDA kernels</name>
  <files>src/eval/dispatch.rs, src/kernel/lda/mod.rs, src/kernel/lda/*.rs (42 kernel files + 42 launch files)</files>
  <read_first>
    - src/kernel/lda/lda_x.rs (canonical translation pattern -- follow EXACTLY)
    - src/kernel/lda/launch_lda_x.rs (canonical launch wrapper pattern)
    - src/eval/dispatch.rs (current dispatch_lda implementation)
    - src/kernel/lda/mod.rs (current module declarations)
    - libxc-master/src/maple2c/lda_exc/ (all 42 C source files to translate)
    - libxc-master/src/maple2c/lda_vxc/lda_xc_tih.c (special _vxc file per D-04)
    - libxc-master/src/util.h (macro definitions for translation reference)
  </read_first>
  <action>
**This is a large batch task. Work through functionals one at a time per D-08. For each functional:**

1. Read the C source file from `libxc-master/src/maple2c/lda_exc/{name}.c`
2. Create `src/kernel/lda/{name}.rs` following the exact lda_x.rs pattern:
   - Preserve exact maple2c variable names (t2, t3, ..., tzk0, tvrho0, etc.)
   - Preserve floating-point operation order (per D-01, KERN-06)
   - Translate numeric literals: `0.2e1` -> `2.0`, `0.XYZeN` -> `0.XYZ * 10^N`
   - Map C macros to Rust functions (POW_1_3 -> pow_1_3, etc.)
   - Use `#[cube(launch_unchecked)]` with ABSOLUTE_POS and bounds check
   - Output accumulation via `+=` (per KERN-08)
   - Density threshold check: `rho[ip] / 2.0 <= dens_threshold` (per KERN-07)
   - Create N functions: up to 5 derivative orders x 2 spin modes = 10 functions (per KERN-09)
   - For functionals with `params->` in C source: add each parameter as a scalar `f64` argument
   - For lda_c_pk09: only 3 derivative orders (no LXC)
3. Create `src/kernel/lda/launch_{name}.rs` following the exact launch_lda_x.rs pattern:
   - Import BufArg from `super::launch_lda_x` (shared type)
   - One safe wrapper function per kernel function
   - All unsafe confined to this module
4. Add module declarations to `src/kernel/lda/mod.rs`

**Dispatch refactoring (two-level dispatch per Research Pitfall 7):**

Modify `dispatch_lda` to accept a `func_id: u32` parameter (or equivalent functional identifier). Change the match from hardcoded LDA_X to:
- First level: match on functional ID to select the launch module
- Second level (within each launch module): match on (order, spin) to select the kernel

The simplest approach: add a `func_id` parameter to `dispatch_lda` and use a match statement that routes to each functional's launch module. The LDA_X case remains but becomes one arm among 42.

**Special file:** `lda_xc_tih.c` from `lda_vxc/` is a _vxc-only functional (per D-04). Translate it the same way but it may only have vxc-level functions (no higher derivatives). Check the C source for which derivative levels are implemented.

**Complete list of 42 LDA kernel files to translate (lda_x.c already done, translate remaining 41 + 1 special):**
lda_c_1d_csc, lda_c_1d_loos, lda_c_2d_amgb, lda_c_2d_prm, lda_c_chachiyo, lda_c_chachiyo_mod, lda_c_gk72, lda_c_gombas, lda_c_hl, lda_c_lp96, lda_c_ml1, lda_c_pk09, lda_c_pmgb06, lda_c_pw, lda_c_pw_erf, lda_c_pz, lda_c_rc04, lda_c_rpa, lda_c_vwn, lda_c_vwn_1, lda_c_vwn_2, lda_c_vwn_3, lda_c_vwn_4, lda_c_vwn_rpa, lda_c_w20, lda_c_wigner, lda_k_gds08_worker, lda_k_tf, lda_k_zlp, lda_x_1d_exponential, lda_x_1d_soft, lda_x_2d, lda_x_erf, lda_x_rel, lda_x_sloc, lda_x_yukawa, lda_xc_1d_ehwlrg, lda_xc_ksdt, lda_xc_teter93, lda_xc_zlp, hyb_lda_xc_bn05, lda_xc_tih (from lda_vxc/)

**Per D-08:** After translating each functional, verify it compiles. After completing all 42, run the full LDA oracle test suite.
  </action>
  <verify>
    <automated>cd /home/chemtech/workspace/libxc_rs && cargo build -p libxc_rs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/kernel/lda/ contains 42 kernel .rs files (lda_x.rs + 41 new + lda_xc_tih.rs)
    - src/kernel/lda/ contains 42 launch_*.rs files
    - src/kernel/lda/mod.rs declares all 42 kernel modules and all 42 launch modules
    - src/eval/dispatch.rs dispatch_lda function has a func_id parameter or equivalent routing mechanism
    - src/eval/dispatch.rs contains match arms for all 42 LDA functional IDs
    - Every kernel .rs file contains `#[cube(launch_unchecked)]`
    - Every kernel .rs file contains `zk[ip] +=` (output accumulation per KERN-08)
    - `cargo build -p libxc_rs` exits 0
  </acceptance_criteria>
  <done>All 42 LDA kernel files translated, launch wrappers created, dispatch wired, project compiles</done>
</task>

<task type="auto">
  <name>Task 2: Run full LDA oracle verification and fix any failures</name>
  <files>verify/tests/lda_oracle.rs</files>
  <read_first>
    - verify/tests/lda_oracle.rs (batch test file from Plan 01)
    - src/eval/dispatch.rs (to understand how to call Rust kernels for comparison)
  </read_first>
  <action>
Update `verify/tests/lda_oracle.rs` to perform actual Rust-vs-oracle comparison for all 42 LDA functionals:

1. For each functional in LDA_FUNCTIONALS:
   a. Call `oracle_lda_all(tc.id, spin, &rho)` to get C libxc reference values
   b. Call `dispatch_lda` with the same inputs to get Rust kernel values
   c. Compare each derivative level at the appropriate tolerance tier (per D-09, D-10):
      - zk (exc): relative error <= 1e-12 (VERIFY-03)
      - vrho (vxc): relative error <= 1e-10 (VERIFY-04)
      - v2rho2 (fxc): relative error <= 1e-8 (VERIFY-05)
      - v3rho3 (kxc): relative error <= 1e-6 (VERIFY-06)
      - v4rho4 (lxc): relative error <= 1e-4 (VERIFY-07)
   d. Test both unpolarized (spin=1) and polarized (spin=2) modes
   e. For functionals with max_order < 4 (e.g., lda_c_pk09 max_order=3), skip higher-order tests

2. Use multiple test densities including edge cases:
   - Low density: 0.001
   - Medium density: 0.1, 0.5, 1.0
   - High density: 5.0, 10.0
   - For polarized: asymmetric spin (0.7, 0.1) to test spin splitting

3. Helper function for relative error comparison:
```rust
fn relative_error(got: f64, expected: f64) -> f64 {
    if expected.abs() < 1e-30 { got.abs() } else { ((got - expected) / expected).abs() }
}
```

4. If any functional fails oracle verification, fix the translation before proceeding (per D-08). Common fixes:
   - Numeric literal mistranslation (check 0.XeN conversion)
   - Wrong power function (pow_1_3 vs pow_2_3)
   - Missing parameter argument
   - Incorrect array indexing in polarized mode
  </action>
  <verify>
    <automated>cd /home/chemtech/workspace/libxc_rs && cargo test -p libxc_rs-verify -- lda_oracle --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p libxc_rs-verify -- lda_oracle` exits 0
    - verify/tests/lda_oracle.rs contains relative_error comparison at each tolerance tier
    - verify/tests/lda_oracle.rs tests both spin=1 and spin=2 for each functional
    - All 42 LDA functionals pass exc tolerance <= 1e-12
    - All 42 LDA functionals pass vxc tolerance <= 1e-10
  </acceptance_criteria>
  <done>All 42 LDA functionals pass oracle verification through all applicable derivative orders and both spin modes at the required tolerance tiers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maple2c C -> Rust translation | Floating-point operation order must be preserved exactly; any reordering changes numerical results |
| kernel scalar params | Functional-specific parameters must match C defaults exactly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T (Tampering) | kernel translation | mitigate | Oracle verification at 10^-12 tolerance ensures translation fidelity |
| T-04-05 | T (Tampering) | numeric literals | mitigate | Systematic 0.XeN -> decimal conversion verified by oracle comparison |
| T-04-06 | D (Denial of Service) | dispatch match | accept | 42-arm match is manageable; compiler optimizes to jump table |
</threat_model>

<verification>
1. `cargo build -p libxc_rs` -- all LDA kernels compile
2. `cargo test -p libxc_rs-verify -- lda_oracle` -- all 42 functionals pass oracle verification
3. `cargo clippy -p libxc_rs` -- no new warnings
</verification>

<success_criteria>
- All 42 LDA kernel files translated from maple2c C to Rust #[cube] functions
- Each functional passes oracle verification at all applicable derivative orders and both spin modes
- Energy relative error <= 10^-12, VXC <= 10^-10, FXC <= 10^-8, KXC <= 10^-6, LXC <= 10^-4
- Dispatch routes by functional ID to correct kernel
</success_criteria>

<output>
After completion, create `.planning/phases/04-bulk-kernel-translation/04-02-SUMMARY.md`
</output>
