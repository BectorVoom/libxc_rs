---
phase: 04-bulk-kernel-translation
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - verify/tests/lda_oracle.rs
  - verify/tests/gga_oracle.rs
  - verify/tests/mgga_oracle.rs
  - src/eval/dispatch.rs
autonomous: false
requirements: [VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06, VERIFY-07]
must_haves:
  truths:
    - "All 265 kernel-translated functionals (42 LDA + 131 GGA + 92 MGGA) pass oracle verification in a single full test run"
    - "Every functional tested at all applicable derivative orders independently per D-09"
    - "Both spin modes (unpolarized and polarized) verified for every functional"
    - "Tolerance tiers confirmed: exc<=1e-12, vxc<=1e-10, fxc<=1e-8, kxc<=1e-6, lxc<=1e-4"
    - "No regressions introduced across families"
  artifacts:
    - path: "verify/tests/lda_oracle.rs"
      provides: "Complete LDA oracle verification with per-order tolerance checks"
      contains: "assert_arrays_match"
    - path: "verify/tests/gga_oracle.rs"
      provides: "Complete GGA oracle verification"
      contains: "assert_arrays_match"
    - path: "verify/tests/mgga_oracle.rs"
      provides: "Complete MGGA oracle verification"
      contains: "assert_arrays_match"
  key_links:
    - from: "verify/tests/*_oracle.rs"
      to: "src/eval/dispatch.rs"
      via: "dispatch_{lda,gga,mgga} called for Rust evaluation"
      pattern: "dispatch_"
    - from: "verify/tests/*_oracle.rs"
      to: "verify/src/lib.rs"
      via: "oracle_{lda,gga,mgga}_all called for C reference"
      pattern: "oracle_"
---

<objective>
Run comprehensive cross-family oracle verification for all 265 kernel-translated functionals, confirm all tolerance tiers are met, and produce a final verification report. This is the quality gate for the entire Phase 4.

Purpose: Prove that the complete bulk kernel translation is numerically correct at all derivative orders and spin modes before Phase 5 builds on top of it.
Output: Green test suite for all 265 functionals, verification checkpoint confirming phase success.
</objective>

<execution_context>
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-bulk-kernel-translation/04-CONTEXT.md
@.planning/phases/04-bulk-kernel-translation/04-02-SUMMARY.md
@.planning/phases/04-bulk-kernel-translation/04-03-SUMMARY.md
@.planning/phases/04-bulk-kernel-translation/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full cross-family oracle verification sweep</name>
  <files>verify/tests/lda_oracle.rs, verify/tests/gga_oracle.rs, verify/tests/mgga_oracle.rs</files>
  <read_first>
    - verify/tests/lda_oracle.rs (LDA oracle tests from Plan 02)
    - verify/tests/gga_oracle.rs (GGA oracle tests from Plan 03)
    - verify/tests/mgga_oracle.rs (MGGA oracle tests from Plan 04)
    - src/eval/dispatch.rs (all three dispatch functions)
  </read_first>
  <action>
1. **Ensure all three oracle test files have complete Rust-vs-oracle comparison tests** activated for every functional. Each test file should:
   - Iterate all functionals in its family
   - Test each derivative order independently (per D-09)
   - Test both unpolarized and polarized modes
   - Compare at correct tolerance tier per D-10:
     - exc (zk): relative error <= 1e-12
     - vxc (vrho, vsigma, vlapl, vtau): relative error <= 1e-10
     - fxc (all 2nd-order cross terms): relative error <= 1e-8
     - kxc (all 3rd-order cross terms): relative error <= 1e-6
     - lxc (all 4th-order cross terms): relative error <= 1e-4
   - Skip functionals with limited max_order (lda_c_pk09 max 3, mgga_c_b94 max 3) for higher orders
   - Use multiple test density points (at least 4 per mode)

2. **Add a summary test** that counts pass/fail across all families:
```rust
#[test]
fn test_full_verification_summary() {
    let mut passed = 0;
    let mut failed = 0;
    // ... run all tests, count results ...
    println!("VERIFICATION: {passed} passed, {failed} failed out of {} total", passed + failed);
    assert_eq!(failed, 0, "{failed} functionals failed oracle verification");
}
```

3. **Run the full test suite:**
   - `cargo test -p libxc_rs-verify -- lda_oracle --test-threads=1`
   - `cargo test -p libxc_rs-verify -- gga_oracle --test-threads=1`
   - `cargo test -p libxc_rs-verify -- mgga_oracle --test-threads=1`

4. **Fix any remaining failures.** At this stage, failures should be rare since each family was verified in its own plan. This sweep catches:
   - Regressions from MGGA work affecting GGA/LDA dispatch
   - Edge cases in test data not covered in per-family verification
   - Any functional skipped during batch translation

5. **Also verify build quality:**
   - `cargo clippy -p libxc_rs -- -D warnings` -- no clippy warnings
   - `cargo build -p libxc_rs` -- clean build, no warnings
  </action>
  <verify>
    <automated>cd /home/chemtech/workspace/libxc_rs && cargo test -p libxc_rs-verify -- oracle --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p libxc_rs-verify -- lda_oracle` exits 0
    - `cargo test -p libxc_rs-verify -- gga_oracle` exits 0
    - `cargo test -p libxc_rs-verify -- mgga_oracle` exits 0
    - All 42 LDA functionals pass all applicable derivative orders in both spin modes
    - All 131 GGA functionals pass all applicable derivative orders in both spin modes
    - All 92 MGGA functionals pass all applicable derivative orders in both spin modes
    - `cargo clippy -p libxc_rs -- -D warnings` exits 0
    - `cargo build -p libxc_rs` exits 0 with no warnings
  </acceptance_criteria>
  <done>Full oracle verification passes for all 265 functionals across all families, derivative orders, and spin modes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 4 completion verification</name>
  <files>verify/tests/lda_oracle.rs, verify/tests/gga_oracle.rs, verify/tests/mgga_oracle.rs</files>
  <action>Human verifies the complete Phase 4 output: all 265 functionals translated and oracle-verified, file counts match expectations, clippy clean, spot-check kernel structure.</action>
  <what-built>All 265 maple2c kernel files (42 LDA + 131 GGA + 92 MGGA) translated to Rust #[cube] functions preserving exact floating-point operation order. Every functional verified against C libxc oracle at all applicable derivative orders and both spin modes. Dispatch layer routes all families by functional ID.</what-built>
  <how-to-verify>
    1. Run: `cargo test -p libxc_rs-verify -- oracle --test-threads=1` -- ALL tests should pass (expect ~300s runtime)
    2. Check functional count: `ls src/kernel/lda/*.rs | wc -l` should show ~85 files (42 kernels + 42 launch + mod.rs)
    3. Check functional count: `ls src/kernel/gga/*.rs | wc -l` should show ~263 files (131 kernels + 131 launch + mod.rs)
    4. Check functional count: `ls src/kernel/mgga/*.rs | wc -l` should show ~185 files (92 kernels + 92 launch + mod.rs)
    5. Run: `cargo clippy -p libxc_rs -- -D warnings` -- no warnings
    6. Spot-check a few functionals: pick 3 random GGA and 3 random MGGA, verify their kernel files exist and contain `#[cube(launch_unchecked)]`
  </how-to-verify>
  <verify>
    <automated>cd /home/chemtech/workspace/libxc_rs && cargo test -p libxc_rs-verify -- oracle --test-threads=1 2>&1 | tail -10</automated>
  </verify>
  <done>Human confirmed all 265 functionals translated and verified, Phase 4 approved complete</done>
  <resume-signal>Type "approved" to mark Phase 4 complete, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-family regression | Changes to shared dispatch/math code could affect previously-verified families |
| oracle test coverage | Test densities must cover realistic DFT range to catch edge cases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | T (Tampering) | cross-family regression | mitigate | Run all three family oracle suites in verification sweep |
| T-04-15 | R (Repudiation) | verification completeness | mitigate | Summary test counts and asserts all functionals tested; no silent skips |
</threat_model>

<verification>
1. `cargo test -p libxc_rs-verify -- oracle --test-threads=1` -- full suite green
2. `cargo clippy -p libxc_rs -- -D warnings` -- clean
3. `cargo build -p libxc_rs` -- no warnings
4. Manual spot-check of kernel file existence and structure
</verification>

<success_criteria>
- 265 functionals pass oracle verification (42 LDA + 131 GGA + 92 MGGA)
- All 5 tolerance tiers confirmed: exc<=1e-12, vxc<=1e-10, fxc<=1e-8, kxc<=1e-6, lxc<=1e-4
- No clippy warnings, clean build
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-bulk-kernel-translation/04-05-SUMMARY.md`
</output>
