---
phase: 08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - crates/kernel-lda/src/lda_x.rs
  - crates/kernel-lda/src/launch_lda_x.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "cargo test --workspace passes with zero failures"
    - "cargo clippy --workspace passes with no errors (warnings from deny(warnings) would be errors)"
    - "Inline tests in kernel-lda crate pass when run via cargo test -p libxc-kernel-lda"
    - "Integration tests in tests/ directory pass unchanged"
  artifacts:
    - path: "crates/kernel-lda/src/lda_x.rs"
      provides: "LDA_X kernel with fixed test imports"
      contains: "mod tests"
    - path: "crates/kernel-lda/src/launch_lda_x.rs"
      provides: "LDA_X launch wrapper with fixed test imports"
      contains: "mod tests"
  key_links:
    - from: "crates/kernel-lda/src/lda_x.rs #[cfg(test)]"
      to: "libxc_rs::kernel::launch"
      via: "dev-dependency on libxc_rs"
      pattern: "libxc_rs::kernel::launch"
---

<objective>
Fix inline test imports in the extracted kernel-lda crate and run full workspace test + clippy verification to confirm the restructuring is complete and correct.

Purpose: The #[cfg(test)] blocks in lda_x.rs and launch_lda_x.rs reference `crate::kernel::launch::*` which no longer exists in the kernel-lda crate. These must be rewritten to use `libxc_rs::kernel::launch::*` (via dev-dependency). Then the full test suite confirms zero regressions.

Output: All tests pass, clippy clean, extraction complete.
</objective>

<execution_context>
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe/08-RESEARCH.md
@.planning/phases/08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix inline test imports in kernel-lda crate</name>
  <files>
    crates/kernel-lda/src/lda_x.rs
    crates/kernel-lda/src/launch_lda_x.rs
  </files>
  <read_first>
    crates/kernel-lda/src/lda_x.rs
    crates/kernel-lda/src/launch_lda_x.rs
    crates/kernel-lda/Cargo.toml
  </read_first>
  <action>
1. Search for ALL `use crate::kernel::launch` references in the kernel-lda crate:
```bash
grep -rn "use crate::kernel::launch" crates/kernel-lda/src/
```
These should only appear inside `#[cfg(test)] mod tests { ... }` blocks.

2. In `crates/kernel-lda/src/lda_x.rs`, find the `#[cfg(test)]` module's import:
```rust
use crate::kernel::launch::{cpu_client, create_input_buffer, create_zero_output_buffer, read_output_buffer, calculate_launch_config};
```
Replace with:
```rust
use libxc_rs::kernel::launch::{cpu_client, create_input_buffer, create_zero_output_buffer, read_output_buffer, calculate_launch_config};
```

3. In `crates/kernel-lda/src/launch_lda_x.rs`, find the `#[cfg(test)]` module's import:
```rust
use crate::kernel::launch::{...};
```
Replace with:
```rust
use libxc_rs::kernel::launch::{...};
```
Keep the exact same imported items, just change the crate path prefix.

4. Also search for any other `use crate::` references that may have been missed in the kernel-lda crate:
```bash
grep -rn "use crate::" crates/kernel-lda/src/ | grep -v "use crate::kernel::launch"
```
If any are found, rewrite them appropriately. Expected: none beyond the test imports.

5. Verify the kernel-lda Cargo.toml has `libxc_rs = { path = "../.." }` under `[dev-dependencies]`. This was added in Plan 01 Task 2. If missing, add it.

6. Run `cargo check -p libxc-kernel-lda` to confirm compilation.
  </action>
  <verify>
    <automated>cd /home/chemtech/workspace/libxc_rs && cargo check -p libxc-kernel-lda 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - crates/kernel-lda/src/lda_x.rs contains `use libxc_rs::kernel::launch::`
    - crates/kernel-lda/src/lda_x.rs does NOT contain `use crate::kernel::launch::`
    - crates/kernel-lda/src/launch_lda_x.rs contains `use libxc_rs::kernel::launch::`
    - crates/kernel-lda/src/launch_lda_x.rs does NOT contain `use crate::kernel::launch::`
    - `grep -c "use crate::kernel::launch" crates/kernel-lda/src/*.rs` returns 0
    - `cargo check -p libxc-kernel-lda` exits 0
  </acceptance_criteria>
  <done>All inline test imports in kernel-lda use libxc_rs:: instead of crate::, crate compiles</done>
</task>

<task type="auto">
  <name>Task 2: Full workspace test suite and clippy verification</name>
  <files></files>
  <read_first>
    Cargo.toml
    src/lib.rs
  </read_first>
  <action>
1. Run the full workspace test suite:
```bash
cargo test --workspace 2>&1
```
All tests must pass. If any fail, diagnose and fix -- likely causes:
  - Missing re-exports in src/kernel/mod.rs or src/math/mod.rs
  - Integration tests in tests/ using paths that no longer resolve
  - Profile/memory issues with CubeCL expansion in new crate context

2. Run clippy on the full workspace:
```bash
cargo clippy --workspace 2>&1
```
The main crate has `#![deny(warnings)]` so clippy warnings are errors. Fix any issues.

3. If `cargo test -p libxc-kernel-lda` has test failures due to the dev-dependency on libxc_rs creating a circular dev-dependency issue (libxc_rs -> libxc-kernel-lda -> [dev] libxc_rs), then:
   - Remove the `libxc_rs` dev-dependency from kernel-lda
   - Move the inline tests from `lda_x.rs` and `launch_lda_x.rs` to integration tests in `tests/` of the main workspace (they already test via libxc_rs paths)
   - OR: Remove the `#[cfg(test)]` blocks entirely from those files (they are covered by the integration tests in `tests/oracle_lda.rs`)

4. Run final clean verification:
```bash
cargo check --workspace && cargo test --workspace && echo "PHASE 8 COMPLETE"
```
  </action>
  <verify>
    <automated>cd /home/chemtech/workspace/libxc_rs && cargo test --workspace 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` exits 0 with all tests passing
    - `cargo clippy --workspace` exits 0 with no errors
    - `cargo check --workspace` exits 0
    - No `use crate::math::` references remain in any file under crates/ (verified by grep)
    - No `use crate::kernel::launch` references remain in any file under crates/ (verified by grep)
  </acceptance_criteria>
  <done>Full workspace compiles, all tests pass, clippy clean -- Phase 8 extraction complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Test import fixes only -- no production code changes, no new functionality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | I (Information Disclosure) | dev-dependency cycle | accept | Dev-dependencies do not create real cycles in Cargo. Test code only; no production impact. |
</threat_model>

<verification>
- `cargo test --workspace` passes (all tests green)
- `cargo clippy --workspace` passes (no warnings/errors)
- `grep -r "use crate::math::" crates/` returns empty
- `grep -r "use crate::kernel::launch" crates/` returns empty
</verification>

<success_criteria>
- Every existing test continues to pass after the restructuring
- No compiler warnings or clippy issues introduced
- The extraction is fully verified as a zero-behavioral-change refactoring
</success_criteria>

<output>
After completion, create `.planning/phases/08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe/08-03-SUMMARY.md`
</output>
