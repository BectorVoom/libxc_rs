---
phase: 08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/kernel-math/src/integrate.rs
  - crates/kernel-math/src/powers.rs
  - crates/kernel-math/src/piecewise.rs
  - crates/kernel-math/src/polynomials.rs
  - crates/kernel-math/src/erf.rs
  - crates/kernel-math/src/spin.rs
  - crates/kernel-math/src/dft_quantities.rs
  - crates/kernel-math/src/bspline.rs
  - crates/kernel-math/src/lambert_w.rs
  - crates/kernel-math/src/expint_e1.rs
  - crates/kernel-math/src/special.rs
  - crates/kernel-math/src/constants.rs
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "cargo clippy -p libxc-kernel-math produces zero warnings"
    - "cargo test -p libxc-kernel-math still passes all 51 tests after clippy fixes"
  artifacts:
    - path: "crates/kernel-math/src/integrate.rs"
      provides: "Clippy-clean integration math module"
  key_links: []
---

<objective>
Fix all 145 clippy warnings in the kernel-math crate, primarily assign_op_pattern warnings (s = s + x -> s += x).

Purpose: Close the clippy warnings gap identified in VERIFICATION.md. The main crate has #![deny(warnings)] so clippy warnings could become blocking errors in workspace-wide checks.

Output: cargo clippy -p libxc-kernel-math produces zero warnings, all 51 existing tests still pass.
</objective>

<execution_context>
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe/08-VERIFICATION.md
@.planning/phases/08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-fix clippy warnings in kernel-math and verify tests</name>
  <files>
    crates/kernel-math/src/integrate.rs
    crates/kernel-math/src/powers.rs
    crates/kernel-math/src/piecewise.rs
    crates/kernel-math/src/polynomials.rs
    crates/kernel-math/src/erf.rs
    crates/kernel-math/src/spin.rs
    crates/kernel-math/src/dft_quantities.rs
    crates/kernel-math/src/bspline.rs
    crates/kernel-math/src/lambert_w.rs
    crates/kernel-math/src/expint_e1.rs
    crates/kernel-math/src/special.rs
    crates/kernel-math/src/constants.rs
  </files>
  <read_first>
    crates/kernel-math/src/lib.rs
    crates/kernel-math/Cargo.toml
  </read_first>
  <action>
1. Run clippy auto-fix on the kernel-math crate. The verification identified 145 warnings, 132 of which are auto-fixable (primarily `assign_op_pattern`: `s = s + x` should be `s += x`):
```bash
cargo clippy --fix --lib -p libxc-kernel-math --allow-dirty --allow-staged
```

2. After auto-fix, run clippy again to check for remaining warnings that could not be auto-fixed:
```bash
cargo clippy -p libxc-kernel-math 2>&1
```
If any warnings remain, manually fix them. The remaining warnings are likely:
- `needless_late_init`: move variable declaration to initialization point
- `excessive_precision`: already allowed via `#![allow(clippy::excessive_precision)]` in lib.rs
- `too_many_arguments`: already allowed via `#![allow(clippy::too_many_arguments)]` in lib.rs

3. For any remaining warnings NOT covered by the existing allows in lib.rs, fix them manually. Do NOT add more blanket allow attributes -- fix the code.

IMPORTANT: These are CubeCL `#[cube]` function bodies. The `assign_op_pattern` fix (s = s + x -> s += x) is safe because CubeCL's `#[cube]` macro expands both forms identically. The floating-point operation order is preserved.

4. Run the existing test suite to confirm no regressions:
```bash
cargo test -p libxc-kernel-math
```
All 51 tests must pass.

5. Final verification -- clippy must be completely clean:
```bash
cargo clippy -p libxc-kernel-math 2>&1 | grep -c "warning:"
```
Must return 0.
  </action>
  <verify>
    <automated>cd /home/chemtech/workspace/libxc_rs && cargo clippy -p libxc-kernel-math 2>&1 | grep -c "warning:" && cargo test -p libxc-kernel-math 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo clippy -p libxc-kernel-math 2>&1 | grep -c "warning:"` returns 0
    - `cargo test -p libxc-kernel-math` exits 0 with 51 tests passing
    - No new `#![allow(...)]` attributes added to lib.rs beyond the 3 already present
  </acceptance_criteria>
  <done>kernel-math crate is clippy-clean with all 51 tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Clippy auto-fix on assign operators -- no behavioral change to math functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | T (Tampering) | clippy --fix modifying math code | accept | assign_op_pattern changes (s = s + x -> s += x) are semantically identical. Test suite confirms no regression. |
</threat_model>

<verification>
- `cargo clippy -p libxc-kernel-math` produces zero warnings
- `cargo test -p libxc-kernel-math` passes all 51 tests
</verification>

<success_criteria>
- All 145 clippy warnings in kernel-math are resolved
- All 51 existing tests pass without regression
- The clippy gap from VERIFICATION.md is fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe/08-05-SUMMARY.md`
</output>
