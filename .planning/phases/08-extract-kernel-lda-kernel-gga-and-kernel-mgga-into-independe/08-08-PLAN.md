---
phase: 08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe
plan: 08
type: execute
wave: 8
depends_on: ["08-07"]
files_modified:
  - tools/batch_translate_mgga.py
  - tools/translate_mgga.py
  - crates/kernel-mgga-1/Cargo.toml
  - crates/kernel-mgga-1/src/lib.rs
  - crates/kernel-mgga-2/Cargo.toml
  - crates/kernel-mgga-2/src/lib.rs
  - crates/kernel-mgga-3/Cargo.toml
  - crates/kernel-mgga-3/src/lib.rs
  - crates/kernel-mgga/Cargo.toml
  - crates/kernel-mgga/src/lib.rs
  - Cargo.toml
  - tests/oracle_mgga.rs
autonomous: true
requirements:
  - KERN-05
  - KERN-06
  - VERIFY-03
  - VERIFY-04
must_haves:
  truths:
    - "All 92 MGGA functionals (90 mgga_exc + 2 mgga_vxc) are translated to Rust kernel files, with large derivative orders (kxc_pol, lxc_pol) deferred and documented where they cause OOM"
    - "Both mgga_vxc/ functionals (mgga_x_2d_prp10, mgga_x_tb09) are translated with vxc-only mode"
    - "MGGA sub-crates compile without OOM by staying under ~15 modules per sub-crate"
    - "kernel-mgga facade crate re-exports all sub-crates"
    - "Multiple MGGA functionals pass oracle comparison across both spin modes"
  artifacts:
    - path: "tools/batch_translate_mgga.py"
      provides: "Batch translation runner for all MGGA functionals"
      min_lines: 50
    - path: "crates/kernel-mgga-2/Cargo.toml"
      provides: "Second MGGA sub-crate"
      contains: "libxc-kernel-mgga-2"
    - path: "crates/kernel-mgga-3/Cargo.toml"
      provides: "Third MGGA sub-crate"
      contains: "libxc-kernel-mgga-3"
    - path: "crates/kernel-mgga/src/lib.rs"
      provides: "MGGA facade re-exporting sub-crates"
      contains: "pub use libxc_kernel_mgga_1"
  key_links:
    - from: "tools/batch_translate_mgga.py"
      to: "tools/translate_mgga.py"
      via: "subprocess call per functional"
      pattern: "translate_mgga"
    - from: "crates/kernel-mgga/Cargo.toml"
      to: "crates/kernel-mgga-1/Cargo.toml"
      via: "path dependency"
      pattern: "libxc-kernel-mgga-1"
---

<objective>
Batch-translate all 92 MGGA functionals (90 mgga_exc + 2 mgga_vxc) into sub-crates, set up the facade re-export pattern, and run oracle comparison sweeps to validate correctness.

Purpose: Complete the MGGA kernel translation coverage using the iteratively-verified tool from Plans 01-02.
Output: All MGGA functionals translated and compiled across 3+ sub-crates, oracle tests passing for representative set.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rebuild-mgga-kernel-conversion-tool-from-scratch-with-iterat/08-RESEARCH.md
@.planning/phases/08-rebuild-mgga-kernel-conversion-tool-from-scratch-with-iterat/08-01-SUMMARY.md
@.planning/phases/08-rebuild-mgga-kernel-conversion-tool-from-scratch-with-iterat/08-02-SUMMARY.md

<interfaces>
From crates/kernel-gga/src/lib.rs (facade pattern reference):
```rust
#![allow(clippy::excessive_precision)]
#![allow(clippy::needless_late_init)]
#![allow(clippy::too_many_arguments)]

pub use libxc_kernel_gga_1 as batch1;
pub use libxc_kernel_gga_2 as batch2;
pub use libxc_kernel_gga_3 as batch3;
```

From crates/kernel-gga/Cargo.toml (facade dependency pattern):
```toml
[dependencies]
cubecl = { version = "0.9.0", default-features = false, features = ["cpu"] }
libxc-kernel-math = { path = "../kernel-math" }
libxc-kernel-gga-1 = { path = "../kernel-gga-1" }
libxc-kernel-gga-2 = { path = "../kernel-gga-2" }
libxc-kernel-gga-3 = { path = "../kernel-gga-3" }
```

From tools/batch_translate_gga.py (batch runner reference):
- Iterates over all .c files in mgga_exc/ and mgga_vxc/ directories
- Calls translate_mgga.py for each
- Handles vxc-only files from mgga_vxc/ directory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create batch_translate_mgga.py and translate all 92 functionals</name>
  <files>
    tools/batch_translate_mgga.py,
    tools/translate_mgga.py,
    crates/kernel-mgga-1/src/lib.rs,
    crates/kernel-mgga-2/Cargo.toml,
    crates/kernel-mgga-2/src/lib.rs,
    crates/kernel-mgga-3/Cargo.toml,
    crates/kernel-mgga-3/src/lib.rs
  </files>
  <read_first>
    - tools/batch_translate_gga.py (reference batch runner)
    - tools/translate_mgga.py (current tool from Plans 01-02)
    - crates/kernel-mgga-1/src/lib.rs (current module declarations -- will be regenerated)
    - .planning/phases/08-extract-kernel-lda-kernel-gga-and-kernel-mgga-into-independe/08-04-SUMMARY.md (GGA sub-crate split experience -- batch sizes, deferred list)
  </read_first>
  <action>
1. **Create tools/batch_translate_mgga.py** modeled on batch_translate_gga.py:
   - Scan `libxc-master/src/maple2c/mgga_exc/` for all .c files (90 files)
   - Scan `libxc-master/src/maple2c/mgga_vxc/` for .c files (2 files: mgga_x_2d_prp10.c, mgga_x_tb09.c)
   - Sort all functional names alphabetically
   - Split into sub-crate batches of ~15 functionals each (GGA used ~35 per batch, but MGGA files are larger, so use smaller batches to avoid OOM)
   - Expected: 6-7 sub-crates for 92 functionals at 15 per batch
   - For each batch:
     a. Create the sub-crate directory: `crates/kernel-mgga-N/`
     b. Create Cargo.toml with dependencies on cubecl and kernel-math
     c. Run translate_mgga.py for each functional in the batch
     d. For mgga_vxc files: pass `--vxc-only` flag
     e. Generate lib.rs with all `pub mod functional_name;` declarations and clippy allows

2. **Create each sub-crate** (kernel-mgga-2 through kernel-mgga-N):
   Each sub-crate follows the same pattern as kernel-mgga-1:
   ```toml
   [package]
   name = "libxc-kernel-mgga-N"
   version = "0.1.0"
   edition = "2024"

   [dependencies]
   cubecl = { version = "0.9.0", default-features = false, features = ["cpu"] }
   libxc-kernel-math = { path = "../kernel-math" }

   [profile.dev]
   debug = 0
   codegen-units = 16

   [profile.test]
   debug = 0
   codegen-units = 16
   ```

3. **Handle OOM**: For functionals whose generated files exceed ~5K lines per #[cube] function:
   - Option A: If translate_mgga.py supports function splitting, use it
   - Option B: Defer those derivative orders (kxc_pol, lxc_pol for large functionals)
   - Document deferred functionals as comments in lib.rs (same pattern as GGA)

4. If any translation fails, fix translate_mgga.py and regenerate the affected batch.

All 92 functionals must have kernel directories generated. Large derivative orders that cause OOM are deferred (commented out in mod.rs) -- this is expected and acceptable per the GGA precedent.
  </action>
  <verify>
    <automated>cd /workspace && ls crates/kernel-mgga-*/src/ | grep -c '/' | xargs -I{} echo "Sub-crate count: {}" && find crates/kernel-mgga-[0-9]*/src -mindepth 1 -maxdepth 1 -type d | wc -l | xargs -I{} echo "Functional directories: {}"</automated>
  </verify>
  <acceptance_criteria>
    - tools/batch_translate_mgga.py exists and is executable
    - At least 3 sub-crates exist (kernel-mgga-1, kernel-mgga-2, kernel-mgga-3)
    - Each sub-crate Cargo.toml has name `libxc-kernel-mgga-N`
    - All 92 MGGA functionals have kernel directories across sub-crates (deferred derivative orders documented as comments in mod.rs)
  </acceptance_criteria>
  <done>All 92 MGGA functionals translated into sub-crate directories with large derivative orders deferred as needed</done>
</task>

<task type="auto">
  <name>Task 2: Compile sub-crates, create facade, and update workspace</name>
  <files>
    crates/kernel-mgga-1/Cargo.toml,
    crates/kernel-mgga/Cargo.toml,
    crates/kernel-mgga/src/lib.rs,
    Cargo.toml
  </files>
  <read_first>
    - crates/kernel-gga/src/lib.rs (facade pattern -- pub use sub-crates)
    - crates/kernel-gga/Cargo.toml (facade Cargo.toml with sub-crate dependencies)
    - crates/kernel-mgga/src/lib.rs (current stubs to replace)
    - crates/kernel-mgga/Cargo.toml (current Cargo.toml to update)
    - Cargo.toml (workspace members to update)
  </read_first>
  <action>
1. **Update workspace Cargo.toml**: Add all new sub-crate directories to workspace members list.

2. **Compile each sub-crate independently** to isolate OOM issues:
   ```bash
   for crate in crates/kernel-mgga-[0-9]*; do
     name=$(basename "$crate")
     echo "Checking $name..."
     RUST_MIN_STACK=67108864 cargo check -p "libxc-$name" 2>&1 | tail -5
   done
   ```

3. If any sub-crate fails to compile:
   - If OOM (SIGKILL): reduce batch size by moving functionals to next sub-crate, or defer large files
   - If translator bug: fix translate_mgga.py, regenerate affected batch, recompile
   - Iterate until all sub-crates compile

4. **Update kernel-mgga facade** (crates/kernel-mgga/):
   - Remove old stub modules (order0.rs through order4.rs) if they exist
   - Update Cargo.toml to depend on all sub-crates:
     ```toml
     [dependencies]
     cubecl = { version = "0.9.0", default-features = false, features = ["cpu"] }
     libxc-kernel-math = { path = "../kernel-math" }
     libxc-kernel-mgga-1 = { path = "../kernel-mgga-1" }
     libxc-kernel-mgga-2 = { path = "../kernel-mgga-2" }
     # ... all sub-crates
     ```
   - Update lib.rs to re-export: `pub use libxc_kernel_mgga_1 as batch1;` etc.

5. **Final compilation check**: `RUST_MIN_STACK=67108864 cargo check -p libxc-kernel-mgga 2>&1 | tail -5`
  </action>
  <verify>
    <automated>cd /workspace && RUST_MIN_STACK=67108864 cargo check -p libxc-kernel-mgga 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - crates/kernel-mgga/src/lib.rs contains `pub use libxc_kernel_mgga_1 as batch1`
    - crates/kernel-mgga/src/lib.rs does NOT contain `pub mod order0`
    - crates/kernel-mgga/Cargo.toml depends on all sub-crates
    - All sub-crate directories are listed in workspace Cargo.toml members
    - `cargo check -p libxc-kernel-mgga` exits with status 0
  </acceptance_criteria>
  <done>All MGGA sub-crates compile, facade re-exports them, workspace is updated</done>
</task>

<task type="auto">
  <name>Task 3: Expand oracle tests to validate multiple MGGA functionals across spin modes</name>
  <files>tests/oracle_mgga.rs</files>
  <read_first>
    - tests/oracle_mgga.rs (current state from Plan 02)
    - verify/src/lib.rs (oracle_mgga_all, functional IDs)
    - crates/kernel-mgga-1/src/lib.rs (which functionals are available)
  </read_first>
  <action>
Expand oracle comparison tests to cover multiple MGGA functionals across both spin modes and derivative orders.

Add tests for at least 5 different functionals spanning the pattern categories:
1. mgga_xc_lp90 -- exc unpol and pol (from Plan 02, verify still passes)
2. mgga_k_gea2 -- exc unpol (kinetic)
3. mgga_x_lta -- exc unpol (exchange, tau-only)
4. mgga_c_b88 -- exc unpol and pol (correlation with lapl)
5. One additional functional from a later sub-crate (e.g., mgga_x_tpss or mgga_c_tpss)

For each test:
- Use physically reasonable input values:
  - rho: [0.1, 0.5, 1.0, 2.0, 5.0] (unpol) or interleaved [0.05, 0.05, 0.25, 0.25, ...] (pol)
  - sigma: [0.01, 0.1, 0.5, 1.0, 2.0] (unpol) or [0.005, 0.002, 0.005, ...] (pol 3-component)
  - lapl: [0.0, 0.001, 0.01, 0.05, 0.1] (unpol)
  - tau: [0.1, 0.3, 0.6, 1.0, 2.0] (unpol)
- Call oracle_mgga_all() with correct func_id and nspin
- Launch Rust kernel via CubeCL CPU runtime
- Compare zk (exc) output with relative error tolerance 1e-12
- If vxc tests are feasible, compare vrho with tolerance 1e-12

Also add a test that verifies at least one polarized mode:
- mgga_xc_lp90 exc pol with interleaved rho, sigma (3-component), lapl (2-component), tau (2-component)

Run all tests: `RUST_MIN_STACK=67108864 cargo test --test oracle_mgga -- --nocapture`

If any test fails with numerical mismatch:
- Print the actual vs expected values
- Check if it's an indexing issue (common in pol mode)
- Fix translate_mgga.py if needed, regenerate affected functionals, recompile

Tests that cannot pass due to known translator limitations should be marked `#[ignore]` with a comment explaining the reason, so `cargo test` exits 0.
  </action>
  <verify>
    <automated>cd /workspace && RUST_MIN_STACK=67108864 cargo test --test oracle_mgga -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/oracle_mgga.rs contains at least 5 test functions (excluding #[ignore]d tests)
    - tests/oracle_mgga.rs contains tests for both unpol and pol spin modes
    - tests/oracle_mgga.rs contains `oracle_mgga_all` calls
    - tests/oracle_mgga.rs contains relative error assertions (1e-12 for exc)
    - `cargo test --test oracle_mgga` exits with status 0 (all non-ignored tests pass)
  </acceptance_criteria>
  <done>Multiple MGGA functionals verified against oracle across spin modes, proving translator correctness at scale; any failing tests are #[ignore]d with documented reasons</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Batch tool -> filesystem | Creates many files; all within crates/ directory |
| Sub-crate compilation -> system memory | OOM risk mitigated by batch size control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Denial of Service | OOM during batch compile | mitigate | Limit sub-crate batch size to ~15 modules; defer large derivative files |
| T-08-06 | Tampering | Generated kernel correctness | mitigate | Oracle comparison tests verify numerical equivalence for representative set |
</threat_model>

<verification>
1. All MGGA sub-crates compile individually
2. kernel-mgga facade compiles and re-exports all sub-crates
3. Oracle tests pass for at least 5 functionals across both spin modes
4. batch_translate_mgga.py handles both mgga_exc and mgga_vxc directories
</verification>

<success_criteria>
- All 92 MGGA functionals translated (deferred large derivatives documented)
- 3+ sub-crates organized and compiling
- Facade crate re-exports all sub-crates
- Oracle tests pass for representative functionals (`cargo test --test oracle_mgga` exits 0)
- batch_translate_mgga.py provides repeatable generation
</success_criteria>

<output>
After completion, create `.planning/phases/08-rebuild-mgga-kernel-conversion-tool-from-scratch-with-iterat/08-03-SUMMARY.md`
</output>
