---
phase: 09-reduce-kernel-build-time
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/kernel/mod.rs
autonomous: true
requirements: [BUILD-OPT-02, BUILD-OPT-03]

must_haves:
  truths:
    - "Default cargo check compiles only LDA kernels, not GGA or MGGA"
    - "cargo check --features gga compiles LDA + GGA kernels"
    - "cargo check --features all-kernels compiles LDA + GGA + MGGA"
    - "cargo check --features mgga compiles LDA + MGGA but not GGA"
  artifacts:
    - path: "Cargo.toml"
      provides: "Feature gates for kernel families"
      contains: "[features]"
    - path: "src/kernel/mod.rs"
      provides: "Cfg-gated re-exports of GGA and MGGA"
      contains: "#[cfg(feature"
  key_links:
    - from: "Cargo.toml"
      to: "src/kernel/mod.rs"
      via: "feature = gga triggers dep:libxc-kernel-gga which enables cfg(feature = gga)"
      pattern: 'optional = true'
---

<objective>
Add Cargo feature gates to the workspace root so that `cargo build` (no features) compiles only LDA kernels (~2 min), `--features gga` adds GGA (~14 min), `--features mgga` adds MGGA, and `--features all-kernels` compiles everything. This satisfies phase success criteria 2 and 3.

Purpose: Reduce default build time from ~30+ min to ~2 min by making GGA and MGGA optional.
Output: Modified Cargo.toml with [features] section; modified src/kernel/mod.rs with cfg gates.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-reduce-kernel-build-time/09-RESEARCH.md
@.planning/phases/09-reduce-kernel-build-time/09-01-SUMMARY.md
@.planning/phases/09-reduce-kernel-build-time/09-02-SUMMARY.md

<interfaces>
From src/kernel/mod.rs (current):
```rust
pub use libxc_kernel_lda as lda;
pub use libxc_kernel_gga as gga;
pub use libxc_kernel_mgga as mgga;
pub mod launch;
pub mod dispatch_key;
pub mod shared;
pub mod mix;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feature gates to root Cargo.toml and make kernel deps optional</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml
  </read_first>
  <action>
Add a `[features]` section to the root Cargo.toml with these exact entries:

```toml
[features]
default = []
gga = ["dep:libxc-kernel-gga"]
mgga = ["dep:libxc-kernel-mgga"]
all-kernels = ["gga", "mgga"]
```

In the `[dependencies]` section, change the kernel-gga and kernel-mgga lines to be optional:
- Change `libxc-kernel-gga = { path = "crates/kernel-gga" }` to `libxc-kernel-gga = { path = "crates/kernel-gga", optional = true }`
- Change `libxc-kernel-mgga = { path = "crates/kernel-mgga", optional = true }` to `libxc-kernel-mgga = { path = "crates/kernel-mgga", optional = true }`

Keep `libxc-kernel-lda` and `libxc-kernel-math` as unconditional dependencies (no change).

Do NOT modify `.cargo/config.toml` -- the existing `rustc-wrapper = "sccache"` is correct.
Do NOT modify any `[profile.*]` sections.
Do NOT modify the `[workspace]` members list.
Do NOT modify `[dev-dependencies]`.
  </action>
  <verify>
    <automated>cd /workspace && grep -A5 '^\[features\]' Cargo.toml && grep 'optional = true' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains `[features]` section
    - Cargo.toml contains `default = []`
    - Cargo.toml contains `gga = ["dep:libxc-kernel-gga"]`
    - Cargo.toml contains `mgga = ["dep:libxc-kernel-mgga"]`
    - Cargo.toml contains `all-kernels = ["gga", "mgga"]`
    - Cargo.toml line for libxc-kernel-gga contains `optional = true`
    - Cargo.toml line for libxc-kernel-mgga contains `optional = true`
    - Cargo.toml line for libxc-kernel-lda does NOT contain `optional`
    - `.cargo/config.toml` is unchanged (still contains `rustc-wrapper = "sccache"`)
  </acceptance_criteria>
  <done>Root Cargo.toml has [features] with gga/mgga/all-kernels; kernel-gga and kernel-mgga are optional deps</done>
</task>

<task type="auto">
  <name>Task 2: Add cfg-gates to src/kernel/mod.rs and verify compilation</name>
  <files>src/kernel/mod.rs</files>
  <read_first>
    - src/kernel/mod.rs
    - Cargo.toml
  </read_first>
  <action>
Replace the contents of `src/kernel/mod.rs` with:

```rust
pub use libxc_kernel_lda as lda;

#[cfg(feature = "gga")]
pub use libxc_kernel_gga as gga;

#[cfg(feature = "mgga")]
pub use libxc_kernel_mgga as mgga;

pub mod launch;
pub mod dispatch_key;
pub mod shared;
pub mod mix;
```

The `lda` re-export has NO cfg gate (always compiled). The `gga` and `mgga` re-exports are gated behind their respective features.

After editing, verify compilation:
1. `cargo check` (no features) -- must succeed, must NOT compile kernel-gga or kernel-mgga crates
2. `cargo check --features gga` -- must succeed, compiles GGA
3. `cargo check --features all-kernels` -- must succeed, compiles everything

For verification step 1, check that `cargo check 2>&1` does NOT contain "Compiling libxc-kernel-gga" or "Compiling libxc-kernel-mgga" (only "libxc-kernel-lda" and "libxc-kernel-math").

NOTE: If any other file in the crate (e.g., src/kernel/dispatch_key.rs, src/kernel/shared.rs, src/kernel/mix.rs, src/kernel/launch.rs) references `gga` or `mgga` unconditionally, those references MUST also be wrapped in `#[cfg(feature = "gga")]` / `#[cfg(feature = "mgga")]`. Check these files first and fix any unconditional references to gga/mgga types/modules before running cargo check.
  </action>
  <verify>
    <automated>cd /workspace && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/kernel/mod.rs contains `#[cfg(feature = "gga")]` before the gga re-export
    - src/kernel/mod.rs contains `#[cfg(feature = "mgga")]` before the mgga re-export
    - src/kernel/mod.rs contains `pub use libxc_kernel_lda as lda;` without any cfg gate
    - `cargo check` (no features) exits 0
    - `cargo check --features gga` exits 0
  </acceptance_criteria>
  <done>Default cargo check compiles only LDA; --features gga adds GGA; --features all-kernels adds everything</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries relevant -- this is a build configuration change with no runtime behavior modification.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | D (Denial of Service) | Build system | accept | Feature misconfiguration could cause longer builds, but this is developer-facing only with easy recovery via feature flags |
</threat_model>

<verification>
1. `cargo check` (no features) succeeds and does not compile GGA or MGGA crates
2. `cargo check --features gga` succeeds
3. `cargo check --features all-kernels` succeeds
4. `.cargo/config.toml` still contains `rustc-wrapper = "sccache"` (not modified)
</verification>

<success_criteria>
- Default build compiles only LDA kernels (~2 min)
- Feature gates allow selective compilation of GGA and MGGA
- No regressions in existing code
</success_criteria>

<output>
After completion, create `.planning/phases/09-reduce-kernel-build-time/09-03-SUMMARY.md`
</output>
