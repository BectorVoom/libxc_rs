---
phase: 10-workspace-level-modular-split
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - log/10-surface-before.log
  - log/10-tree-libxc_rs-before.log
  - log/10-generated-snapshot/
  - log/10-baseline-parity-lda.log
  - log/10-baseline-parity-gga.log
  - log/10-baseline-parity-mgga.log
autonomous: false
requirements: []
must_haves:
  truths:
    - "A byte-snapshot of the pre-refactor public surface (use libxc_rs::... paths) exists for SC-5 diffing"
    - "A byte-snapshot of the xtask-generated files exists so the D-03 path edit can be proven byte-identical"
    - "The per-family oracle pass/fail baseline (incl. the 6 expected MGGA Phase-12 fails + 3 math-precision fails) is recorded for SC-6 comparison"
    - "The phase-start green gate (cargo check -p libxc_rs --lib EXIT 0) is confirmed before any move"
  artifacts:
    - path: "log/10-surface-before.log"
      provides: "Pre-refactor libxc_rs:: path set (SC-5 reference)"
    - path: "log/10-generated-snapshot/"
      provides: "Byte-snapshot of xtask-generated files (D-03 idempotency reference)"
    - path: "log/10-baseline-parity-mgga.log"
      provides: "MGGA per-family pass/fail set incl. expected failures (SC-6 reference)"
  key_links:
    - from: "log/10-surface-before.log"
      to: "V-10-05 (SC-5)"
      via: "diff against log/10-surface-after.log after the facade lands"
      pattern: "use libxc_rs::"
---

<objective>
Capture the pre-refactor baselines that every later Phase-10 success criterion diffs against. This is a SNAPSHOT-ONLY plan — zero `git mv`, zero source edits. It runs BEFORE 10-01 and produces the SC-5 surface reference, the D-03 generated-files byte reference, and the SC-6/SC-7 per-family oracle pass/fail reference (which MUST record the 6 known MGGA Phase-12 failures + 3 math-precision failures as "expected fail").

Purpose: A refactor's correctness is "no observable change." That claim is only checkable against a recorded before-state. Without these anchors, SC-5 (surface unchanged) and SC-6 (test parity) are unverifiable.

Output: Six log artifacts under `log/` capturing the public surface, the monolithic dep closure, the generated-file bytes, and the three per-family oracle results.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-workspace-level-modular-split/10-VALIDATION.md
@.planning/phases/10-workspace-level-modular-split/10-RESEARCH.md

# RAM-CONSTRAINED BOX. NO `cargo {check,test,build} --workspace` anywhere. All cargo
# is per-`-p` / per-family at `-j1`. NEVER edit .cargo/config.toml. The heavy per-family
# oracle sweeps are USER-RUN (Task 2 is a human-action checkpoint).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture surface, dep-closure, generated-file, and green-gate baselines</name>
  <files>log/10-surface-before.log, log/10-tree-libxc_rs-before.log, log/10-generated-snapshot/, log/10-check-libxc_rs-baseline.log</files>
  <read_first>
    - .planning/phases/10-workspace-level-modular-split/10-VALIDATION.md (## Wave 0 Requirements — the exact commands)
    - src/lib.rs lines 1-39 (the public surface being snapshotted)
    - Cargo.toml lines 6-321 (the monolithic dep closure)
  </read_first>
  <action>
    These are CHEAP commands (no heavy compile except the green gate). Run each and tee to the named log.

    1. Public-surface snapshot (SC-5 reference) — the exact set of `use libxc_rs::...` paths downstream code relies on:
       `grep -rhoE "use libxc_rs::[A-Za-z0-9_:]+" verify/ verify-canary/ tests/ examples/ 2>/dev/null | sort -u > log/10-surface-before.log`
       (tests/ and examples/ may not exist — the `2>/dev/null` swallows that; the verify/ and verify-canary/ paths are the real surface.)

    2. Monolithic dep-closure snapshot (informational diff reference) — CHEAP, no compile:
       `cargo tree -p libxc_rs -e no-dev 2>&1 | tee log/10-tree-libxc_rs-before.log`

    3. Generated-file byte-snapshot (D-03 idempotency reference). The 6 xtask outputs are: `src/meta/generated.rs`, `src/meta/generated_hybrid.rs`, `src/meta/generated_propagation.rs`, `src/registry/by_id.rs`, `src/registry/by_name.rs`, `src/registry/removed.rs`. Copy them verbatim:
       `mkdir -p log/10-generated-snapshot && cp src/meta/generated.rs src/meta/generated_hybrid.rs src/meta/generated_propagation.rs src/registry/by_id.rs src/registry/by_name.rs src/registry/removed.rs log/10-generated-snapshot/`
       Then record their sha256 for fast later comparison:
       `sha256sum log/10-generated-snapshot/*.rs > log/10-generated-snapshot/SHA256SUMS.txt`

    4. Phase-start green gate (D-13, reframed per-`-p`). This is the ONE heavy command in this task — it is the ~536 MB umbrella check, NOT `--workspace`. Confirm Phase 10 starts from green:
       `cargo check -p libxc_rs --lib -j1 2>&1 | tee log/10-check-libxc_rs-baseline.log`
       Expect EXIT 0 (Phase 11 already satisfies this per STATE.md). If it does NOT exit 0, STOP and surface to the user — Phase 10 cannot start from a broken baseline (10-VALIDATION.md Wave 0 note).
  </action>
  <acceptance_criteria>
    - `test -s log/10-surface-before.log` (non-empty; contains at least the verify/ `use libxc_rs::` paths)
    - `grep -q 'use libxc_rs::' log/10-surface-before.log`
    - `test -f log/10-tree-libxc_rs-before.log && grep -q 'libxc_rs' log/10-tree-libxc_rs-before.log`
    - `test -f log/10-generated-snapshot/generated.rs -a -f log/10-generated-snapshot/generated_propagation.rs -a -f log/10-generated-snapshot/by_id.rs`
    - `test -f log/10-generated-snapshot/SHA256SUMS.txt && [ "$(wc -l < log/10-generated-snapshot/SHA256SUMS.txt)" -eq 6 ]`
    - `grep -qE 'Finished|Compiling' log/10-check-libxc_rs-baseline.log` AND the `cargo check -p libxc_rs --lib -j1` command exited 0 (if non-zero, HALT and surface to user)
  </acceptance_criteria>
  <verify>
    <automated>test -s log/10-surface-before.log && test -f log/10-generated-snapshot/SHA256SUMS.txt && [ "$(wc -l < log/10-generated-snapshot/SHA256SUMS.txt)" -eq 6 ]</automated>
  </verify>
  <done>Surface, dep-closure, and generated-file snapshots exist; phase-start green gate confirmed EXIT 0 (or HALTED for user if not).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: USER-RUN per-family oracle baseline capture (SC-6/SC-7 reference)</name>
  <what-built>
    Task 1 captured the cheap snapshots (surface, dep-closure, generated files) and confirmed the green gate. The remaining baseline — the per-family oracle pass/fail set — requires running the verify/ oracle suite per family at `-j1`. These are MEMORY-HEAVY, multi-minute builds+runs that OOM if run any way other than per-family at `-j1`, so they are USER-RUN (the executor must NOT attempt them — it would risk an OOM SIGKILL on this 30 GB box).
  </what-built>
  <how-to-verify>
    Run each of these THREE commands separately (one family at a time, `-j1`), and tee each to its log. Do NOT run `--workspace` and do NOT combine families:

    1. LDA:  `cargo test -p libxc_rs-verify --no-default-features --features oracle-lda --test lda_oracle -j1 2>&1 | tee log/10-baseline-parity-lda.log`
    2. GGA:  `cargo test -p libxc_rs-verify --no-default-features --features oracle-gga --test gga_oracle -j1 2>&1 | tee log/10-baseline-parity-gga.log`
    3. MGGA: `cargo test -p libxc_rs-verify --no-default-features --features oracle-mgga --test mgga_oracle -j1 2>&1 | tee log/10-baseline-parity-mgga.log`

    EXPECTED baseline (this is the reference SC-6 will diff against — these failures are NOT regressions, they are the recorded "expected fail" set):
    - LDA: all pass.
    - GGA: all pass.
    - MGGA: the 6 routed-to-Phase-12 functionals are expected to FAIL the f64 oracle — `mgga_x_th` (~2e-1), `mgga_x_2d_js17` (~1.1e-2), `mgga_c_cs` (~9.2e-3), `mgga_x_pkzb` (~3.7e-3), `mgga_x_pbe_gx` (~1.5e-3), `mgga_x_tm` (~9.2e-4). Additionally 3 math-precision tests are pre-existing failures (`bessel::ref_i0_small_arg`, `dft_quantities::test_dimensionless_alpha_uniform` ~4e-8, `dft_quantities::test_tf_kinetic_rho_one`).

    If you observe any failure OUTSIDE that expected set, STOP and report it — Phase 10 must not start from an unexpectedly-broken baseline. If the actual exact MGGA test names differ from the above (test-name drift since 11-12), record the ACTUAL pass/fail set in the log — that recorded set becomes the SC-6 reference regardless.
  </how-to-verify>
  <resume-signal>Type "approved" once the three log files exist and the pass/fail set matches the expected baseline (LDA/GGA pass; the 6 MGGA + 3 math-precision expected fails recorded). If anything else failed, describe it instead of approving.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only reads source and runs cargo/grep. It captures snapshots; it crosses no trust boundary and introduces no input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-00-01 | Tampering | baseline log artifacts | accept | Snapshots are local dev-tree files, not shipped; corruption only weakens a later diff, surfaced when SC-5/SC-6 diffs fail. No external surface. |
| T-10-00-02 | DoS (self) | per-family oracle build OOM | mitigate | Heavy sweeps are USER-RUN per-family at `-j1` (Task 2 human-action checkpoint); executor never runs `--workspace`. |
</threat_model>

<verification>
- `log/10-surface-before.log`, `log/10-tree-libxc_rs-before.log`, `log/10-generated-snapshot/` (+ SHA256SUMS.txt) exist after Task 1.
- `cargo check -p libxc_rs --lib -j1` confirmed EXIT 0 at phase start.
- `log/10-baseline-parity-{lda,gga,mgga}.log` exist after Task 2; pass/fail set matches the expected baseline (LDA/GGA pass; 6 MGGA + 3 math-precision expected fails recorded).
</verification>

<success_criteria>
All six baseline artifacts exist; phase-start green gate confirmed; per-family oracle pass/fail set recorded with the expected failures noted. This unblocks the SC-5 (surface) and SC-6 (parity) verifications in 10-01..10-03.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workspace-level-modular-split/10-00-SUMMARY.md`
</output>
