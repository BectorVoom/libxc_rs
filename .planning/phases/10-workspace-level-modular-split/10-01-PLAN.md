---
phase: 10-workspace-level-modular-split
plan: 01
type: execute
wave: 2
depends_on: ["10-00"]
files_modified:
  - crates/libxc-core/Cargo.toml
  - crates/libxc-core/src/lib.rs
  - crates/libxc-core/src/model/
  - crates/libxc-core/src/meta/
  - crates/libxc-core/src/registry/
  - crates/libxc-core/src/input/
  - crates/libxc-core/src/output/
  - crates/libxc-core/src/layout/
  - crates/libxc-core/src/dims/
  - crates/libxc-core/src/error/
  - crates/libxc-core/src/deferred.rs
  - crates/kernels/math/src/lib.rs
  - crates/kernels/math/src/deferred.rs
  - src/model/lda_functional.rs
  - src/model/mgga_functional.rs
  - xtask/src/main.rs
  - xtask/src/generate_metadata.rs
  - Cargo.toml
autonomous: true
requirements: []
must_haves:
  truths:
    - "crates/libxc-core exists and carries model/meta/registry/input/output/layout/dims/error + the relocated deferred module"
    - "cargo tree -p libxc-core shows zero cubecl and zero libxc-kernel-* dependencies (SC-2)"
    - "the deferred id-registry lives in libxc-core (not kernel-math), preserving the lda::/mgga:: submodule shape, with both model consumers repointed"
    - "xtask writes its 7 generated outputs into crates/libxc-core/src/... and a regen is byte-identical to the Wave-0 snapshot"
    - "the meta::generated_propagation::PROPAGATION_RULES symbol is reachable cross-crate (survives xtask regen)"
  artifacts:
    - path: "crates/libxc-core/Cargo.toml"
      provides: "Leaf rlib manifest: bitflags + bytemuck + thiserror, NO cubecl, NO kernel deps"
      contains: "name = \"libxc-core\""
    - path: "crates/libxc-core/src/lib.rs"
      provides: "Module declarations for the 8 moved modules + deferred"
      contains: "pub mod deferred"
    - path: "crates/libxc-core/src/deferred.rs"
      provides: "Relocated deferred id-registry (lda::/mgga:: submodules)"
      contains: "pub mod lda"
  key_links:
    - from: "src/model/lda_functional.rs"
      to: "crate::deferred::lda::is_deferred"
      via: "crate-local call after the move (was libxc_kernel_math::deferred)"
      pattern: "crate::deferred::lda::is_deferred"
    - from: "xtask/src/generate_metadata.rs"
      to: "crates/libxc-core/src/meta/generated.rs"
      via: "root.join path-string prefix"
      pattern: "crates/libxc-core/src/meta/generated"
---

<objective>
Extract `crates/libxc-core` — the pure data layer (`model, meta, registry, input, output, layout, dims, error`) — out of the root crate, plus relocate the `deferred` id-registry from `libxc-kernel-math` into core (D-11) so core can stay CubeCL-free. Repoint the xtask generated-file write paths into core (D-03) and fix the one cross-crate visibility break (`PROPAGATION_RULES`). This is the leaf-most, highest-churn extraction — it goes first.

Purpose: `libxc-core` is the foundation both `libxc-eval` (10-02) and `libxc-compat` (10-03) depend on. Getting its boundary right (SC-2: zero cubecl/kernel deps) is the whole reason D-11 relocates `deferred` — `model/` calls `is_deferred`, and `kernel-math` pulls cubecl, so the call had to move to a cubecl-free home.

Output: A new `crates/libxc-core/` crate that `cargo tree` proves is cubecl-free, with the deferred module relocated, the 4 consumers repointed (2 model consumers this plan; verify's 2 in 10-03), xtask repointed, and the visibility fix landed regen-proof.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-workspace-level-modular-split/10-CONTEXT.md
@.planning/phases/10-workspace-level-modular-split/10-RESEARCH.md
@.planning/phases/10-workspace-level-modular-split/10-PATTERNS.md
@.planning/phases/10-workspace-level-modular-split/10-VALIDATION.md

# Analogs to copy from:
@crates/kernels/math/Cargo.toml
@crates/kernels/math/src/deferred.rs

<interfaces>
<!-- Live facts the executor needs — do NOT re-explore. -->

Root Cargo.toml deps to PARTITION INTO libxc-core (Cargo.toml:7,8,10 — NOT line 9 cubecl, NOT line 11 kernel-math):
  bitflags = "2.10.0"
  bytemuck = { version = "1.25.0", features = ["derive"] }
  thiserror = "2.0.18"

src/lib.rs:1-7 attrs (copy #![deny(warnings)]; the 3 clippy allows are for CubeCL macro expansion — core is cubecl-free, so copy them ONLY if a cargo check surfaces a lint).

deferred.rs shape (crates/kernels/math/src/deferred.rs — VERIFIED at HEAD):
  line 15:  pub mod lda  { ... pub fn is_deferred(id: u16) -> bool { ... } (line 77) }
  line 109: pub mod mgga { ... pub fn is_deferred(id: u16) -> bool { ... } (line 186) }
  Declared at crates/kernels/math/src/lib.rs:22 as `pub mod deferred;`

deferred consumers (VERIFIED live line numbers):
  src/model/lda_functional.rs:80   if libxc_kernel_math::deferred::lda::is_deferred(id.raw()) {
  src/model/lda_functional.rs:13   (doc comment referencing old path)
  src/model/mgga_functional.rs:43  use libxc_kernel_math::deferred::mgga::is_deferred as is_deferred_mgga;
  src/model/mgga_functional.rs:30  (doc comment referencing old path)
  (verify/tests/{lda,mgga}_oracle.rs are repointed in 10-03, after the facade exists)
  CONFIRMED: verify/ uses libxc_kernel_math:: ONLY for deferred (grep -v deferred = empty).

xtask write paths to PREFIX with `crates/libxc-core/` (VERIFIED):
  xtask/src/main.rs:291  root.join("src/meta/generated.rs")
  xtask/src/main.rs:329  root.join("src/registry/by_id.rs")
  xtask/src/main.rs:355  root.join("src/registry/by_name.rs")
  xtask/src/main.rs:387  root.join("src/registry/removed.rs")
  xtask/src/generate_metadata.rs:445  root.join("src/meta/generated.rs")
  xtask/src/generate_metadata.rs:595  root.join("src/meta/generated_hybrid.rs")
  xtask/src/generate_metadata.rs:643  root.join("src/meta/generated_propagation.rs")
  (find_output_root walking-up logic UNCHANGED — D-06a. xtask gets NO libxc-core dep — D-06.)

Visibility break (the ONLY cross-crate one — VERIFIED):
  src/meta/mod.rs:3                   pub(crate) mod generated_propagation;
  src/meta/generated_propagation.rs:8 pub(crate) const PROPAGATION_RULES: &[PropagationRule] = &[
  Consumed cross-crate by src/functional/lifecycle.rs:13 (moves to libxc-eval in 10-02).

NOTE on layout/: src/layout/ is a placeholder dir (NOT declared as a mod in src/lib.rs, zero
consumers — analogous to the dead src/workspace/). CONTEXT lists it in the core set, so it moves
to libxc-core as dead weight but is NOT declared in libxc-core/src/lib.rs (matching its current
undeclared state). Move the files via git mv; do not add a `pub mod layout;`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold libxc-core, relocate deferred (D-11), repoint the 2 model consumers + xtask paths (D-03)</name>
  <files>crates/libxc-core/Cargo.toml, crates/libxc-core/src/lib.rs, crates/libxc-core/src/deferred.rs, crates/kernels/math/src/lib.rs, crates/kernels/math/src/deferred.rs, src/model/lda_functional.rs, src/model/mgga_functional.rs, xtask/src/main.rs, xtask/src/generate_metadata.rs</files>
  <read_first>
    - crates/kernels/math/Cargo.toml (package-header + dep-shape analog)
    - crates/kernels/math/src/deferred.rs (the module being relocated — read its full lda::/mgga:: shape)
    - crates/kernels/math/src/lib.rs lines 1-23 (the `pub mod` skeleton analog + the line-22 `pub mod deferred;` to delete)
    - src/model/lda_functional.rs lines 1-90, src/model/mgga_functional.rs lines 1-50 (the 2 consumer call/use sites + doc comments)
    - xtask/src/main.rs lines 285-390 and xtask/src/generate_metadata.rs lines 440-650 (the 7 write-path strings)
    - 10-CONTEXT.md D-11 (deferred relocation rationale + the 4-consumer list + "preserve lda::/mgga:: shape")
    - 10-PATTERNS.md "crates/libxc-core/src/deferred*" section (the verbatim-move recipe + provenance-header update)
  </read_first>
  <action>
    Create the libxc-core crate skeleton, relocate `deferred` into it, repoint the 2 model consumers, and repoint xtask. Do this BEFORE moving the bulk modules so the crate exists to receive them.

    1. `mkdir -p crates/libxc-core/src`

    2. Write `crates/libxc-core/Cargo.toml` (model on crates/kernels/math/Cargo.toml:1-4; partition the 3 deps per D-14 — NO cubecl, NO kernel-math):
       ```toml
       [package]
       name = "libxc-core"
       version = "0.1.0"
       edition = "2024"

       [dependencies]
       bitflags = "2.10.0"
       bytemuck = { version = "1.25.0", features = ["derive"] }
       thiserror = "2.0.18"
       ```

    3. Relocate `deferred` (D-11) via git mv to preserve blame:
       `git mv crates/kernels/math/src/deferred.rs crates/libxc-core/src/deferred.rs`
       Then DELETE the declaration line `pub mod deferred;` (crates/kernels/math/src/lib.rs:22) from kernel-math's lib.rs.
       Update the `//!` provenance header at the top of `crates/libxc-core/src/deferred.rs` (lines 1-13): it currently says model "depends on it too, so this is the natural home." Phase 10 REVERSES that premise (model → core, core must NOT dep kernel-math). Rewrite the header to: "Relocated into libxc-core by Phase 10 (D-11): the model layer moved into this crate, and libxc-core must stay CubeCL-free (SC-2), so the deferred registry — pure metadata, not compute — moves here from libxc-kernel-math (which depends on cubecl)." PRESERVE the `pub mod lda { ... }` / `pub mod mgga { ... }` two-submodule structure and the `is_deferred(id: u16) -> bool` signatures VERBATIM (Pitfall 6).

    4. Repoint the 2 model consumers (they still live in root src/model/ at this point — that's fine, they reference `libxc_kernel_math::deferred` today which no longer exists, so they must change NOW or root won't compile). Since model/ moves into libxc-core in Task 2 and deferred is now a libxc-core sibling module, the target is `crate::deferred::...`:
       - src/model/lda_functional.rs:80 — change `libxc_kernel_math::deferred::lda::is_deferred(id.raw())` → `crate::deferred::lda::is_deferred(id.raw())`. Also fix the doc comment at :13.
       - src/model/mgga_functional.rs:43 — change `use libxc_kernel_math::deferred::mgga::is_deferred as is_deferred_mgga;` → `use crate::deferred::mgga::is_deferred as is_deferred_mgga;`. Also fix the doc comment at :30.

    5. Write `crates/libxc-core/src/lib.rs` skeleton (the 8 module decls will be satisfied once Task 2 moves the dirs; declare them now so the crate is coherent after the move). Add `deferred` now (its file already exists from step 3):
       ```rust
       #![deny(warnings)]

       pub mod model;
       pub mod meta;
       pub mod error;
       pub mod dims;
       pub mod registry;
       pub mod input;
       pub mod output;
       pub mod deferred;
       ```
       Do NOT declare `layout` (it is an undeclared placeholder dir today — keep that state). Do NOT add `pub use` item re-exports — those live in the ROOT facade (10-03), not core (SC-5).
       (NOTE: this lib.rs will not compile until Task 2 moves the module dirs in. That is expected and acceptable — Task 1 and Task 2 land in ONE commit per the bisectability rule. Do NOT run `cargo check -p libxc-core` between Task 1 and Task 2.)

    6. Repoint xtask write paths (D-03) — 7 string edits, prefix `crates/libxc-core/`, NO logic change (D-06a):
       - xtask/src/main.rs:291 → `root.join("crates/libxc-core/src/meta/generated.rs")`
       - xtask/src/main.rs:329 → `root.join("crates/libxc-core/src/registry/by_id.rs")`
       - xtask/src/main.rs:355 → `root.join("crates/libxc-core/src/registry/by_name.rs")`
       - xtask/src/main.rs:387 → `root.join("crates/libxc-core/src/registry/removed.rs")`
       - xtask/src/generate_metadata.rs:445 → `root.join("crates/libxc-core/src/meta/generated.rs")`
       - xtask/src/generate_metadata.rs:595 → `root.join("crates/libxc-core/src/meta/generated_hybrid.rs")`
       - xtask/src/generate_metadata.rs:643 → `root.join("crates/libxc-core/src/meta/generated_propagation.rs")`
  </action>
  <acceptance_criteria>
    - `grep -q 'name = "libxc-core"' crates/libxc-core/Cargo.toml`
    - `! grep -qE 'cubecl|libxc-kernel' crates/libxc-core/Cargo.toml` (NO cubecl, NO kernel deps in the manifest)
    - `test -f crates/libxc-core/src/deferred.rs && grep -q 'pub mod lda' crates/libxc-core/src/deferred.rs && grep -q 'pub mod mgga' crates/libxc-core/src/deferred.rs`
    - `! test -f crates/kernels/math/src/deferred.rs` (moved out)
    - `! grep -q 'pub mod deferred' crates/kernels/math/src/lib.rs` (declaration removed from kernel-math)
    - `grep -q 'crate::deferred::lda::is_deferred' src/model/lda_functional.rs` AND `! grep -q 'libxc_kernel_math::deferred' src/model/lda_functional.rs`
    - `grep -q 'crate::deferred::mgga::is_deferred' src/model/mgga_functional.rs` AND `! grep -q 'libxc_kernel_math::deferred' src/model/mgga_functional.rs`
    - `grep -q 'pub mod deferred;' crates/libxc-core/src/lib.rs`
    - `grep -c 'crates/libxc-core/src/' xtask/src/main.rs` returns 4 AND `grep -c 'crates/libxc-core/src/' xtask/src/generate_metadata.rs` returns 3
    - `! grep -qE 'root.join\("src/(meta|registry)/' xtask/src/main.rs xtask/src/generate_metadata.rs` (no un-prefixed paths remain)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'name = "libxc-core"' crates/libxc-core/Cargo.toml && grep -q 'pub mod lda' crates/libxc-core/src/deferred.rs && ! test -f crates/kernels/math/src/deferred.rs && [ "$(grep -c 'crates/libxc-core/src/' xtask/src/main.rs)" = 4 ] && [ "$(grep -c 'crates/libxc-core/src/' xtask/src/generate_metadata.rs)" = 3 ]</automated>
  </verify>
  <done>libxc-core scaffold + manifest exist; deferred relocated with lda::/mgga:: shape preserved; both model consumers repointed to crate::deferred; kernel-math's deferred declaration removed; xtask's 7 write paths prefixed.</done>
</task>

<task type="auto">
  <name>Task 2: git mv the 8 data modules into libxc-core, fix PROPAGATION_RULES visibility, wire root to dep libxc-core, verify SC-2</name>
  <files>crates/libxc-core/src/model/, crates/libxc-core/src/meta/, crates/libxc-core/src/registry/, crates/libxc-core/src/input/, crates/libxc-core/src/output/, crates/libxc-core/src/layout/, crates/libxc-core/src/dims/, crates/libxc-core/src/error/, crates/libxc-core/src/meta/mod.rs, Cargo.toml</files>
  <read_first>
    - src/meta/mod.rs lines 1-10 (the `pub(crate) mod generated_propagation;` to bump + where to add the re-export)
    - src/error/mod.rs lines 1-20 (confirm LibxcRsError imports only `crate::model::` types — zero import edits expected after the move)
    - Cargo.toml lines 6-12 (root dep block — to remove bitflags/bytemuck/thiserror/cubecl as direct root deps is a 10-03 concern; this plan ADDS libxc-core dep + makes root modules re-point)
    - 10-RESEARCH.md "Module Move Recipe" table + "Cross-crate visibility changes" + Pitfall 2
    - 10-PATTERNS.md "meta::generated_propagation visibility" section (option B — regen-proof re-export — is preferred)
  </read_first>
  <action>
    Move the 8 data modules into libxc-core, fix the visibility break regen-proof, and make root depend on libxc-core so root still compiles (root keeps `pub mod model;`-style facades temporarily via re-export — the FULL facade reduction is 10-03; here root just needs to compile with model/meta/etc. living in libxc-core).

    1. git mv the 8 module dirs (preserves blame). The 2 model consumer files edited in Task 1 move WITH model/:
       ```
       git mv src/model    crates/libxc-core/src/model
       git mv src/meta      crates/libxc-core/src/meta
       git mv src/registry  crates/libxc-core/src/registry
       git mv src/input     crates/libxc-core/src/input
       git mv src/output    crates/libxc-core/src/output
       git mv src/layout    crates/libxc-core/src/layout
       git mv src/dims      crates/libxc-core/src/dims
       git mv src/error     crates/libxc-core/src/error
       ```
       Delete the now-empty stubs in error/ if they are empty (D-01b): check `src/error/{ffi,internal,public}.rs` — wc -l each; if 2 lines / empty, `git rm` them and drop their `mod` decls from `crates/libxc-core/src/error/mod.rs`. If non-empty, leave them.

    2. Fix the PROPAGATION_RULES cross-crate visibility (Pitfall 2 — option B, regen-proof). In `crates/libxc-core/src/meta/mod.rs`:
       - Change line 3 `pub(crate) mod generated_propagation;` → `pub mod generated_propagation;`
       - Add a hand-written re-export so the symbol is reachable even if the generated file keeps `pub(crate)`:
         `pub use generated_propagation::PROPAGATION_RULES;`
       Rationale: the const itself is xtask-emitted (`generate_metadata.rs:643`); a `pub use` in the hand-written `meta/mod.rs` survives regen. (If you prefer option A — bumping the emitter token — you must also edit the `pub(crate)` that generate_metadata.rs writes for this const; option B is lower-risk and preferred.)

    3. Make root depend on libxc-core and re-point root's module declarations. In root `Cargo.toml [dependencies]` ADD:
       `libxc-core = { path = "crates/libxc-core" }`
       In root `src/lib.rs`, the moved modules can no longer be `pub mod model;` (the dirs are gone). Replace the moved-module declarations with re-exports of libxc-core so root and `src/api/`, `src/eval/`, `src/functional/`, `src/compat/` (still in root) keep their `crate::model::` etc. paths resolving. Because those sibling modules use `crate::model::`, the cleanest bridge that keeps them compiling is to add at the top of root `src/lib.rs`:
       ```rust
       pub use libxc_core::{model, meta, error, dims, registry, input, output};
       pub use libxc_core::deferred;
       ```
       and REMOVE the old `pub mod model; pub mod meta; pub mod error; pub mod dims; pub mod registry; pub mod input; pub mod output;` lines (lines 9-13, 16-17). Keep `pub mod math; pub mod kernel; pub mod eval; pub mod functional; pub mod api; pub mod compat;` for now (those move in 10-02/10-03).
       NOTE: sibling root modules referencing `crate::model::X` will now resolve via the `pub use libxc_core::model;` re-export (it makes `crate::model` a valid path). If any sibling uses `crate::error::PROPAGATION`-style deep `pub(crate)` items that DON'T cross cleanly, surface them — but the RESEARCH audit found only PROPAGATION_RULES crosses, and that's fixed in step 2.

    4. Verify SC-2 (cheap, no compile) and the per-`-p` core check:
       - `cargo tree -p libxc-core -e no-dev 2>&1 | tee log/10-01-tree-core.log`
       - `cargo check -p libxc-core --no-default-features -j1 2>&1 | tee log/10-01-libxc-core-check.log` (EXPECT EXIT 0 — light cubecl-free crate)
       - Optionally repoint+regen idempotency: run `cargo xtask generate-metadata` (or the relevant xtask subcommand) and diff the regenerated `crates/libxc-core/src/meta/generated*.rs` against `log/10-generated-snapshot/` via sha256 — they MUST match (D-03 byte-identity). If the xtask subcommand is heavy/unclear, defer this to the final verification and note it.
  </action>
  <acceptance_criteria>
    - `test -d crates/libxc-core/src/model -a -d crates/libxc-core/src/meta -a -d crates/libxc-core/src/error -a -d crates/libxc-core/src/registry`
    - `! test -d src/model && ! test -d src/meta && ! test -d src/error` (moved out of root)
    - `grep -q 'pub mod generated_propagation;' crates/libxc-core/src/meta/mod.rs` AND `grep -q 'pub use generated_propagation::PROPAGATION_RULES;' crates/libxc-core/src/meta/mod.rs`
    - `grep -q 'libxc-core = { path = "crates/libxc-core" }' Cargo.toml`
    - `grep -q 'pub use libxc_core::{' src/lib.rs` AND `! grep -qE '^pub mod model;' src/lib.rs`
    - `! cargo tree -p libxc-core -e no-dev | grep -qE 'cubecl|libxc-kernel'` (SC-2 — the canonical assertion)
    - `cargo check -p libxc-core --no-default-features -j1` EXIT 0 (logged to log/10-01-libxc-core-check.log)
  </acceptance_criteria>
  <verify>
    <automated>test -d crates/libxc-core/src/model && ! test -d src/model && grep -q 'pub use generated_propagation::PROPAGATION_RULES;' crates/libxc-core/src/meta/mod.rs && ! cargo tree -p libxc-core -e no-dev | grep -qE 'cubecl|libxc-kernel'</automated>
  </verify>
  <done>The 8 data modules live in libxc-core; PROPAGATION_RULES is cross-crate reachable regen-proof; root deps libxc-core and re-exports its modules; SC-2 holds (cargo tree shows zero cubecl/kernel-* in core); `cargo check -p libxc-core` EXIT 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure module relocation. No new external input, no new FFI surface, no new network/file ingress. The crate wall added (`libxc-core`) is a COMPILER boundary, not a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | xtask generated-file write path | mitigate | D-03 path edit lands in the SAME commit as the meta/registry move (Pitfall: a stray `cargo xtask` between move and edit writes stale orphans into empty root src/). Byte-diff regen vs Wave-0 snapshot proves no content tamper. |
| T-10-01-02 | Information disclosure | PROPAGATION_RULES visibility bump | accept | Bumping `pub(crate)` → `pub` exposes a static const data table (functional-id propagation rules) at the crate boundary. It is non-secret reference data already shipped in the binary; widening visibility within a single-repo pre-1.0 library adds no disclosure risk. |
| T-10-01-03 | Tampering | extern "C" ABI surface | accept | This plan does NOT touch compat/ or any extern "C" symbol — the libxc_rs.so/.a symbol surface is unaffected (it is produced by libxc-compat, extracted in 10-03). No exported symbol is dropped/renamed by a data-layer move. |
</threat_model>

<verification>
- SC-1 (partial): `crates/libxc-core/` exists with Cargo.toml + lib.rs + the 8 modules + deferred.rs.
- SC-2 (V-10-02): `! cargo tree -p libxc-core -e no-dev | grep -qE 'cubecl|libxc-kernel'` — the defining assertion for this plan.
- `cargo check -p libxc-core --no-default-features -j1` EXIT 0.
- Bisectability: at end-of-plan, `cargo check -p libxc_rs --lib -j1` should still be green (root now reaches model/meta/etc. via the libxc-core re-export). If root fails, the cause is a sibling module using a `pub(crate)` core item beyond PROPAGATION_RULES — surface it.
- D-03 idempotency: regenerated `crates/libxc-core/src/meta/generated*.rs` sha256 == Wave-0 `log/10-generated-snapshot/` (run if the xtask subcommand is light; else defer with note).
</verification>

<success_criteria>
libxc-core is a self-contained cubecl-free data crate (SC-2 proven via cargo tree); the deferred registry is relocated with its submodule shape intact; the 2 model consumers are repointed; xtask emits into core (D-03) idempotently; the lone visibility break is fixed regen-proof; root still compiles by re-exporting libxc-core modules.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workspace-level-modular-split/10-01-SUMMARY.md`
</output>
