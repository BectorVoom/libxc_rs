---
phase: 10-workspace-level-modular-split
plan: 02
type: execute
wave: 3
depends_on: ["10-01"]
files_modified:
  - crates/libxc-eval/Cargo.toml
  - crates/libxc-eval/src/lib.rs
  - crates/libxc-eval/src/eval/
  - crates/libxc-eval/src/functional/
  - crates/libxc-eval/src/kernel/
  - crates/libxc-eval/src/workspace/
  - Cargo.toml
  - src/lib.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "crates/libxc-eval exists and carries eval/functional/kernel/workspace, deps libxc-core one-way"
    - "all 306 libxc-kernel-* path-deps and the entire [features] oracle-{lda,gga,mgga} block live in libxc-eval/Cargo.toml (moved verbatim from root), with paths rewritten crates/kernels/ -> ../kernels/ and optional=true preserved on the 305 (math non-optional)"
    - "cargo tree -p libxc-eval shows libxc-core present and libxc-compat ABSENT (SC-3)"
    - "cargo tree -p libxc-eval --no-default-features shows ONLY libxc-core + libxc-kernel-math (no per-functional kernels leak)"
    - "[workspace] default-members includes crates/libxc-core + crates/libxc-eval, EXCLUDES crates/libxc-compat, preserving the 7-deferred-kernel exclusion comment verbatim"
  artifacts:
    - path: "crates/libxc-eval/Cargo.toml"
      provides: "Eval manifest: libxc-core + cubecl + bytemuck + 306 kernel deps + the [features] block"
      contains: "name = \"libxc-eval\""
    - path: "crates/libxc-eval/src/lib.rs"
      provides: "eval/functional/kernel/workspace module decls + 3 clippy allows"
      contains: "pub mod kernel"
  key_links:
    - from: "crates/libxc-eval/Cargo.toml [features] oracle-lda"
      to: "dep:libxc-kernel-lda_x (local eval dep)"
      via: "dep: feature activation resolving against eval's now-local kernel deps"
      pattern: "dep:libxc-kernel-lda_x"
    - from: "crates/libxc-eval/src/functional/lifecycle.rs"
      to: "libxc_core::meta::PROPAGATION_RULES"
      via: "cross-crate import of the 10-01 re-exported symbol"
      pattern: "use libxc_core::meta"
---

<objective>
Extract `crates/libxc-eval` — the orchestration layer (`eval, functional, kernel, workspace`) — out of root, and migrate the ENTIRE kernel-dependency + feature machinery (all 306 `libxc-kernel-*` deps + the `[features] oracle-{lda,gga,mgga}` block) from root `Cargo.toml` into `crates/libxc-eval/Cargo.toml` (D-10). Repoint the cross-crate `crate::meta::` references to `libxc_core::meta::`, and perform the `[workspace] default-members` surgery (add core+eval, exclude compat — D-10a).

Purpose: `libxc-eval` owns all compute orchestration and the 306 kernel deps. Its one-way dependency on `libxc-core` (and the ABSENCE of any `libxc-compat` dep) is SC-3 — the compiler wall that separates orchestration from FFI. The feature-machinery move is the single highest-risk edit in the whole phase: a kernel left non-optional or a dropped `dep:` prefix breaks the chunked-build memory safety that keeps verify off the OOM cliff.

Output: A `crates/libxc-eval/` crate whose `cargo tree` proves SC-3, with the full feature block relocated verbatim (paths rewritten `crates/kernels/` -> `../kernels/`), and default-members updated.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-workspace-level-modular-split/10-CONTEXT.md
@.planning/phases/10-workspace-level-modular-split/10-RESEARCH.md
@.planning/phases/10-workspace-level-modular-split/10-PATTERNS.md
@.planning/phases/10-workspace-level-modular-split/10-VALIDATION.md

<interfaces>
<!-- Live facts the executor needs — do NOT re-explore. -->

Root Cargo.toml layout (VERIFIED at HEAD):
  line 6:    [dependencies]
  line 7:    bitflags  (stays-for-now; full root shrink is 10-03)
  line 8:    bytemuck  -> ALSO needed by eval
  line 9:    cubecl = { version = "0.10.0", default-features = false, features = ["cpu"] }  -> eval
  line 10:   thiserror (stays-for-now)
  line 11:   libxc-kernel-math = { path = "crates/kernels/math" }   -> eval, NON-optional (no optional=true)
  lines 16-321: 305 per-functional kernel deps, each { path = "crates/kernels/...", optional = true }
  lines 323-645: [features] block:
      line 323: [features]  + comment block 324-330 (PRESERVE verbatim)
      line 331: default = ["oracle-lda", "oracle-gga", "oracle-mgga"]
      lines 333-377: oracle-lda  = [ "dep:libxc-kernel-..." x 43 ]
      lines 379-511: oracle-gga  = [ ... ]
      lines 513-645: oracle-mgga = [ ... ]   (incl. all _pK shard crates)
  Kernel-dep count (re-derive, do NOT hardcode): grep -cE "^libxc-kernel-" Cargo.toml  == 306

[workspace] block (VERIFIED):
  line 652: [workspace]
  lines 653-656: members = [ "xtask", "verify", "verify-canary", "libxc-sys" ]
  lines 657-661: comment block explaining the 7 D-11-deferred exclusion (PRESERVE VERBATIM)
  lines 662-932: default-members = [ "crates/kernels/math", <every per-functional subcrate
                 EXCEPT the 7 deferred> ]  (closing ] at line 932)

eval-side cross-crate imports to repoint (was crate::meta:: while in root; now libxc_core::):
  src/functional/lifecycle.rs:13  use crate::meta::generated_propagation::PROPAGATION_RULES;
    -> use libxc_core::meta::PROPAGATION_RULES;   (the 10-01 re-export)
  RESEARCH found exactly 5 `crate::meta::` refs from eval + functional; all resolve to libxc_core::meta
  symbols already pub (FunctionalMeta, HybridTerm) or the now-re-exported PROPAGATION_RULES.
  The 487 `crate::kernel` refs stay INTRA-crate (kernel/ moves with eval) — no edit.

NAMING TRAP (RESEARCH): two "workspace" modules. src/eval/workspace.rs (LIVE EvaluationWorkspace,
declared `pub mod workspace;` inside src/eval/mod.rs:13 — moves WITH eval/) AND src/workspace/
(top-level DEAD placeholder, zero consumers — moves to eval as dead weight, declared `pub mod workspace;`
in eval's lib.rs). Do NOT conflate or drop the live one.

3 clippy allows to COPY into eval's lib.rs (src/lib.rs:5-7) — eval owns the kernel/ #[cube] dispatch glue:
  #![allow(clippy::excessive_precision)]
  #![allow(clippy::needless_late_init)]
  #![allow(clippy::too_many_arguments)]

7 D-11-deferred kernels (kept as deps, excluded from default-members):
  mgga_c_b94, mgga_x_br89, mgga_x_mbr, mgga_x_mbrxc_bg, mgga_x_mbrxh_bg, mgga_x_mggac, mgga_x_br89_explicit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold libxc-eval, git mv the 4 orchestration modules, repoint crate::meta:: cross-crate refs</name>
  <files>crates/libxc-eval/Cargo.toml, crates/libxc-eval/src/lib.rs, crates/libxc-eval/src/eval/, crates/libxc-eval/src/functional/, crates/libxc-eval/src/kernel/, crates/libxc-eval/src/workspace/, src/lib.rs, Cargo.toml</files>
  <read_first>
    - src/lib.rs lines 1-21 (current attrs + the eval/functional/kernel/workspace mod decls + the 10-01 libxc_core re-exports)
    - src/eval/mod.rs lines 1-20 (the `pub mod workspace;` LIVE decl at :13 — moves with eval/)
    - src/functional/lifecycle.rs lines 1-20 (the `use crate::meta::generated_propagation::PROPAGATION_RULES;` at :13 to repoint)
    - crates/libxc-core/src/meta/mod.rs (confirm the `pub use generated_propagation::PROPAGATION_RULES;` re-export from 10-01)
    - 10-RESEARCH.md "Module Move Recipe" table + "api/ dual-dependency fact" + NAMING TRAP
    - 10-PATTERNS.md "crates/libxc-eval/src/lib.rs" section (the skeleton + 3-clippy-allows + naming trap)
  </read_first>
  <action>
    Create the eval crate skeleton (manifest deps minus the kernel block — that lands in Task 2), move the 4 modules, and repoint the 5 cross-crate meta refs.

    1. `mkdir -p crates/libxc-eval/src`

    2. Write the FIRST HALF of `crates/libxc-eval/Cargo.toml` (deps minus kernels; Task 2 appends the kernel block + features):
       ```toml
       [package]
       name = "libxc-eval"
       version = "0.1.0"
       edition = "2024"

       [dependencies]
       libxc-core = { path = "../libxc-core" }
       cubecl = { version = "0.10.0", default-features = false, features = ["cpu"] }
       bytemuck = { version = "1.25.0", features = ["derive"] }
       # libxc-kernel-math + the 305 optional per-functional deps + [features] block: added in Task 2
       ```

    3. git mv the 4 orchestration modules (preserve blame):
       ```
       git mv src/eval        crates/libxc-eval/src/eval
       git mv src/functional  crates/libxc-eval/src/functional
       git mv src/kernel      crates/libxc-eval/src/kernel
       git mv src/workspace   crates/libxc-eval/src/workspace
       ```

    4. Write `crates/libxc-eval/src/lib.rs` (copy ALL 3 clippy allows — eval owns the #[cube] dispatch glue):
       ```rust
       #![deny(warnings)]
       #![allow(clippy::excessive_precision)]
       #![allow(clippy::needless_late_init)]
       #![allow(clippy::too_many_arguments)]

       pub mod eval;
       pub mod functional;
       pub mod kernel;
       pub mod workspace;   // top-level placeholder (dead) — distinct from eval::workspace (live)
       ```

    5. Repoint the cross-crate meta references. The eval modules referenced `crate::meta::` while in root; now meta lives in libxc-core. Edit `crates/libxc-eval/src/functional/lifecycle.rs:13`:
       `use crate::meta::generated_propagation::PROPAGATION_RULES;` -> `use libxc_core::meta::PROPAGATION_RULES;`
       Then grep the moved eval+functional trees for any remaining `crate::(meta|model|registry|input|output|dims|error)::` and repoint each to `libxc_core::...`:
       `grep -rn "crate::\(meta\|model\|registry\|input\|output\|dims\|error\)" crates/libxc-eval/src/`
       For each hit, change `crate::` -> `libxc_core::`. This is the LARGEST hand-edit in the phase: ~435 cross-crate refs (measured live: ~244 `crate::model::`, ~158 `crate::error::`, ~11 `crate::input::`, ~11 `crate::output::`, ~8 `crate::dims::`, ~3 `crate::registry::`, plus the meta ref above) — NOT ~5. Repoint EVERY hit to `libxc_core::...`; do not stop early. (kernel-internal `crate::kernel::` refs — ~487 — stay UNCHANGED, they are intra-eval; intra-eval `crate::eval::`/`crate::functional::`/`crate::workspace::` also stay.)

    6. Make root depend on libxc-eval and repoint root's eval-module decls. In root `Cargo.toml [dependencies]` ADD:
       `libxc-eval = { path = "crates/libxc-eval", default-features = false }`
       (the `default-features = false` is LOAD-BEARING — Pitfall 1 — but the root feature-forward block is finalized in 10-03; for now this pin plus the temporary [features] block in Task 2 keep root resolvable.)
       In root `src/lib.rs`, REMOVE the moved-module decls `pub mod eval; pub mod functional; pub mod kernel; pub mod workspace;`. Replace with re-exports so root's `src/api/` (still local) and the facade keep resolving:
       ```rust
       pub use libxc_eval::{eval, functional, kernel, workspace};
       ```
       D-02 (delete math shim): `git rm src/math/mod.rs` (and `git rm -r src/math/` if the dir is otherwise empty) and REMOVE `pub mod math;` from root src/lib.rs. The Wave-0 surface snapshot found zero `libxc_rs::math` consumers; if it DID find one, instead add `pub use libxc_kernel_math as math;` — but none was found.
       root `src/api/` references `crate::eval::workspace::EvaluationWorkspace` + `crate::functional::Functional` — these now resolve via the `pub use libxc_eval::{eval, functional}` re-export. If api/ uses fully-qualified `crate::eval::...` paths that don't resolve through the re-export, repoint them to `libxc_eval::...` (the full api/ repoint is finalized in 10-03; here just keep root compiling).
  </action>
  <acceptance_criteria>
    - `grep -q 'name = "libxc-eval"' crates/libxc-eval/Cargo.toml`
    - `grep -q 'libxc-core = { path = "../libxc-core" }' crates/libxc-eval/Cargo.toml`
    - `test -d crates/libxc-eval/src/eval -a -d crates/libxc-eval/src/functional -a -d crates/libxc-eval/src/kernel -a -d crates/libxc-eval/src/workspace`
    - `! test -d src/eval && ! test -d src/functional && ! test -d src/kernel && ! test -d src/workspace` (moved out)
    - `grep -q 'pub mod kernel;' crates/libxc-eval/src/lib.rs` AND `grep -c 'allow(clippy::' crates/libxc-eval/src/lib.rs` returns 3
    - `grep -q 'use libxc_core::meta::PROPAGATION_RULES;' crates/libxc-eval/src/functional/lifecycle.rs`
    - `! grep -rqn 'crate::meta::generated_propagation' crates/libxc-eval/src/` (no stale crate::meta refs to the moved module)
    - `! grep -rqE 'crate::(meta|model|registry|input|output|dims|error)::' crates/libxc-eval/src/` (ALL ~435 cross-crate core-module refs repointed to libxc_core:: — zero remain under crate::; this is the single most error-prone edit in the phase, so it fails fast at THIS task rather than at the umbrella `cargo check -p libxc_rs --lib`)
    - `grep -q 'libxc-eval = { path = "crates/libxc-eval", default-features = false }' Cargo.toml`
    - `! grep -qE '^pub mod (eval|functional|kernel|workspace);' src/lib.rs` (root no longer declares the moved modules)
    - D-02: `! test -f src/math/mod.rs` AND `! grep -q 'pub mod math;' src/lib.rs`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'name = "libxc-eval"' crates/libxc-eval/Cargo.toml && test -d crates/libxc-eval/src/kernel && ! test -d src/kernel && grep -q 'use libxc_core::meta::PROPAGATION_RULES;' crates/libxc-eval/src/functional/lifecycle.rs && [ "$(grep -c 'allow(clippy::' crates/libxc-eval/src/lib.rs)" = 3 ] && ! test -f src/math/mod.rs</automated>
  </verify>
  <done>libxc-eval scaffold + first-half manifest exist; the 4 orchestration modules moved; cross-crate meta refs repointed to libxc_core::; root deps libxc-eval (default-features=false) and re-exports its modules; src/math/ deleted (D-02).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 306 kernel deps + [features] block to libxc-eval (paths ../kernels/), default-members surgery, verify SC-3</name>
  <files>crates/libxc-eval/Cargo.toml, Cargo.toml</files>
  <read_first>
    - Cargo.toml lines 11, 16-321 (the libxc-kernel-math non-optional line + the 305 optional kernel deps to move)
    - Cargo.toml lines 323-645 (the FULL [features] block incl. comment 324-330 to move verbatim)
    - Cargo.toml lines 652-932 (the [workspace] members + the 7-deferred comment + default-members to amend)
    - 10-RESEARCH.md "Feature-Forwarding Chain" (the migration steps + the failure-mode checklist table)
    - 10-PATTERNS.md "crates/libxc-eval/Cargo.toml" section (path-prefix rewrite recipe + verbatim feature-block move) AND "root Cargo.toml" section (members/default-members amend)
  </read_first>
  <action>
    Move the kernel dependency machinery. This is the highest-risk edit — every claim below is checkable with `cargo tree` (zero compile, zero OOM).

    1. Move the kernel deps from root `[dependencies]` to libxc-eval `[dependencies]`, rewriting paths `crates/kernels/` -> `../kernels/` (Pitfall 3: path-deps are manifest-relative). Uniform bulk transform:
       - The NON-optional math line (root:11) `libxc-kernel-math = { path = "crates/kernels/math" }` -> in eval: `libxc-kernel-math = { path = "../kernels/math" }` (KEEP non-optional — no `optional = true`).
       - The 305 optional lines (root:16-321), e.g. `libxc-kernel-gga_c_acgga = { path = "crates/kernels/gga/gga_c_acgga", optional = true }` -> in eval: `libxc-kernel-gga_c_acgga = { path = "../kernels/gga/gga_c_acgga", optional = true }` (KEEP `optional = true`).
       Mechanical approach: copy lines 11+16-321 out of root, run `s#path = "crates/kernels/#path = "../kernels/#` on the copied block, append to eval's `[dependencies]`, then DELETE those lines from root's `[dependencies]`. After: root `[dependencies]` has NO `libxc-kernel-*` line.

    2. Move the WHOLE `[features]` block (root:323-645) verbatim into libxc-eval/Cargo.toml (append after the deps). PRESERVE the comment block (324-330), `default = ["oracle-lda","oracle-gga","oracle-mgga"]`, and all three `oracle-*` lists with their `dep:libxc-kernel-*` entries UNCHANGED — the `dep:` references now resolve against eval's local kernel deps. Then DELETE the `[features]` block from root. To keep root resolvable across the 10-02 boundary, ADD a temporary root `[features]` re-forward block now (this is the SAME block 10-03 finalizes — idempotent):
       ```toml
       [features]
       default     = ["oracle-lda", "oracle-gga", "oracle-mgga"]
       oracle-lda  = ["libxc-eval/oracle-lda"]
       oracle-gga  = ["libxc-eval/oracle-gga"]
       oracle-mgga = ["libxc-eval/oracle-mgga"]
       ```

    3. `[workspace] default-members` surgery (D-10a). In root Cargo.toml:
       - APPEND `"crates/libxc-core"`, `"crates/libxc-eval"` to the `default-members` list (lines 662-932). Do NOT append `"crates/libxc-compat"` (EXCLUDED — its cdylib links all kernels -> OOM at jobs=1). Follow the existing list style/indentation.
       - PRESERVE the 7-deferred-kernel exclusion comment block (657-661) and the existing kernel enumeration VERBATIM.
       - `[workspace] members` (653-656): ADD `"crates/libxc-core"`, `"crates/libxc-eval"`. (compat is added in 10-03.) PRESERVE `verify-canary` (Pitfall 5). `default-members ⊆ members` must hold.

    4. Verify the feature-forwarding chain and SC-3 — ALL `cargo tree`, zero compile, zero OOM (log each):
       - SC-3 fwd: `cargo tree -p libxc-eval -e no-dev 2>&1 | tee log/10-02-tree-eval.log` — MUST contain `libxc-core`, MUST NOT contain `libxc-compat`.
       - eval --no-default leak check: `cargo tree -p libxc-eval -e no-dev --no-default-features 2>&1 | tee log/10-02-tree-eval-nodefault.log` — MUST show `libxc-kernel-math` and `libxc-core` ONLY; NO per-functional kernels.
       - root single-family forward: `cargo tree -p libxc_rs --no-default-features --features oracle-lda 2>&1 | tee log/10-02-tree-root-lda.log` — `grep -c libxc-kernel` ~= 43 (LDA + math).
       - root mgga forward: `cargo tree -p libxc_rs --no-default-features --features oracle-mgga | grep -c libxc-kernel` — MGGA count incl _pK shards (non-zero).
       - light per-`-p` check (single family, no OOM): `cargo check -p libxc-eval --no-default-features --features oracle-lda -j1 2>&1 | tee log/10-02-libxc-eval-lda-check.log` (EXPECT EXIT 0 — only LDA kernels + core + math compile). NEVER run `cargo check -p libxc-eval` with default features (pulls all 306 -> OOM).
  </action>
  <acceptance_criteria>
    - `[ "$(grep -cE '^libxc-kernel-' crates/libxc-eval/Cargo.toml)" = 306 ]` (all kernel deps moved; re-derive — matches root's prior 306)
    - `! grep -qE '^libxc-kernel-' Cargo.toml` (root [dependencies] has ZERO kernel lines)
    - `grep -q 'libxc-kernel-math = { path = "../kernels/math" }' crates/libxc-eval/Cargo.toml` (math non-optional, path rewritten)
    - `! grep -qE '^libxc-kernel-math.*optional' crates/libxc-eval/Cargo.toml` (math NOT optional)
    - `! grep -qE 'path = "crates/kernels/' crates/libxc-eval/Cargo.toml` (NO un-rewritten root-relative kernel paths leaked into eval)
    - `grep -q 'oracle-lda = \[' crates/libxc-eval/Cargo.toml` AND `grep -q 'dep:libxc-kernel-lda_x' crates/libxc-eval/Cargo.toml` (features block moved with dep: prefixes intact)
    - `! grep -qE '^oracle-lda = \[' Cargo.toml` (root no longer owns the dep: feature lists)
    - root re-forward present: `grep -qE 'oracle-lda *= *\["libxc-eval/oracle-lda"\]' Cargo.toml` AND `grep -qE 'oracle-mgga *= *\["libxc-eval/oracle-mgga"\]' Cargo.toml`
    - root eval-dep pin: `grep -q 'libxc-eval = { path = "crates/libxc-eval", default-features = false }' Cargo.toml`
    - default-members: `grep -q '"crates/libxc-eval"' Cargo.toml` AND `grep -q '"crates/libxc-core"' Cargo.toml` (both in members/default-members) ; `! grep -q '"crates/libxc-compat"' Cargo.toml` (compat NOT in default-members) ; 7-deferred comment preserved (`grep -q 'mgga_x_br89_explicit' Cargo.toml`)
    - SC-3: `cargo tree -p libxc-eval -e no-dev | grep -q libxc-core` AND `! cargo tree -p libxc-eval -e no-dev | grep -q libxc-compat`
    - leak check: `cargo tree -p libxc-eval -e no-dev --no-default-features | grep -q libxc-kernel-math` AND `! cargo tree -p libxc-eval -e no-dev --no-default-features | grep -q libxc-kernel-lda_x`
    - `cargo check -p libxc-eval --no-default-features --features oracle-lda -j1` EXIT 0
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -cE '^libxc-kernel-' crates/libxc-eval/Cargo.toml)" = 306 ] && ! grep -qE '^libxc-kernel-' Cargo.toml && grep -q 'libxc-kernel-math = { path = "../kernels/math" }' crates/libxc-eval/Cargo.toml && ! grep -qE 'path = "crates/kernels/' crates/libxc-eval/Cargo.toml && cargo tree -p libxc-eval -e no-dev | grep -q libxc-core && ! cargo tree -p libxc-eval -e no-dev | grep -q libxc-compat && cargo tree -p libxc-eval -e no-dev --no-default-features | grep -q libxc-kernel-math && ! cargo tree -p libxc-eval -e no-dev --no-default-features | grep -q libxc-kernel-lda_x</automated>
  </verify>
  <done>All 306 kernel deps + the full [features] block live in libxc-eval (paths rewritten ../kernels/, optional flags preserved, math non-optional); root [dependencies] has zero kernel lines and re-forwards oracle-* to eval; default-members includes core+eval and excludes compat with the 7-deferred comment preserved; SC-3 + the no-default-features leak check both pass via cargo tree; single-family eval check EXIT 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure module + dependency-manifest relocation. No new external input, FFI surface, or network/file ingress. The `libxc-eval` crate wall is a COMPILER boundary, not a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | DoS (self) | feature-forwarding chain / kernel-dep optionality | mitigate | A kernel left non-optional, a dropped `dep:` prefix, or a missing `default-features = false` re-activates all 306 kernels -> OOM SIGKILL. Mitigated by the `cargo tree --no-default-features` leak-check asserting only math+core resolve, plus the single-family `cargo check` gate. |
| T-10-02-02 | Tampering | path-dep rewrite (crates/kernels/ -> ../kernels/) | mitigate | A missed path rewrite makes Cargo fail to load the dependency source. Asserted by `! grep -qE 'path = "crates/kernels/' crates/libxc-eval/Cargo.toml` and the eval cargo tree resolving. |
| T-10-02-03 | Tampering | extern "C" ABI surface | accept | This plan does not touch compat/ or any extern "C" symbol; the libxc_rs.so/.a symbol surface (produced by libxc-compat, 10-03) is unaffected by an orchestration-layer move. |
</threat_model>

<verification>
- SC-1 (partial): `crates/libxc-eval/` exists with manifest + lib.rs + the 4 modules.
- SC-3 (V-10-03): `cargo tree -p libxc-eval -e no-dev | grep -q libxc-core` AND `! ... | grep -q libxc-compat`.
- Feature chain: `cargo tree -p libxc-eval -e no-dev --no-default-features` shows ONLY core + kernel-math; `cargo tree -p libxc_rs --no-default-features --features oracle-lda` shows ~43 LDA kernels.
- `cargo check -p libxc-eval --no-default-features --features oracle-lda -j1` EXIT 0.
- Bisectability: end-of-plan, `cargo check -p libxc_rs --lib -j1` should still be green (root reaches eval modules via re-export). If it fails, an api/ path didn't resolve through the re-export — repoint to `libxc_eval::...`.
</verification>

<success_criteria>
libxc-eval owns orchestration + all 306 kernel deps + the feature machinery (SC-3 proven via cargo tree: deps core, not compat); the no-default-features leak check confirms the family-chunked build stays memory-safe; default-members includes core+eval and excludes compat; root re-forwards oracle-* features and has zero direct kernel deps.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workspace-level-modular-split/10-02-SUMMARY.md`
</output>
