---
phase: 10-workspace-level-modular-split
plan: 03
type: execute
wave: 4
depends_on: ["10-02"]
files_modified:
  - crates/libxc-compat/Cargo.toml
  - crates/libxc-compat/src/lib.rs
  - crates/libxc-compat/src/c_layout.rs
  - crates/libxc-compat/src/errno.rs
  - crates/libxc-compat/src/ids.rs
  - crates/libxc-compat/src/legacy_eval.rs
  - crates/libxc-compat/src/macros.rs
  - crates/libxc-compat/src/raw_handle.rs
  - crates/libxc-compat/src/removed.rs
  - src/lib.rs
  - src/api/
  - Cargo.toml
  - verify/tests/lda_oracle.rs
  - verify/tests/mgga_oracle.rs
autonomous: false
requirements: []
must_haves:
  truths:
    - "crates/libxc-compat exists with [lib] crate-type = [rlib, cdylib, staticlib], deps libxc-core + libxc-eval; nothing depends on it (SC-4)"
    - "the root libxc_rs crate is a thin facade: pub mod api + curated pub use re-exports of libxc-core/libxc-eval preserving today's public surface line-for-line (SC-5)"
    - "the 4-link feature-forwarding chain resolves: verify -> libxc_rs -> libxc-eval -> libxc-kernel-*, with libxc-eval pinned default-features=false at the root"
    - "verify's deferred imports route through the facade (libxc_rs::deferred); verify needs zero other test-source changes"
    - "the extern C symbol surface of libxc_rs.so/.a is preserved (no dropped/renamed exported symbols)"
  artifacts:
    - path: "crates/libxc-compat/Cargo.toml"
      provides: "cdylib/staticlib manifest, deps core+eval+thiserror"
      contains: "crate-type = [\"rlib\", \"cdylib\", \"staticlib\"]"
    - path: "src/lib.rs"
      provides: "Thin facade: pub mod api + curated re-exports"
      contains: "pub use libxc_eval::eval::{dispatch_lda"
  key_links:
    - from: "verify/Cargo.toml libxc_rs/oracle-lda"
      to: "libxc-eval/oracle-lda -> dep:libxc-kernel-*"
      via: "root facade [features] re-forward (oracle-lda = [libxc-eval/oracle-lda]) + libxc-eval default-features=false pin"
      pattern: "oracle-lda = \\[\"libxc-eval/oracle-lda\"\\]"
    - from: "verify/tests/lda_oracle.rs"
      to: "libxc_rs::deferred::lda::is_deferred"
      via: "root facade pub use libxc_core::deferred"
      pattern: "libxc_rs::deferred::lda::is_deferred"
---

<objective>
Close the phase: extract `crates/libxc-compat` (the extern "C" shim, cdylib/staticlib — D-07/D-08), reduce the root `libxc_rs` crate to a thin facade that preserves today's public surface line-for-line (SC-5), finalize and prove the 4-link feature-forwarding chain end-to-end, repoint verify's deferred imports through the facade, and run the USER-RUN per-family oracle parity gate (SC-6/SC-7).

Purpose: This is the seam that makes the whole layering observable from outside. SC-4 (nothing depends on libxc-compat) is the FFI-isolation wall; SC-5 (root surface unchanged) is the downstream-compatibility contract; the feature chain is the make-or-break memory-safety mechanic (`default-features = false` on the root's libxc-eval dep — omit it and all 306 kernels activate -> OOM). The extern "C" ABI surface of `libxc_rs.so`/`.a` must survive the move byte-for-byte (no dropped/renamed symbols).

Output: A `crates/libxc-compat/` cdylib crate (SC-4 proven via inverted cargo tree), a thin root facade (SC-5 proven via surface diff + per-`-p` check), the finalized feature chain, and the USER-RUN oracle parity verdict (SC-6/SC-7).
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-workspace-level-modular-split/10-CONTEXT.md
@.planning/phases/10-workspace-level-modular-split/10-RESEARCH.md
@.planning/phases/10-workspace-level-modular-split/10-PATTERNS.md
@.planning/phases/10-workspace-level-modular-split/10-VALIDATION.md

<interfaces>
<!-- Live facts the executor needs — do NOT re-explore. -->

src/compat/ contents (move as a unit — Phase 6 already partitioned correctly):
  c_layout.rs, errno.rs, ids.rs, legacy_eval.rs, macros.rs, mod.rs, raw_handle.rs, removed.rs
  src/compat/mod.rs declares: pub mod {c_layout, errno, ids, legacy_eval, macros, raw_handle, removed};
  raw_handle.rs:11 uses `crate::compat::c_layout::{...}` (intra-compat — becomes `crate::c_layout::` after re-root).

D-07 libxc-compat Cargo.toml (CONTEXT specifics 254-269):
  [package] name = "libxc-compat", version = "0.1.0", edition = "2024"
  [lib] crate-type = ["rlib", "cdylib", "staticlib"]   # NO name override -> libxc_rs.so/.a/.rlib (D-08); NO [bin]
  [dependencies] libxc-core = { path = "../libxc-core" }; libxc-eval = { path = "../libxc-eval" }; thiserror = "2.0.18"

D-09a header: Phase 6 has NOT committed crates/libxc-compat/include/xc_rs.h (VERIFIED: no xc_rs.h
in tree; only libxc-sys build-output xc.h from libxc-master). Phase 6's 06-02a/02b/03 are NOT
executed (ROADMAP: Phase 6 = 1/4 plans). Per D-09a, Phase 10 does NOT block on writing the header —
it ships ONLY if Phase 6 produced it first. So: do NOT hand-write xc_rs.h in this plan; note it as
a Phase-6 deliverable in the SUMMARY.

Root facade public surface to preserve (src/lib.rs:23-38, VERIFIED — strategy (a) line-for-line):
  pub use model::{Family, Kind, Spin, DerivativeOrder, FunctionalId, FunctionalFlags,
      HybridType, HybridTermKind, Dimensionality, Thresholds, LdaFunctional, GgaFunctional, MggaFunctional};
  pub use meta::{FunctionalMeta, Reference, ExtParamSpec, HybridTerm};
  pub use error::LibxcRsError;
  pub use dims::Dimensions;
  pub use registry::{lookup_by_id, lookup_by_name, functional_count, version, version_string};
  pub use input::{LdaInput, GgaInput, MggaInput};
  pub use output::{LdaOutput, GgaOutput, MggaOutput, OutputMask};
  pub use eval::{dispatch_lda, dispatch_gga, dispatch_mgga};
  pub use functional::{classify_hybrid, CamCoefficients, Functional, FunctionalParams, NlcCoefficients, NoParams};
  pub use api::{BatchEvaluator, EvaluateInput, FunctionalBuilder};

api/ dual-dependency (src/api/ VERIFIED refs): crate::error/model/input/output/registry (->libxc_core::)
  + crate::eval::workspace::EvaluationWorkspace + crate::functional::Functional (->libxc_eval::).
  ZERO crate::compat refs -> root needs libxc-compat dep ONLY for the surface `pub use libxc_compat as compat;`.

verify deferred imports to repoint (4th consumer pair from D-11; VERIFIED):
  verify/tests/lda_oracle.rs:36   use libxc_kernel_math::deferred::lda::is_deferred;
    -> use libxc_rs::deferred::lda::is_deferred;
  verify/tests/mgga_oracle.rs:44  use libxc_kernel_math::deferred::mgga::is_deferred as is_deferred_mgga;
    -> use libxc_rs::deferred::mgga::is_deferred as is_deferred_mgga;
  (requires root facade: `pub use libxc_core::deferred;`)
  verify uses libxc_kernel_math:: ONLY for deferred (grep -v deferred = empty, VERIFIED) -> the
  verify/Cargo.toml:41 `libxc-kernel-math` dev-dep becomes droppable; it is still pulled transitively
  via libxc_rs, so leaving it is harmless. RECOMMEND leave it (conservative) + note in SUMMARY.

Feature-forward chain (finalize at root): verify says libxc_rs/oracle-lda; root must forward to
  libxc-eval/oracle-lda AND pin libxc-eval default-features=false (Pitfall 1). 10-02 already added
  the temporary root [features] block + the pin — this plan CONFIRMS them and proves the full chain.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract libxc-compat (cdylib/staticlib), reduce root to thin facade, finalize the feature chain</name>
  <files>crates/libxc-compat/Cargo.toml, crates/libxc-compat/src/lib.rs, crates/libxc-compat/src/c_layout.rs, crates/libxc-compat/src/errno.rs, crates/libxc-compat/src/ids.rs, crates/libxc-compat/src/legacy_eval.rs, crates/libxc-compat/src/macros.rs, crates/libxc-compat/src/raw_handle.rs, crates/libxc-compat/src/removed.rs, src/lib.rs, src/api/, Cargo.toml</files>
  <read_first>
    - src/compat/mod.rs (the `pub mod` list to re-root into libxc-compat/src/lib.rs)
    - src/compat/raw_handle.rs lines 1-15 (the `crate::compat::c_layout::` ref to fix to `crate::c_layout::`)
    - src/api/batch.rs, src/api/builder.rs, src/api/evaluate.rs (the crate::error/model/input/output/registry/eval/functional refs to repoint)
    - src/lib.rs lines 1-39 (the current facade surface + the 10-01/10-02 re-exports already added)
    - Cargo.toml lines 6-12 (root deps to shrink) + the 10-02 root [features] re-forward block + libxc-eval pin
    - 10-PATTERNS.md "crates/libxc-compat/Cargo.toml" + "crates/libxc-compat/src/lib.rs" + "src/lib.rs (root facade)" + "root Cargo.toml" sections
    - 10-RESEARCH.md "Public Surface Preservation" + "api/ dual-dependency fact" + Pitfall 1
  </read_first>
  <action>
    Extract compat, finalize the root facade, shrink root deps, and confirm the feature chain.

    1. Scaffold libxc-compat and move the compat module as a unit:
       `mkdir -p crates/libxc-compat/src`
       `git mv src/compat/c_layout.rs src/compat/errno.rs src/compat/ids.rs src/compat/legacy_eval.rs src/compat/macros.rs src/compat/raw_handle.rs src/compat/removed.rs crates/libxc-compat/src/`
       Turn `src/compat/mod.rs` into the crate root: `git mv src/compat/mod.rs crates/libxc-compat/src/lib.rs` then prepend the attrs and adjust. Final `crates/libxc-compat/src/lib.rs`:
       ```rust
       #![deny(warnings)]
       #![allow(clippy::excessive_precision)]
       #![allow(clippy::needless_late_init)]
       #![allow(clippy::too_many_arguments)]

       pub mod c_layout;
       pub mod errno;
       pub mod ids;
       pub mod legacy_eval;
       pub mod macros;
       pub mod raw_handle;
       pub mod removed;
       ```
       Then `rmdir src/compat` (now empty).

    2. Re-root intra-compat paths: the compat files used `crate::compat::X` while in root; now compat IS the crate, so `crate::compat::c_layout::` -> `crate::c_layout::`. Grep + fix:
       `grep -rn "crate::compat::" crates/libxc-compat/src/` -> change each `crate::compat::` to `crate::`.
       The compat files also reference core/eval types (`crate::error::`, `crate::model::`, `crate::eval::`, `crate::functional::`, `crate::registry::`) — repoint each cross-crate ref to `libxc_core::` or `libxc_eval::` respectively:
       `grep -rn "crate::\(error\|model\|input\|output\|dims\|registry\|meta\)" crates/libxc-compat/src/` -> `libxc_core::`
       `grep -rn "crate::\(eval\|functional\|kernel\|workspace\)" crates/libxc-compat/src/` -> `libxc_eval::`

    3. Write `crates/libxc-compat/Cargo.toml` (D-07/D-08):
       ```toml
       [package]
       name = "libxc-compat"
       version = "0.1.0"
       edition = "2024"

       [lib]
       crate-type = ["rlib", "cdylib", "staticlib"]
       # default name "libxc_rs" -> libxc_rs.so / libxc_rs.a / libxc_rs.rlib (D-08); NO [bin] (D-07)

       [dependencies]
       libxc-core = { path = "../libxc-core" }
       libxc-eval = { path = "../libxc-eval" }
       thiserror = "2.0.18"
       ```

    4. Reduce root `src/lib.rs` to the thin facade (SC-5, strategy (a) line-for-line). The 10-01/10-02 steps already added `pub use libxc_core::{...}` and `pub use libxc_eval::{...}` module re-exports; FINALIZE the full surface. Final root src/lib.rs:
       ```rust
       #![deny(warnings)]
       #![allow(clippy::excessive_precision)]
       #![allow(clippy::needless_late_init)]
       #![allow(clippy::too_many_arguments)]

       pub mod api;   // stays LOCAL

       // module-path surface (these paths are PART of the API)
       pub use libxc_core::{model, meta, error, dims, registry, input, output};
       pub use libxc_eval::{eval, functional, kernel, workspace};
       pub use libxc_core::deferred;     // NEW (D-11) — verify *_oracle.rs route through this
       pub use libxc_compat as compat;   // preserves libxc_rs::compat::* (RESEARCH Open Q 1)

       // item re-exports — line-for-line from old src/lib.rs:23-38, repointed
       pub use libxc_core::model::{
           Family, Kind, Spin, DerivativeOrder, FunctionalId, FunctionalFlags,
           HybridType, HybridTermKind, Dimensionality, Thresholds,
           LdaFunctional, GgaFunctional, MggaFunctional,
       };
       pub use libxc_core::meta::{FunctionalMeta, Reference, ExtParamSpec, HybridTerm};
       pub use libxc_core::error::LibxcRsError;
       pub use libxc_core::dims::Dimensions;
       pub use libxc_core::registry::{lookup_by_id, lookup_by_name, functional_count, version, version_string};
       pub use libxc_core::input::{LdaInput, GgaInput, MggaInput};
       pub use libxc_core::output::{LdaOutput, GgaOutput, MggaOutput, OutputMask};
       pub use libxc_eval::eval::{dispatch_lda, dispatch_gga, dispatch_mgga};
       pub use libxc_eval::functional::{
           classify_hybrid, CamCoefficients, Functional, FunctionalParams, NlcCoefficients, NoParams,
       };
       pub use api::{BatchEvaluator, EvaluateInput, FunctionalBuilder};
       ```
       (OMIT `pub use libxc_kernel_math as math;` — D-02 deleted src/math, Wave-0 found zero consumers.)

    5. Repoint `src/api/` (it stays local but its `crate::` paths now break). In src/api/{batch,builder,evaluate}.rs:
       - `crate::error::` / `crate::model::` / `crate::input::` / `crate::output::` / `crate::registry::` -> `libxc_core::...`
       - `crate::eval::workspace::EvaluationWorkspace` -> `libxc_eval::eval::workspace::EvaluationWorkspace`; `crate::functional::Functional` -> `libxc_eval::functional::Functional`
       (Alternatively, api/ can use the root facade's own `crate::model::` re-exports since they resolve through `pub use libxc_core::model;` — but the explicit `libxc_core::`/`libxc_eval::` form is unambiguous. Pick whichever compiles; prefer explicit.)

    6. Shrink root `[dependencies]` to the facade set (cubecl/bitflags/bytemuck/thiserror are no longer direct root deps — they live in the leaf crates). Final root `[dependencies]`:
       ```toml
       [dependencies]
       libxc-core   = { path = "crates/libxc-core" }
       libxc-eval   = { path = "crates/libxc-eval", default-features = false }
       libxc-compat = { path = "crates/libxc-compat" }
       ```
       (Remove the standalone `bitflags`/`bytemuck`/`cubecl`/`thiserror` root dep lines — they were only needed by the modules that now live in leaf crates. If a root-local item — api/ or the 3-line src/main.rs — still needs one of them directly, keep just that one; api/ does not, per the verified refs.)
       CONFIRM the root `[features]` re-forward block (added in 10-02) is present and correct:
       ```toml
       [features]
       default     = ["oracle-lda", "oracle-gga", "oracle-mgga"]
       oracle-lda  = ["libxc-eval/oracle-lda"]
       oracle-gga  = ["libxc-eval/oracle-gga"]
       oracle-mgga = ["libxc-eval/oracle-mgga"]
       ```

    7. Add libxc-compat to `[workspace] members` (D-10b) — but NOT to default-members (D-10a, OOM). PRESERVE verify-canary + libxc-sys + the 7-deferred comment.
       `[workspace] members = ["xtask", "verify", "verify-canary", "libxc-sys", "crates/libxc-core", "crates/libxc-eval", "crates/libxc-compat"]`

    8. src/main.rs disposition (planner discretion -> delete, cleaner): `git rm src/main.rs` (3-line "Hello, world!" vestige; if it references a moved type, deleting is simplest). If deleting causes a `[[bin]]`-implicit issue, leave it and note in SUMMARY.

    9. Verify SC-4 + SC-5 + the feature chain (cheap + one per-`-p` check):
       - SC-4 fwd: `cargo tree -p libxc-compat -e no-dev 2>&1 | tee log/10-03-tree-compat.log` — MUST show `libxc-core` AND `libxc-eval`.
       - SC-4 inverted: `cargo tree -p libxc-compat --invert -e no-dev 2>&1 | tee log/10-03-tree-compat-inv.log` — MUST show ONLY libxc-compat itself (nothing depends on it).
       - feature chain (root forward): `cargo tree -p libxc_rs --no-default-features --features oracle-lda 2>&1 | tee log/10-03-tree-root-lda.log` — LDA kernels resolve; `cargo tree -p libxc_rs --no-default-features | grep -c libxc-kernel` should show ONLY math (no per-functional) confirming the `default-features=false` pin holds (Pitfall 1).
       - SC-5 surface diff: `grep -rhoE "use libxc_rs::[A-Za-z0-9_:]+" verify/ verify-canary/ tests/ examples/ 2>/dev/null | sort -u > log/10-surface-after.log` then `diff log/10-surface-before.log log/10-surface-after.log` (expect EMPTY diff — the facade preserves every path; verify's deferred repoint in Task 2 ADDS `libxc_rs::deferred::...`, so run this AFTER Task 2 or accept the deferred-path delta as the only expected addition).
       - SC-5 per-`-p`: `cargo check -p libxc_rs --lib -j1 2>&1 | tee log/10-03-check-libxc_rs.log` (umbrella, ~536 MB — NOT --workspace; EXPECT EXIT 0).
  </action>
  <acceptance_criteria>
    - `grep -q 'crate-type = \["rlib", "cdylib", "staticlib"\]' crates/libxc-compat/Cargo.toml`
    - `grep -q 'name = "libxc-compat"' crates/libxc-compat/Cargo.toml` AND `! grep -q '^name = ' crates/libxc-compat/Cargo.toml | grep -i 'lib'` (no [lib] name override -> default libxc_rs)
    - `grep -q 'libxc-core = { path = "../libxc-core" }' crates/libxc-compat/Cargo.toml` AND `grep -q 'libxc-eval = { path = "../libxc-eval" }' crates/libxc-compat/Cargo.toml`
    - `test -f crates/libxc-compat/src/lib.rs -a -f crates/libxc-compat/src/errno.rs -a -f crates/libxc-compat/src/raw_handle.rs` AND `! test -d src/compat`
    - `! grep -rq 'crate::compat::' crates/libxc-compat/src/` (intra-compat paths re-rooted)
    - root facade: `grep -q 'pub use libxc_eval::eval::{dispatch_lda' src/lib.rs` AND `grep -q 'pub use libxc_core::error::LibxcRsError;' src/lib.rs` AND `grep -q 'pub use libxc_core::deferred;' src/lib.rs` AND `grep -q 'pub use libxc_compat as compat;' src/lib.rs`
    - root deps shrunk: `! grep -qE '^bitflags|^cubecl' Cargo.toml` (no longer direct root deps) AND `grep -q 'libxc-compat = { path = "crates/libxc-compat" }' Cargo.toml`
    - feature re-forward present: `grep -qE 'oracle-lda *= *\["libxc-eval/oracle-lda"\]' Cargo.toml` AND root eval pin `grep -q 'libxc-eval = { path = "crates/libxc-eval", default-features = false }' Cargo.toml`
    - members: `grep -q '"crates/libxc-compat"' Cargo.toml` (in members) AND `grep -q '"verify-canary"' Cargo.toml` (preserved)
    - SC-4: `cargo tree -p libxc-compat -e no-dev | grep -q libxc-core` AND `cargo tree -p libxc-compat -e no-dev | grep -q libxc-eval` AND `[ "$(cargo tree -p libxc-compat --invert -e no-dev | grep -c 'libxc-compat')" -le 1 ]` (nothing else depends on it)
    - SC-1: `cargo metadata --no-deps --format-version 1 | grep -o '"name":"libxc-[a-z]*"' | sort -u` shows libxc-compat, libxc-core, libxc-eval
    - SC-5: `cargo check -p libxc_rs --lib -j1` EXIT 0 (logged)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'crate-type = \["rlib", "cdylib", "staticlib"\]' crates/libxc-compat/Cargo.toml && ! test -d src/compat && grep -q 'pub use libxc_core::deferred;' src/lib.rs && grep -q 'libxc-compat = { path = "crates/libxc-compat" }' Cargo.toml && cargo tree -p libxc-compat -e no-dev | grep -q libxc-core && cargo tree -p libxc-compat -e no-dev | grep -q libxc-eval && [ "$(cargo tree -p libxc-compat --invert -e no-dev | grep -c 'libxc-compat')" -le 1 ]</automated>
  </verify>
  <done>libxc-compat exists as a cdylib/staticlib crate depending on core+eval with nothing depending on it (SC-4); root is a thin facade preserving the public surface line-for-line (SC-5); root deps shrunk to the 3 leaf crates; the feature re-forward chain + default-features=false pin are in place; `cargo check -p libxc_rs --lib` EXIT 0.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint verify deferred imports through the facade; confirm SC-5 surface diff is clean</name>
  <files>verify/tests/lda_oracle.rs, verify/tests/mgga_oracle.rs</files>
  <read_first>
    - verify/tests/lda_oracle.rs lines 1-40 (the `use libxc_kernel_math::deferred::lda::is_deferred;` at :36 + doc at :14)
    - verify/tests/mgga_oracle.rs lines 1-50 (the `use libxc_kernel_math::deferred::mgga::is_deferred as is_deferred_mgga;` at :44)
    - verify/Cargo.toml lines 13-41 (the `libxc_rs default-features=false`, oracle-* forwards, kernel-math dev-dep)
    - src/lib.rs (confirm the `pub use libxc_core::deferred;` facade re-export from Task 1)
    - 10-RESEARCH.md "Code Examples — D-11 deferred relocation consumer repoint" + Open Question 2
  </read_first>
  <action>
    Repoint verify's deferred imports (the 4th D-11 consumer pair) through the root facade — this is the change that makes verify need ZERO other test-source edits (it routes everything else through `libxc_rs::eval::dispatch_*` / `libxc_rs::kernel::...`, all preserved by the facade in Task 1).

    1. verify/tests/lda_oracle.rs:36 — `use libxc_kernel_math::deferred::lda::is_deferred;` -> `use libxc_rs::deferred::lda::is_deferred;`. Also fix the doc comment at :14 (references the old `libxc_kernel_lda::deferred::is_deferred` path) for accuracy.
    2. verify/tests/mgga_oracle.rs:44 — `use libxc_kernel_math::deferred::mgga::is_deferred as is_deferred_mgga;` -> `use libxc_rs::deferred::mgga::is_deferred as is_deferred_mgga;`.
    3. verify/Cargo.toml:41 `libxc-kernel-math` dev-dep: verify uses `libxc_kernel_math::` ONLY for deferred (VERIFIED grep). After the repoint it's unused directly, BUT it is still pulled transitively via libxc_rs and is harmless. RECOMMEND: leave it in place (conservative — removing it risks a surprise if any other test reaches a math symbol) and note in the SUMMARY that it is now droppable. Do NOT expand verify's curated kernel dev-dep subset (OOM — D-12a).
    4. SC-5 surface diff (finalize): `grep -rhoE "use libxc_rs::[A-Za-z0-9_:]+" verify/ verify-canary/ tests/ examples/ 2>/dev/null | sort -u > log/10-surface-after.log` ; `diff log/10-surface-before.log log/10-surface-after.log 2>&1 | tee log/10-03-surface-diff.log`. The ONLY expected delta is the NEW `libxc_rs::deferred::...` lines (the deferred import was `libxc_kernel_math::...` before, now `libxc_rs::deferred::...`) — that is an intentional D-11 consequence, not a surface regression. Every OTHER `use libxc_rs::` path must be unchanged.
  </action>
  <acceptance_criteria>
    - `grep -q 'use libxc_rs::deferred::lda::is_deferred;' verify/tests/lda_oracle.rs` AND `! grep -q 'libxc_kernel_math::deferred' verify/tests/lda_oracle.rs`
    - `grep -q 'use libxc_rs::deferred::mgga::is_deferred' verify/tests/mgga_oracle.rs` AND `! grep -q 'libxc_kernel_math::deferred' verify/tests/mgga_oracle.rs`
    - `test -f log/10-surface-after.log` AND the diff vs log/10-surface-before.log shows ONLY `libxc_rs::deferred` additions (no removals of any pre-existing `use libxc_rs::` path) — inspect log/10-03-surface-diff.log
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'use libxc_rs::deferred::lda::is_deferred;' verify/tests/lda_oracle.rs && ! grep -q 'libxc_kernel_math::deferred' verify/tests/lda_oracle.rs && grep -q 'use libxc_rs::deferred::mgga::is_deferred' verify/tests/mgga_oracle.rs && test -f log/10-surface-after.log</automated>
  </verify>
  <done>verify's 2 deferred imports route through libxc_rs::deferred (facade); SC-5 surface diff shows only the intentional deferred-path delta; verify needs no other test-source changes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: USER-RUN per-family oracle parity gate (SC-6 + SC-7)</name>
  <what-built>
    Tasks 1-2 completed the layering: libxc-compat extracted, root reduced to a facade, the feature chain finalized, verify repointed. The cheap boundary SCs (1/2/3/4) and SC-5 (surface + per-`-p` check) are already proven via `cargo tree` and `cargo check -p libxc_rs --lib`. What remains is SC-6 (test parity vs the Wave-0 baseline) and SC-7 (1e-12 oracle parity on the curated LDA/GGA/MGGA witness). These require running the verify/ oracle suite per family at `-j1` — MEMORY-HEAVY, multi-minute builds that OOM if run as `--workspace` or with multiple families at once. They are USER-RUN; the executor must NOT attempt them on this 30 GB box.
  </what-built>
  <how-to-verify>
    Run each command SEPARATELY (one family, `-j1`), tee to its log, and diff the pass/fail set against the Wave-0 baseline (`log/10-baseline-parity-{lda,gga,mgga}.log`). NEVER run `--workspace` and never combine families.

    1. LDA:  `cargo test -p libxc_rs-verify --no-default-features --features oracle-lda --test lda_oracle -j1 2>&1 | tee log/10-final-parity-lda.log`
    2. GGA:  `cargo test -p libxc_rs-verify --no-default-features --features oracle-gga --test gga_oracle -j1 2>&1 | tee log/10-final-parity-gga.log`
    3. MGGA: `cargo test -p libxc_rs-verify --no-default-features --features oracle-mgga --test mgga_oracle -j1 2>&1 | tee log/10-final-parity-mgga.log`

    Optional light non-regression (executor MAY run these — they are per-`-p`, not workspace):
       `cargo test -p libxc-core -j1` and `cargo check -p libxc-eval --no-default-features --features oracle-lda -j1`.

    PASS CONDITION (SC-6 + SC-7): the post-refactor per-family pass/fail set must MATCH the Wave-0 baseline EXACTLY:
    - LDA: all pass (1e-12). GGA: all pass (1e-12).
    - MGGA: the SAME 6 expected Phase-12 failures (mgga_x_th ~2e-1, mgga_x_2d_js17 ~1.1e-2, mgga_c_cs ~9.2e-3, mgga_x_pkzb ~3.7e-3, mgga_x_pbe_gx ~1.5e-3, mgga_x_tm ~9.2e-4) + the SAME 3 math-precision failures — and NOTHING ELSE.
    A NEW failure (anything not in the Wave-0 baseline) = a refactor regression -> STOP and report it (do NOT approve). A previously-failing test now PASSING is also a discrepancy worth noting but is not a blocker.
    The curated witness subset (do NOT expand — OOM): LDA lda_x/lda_c_pw/lda_xc_teter93; GGA gga_x_pbe/gga_c_pbe/gga_x_b88; MGGA mgga_x_lta/tpss/pkzb/th + worst-case revtpss/kcisk/b94/r4scan/br89_explicit/b97mv.
  </how-to-verify>
  <resume-signal>Type "approved" once the three per-family logs exist and the pass/fail set matches the Wave-0 baseline exactly (LDA/GGA pass at 1e-12; the same 6 MGGA + 3 math-precision expected fails; no NEW failures). If any new failure appeared, describe it instead of approving — it is a refactor regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| C/Fortran caller -> libxc_rs.so/.a (extern "C" FFI) | The cdylib/staticlib produced by libxc-compat is the external ABI surface. This phase RELOCATES the existing Phase-6 extern "C" shim; it adds no new entry point and changes no signature. The invariant is byte-for-byte symbol-surface preservation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | extern "C" symbol surface of libxc_rs.so/.a | mitigate | The move must NOT drop or rename any exported symbol. `src/compat/` moves as a UNIT (Phase 6 already partitioned it) via git mv with only `crate::compat::` -> `crate::` re-rooting + cross-crate ref repoint — no symbol added/removed/renamed. Verify post-move with `cargo build -p libxc-compat` (USER/on-demand) + `nm -D target/<profile>/libxc_rs.so | sort` diff vs a pre-refactor symbol dump if one exists; at minimum the SC-5 surface diff + `cargo check -p libxc_rs --lib` confirm no Rust-side surface change. block-on: high — there should be none. |
| T-10-03-02 | Elevation of privilege | unsafe code confinement (BUILD-04 budget) | accept | Phase 6's unsafe is confined to compat/, kernel/launch.rs, GPU buffer mgmt. Moving compat/ into its own crate preserves that confinement (the unsafe travels with the code); the crate boundary does not widen the unsafe surface. No new unsafe is written. |
| T-10-03-03 | DoS (self) | root pulls all 306 kernels via missing default-features=false | mitigate | Pitfall 1: the root `libxc-eval = { ..., default-features = false }` pin is asserted by acceptance criteria + the `cargo tree -p libxc_rs --no-default-features` check showing only math. Without it, `--no-default-features` verify builds OOM. |
</threat_model>

<verification>
- SC-1 (V-10-01): all four crates exist (`cargo metadata --no-deps` shows libxc-core/eval/compat; root libxc_rs is the facade).
- SC-4 (V-10-04): `cargo tree -p libxc-compat -e no-dev` shows core+eval; `--invert` shows only libxc-compat itself.
- SC-5 (V-10-05): surface diff (`log/10-surface-before.log` vs `log/10-surface-after.log`) shows only the intentional `libxc_rs::deferred` delta; `cargo check -p libxc_rs --lib -j1` EXIT 0.
- Feature chain: `cargo tree -p libxc_rs --no-default-features` shows ONLY kernel-math (default-features=false pin holds); `--features oracle-lda` resolves LDA.
- SC-6/SC-7 (V-10-06/07): USER-RUN per-family oracle sweeps match the Wave-0 baseline exactly (Task 3).
- SC-8 (V-10-08): `#![deny(warnings)]` on every crate makes any warning a hard error; the per-`-p` checks already enforce zero warnings.
</verification>

<success_criteria>
All four crates exist (SC-1); libxc-compat deps core+eval with nothing depending on it (SC-4); root facade preserves the public surface (SC-5, surface diff clean + per-`-p` check green); the 4-link feature chain resolves with the default-features=false pin holding; verify routes deferred through the facade with no other test-source changes; the USER-RUN per-family oracle parity matches the Wave-0 baseline (SC-6/SC-7); zero new warnings (SC-8). The extern "C" symbol surface is preserved through the move.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workspace-level-modular-split/10-03-SUMMARY.md`
</output>
