---
phase: 11-splitter-v2-unified-5k-cap
plan: 15
task: 0
status: READY-TO-EXECUTE
captured: 2026-05-24
severity: info
trigger: /gsd:plan-phase 11 --gaps RE-PLAN DONE (commit c4377a1e20, plan-checker PASSED). The two STALE plans are corrected and a new pre-req plan added. NEXT: /gsd:execute-phase 11 --gaps-only (11-15 first).
---

# Phase 11 — RE-PLAN COMPLETE; ready to execute the last gap plans (11-15 → 11-10 → 11-13)

## Re-plan result (2026-05-24, commit c4377a1e20)

`/gsd:plan-phase 11 --gaps` re-planned the two STALE gap plans against the f64-concrete / f32-defer
decision and added a pre-req. Plan-checker PASSED (all 12 dims; re-scope honored). Three plans, three waves:

| Wave | Plan | depends_on | Actor | What |
|---|---|---|---|---|
| 1 | **11-15** (NEW) | [] | ASSISTANT/auto | Migrate the 5 `crates/kernels/math/src` test-module 0.9 launch+readback drift to the 0.10 ABI; LIGHT `-p libxc-kernel-math --tests` gate (no umbrella, no OOM). Gates the 11-10 sweep's cargo-test path. |
| 2 | **11-10** (REVISED) | [11-15] | USER-RUN multi-day | f64-ONLY full-roster sweep to `len(build_roster())`==280; f32 sweep DROPPED (false f64-vs-f64 pass = T-11-12-01). Assistant runs `--dry-run`/`cargo tree` only. |
| 3 | **11-13** (REVISED) | [11-09,11-10,11-11,11-12] | mixed (human-verify checkpoint) | Closure: INVERT f32 wording (CLAUDE.md stays "f64 only"); fix ROADMAP #4/#5/G4; ADD a Phase 12 MGGA f64-parity entry (6 functionals, flagged for user redirect at the Task-1 checkpoint); delete 5 tools / remove `LIBXC_RS_BYPASS_DEFERRED` / 11-06-SUMMARY PARTIAL→COMPLETE / manual phase-close. |

The 4 closed gaps (G-1/11-09, G-2/11-12 f64, G-4/11-11, G-6/11-14) were NOT re-planned.

## Prior state at hand-off (2026-05-24, kept for history)

Gap closure status: **G-1 (11-09) ✓ · G-2 (11-12) ✓ f64 oracle · G-4 (11-11) ✓ · G-6 (11-14) ✓**.
**Incomplete: G-3 (11-10, no SUMMARY) and G-5 (11-13, no SUMMARY).** Both were STALE (now re-planned).

## Critical Anti-Patterns

| # | name | severity | rule |
|---|---|---|---|
| AP-Rule3-broad | Translator emits named-const refs (`M_PI`, `M_CBRT2`, …) inline in chunk-body arithmetic against `F`-typed values without `F::cast_from(...)`. | RESOLVED | Was `blocking` at 11-06. **No longer applies**: the 11-12 f64-concrete pivot (2026-05-23) abandoned the `<F: Float>` generic ABI — every kernel is now `&Array<f64>` with `f64` scalars (2491 files, 0 generic), so `M_PI: f64 × f64` is not a type error. Remaining work is only to *verify* benignancy at full-tree scope = G-3's f64 sweep. Do NOT treat as a live blocker. |

## Why the two remaining plans are stale (re-plan inputs)

**11-10 (G-3 — full-roster compile sweep):**
- **Task 1 (f64 sweep) — KEEP.** Drive `tools/batched_compile_sweep.py` to ALL_OK across the live roster = `len(build_roster())` = **280** on-disk packages (LDA 43 + GGA 131 + MGGA 106; MGGA 106 includes the 14 `mgga_c_tpssloc`/`mgga_c_revtpss` `_pK` shard crates + 2 facades). This is the per-`-p` ENTRY-gate codification. It is a **USER-RUN, multi-day, paced/resumable** job (jobs=1, ~22–44h; assistant runs `cargo tree`/`--dry-run` only — never the compile). **Pre-req:** the `crates/kernels/math/src/{piecewise,powers,polynomials,erf,dft_quantities}.rs` `#[cfg(test)] mod tests` still carry cubecl 0.9 launch+readback drift (STATE follow-up from 11-14) — that breaks the sweep's cargo-test path; migrate it first.
- **Task 2 (f32 sweep `LIBXC_RS_F32=1`) — DROP.** No f32 kernel path exists (kernels f64-concrete). Running it = false f64-vs-f64 pass = **threat T-11-12-01, already rejected by the user**. f32/G4 is a milestone-scale follow-up, not a Phase-11 gate. See memory `project_kernels_f64_concrete_f32_milestone`.

**11-13 (G-5 — phase closure, depends 11-09/10/11/12):**
- **Blocking human-verify checkpoint (Task 1)** as written requires the G-3 manifest (doesn't exist yet) AND "11-12 full-649 f32 oracle within 1e-3" — but G-2 was re-scoped to the **f64** oracle and f32 re-deferred. Re-scope the checkpoint to the f64 evidence actually produced.
- **Task 2/3 edits encode inverted wording.** Per 11-12-SUMMARY "Next Phase Readiness", 11-13 must instead:
  - Keep CLAUDE.md "Precision: f64 only" (do NOT replace with "f32 secondary 1e-6"); add a note that f32 is a milestone follow-up.
  - Correct ROADMAP SC-#4/#5/G4: "full-649 f32 oracle" is a **milestone follow-up, NOT a Phase-11 gate**; #4 manifest evidence = record count == `len(build_roster())` = **280** on-disk packages (not the stale 266 logical-functional figure).
  - **ADD a new ROADMAP gap: MGGA f64 parity** — 6 routed exc functionals fail vs C oracle at f64: `mgga_x_th` 2.0e-1 · `mgga_x_2d_js17` 1.1e-2 · `mgga_c_cs` 9.2e-3 · `mgga_x_pkzb` 3.7e-3 · `mgga_x_pbe_gx` 1.5e-3 · `mgga_x_tm` 9.2e-4. Root: per-functional translation (mgga_x_th likely) + residual `work_mgga` regularization beyond the G-1 τ-clamp (τ-clamp IS applied @ `mgga_dispatch/mod.rs:280-282`, NOT the cause).
- **Still valid / unchanged in 11-13:** delete the 5 obsolete numbered-subcrate-era tools (`split_oversized_kernel.py`, `split_oversized_mgga.py`, `split_mgga_7_kcis.py`, `rebatch_mgga.py`, `regen_phase09.py` — all confirmed present 2026-05-24); RETAIN `batched_compile_sweep.py` + `refactor_helpers_generic.py` + `refactor_test_fixtures/symbol_class_matrix.rs`; remove `LIBXC_RS_BYPASS_DEFERRED` from `src/model/mgga_functional.rs` (still present, D-11); rewrite 11-06-SUMMARY PARTIAL→COMPLETE (preserve FAILED-iteration history, AP-5); close Phase 11 ROADMAP/STATE-effective (gsd phase.complete handler unimplemented — manual, c4480dd881).

## Route

Re-plan DONE (commit c4377a1e20). **NEXT: `/gsd:execute-phase 11 --gaps-only`** — wave 1 (11-15, assistant-runnable, unblocks the sweep) → wave 2 (11-10, USER-RUN multi-day f64 sweep) → wave 3 (11-13 closure; Task 1 is a blocking human-verify checkpoint that also confirms the Phase 12 MGGA-parity placement). `/clear` first for a fresh context window.

## Machine constraints (NON-NEGOTIABLE)
- `.cargo/config.toml` hand-pinned `jobs = 1` — DO NOT edit (user caps by hand). The sweep tool's own `--jobs` (D-32) is the sole >1 path, passed on the command line.
- Assistant runs `cargo tree` / `--dry-run` only; **USER runs all real compiles** (OOM-prone, 30 GB box). No `cargo build --workspace`; no `-p libxc_rs` umbrella build.
- Scope every commit with `git commit --only -- <path>` (sessions open with thousands of pre-staged files).
