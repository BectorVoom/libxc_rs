---
phase: 11-splitter-v2-unified-5k-cap
plan: 13
task: 1
status: WAVE-3-READY-11-13-CLOSURE
captured: 2026-05-25
severity: info
trigger: Waves 1+2 done — 11-15 ✓ + 11-10 (G-3) ✓ f64 sweep ALL_OK across 305 pkgs (commit 6a3eecb299). NEXT = 11-13 (G-5 closure). Task 1 is a BLOCKING human-verify checkpoint confirming the G-1..G-4 f64 evidence + the Phase 12 MGGA-parity placement, before the closure edits.
---

# Phase 11 — Waves 1+2 ✓; NEXT = 11-13 (G-5 closure, human-verify checkpoint)

## Status (2026-05-25)

| Wave | Plan | Status |
|---|---|---|
| 1 | 11-15 | ✓ DONE (math test-module 0.10 ABI; e04eb5f863 + e9bd1c8a1f) |
| 2 | 11-10 | ✓ DONE (G-3 f64 sweep ALL_OK, 305 pkgs; sharding 9fb8c18ac2 + 6a3eecb299) |
| 3 | **11-13** | ○ **READY — G-5 closure.** autonomous:false; Task 1 = blocking human-verify checkpoint. |

**Gap status: G-1 (11-09) ✓ · G-2 (11-12 f64) ✓ · G-3 (11-10) ✓ · G-4 (11-11) ✓ · G-6 (11-14) ✓.** Only G-5 (11-13 closure) remains.

## 11-10 (G-3) outcome — for 11-13's Task-1 checkpoint

f64 per-`-p` compile sweep VERDICT **ALL_OK across all 305 packages** (LDA 43 + GGA 131 + MGGA 131); merged `.cache/batched-compile-sweep-manifest.json`, zero fail, zero pass=-1, count == `len(build_roster())` == **305**. NO f32. **Zero codegen defects** — the only interventions were `cargo clean` (disk) + sharding 3 oversized MGGA functionals (rmggac/tpss/kcisk → 25 `_pK` shards, commit 9fb8c18ac2) to clear the 30 GB OOM ceiling. So **SC #4 evidence = 305** (not 280 — roster grew via sharding; the gate is roster-relative).

## NEXT — 11-13 (G-5) closure (see 11-13-PLAN.md)

**Task 1 = BLOCKING human-verify checkpoint:** confirm G-1..G-4 all passed at f64 (they have — τ-clamp / f64 oracle / 305-sweep / idempotency) and confirm the **Phase 12 MGGA-f64-parity** entry placement (6 routed exc functionals that fail vs the C oracle at f64: mgga_x_th 2.0e-1, mgga_x_2d_js17 1.1e-2, mgga_c_cs 9.2e-3, mgga_x_pkzb 3.7e-3, mgga_x_pbe_gx 1.5e-3, mgga_x_tm 9.2e-4). Only after this checkpoint do the closure edits run:

- Rewrite `11-06-SUMMARY.md` PARTIAL→COMPLETE (honest; preserve FAILED-iteration history; Leg 3 f32 = milestone follow-up, not claimed passed).
- CLAUDE.md: KEEP "Precision: f64 only / 1e-12" + add f32-is-a-milestone note (INVERT the old "f32 secondary 1e-6").
- ROADMAP: SC #4 → "record count == len(build_roster()) = **305**, NOT a phase gate"; SC #5 + G4 → "full-649 f32 oracle is MILESTONE-scale, NOT a Phase-11 gate"; mark all gap plans `[x]`; add Progress row; **ADD a new Phase 12 — MGGA f64 Parity entry** (the 6 functionals; root cause = per-functional translation [mgga_x_th likely] + residual work_mgga regularization beyond the G-1 τ-clamp, which IS applied @ mgga_dispatch/mod.rs:280-282 and is NOT the cause).
- Delete 5 obsolete numbered-subcrate-era tools (split_oversized_kernel.py, split_oversized_mgga.py, split_mgga_7_kcis.py, rebatch_mgga.py, regen_phase09.py). RETAIN batched_compile_sweep.py + refactor_helpers_generic.py + refactor_test_fixtures/symbol_class_matrix.rs + **split_per_functional_subcrate.py** (still in use — it sharded the 3 funcs in 11-10).
- Remove `LIBXC_RS_BYPASS_DEFERRED` from src/model/mgga_functional.rs (D-11 production semantics).
- Manual phase-close (ROADMAP/STATE; gsd phase.complete handler unimplemented here).

## Machine constraints (NON-NEGOTIABLE)
- `.cargo/config.toml` jobs=1 — DO NOT edit. USER runs all real compiles. Scope commits with `git commit --only -- <path>`.

See memory [[project_11_10_sweep_disk_full]] for the sweep disk+OOM+sharding recipe.
