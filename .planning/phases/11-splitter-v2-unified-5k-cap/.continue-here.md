---
phase: 11-splitter-v2-unified-5k-cap
plan: 10
task: 1
status: WAVE-2-RESWEEP-MGGA-USER-RUN
captured: 2026-05-24
severity: info
trigger: 11-15 ✓ done; 11-10 sweep — LDA ✓ + GGA ✓; MGGA was blocked twice (disk-full, then OOM on 3 oversized functionals) → the 3 are now SHARDED (commit 9fb8c18ac2). NEXT = USER re-runs the MGGA sweep on the new 305-pkg roster. (Supersedes the tool-auto-written kcisk HALT breadcrumb — that OOM is now RESOLVED by sharding, NOT a live blocker.)
---

# Phase 11 — 11-15 ✓; 11-10 MGGA needs a RE-SWEEP after sharding 3 oversized functionals

> NOTE: `batched_compile_sweep.py` auto-overwrites this file with a `severity: blocking`
> HALT breadcrumb whenever an MGGA segment dies. The last such halt (kcisk SIGKILL at
> 30.29 GB) is RESOLVED below by sharding — do NOT treat it as a live blocker. The tool
> will re-write this file if the RE-SWEEP halts again.

## Status (2026-05-24)

| Wave | Plan | Status |
|---|---|---|
| 1 | 11-15 | ✓ DONE (math test-module 0.10 ABI; commits e04eb5f863 + e9bd1c8a1f) |
| 2 | 11-10 | ◑ IN PROGRESS — **LDA 43/43 ✓ · GGA 131/131 ✓** (ALL_OK, in .cache manifests). **MGGA needs re-sweep** (roster grew to 131 after sharding). |
| 3 | 11-13 | ○ Blocked on 11-10's merged 305-record manifest + human-verify checkpoint |

## What happened to MGGA (two blockers, neither a code defect)

1. **Disk-full** — cargo-target hit 82 GB → ENOSPC on `mgga_c_r2scan`. RESOLVED (user `cargo clean` → 69 GB free).
2. **OOM on oversized functionals** — `mgga_c_kcisk` rustc SIGKILL at **30.29 GB** (jobs=1) on the 30 GB box. Driver = top-level PART count (~68 = edge). Three were over: **rmggac (115), tpss (106), kcisk (69)**.

**FIX DONE (commit 9fb8c18ac2):** sharded each one's `lxc_pol` via `split_per_functional_subcrate.py --budget 40` (every shard ≤40 parts; dense parts → solo shards; facades now 11-18 parts). New shards: kcisk_p0..p5 (6), tpss_p0..p5 (6), rmggac_p0..p12 (13) = 25. All added as `optional` deps + to the `oracle-mgga` feature list (manual — the pre-11-12 tools don't compose with optional deps; recipe in the commit msg). **Roster 280 → 305** (MGGA 106 → 131). Verified: `cargo tree -p libxc_rs --no-default-features --features oracle-mgga` resolves exit 0; `--dry-run` total_packages=305. Per-shard COMPILE is the re-sweep below.

## NEXT — USER: re-sweep MGGA (jobs=1) on the 305-pkg roster

```bash
# free disk first if tight (re-sweep re-accumulates ~55 GB)
python3 tools/batched_compile_sweep.py --families mgga --jobs 1 \
  --manifest .cache/sweep-mgga-manifest.json --summary .cache/sweep-mgga-summary.md
```

- Use **`--jobs 1`** (jobs=3 pass-1 peaks ~29.6 GB — near-OOM; skip it). ~9-12 h for 131 pkgs.
- Re-sweeps ALL 131 mgga pkgs incl the 25 new shards + the 3 drained facades.
- Watch the 4 BORDERLINE unsharded functionals (under the 68 edge, should build): `mgga_x_br89` (64), `mgga_c_kcis` (60), `mgga_c_r2scan` (58), `hyb_mgga_x_js18` (51). If any OOMs, shard it the same way and repeat the de-optional→split→re-optional→add-to-oracle-mgga recipe (commit 9fb8c18ac2).
- Disk-full → `cargo clean`, resume to a SEPARATE manifest with `--start-after <last-ok>` (the tool CLEARS --manifest on start).

## Then (assistant, non-compile)
Merge `cat .cache/sweep-lda + gga + mgga > .cache/batched-compile-sweep-manifest.json`; validate record count == **305** (re-derive via `--dry-run`), zero fail, zero pass=-1; write `11-10-SWEEP-LOG.md` + `11-10-SUMMARY.md`; then Wave 3 = **11-13** closure.

## Machine constraints (NON-NEGOTIABLE)
- `.cargo/config.toml` jobs=1 — DO NOT edit. USER runs all real compiles; assistant runs `cargo tree`/`--dry-run`/Python tools only.
- No `cargo build --workspace`; no `-p libxc_rs` umbrella build. Scope commits with `git commit --only -- <path>`.

See memory [[project_11_10_sweep_disk_full]] for the full disk + OOM + sharding recipe.
