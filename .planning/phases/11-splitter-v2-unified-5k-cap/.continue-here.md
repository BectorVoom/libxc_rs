---
phase: 11-splitter-v2-unified-5k-cap
plan: 10
task: 1
status: WAVE-2-READY-USER-RUN
captured: 2026-05-24
severity: info
trigger: /gsd:execute-phase 11 --gaps-only — WAVE 1 (11-15) ✓ DONE (commits e04eb5f863 + e9bd1c8a1f, LIGHT gate EXIT 0). NEXT = Wave 2 = 11-10, a USER-RUN multi-day f64-only compile sweep. Assistant runs cargo tree/--dry-run prep ONLY.
---

# Phase 11 — Wave 1 (11-15) DONE; Wave 2 (11-10) is a USER-RUN multi-day sweep

## Where we are (2026-05-24)

| Wave | Plan | Status |
|---|---|---|
| 1 | **11-15** (math test-module 0.10 launch-ABI migration) | ✓ **DONE** — `cargo build -p libxc-kernel-math --tests --jobs 1` EXIT 0 (~800 MB, no OOM). 6 files migrated (5 planned + spin.rs deviation). Commits e04eb5f863 (src) + e9bd1c8a1f (SUMMARY). |
| 2 | **11-10** (f64-only full-roster compile sweep) | ○ **READY — USER-RUN multi-day.** Blocked-on-11-15 now cleared. |
| 3 | **11-13** (G-5 closure) | ○ Blocked — needs 11-10's merged manifest + has a blocking human-verify checkpoint. |

The 4 closed gaps (G-1/11-09, G-2/11-12 f64, G-4/11-11, G-6/11-14) and 11-15 are NOT re-runnable.

## 11-15 outcome (for the SUMMARY, see 11-15-SUMMARY.md)

Migrated the cubecl-0.9 launch+readback drift in the math crate's `#[cfg(test)] mod tests` to 0.10:
`from_raw_parts` 2-arg/by-value (`.clone()` on read-back outputs), `launch_unchecked` drops `.unwrap()`,
`read_one().expect()`. **Two required deviations** (both documented): (1) **spin.rs** was a 6th file with
identical drift the plan's 5-file list missed; (2) **15 `::<f64>` turbofish** added to generic `<F:Float>`
helper calls in powers(9)/piecewise(2)/spin(4) test kernels (E0282 under 0.10 `#[cube]`, Rule 9/10).

**Flagged, OUT-OF-SCOPE (do NOT fix in Phase 11):** running the suite gave 62 pass / **3 PRE-EXISTING
numerical failures** — `bessel::ref_i0_small_arg` (a file 11-15 never touched), `dft_quantities::{test_dimensionless_alpha_uniform`
(0.999999960263572 vs 1.0, ~4e-8)`, test_tf_kinetic_rho_one}`. These are math-precision, **NOT** launch-ABI
(a buffer bug → garbage/panic, not a 4e-8 drift; erf/powers use the same pattern and pass). Route to the
**MGGA f64-parity follow-up** (the planned Phase 12 entry that 11-13 records). They do **NOT** block 11-10
(a per-`-p` COMPILE sweep that runs no tests).

## NEXT — Wave 2: 11-10 (G-3 f64-only compile sweep) — USER-RUN, multi-day

Drive `tools/batched_compile_sweep.py` to **VERDICT: ALL_OK** across the live roster =
`len(build_roster())` = **280** on-disk packages (LDA 43 + GGA 131 + MGGA 106; MGGA 106 includes the 14
`mgga_c_tpssloc`/`mgga_c_revtpss` `_pK` shard crates + 2 facades), at **f64**. f32 sweep is DROPPED
(false f64-vs-f64 pass = threat T-11-12-01; kernels are f64-concrete — memory
`project_kernels_f64_concrete_f32_milestone`). Run **resumable/chunked** (per-family segments → merge into
`.cache/batched-compile-sweep-manifest.json`). See 11-10-PLAN.md for the full procedure.

## Machine constraints (NON-NEGOTIABLE)
- `.cargo/config.toml` hand-pinned `jobs = 1` — DO NOT edit. The sweep tool's own `--jobs` (D-32) is the
  ONLY parallel-cargo path, passed on the command line.
- **USER runs all real compiles** (OOM-prone, 30 GB box). Assistant runs `cargo tree` / `--dry-run` ONLY.
  No `cargo build --workspace`; no `-p libxc_rs` umbrella build. (Exception already used + done: the LIGHT
  single-crate `-p libxc-kernel-math --tests` gate in 11-15 — explicitly carved out as non-OOM.)
- Scope every commit with `git commit --only -- <path>` (sessions open with thousands of pre-staged files).

## Route
`/gsd:execute-phase 11 --gaps-only` resumes here (discovers 11-15 has a SUMMARY → skips it → next incomplete
is 11-10). Because 11-10 is a USER-RUN multi-day job, the assistant should prep (`--dry-run`/`cargo tree`)
and hand the actual paced compile segments to the user, not attempt to run the full sweep inline. `/clear`
first for a fresh window.
