---
phase: 11-splitter-v2-unified-5k-cap
plan: 06
task: 6
leg: 2
status: HALTED
captured: 2026-05-18
severity: blocking
trigger: Gate 3 Leg 2 f64 parity attempted → cargo build exited 101 during verify-crate transitive compile, blocking the parity assertion.
---

# Phase 11 — HALT at 11-06 Task 6 Leg 2 (f64 parity)

## Critical Anti-Patterns

| # | name | severity | rule |
|---|---|---|---|
| AP-Rule3-broad | Translator emits named-const refs (`M_PI`, `M_CBRT2`, `M_CBRT3`, etc.) inline in chunk-body arithmetic against `F`-typed values without `F::cast_from(...)` wrapping. | blocking | Deviation F regen (commits `4aaaaa7739`/`8a9f32091e`/`d26efabda6`) extended Rule 10 turbofish to cross-fn calls but did NOT extend Rule 3 (named-const → `F::cast_from`) to in-body literals. ~1,357 chunk files across the regenerated GGA family exhibit this pattern. Suspected to affect LDA and MGGA chunks identically wherever a named const multiplies an F-typed CSE temp. |

## Failure Detail

**Invocation:** `LIBXC_RS_BYPASS_DEFERRED=1 cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case -- --test-threads=1 --nocapture`

**Exit:** `101` (compile failure; parity assertion never reached).

**Representative error** (`crates/kernels/gga/gga_c_gaploc/src/lxc_pol/part53/chunk804.rs:13`):

```rust
pub fn gga_c_gaploc_lxc_pol_part53_v4rhosigma3_18_chunk804<F: Float>(t12557: F, ..., t458: F) -> (F, F, F, F) {
    let t40614 = t2518 * t12557;
    let t40620 = t9105 * t5337 * M_PI * t1691 * t135 * t458;
    //                            ^^^^ expected type parameter `F`, found `f64`
```

Constants currently declared at `crates/kernels/math/src/constants.rs:4`: `pub const M_PI: f64 = std::f64::consts::PI;` and analogous `M_CBRT*`.

**Aggregate** (one subcrate alone): 2920 errors in `libxc-kernel-gga_c_gaploc`. Wider scan (`grep -rE 'let t[0-9]+ = [^F].*M_PI \*' crates/kernels/`) returns **1,357 hits**.

**Canary status:** `cargo build -p libxc-kernel-mgga_c_b94` at f64 + f32 — both **PASS** (Leg 1 re-confirmed post-Deviation-E/F). b94's chunks do not trip this pattern; the failure is graph-wide, not canary-local.

## Why this blocks 11-06 Legs 2/3/4 + Task 8

- Leg 2 (f64 parity) compiles verify/'s transitive graph → 273 subcrates → hits gga_c_gaploc → fails before any parity assertion.
- Leg 3 (f32 parity) inherits the same transitive compile path → same failure.
- Leg 4 (idempotency: `python3 tools/maple_to_kernels.py translate --family mgga --func mgga_c_b94`) is mechanically runnable, but its result is gated on Legs 2/3, and a clean idempotency proof on a single canary doesn't bound the Rule 3 emission defect anyway.
- Task 8 (rewrite SUMMARY PARTIAL → COMPLETE) cannot honestly record Gate 3 EXIT GREEN — Gate 3 is RED.

## Routes (per plan 11-06 AP-8: "Real translator bug routes through /gsd:discuss-phase")

1. **Surface translator fix as its own scoped work.** Amend translator's Rule 3 emission so named-const refs in chunk-body arithmetic are wrapped in `F::cast_from(<const>)` (or otherwise resolved to F via `let pi = F::cast_from(M_PI)` rebinding). Full-tree regen, then resume Legs 2/3/4.
2. **Pivot to 11-07 sweep first.** The 6th-iter-rescoped `batched_compile_sweep.py` is the diagnostic instrument for exactly this class of failure. Running it would produce a per-subcrate manifest enumerating which of the 273 subcrates fail and why — superior evidence than a single anecdotal compile failure. Then a translator-fix plan can be scoped against the manifest.
3. **Halt phase 11 entirely; replan.** Drop out of execute-phase, run `/gsd:discuss-phase 11` to decide Rule 3 augmentation scope, re-plan downstream.

## Resume guidance

- `.cargo/config.toml` is restored to `jobs = 1` (user did this manually before this session).
- `mgga_c_b94` canary itself is fine at both precisions — keep that proof.
- 11-PATTERN.md Rules 1-10 are correct as written; the gap is that **Rule 3 enforcement was never extended to the translator's Phase-2-chunk emission path**, only to math/ helpers (manual conversion in Task 5).
- Any decision that re-runs the translator must also re-run a full-tree regen — Deviation F's commits would be superseded.
- Per memory `project_phase11_structural_without_compile`: this is the 4th instance of declaring structural completion without a per-`-p` compile entry gate covering the dispatch tree. The new 11-07 batched_compile_sweep IS the entry-gate codification. Running 11-07 before declaring 11-06 GREEN closes that loop.
