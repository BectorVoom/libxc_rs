<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
<!-- D-10/D-10a/D-10b architecture: splitter emits unified — write_dir = crates/kernels/lda/src always. -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - crates/kernels/lda/src/lib.rs
  - crates/kernels/lda/src/**/*.rs           # populated from scratch by tools/regen_unified.py --family lda
  - src/eval/gga_dispatch/mod.rs              # extended: LDA-related references (none today; this is for any cross-family touch)
  - src/eval/mgga_dispatch/mod.rs             # extended: same
autonomous: false   # cargo build under D-07 inline
requirements:
  - P11-INV-2
  - P11-INV-3
  - P11-INV-4
  - P11-INV-5
  - P11-INV-6

must_haves:
  truths:
    - "Per D-10: every LDA functional (~41 today, sourced from libxc-master/src/maple2c/lda_{exc,vxc}/*.c) is emitted under crates/kernels/lda/src/<func>/ — no numbered-subcrate intermediate ever exists."
    - "Per D-LOCK-B: every .rs file under crates/kernels/lda/src/ is ≤5000 lines (P11-INV-2 holds for LDA)."
    - "crates/kernels/lda/src/lib.rs has a `pub mod <func>;` line for every emitted functional, alphabetically sorted (splitter-emit determinism)."
    - "Every regenerated LDA chunk helper file declares `fn chunk_N<F: Float>(args: F, ...) -> (F, F, ...)` — no `&mut Array<f64>` shared scratch (D-02), no hardcoded f64 in chunk signatures (D-03)."
    - "After regen, `cargo build -p libxc-kernel-lda` succeeds inline under the project envelope."
    - "After regen, `cargo build --workspace` succeeds inline. **B-D10-1: this is where P11-INV-3 (cargo build --workspace exits 0) is FIRST satisfied** — the LDA façade now exposes `pub mod <func>;` lines matching the 36 `use libxc_kernel_lda::<func>::*;` references in `src/eval/dispatch.rs` that have been pending resolution since plan 11-02. P11-INV-3 acceptance ownership moved from 11-02 to 11-03 per checker iteration 1."
    - "After regen, parity_phase11::phase11_smoke passes for all LDA entries (lda_x, lda_xc_ksdt, lda_c_pw — minimum 3 LDA entries per W8 floor) at strict 1e-12 (D-05)."
    - "Re-running `tools/regen_unified.py --family lda` on unchanged Maple input produces zero git diff under crates/kernels/lda/ (LDA portion of P11-INV-6)."
    - "Dispatch tree (src/eval/{gga,mgga}_dispatch/mod.rs) regenerated against the now-populated LDA façade — every `crate::kernel::lda::<func>::...` reference resolves; bash tools/audit_dispatch_tree.sh exits 0 for LDA refs."
  artifacts:
    - path: "crates/kernels/lda/src/lib.rs"
      provides: "Family façade with `pub mod <func>;` per LDA functional, alphabetically sorted (D-10)"
      contains: "pub mod lda_x"
  key_links:
    - from: "tools/regen_unified.py --family lda"
      to: "crates/kernels/lda/src/<func>/<level>_<spin>*.rs"
      via: "translate_lda_v2.py invoked with write_dir = crates/kernels/lda/src for ALL LDA functionals"
      pattern: "fn .*_chunk[0-9]+<F: Float>"
    - from: "Each regenerated LDA wrapper file (e.g. lda_xc_ksdt/lxc_pol_part5_v4rho4_1.rs)"
      to: "Its sibling _chunkN.rs files"
      via: "tuple destructure call site"
      pattern: "let \\(.*\\) = .*chunk[0-9]+::<F>"
---

<objective>
Wave 2 — populate the unified LDA façade. With the splitter v2 wired (plan 11-02) and the family façades empty, this plan runs `tools/regen_unified.py --family lda` to populate `crates/kernels/lda/src/` per-functional from `libxc-master/src/maple2c/lda_{exc,vxc}/*.c`, then verifies the LDA family builds and oracle-parity-passes at strict 1e-12.

Smallest blast radius for the new D-10 emit path (LDA has the smallest functional count: 41 today). Proving the unified emit on LDA gives high confidence that GGA (plan 11-04) and MGGA (plan 11-05) will follow the same pattern.

Per RESEARCH.md guardrails:
- Q1: chunk graphs FLAT (wrapper calls chunk_0..N in sequence; chunks never call other chunks).
- Q3: forced breakpoint at lowest-arity cut point in next 500 lines if no natural CSE break.
- Q4: chunk indices reset per `_partN`, not global; filename basename ≤200 chars.

Output: populated LDA family crate; verified parity; dispatch regenerated against the populated façade.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-02-SUMMARY.md
@.cargo/config.toml
@tools/regen_unified.py
@tools/translate_lda_v2.py
@tools/translate_v2/cse.py
@tools/translate_v2/emit.py
@crates/kernels/lda/src/lib.rs
@crates/kernels/lda/Cargo.toml
@verify/tests/parity_phase11.rs

<interfaces>
LDA functional inventory (the 41 functionals to be emitted; verified pre-clean-slate against `crates/kernels/lda-1/src/lib.rs` lines 7-21 + `crates/kernels/lda-2/src/lib.rs`):

```
hyb_lda_xc_bn05, lda_c_1d_csc, lda_c_1d_loos, lda_c_2d_amgb, lda_c_2d_prm,
lda_c_chachiyo, lda_c_chachiyo_mod, lda_c_gk72, lda_c_gombas, lda_c_hl,
lda_c_lp96, lda_c_ml1, lda_c_pk09, lda_c_pmgb06, lda_c_pw, lda_c_pw_erf,
lda_c_pz, lda_c_rc04, lda_c_rpa, lda_c_vwn, lda_c_vwn_1, lda_c_vwn_2,
lda_c_vwn_3, lda_c_vwn_4, lda_c_vwn_rpa, lda_c_w20, lda_c_wigner,
lda_k_gds08_worker, lda_k_tf, lda_k_zlp, lda_x, lda_x_2d, lda_x_erf,
lda_x_rel, lda_x_sloc, lda_x_yukawa, lda_xc_1d_ehwlrg, lda_xc_ksdt,
lda_xc_teter93, lda_xc_tih, lda_xc_zlp
```

Source-of-truth: `libxc-master/src/maple2c/lda_exc/*.c` and `libxc-master/src/maple2c/lda_vxc/*.c`. The discovery walks both subdirectories per `tools/regen_unified.py:discover_functionals`.

LDA chunked-emission worst case (per RESEARCH.md "Cross-check"): `lda_xc_ksdt/lxc_pol_part5_v4rho4_1.rs` was 5,995 lines pre-clean-slate (chunked into 1 chunk under SPLIT_THRESHOLD=6000). Under SPLIT_THRESHOLD=5000 + the new D-02 ABI it becomes 2-3 chunks. This is the only LDA functional that exercises the chunked-emission path; the other 40 functionals emit single-file with no chunked split needed.

LDA dispatch shape: under D-10 there are no batchN intermediate paths. The dispatch generator (Task 4 of plan 11-02) emits per-functional dispatch helpers with `crate::kernel::lda::<func>::...` paths. Note: there is NO `src/eval/lda_dispatch/` tree today (verified: `ls src/eval/` shows `gga_dispatch` and `mgga_dispatch` only); LDA dispatch lives directly in `src/eval/dispatch.rs` (line 556+ uses `libxc_kernel_lda::lda_x::*` etc.). Post-D-10 those `use libxc_kernel_lda::<func>::*;` lines still resolve because the LDA façade now has `pub mod <func>;` for each emitted functional. NO new lda_dispatch generator is required.

Build envelope reminder (D-08, D-09 — read from .cargo/config.toml, NEVER inline):
- jobs = 1
- target-dir = .cache/cargo-target
- RUST_MIN_STACK = 67108864 (load-bearing — must not be reduced)

verify/tests/parity_phase11.rs (built in plan 11-01) PHASE11_SMOKE LDA entries (per W8 floor: ≥3 LDA entries):
- lda_x (id 1), lda_xc_ksdt (id 259), lda_c_pw (id 12)

These three entries MUST pass at strict 1e-12 after this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate LDA family into unified layout — `tools/regen_unified.py --family lda`</name>
  <files>crates/kernels/lda/src/lib.rs, crates/kernels/lda/src/**/*.rs (~41 functional directories created from scratch under crates/kernels/lda/src/)</files>
  <read_first>
    - tools/regen_unified.py (post-plan-11-02 state — the discovery + emit driver)
    - tools/translate_lda_v2.py (post-plan-11-02 state — SPLIT_THRESHOLD=5000 + D-02 wedge)
    - tools/translate_v2/cse.py + tools/translate_v2/emit.py (the partitioner + emitter)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-05 verify gate, D-07/D-08/D-09 build envelope, D-10/D-10a/D-10b emit target)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "Cross-check" — `lda_xc_ksdt/lxc_pol_part5_v4rho4_1` chunk0 was 5,995 lines pre-clean-slate; lowering threshold should produce 2 chunks where there was 1
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md "Splitter emission target redirect" — the new write_dir is crates/kernels/lda/src always
    - .cargo/config.toml (build envelope — cite, do NOT duplicate)
    - libxc-master/src/maple2c/lda_exc/ + lda_vxc/ (verify the 41 functional .c files exist and are the discovery source)
    - crates/kernels/lda/src/lib.rs (post-clean-slate state from plan 11-02 — should have `pub mod deferred;` only)
  </read_first>
  <action>
    Three sequential operations, all inline (D-07).

    --- Operation 1: regen LDA family ---

    Confirm pre-conditions:

    ```bash
    # Confirm clean-slate state — façade is empty (only deferred + header).
    grep -cE '^pub mod ' crates/kernels/lda/src/lib.rs   # MUST equal 1 (just `pub mod deferred;`)
    grep -cE '^pub use libxc_kernel_' crates/kernels/lda/src/lib.rs   # MUST equal 0
    # Confirm working tree is clean for crates/kernels/lda
    git status --porcelain crates/kernels/lda
    # MUST be empty (no leftover from clean-slate plan).
    ```

    All three must hold; if not, STOP and return CHECKPOINT REACHED.

    Run the regen:

    ```bash
    python3 tools/regen_unified.py --family lda
    ```

    The driver:
    1. Walks `libxc-master/src/maple2c/lda_exc/*.c` + `lda_vxc/*.c`, enumerates ~41 functionals.
    2. For each functional, invokes `tools/translate_lda_v2.py` with `--write-to <staging> --split --incremental` (and `--vxc-only` if the functional has only a vxc source).
    3. Atomically copies the staged output into `crates/kernels/lda/src/<func>/`.
    4. Rewrites `crates/kernels/lda/src/lib.rs` with `pub mod <func>;` lines, alphabetically sorted (preserves the file header above the splitter-managed marker comment).

    Operation 1 success = exit code 0 + crates/kernels/lda/src/ contains ~41 functional subdirectories + lib.rs has ~41 `pub mod` lines.

    Iteration loop if any LDA file >5000 lines:

    1. Run audit: `find crates/kernels/lda/src -name '*.rs' -exec wc -l {} + | awk '$1 > 5000 && $2 != "total"'`
    2. If non-empty, identify the failing functional (likely `lda_xc_ksdt`).
    3. Lower `min_uses` in the cse.py call site for that functional (default 5 → 3 first; 2 floor) by passing a per-functional override through emit.py — **W-D10-5: EXTEND** the existing module-level `PER_FUNCTIONAL_CSE_OVERRIDES: dict[str, dict] = {}` constant in `tools/translate_v2/emit.py` (created in plan 11-02 Task 2) with `PER_FUNCTIONAL_CSE_OVERRIDES["lda_xc_ksdt"] = {"min_uses": 3}` (commit the override edit alongside the regen output). Do NOT introduce the dict here; it already exists.
    4. Re-run regen for just that functional via `python3 tools/translate_lda_v2.py libxc-master/src/maple2c/lda_exc/lda_xc_ksdt.c lda_xc_ksdt --write-to crates/kernels/lda/src --split --incremental` (driver loops are fine; the per-functional invocation is faster for retry).
    5. Re-audit. If after 3 retries the file still exceeds 5000 lines, STOP and return CHECKPOINT REACHED naming the functional + the chunk arity + the chosen mitigation.

    --- Operation 2: build + parity verify ---

    All inline:

    ```bash
    cargo build -p libxc-kernel-lda
    cargo build --workspace                          # validates root crate's `use libxc_kernel_lda::<func>::*;` references resolve against the populated façade
    cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture
    ```

    Each must exit 0. The smoke test must pass for the 3 LDA entries (lda_x, lda_xc_ksdt, lda_c_pw) at strict 1e-12.

    Per RESEARCH.md "Risks": if a parity failure stays within 10× of strict_tol (e.g., max rel err 5e-12 vs target 1e-12), the CSE-introduced reordering is the cause. Mitigations to try in order:
    1. Tighten `min_uses` for the failing functional (default 5 → 8) to reduce CSE breakpoints.
    2. Per-functional `chunk_max_lines` override: increase to 5500 to avoid a problematic breakpoint.
    3. If failure exceeds 1e-9 — there's a translation bug; STOP and return CHECKPOINT REACHED.

    --- Operation 3: regen dispatch + LDA-only idempotency check ---

    Re-run the dispatch generators (they're now seeded with a non-empty LDA façade for any cross-family references; LDA itself is dispatched from `src/eval/dispatch.rs`, not from `src/eval/{gga,mgga}_dispatch/`, so the LDA contribution to dispatch regen is mostly the workspace-wide path resolution check):

    ```bash
    python3 tools/generate_gga_dispatch.py
    python3 tools/generate_mgga_dispatch.py
    bash tools/audit_dispatch_tree.sh
    ```

    Audit must exit 0.

    LDA-only idempotency check (per P11-INV-6):

    ```bash
    git status --porcelain crates/kernels/lda > /tmp/lda_status1.txt
    python3 tools/regen_unified.py --family lda
    git status --porcelain crates/kernels/lda > /tmp/lda_status2.txt
    diff /tmp/lda_status1.txt /tmp/lda_status2.txt
    ```

    `diff` MUST exit 0. If non-zero, identify the non-determinism source (likely set iteration order in cse.py / emit.py / regen_unified.py — see PATTERNS.md "Python translator deterministic-emit conventions") and fix in the responsible tool before re-running.

    Path-scoped commit:

    ```bash
    git commit --only -m "feat(phase-11/lda): regen LDA family into unified façade (D-10) at SPLIT_THRESHOLD=5000

    - Populated crates/kernels/lda/src/ with ~41 functional subdirs from libxc-master/src/maple2c/lda_{exc,vxc}/.
    - Every LDA .rs file ≤5000 lines (P11-INV-2 met for LDA).
    - LDA chunked emission uses D-02 tuple-return <F: Float> ABI (no shared scratch).
    - Smoke parity green at strict 1e-12 on lda_x, lda_xc_ksdt, lda_c_pw (D-05).
    - LDA splitter is empirically idempotent.
    - Dispatch regenerated against populated LDA façade." -- \
      crates/kernels/lda \
      src/eval/gga_dispatch src/eval/mgga_dispatch
    # Include any per-functional CSE override edit if one was needed:
    # tools/translate_v2/emit.py
    ```
  </action>
  <verify>
    <automated>find crates/kernels/lda/src -name '*.rs' -exec wc -l {} + | awk '$2 != "total" && $1 > 5000 {c++} END {exit c==0?0:1}' && cargo build --workspace && cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture && bash tools/audit_dispatch_tree.sh</automated>
  </verify>
  <acceptance_criteria>
    - `find crates/kernels/lda/src -name '*.rs' -exec wc -l {} + | awk '$2 != "total" && $1 > 5000' | wc -l` equals `0` (P11-INV-2 for LDA).
    - `crates/kernels/lda/src/lib.rs` contains ≥30 `pub mod` lines: `grep -cE '^pub mod ' crates/kernels/lda/src/lib.rs` is at least `30` (~41 functionals + deferred expected).
    - Specific high-value functionals exist as subdirs: `test -d crates/kernels/lda/src/lda_x && test -d crates/kernels/lda/src/lda_xc_ksdt && test -d crates/kernels/lda/src/lda_c_pw` exits `0`.
    - `cargo build -p libxc-kernel-lda` exits `0`.
    - `cargo build --workspace` exits `0` (P11-INV-3 — first valid post-LDA-populate workspace build). **B-D10-1: ownership of this gate moved from 11-02 to 11-03.** Plan 11-02 ends with this gate intentionally failing because src/eval/dispatch.rs has 36 stale LDA refs; this plan resolves them by populating the LDA façade.
    - `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture` exits `0` (per W8: ≥3 LDA entries; assert via `grep -E '"lda_' verify/tests/parity_phase11.rs | grep -c PHASE11_SMOKE` is at least `3`).
    - `bash tools/audit_dispatch_tree.sh` exits `0` (B1 closure preserved with populated LDA façade).
    - `bash tools/audit_cube_launch.sh` exits `0` (count still ≤ 22 — chunk helpers are plain `#[cube]`, not `(launch_unchecked)`).
    - At least one regenerated LDA chunk file matches the D-02 ABI: `find crates/kernels/lda/src -name '*_chunk*.rs' -exec grep -l 'fn .*_chunk[0-9]\+<F: Float>' {} +` returns ≥ 1 file (likely lda_xc_ksdt's lxc_pol_part5_v4rho4_1_chunkN).
    - No `&mut Array<f64>` shared-scratch chunks: `find crates/kernels/lda/src -name '*.rs' -exec grep -lE 'fn .*_chunk[0-9]+\\(.*&mut Array<f64>' {} +` returns no matches.
    - Idempotency: re-running `python3 tools/regen_unified.py --family lda` produces zero `git status --porcelain crates/kernels/lda` diff.
    - mgga_x_2d_prp10 deferral marker preserved in MGGA façade (NOT touched by this plan, but assert as a regression check): the MGGA façade lib.rs is unchanged from plan 11-02 state — `git diff crates/kernels/mgga/src/lib.rs` empty since plan 11-02 commit.
    - No `.cargo/config.toml` change: `git diff --name-only HEAD~1 HEAD .cargo/config.toml` returns empty.
  </acceptance_criteria>
  <done>The LDA family is populated under the D-10 unified layout at SPLIT_THRESHOLD=5000; every LDA file is ≤5000 lines; cargo builds the workspace; oracle parity holds at 1e-12 on the LDA smoke entries; the LDA portion of the splitter is empirically idempotent; the dispatch tree resolves against the populated LDA façade.</done>
</task>

</tasks>

<verification>
End-of-plan verification:

1. P11-INV-2 (LDA portion): zero LDA files >5000 lines.
2. P11-INV-3: `cargo build --workspace` exits `0` (workspace builds with populated LDA + empty GGA + empty MGGA + minimal dispatch).
3. P11-INV-4 (LDA smoke): `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture` exits `0` for the 3 LDA entries.
4. P11-INV-5: `bash tools/audit_cube_launch.sh` exits `0`.
5. P11-INV-6 (LDA portion): `python3 tools/regen_unified.py --family lda` produces no diff on second run.
6. Blocker B1: `bash tools/audit_dispatch_tree.sh` exits `0`.
7. crates/kernels/lda/src/lib.rs contains ≥30 `pub mod` lines (one per emitted functional).

If verification 3 (smoke parity) fails on any LDA functional, STOP and return CHECKPOINT REACHED — D-05 may need per-functional CSE tuning.
</verification>

<success_criteria>
- D-10 unified emit proven on the smallest family (LDA — 41 functionals).
- SPLIT_THRESHOLD=5000 enforced for LDA (D-LOCK-B contribution).
- Every LDA `.rs` file ≤5000 lines (P11-INV-2 contribution from LDA).
- LDA family builds and oracle-parity-passes at strict 1e-12 (P11-INV-3, P11-INV-4 contributions).
- LDA splitter is empirically idempotent (P11-INV-6 contribution from LDA).
- Dispatch tree resolves against the populated LDA façade (Blocker B1 still closed).
- No `.cargo/config.toml` modification (D-07/D-08/D-09 invariants).
- All cargo commands ran inline (D-07).
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md` covering:
- Per-functional emit count (~41 LDA functionals; verify the actual count from regen_unified.py output).
- Worst-case LDA file: pre-clean-slate `lda_xc_ksdt/lxc_pol_part5_v4rho4_1.rs` was 5,995 lines (per RESEARCH.md cross-check); post-Phase-11 chunk count + max line count.
- Per-functional CSE knob overrides (if any) with rationale.
- Parity result on phase11_smoke LDA entries (max relative error per entry).
- Idempotency result.
- Dispatch regen result (any change to src/eval/{gga,mgga}_dispatch/mod.rs from the populated LDA façade — likely none, since LDA dispatches from src/eval/dispatch.rs).
- Build envelope confirmation.
- Pointer to next plan: `/gsd-execute-phase 11` resumes at plan 11-04 (GGA family populate).
</output>
