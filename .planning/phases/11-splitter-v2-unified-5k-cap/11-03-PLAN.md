<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - crates/kernels/lda
  - crates/kernels/gga
  - crates/kernels/mgga
  - Cargo.toml
  - tools/generate_gga_dispatch.py
  - tools/generate_mgga_dispatch.py
  - src/eval/gga_dispatch
  - src/eval/mgga_dispatch
  - src/kernel/mod.rs
autonomous: false
requirements:
  - P11-INV-1
  - P11-INV-2
  - P11-INV-5
  - P11-INV-6

must_haves:
  truths:
    - "All 27 numbered subcrates (`lda-{1,2}`, `gga-{1..8}`, `mgga-{1..7,8a,8b,9a,9b,10,11a,11b,12,13,14}`) and the 3 family-level crates (`crates/kernels/{lda,gga,mgga}/Cargo.toml` + `src/lib.rs`) are deleted; the family directories survive as plain folders."
    - "The splitter v2 has regenerated ~264 per-functional subcrates from Maple into `crates/kernels/{family}/<func>/`, each a complete Cargo crate; `tools/audit_kernel_size.py --strict` exits 0 (zero files >5000 lines)."
    - "Root `Cargo.toml` `[dependencies]` lists per-functional `libxc-kernel-<func>` path-deps (no numbered, no family-façade deps); `[workspace] default-members` lists the ~257 routed per-functional subcrates and EXCLUDES the 7 deferred kernels (mgga_c_b94, mgga_x_br89, mgga_x_mbr, mgga_x_mbrxc_bg, mgga_x_mbrxh_bg, mgga_x_mggac, mgga_x_br89_explicit)."
    - "The dispatch tree (`src/eval/{gga,mgga}_dispatch/`) is regenerated against per-functional subcrates — no `batchN::` segment survives; `tools/audit_dispatch_tree.sh` exits 0 (B1 closed) AND the `crate::kernel::{family}::<func>` re-export paths are type-checked by a cargo/rustc invocation."
    - "`tools/audit_subcrate_collapse.sh` exits 0 (no numbered subcrates, no family-level crates)."
  artifacts:
    - path: "Cargo.toml"
      provides: "Root manifest with per-functional kernel deps + default-members (deferred excluded)"
      contains: "default-members"
    - path: "tools/generate_mgga_dispatch.py"
      provides: "MGGA dispatch generator emitting per-functional paths (D-10b)"
      min_lines: 100
    - path: "tools/generate_gga_dispatch.py"
      provides: "GGA dispatch generator emitting per-functional paths (no batchN segment)"
      min_lines: 100
  key_links:
    - from: "src/eval/mgga_dispatch"
      to: "crates/kernels/mgga/<func>"
      via: "crate::kernel::mgga::<func>::... re-export (no batchN segment)"
      pattern: "crate::kernel::(gga|mgga)::[a-z]"
    - from: "Cargo.toml"
      to: "crates/kernels/{family}/<func>"
      via: "per-functional path-dep + default-members entry"
      pattern: "libxc-kernel-[a-z].*path.*crates/kernels"
---

<objective>
Execute the D-10a clean-slate restructure: delete all 27 numbered subcrates and 3 family-level crates, run splitter v2 to regenerate ~264 per-functional subcrates from Maple, rewrite root `Cargo.toml` (`[dependencies]` + `[workspace] default-members`, deferred kernels excluded per D-11), and regenerate the dispatch tree against per-functional paths (closing Blocker B1). This is the atomic structural transformation — there is NO state where both layouts coexist (D-10a).

Purpose: D-10 changes the unification target to per-functional subcrates — the structural fix for the `cargo check`/`cargo build` OOM at the compilation-unit boundary. D-10a mandates a clean-slate regen (q01/q02 hand-built output discarded). D-10b regenerates dispatch against the new layout, closing the pre-existing B1 staleness.

Output: A fully restructured `crates/kernels/` tree (~264 per-functional subcrates, family dirs are plain folders), a rewritten root `Cargo.toml`, a regenerated dispatch tree, and two updated dispatch generators. Per-subcrate `cargo build -p` spot-checks confirm representative subcrates compile; whole-workspace build is NOT a gate (D-12) — verification is per-subcrate and incremental.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-DISPATCH-AUDIT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-02-SUMMARY.md
@.cargo/config.toml

<interfaces>
<!-- The restructure surface. Extracted from the codebase — do NOT re-explore. -->

Current numbered subcrates (27, all DELETED by this plan):
  lda-1, lda-2, gga-1..gga-8,
  mgga-1..mgga-7, mgga-8a, mgga-8b, mgga-9a, mgga-9b, mgga-10, mgga-11a, mgga-11b, mgga-12, mgga-13, mgga-14

Current family-level crates (3, DELETED as crates — dirs survive as plain folders):
  crates/kernels/lda/{Cargo.toml,src/lib.rs}
  crates/kernels/gga/{Cargo.toml,src/lib.rs}
  crates/kernels/mgga/{Cargo.toml,src/lib.rs}

crates/kernels/math/ — STAYS A CRATE (out of scope; every per-functional subcrate depends on it).

Root Cargo.toml current shape:
  L11: libxc-kernel-math = { path = "crates/kernels/math" }   ← KEEP
  L12-15: libxc-kernel-{lda,lda-1,lda-2,gga} path deps         ← DELETE numbered + family
  L16: libxc-kernel-mgga = { path = "crates/kernels/mgga" }    ← DELETE
  L18-34: libxc-kernel-mgga-{1..14} path deps                  ← DELETE all
  L40 [workspace], L44 default-members = [ ... ]               ← REWRITE
  L45-48 members: xtask, verify, libxc-sys                     ← UNCHANGED

The 7 deferred kernels (D-11) — get a path-dep entry so `cargo build -p` works,
but are EXCLUDED from default-members:
  mgga_c_b94, mgga_x_br89, mgga_x_mbr, mgga_x_mbrxc_bg, mgga_x_mbrxh_bg,
  mgga_x_mggac, mgga_x_br89_explicit

GGA dispatch generator (tools/generate_gga_dispatch.py):
  ROSTER = .planning/phases/04-bulk-kernel-translation/gga_roster.tsv
  load_roster() returns (name, batch, libxc_id, completeness, has_exc, scalars)
  L344: `mod_lines.append(f"pub mod batch{b};")`            ← DROP batchN layer
  L525: `... => batch{batch}::dispatch_{name}(...)`         ← retarget to per-func path
  D-10b recommended shape: emit one dispatch_<func> helper per functional under
  src/eval/{family}_dispatch/funcs/<func>.rs; mod.rs top-level match delegates to
  funcs::<func>::dispatch_<func>. The `batch` roster column becomes degenerate.

MGGA dispatch generator: confirm whether tools/generate_mgga_dispatch.py exists;
if not present, create it mirroring generate_gga_dispatch.py against
.planning/phases/04-bulk-kernel-translation/mgga_roster.tsv.

11-DISPATCH-AUDIT.md (Blocker B1): GGA dispatch references batch13,15..22,5g
(10 unresolved); MGGA references batch17,21,23,28,29,30,34,35 (8 unresolved).
This plan's dispatch regen MUST make `tools/audit_dispatch_tree.sh` exit 0.
Note: audit_dispatch_tree.sh itself greps for `crate::kernel::{family}::batchN`
references — after the regen there are NO batchN references, so the script's
"referenced" set is empty and it exits 0 trivially. That is the correct
post-collapse state; do not "fix" the audit script to keep looking for batchN.
BUT: because it passes trivially, audit_dispatch_tree.sh proves NOTHING about
whether the new `crate::kernel::{family}::<func>` re-export paths actually
resolve — that proof comes from the cargo/rustc path-resolution check in Task 2
(the fallback ladder), which is a SEPARATE load-bearing gate.

src/kernel/mod.rs — root kernel re-export tree (`pub use libxc_kernel_lda as lda;`
etc). Must be updated: the family façade crates no longer exist; `crate::kernel::lda`
must now re-export the per-functional subcrates directly (`pub mod lda { pub use
libxc_kernel_lda_x as lda_x; ... }` or equivalent). Planner's call confirmed:
emit a generated `src/kernel/{family}.rs` (or inline module) that `pub use`s each
per-functional subcrate under the family namespace, so dispatch's
`crate::kernel::{family}::<func>::...` paths resolve.

From .cargo/config.toml (D-08/D-09 — do NOT duplicate, do NOT modify):
  [build] jobs = 1, target-dir = .cache/cargo-target
  [env] RUST_MIN_STACK = "67108864"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean-slate delete + splitter v2 full regen into per-functional subcrates</name>
  <files>crates/kernels/lda, crates/kernels/gga, crates/kernels/mgga, Cargo.toml</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-10a clean-slate scope, D-10 emit target, D-11 deferred-kernel default-members exclusion, D-LOCK-D idempotency)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md (lines 39-204 emitted-artifact specs; lines 207-231 root Cargo.toml rewrite rules)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-02-SUMMARY.md (what splitter v2 actually does — the tooling built in plan 11-02)
    - Cargo.toml (entire file — the [dependencies] and [workspace] sections being rewritten)
    - tools/maple_to_kernels.py (the driver — how to invoke a full regen; check whether `all --family all` works or families must be run sequentially)
    - tools/regen_phase09.py (the per-functional discovery + atomic dir-replace flow the driver delegates to — confirm it works with the new emit target)
    - crates/kernels/lda-1/Cargo.toml (per-functional subcrate Cargo.toml template)
    - .cargo/config.toml (D-08/D-09 invariant — build env source of truth, do NOT modify)
    - ~/.claude/projects/-home-user-Documents-workspace-libxc-rs/memory/feedback_ram_constraints.md and project_split_threshold_history.md (RAM ceiling + threshold floor context)
  </read_first>
  <action>
    Execute the D-10a clean-slate restructure. Work in this exact order — each step is committed atomically so `git checkout HEAD -- crates/kernels/` can roll back a mid-flight failure.

    STEP 1 — Delete the old layout. Using `git rm -r`:
    - All 27 numbered subcrates: `crates/kernels/lda-1`, `crates/kernels/lda-2`, `crates/kernels/gga-1` ... `crates/kernels/gga-8`, `crates/kernels/mgga-1` ... `crates/kernels/mgga-7`, `crates/kernels/mgga-8a`, `crates/kernels/mgga-8b`, `crates/kernels/mgga-9a`, `crates/kernels/mgga-9b`, `crates/kernels/mgga-10`, `crates/kernels/mgga-11a`, `crates/kernels/mgga-11b`, `crates/kernels/mgga-12`, `crates/kernels/mgga-13`, `crates/kernels/mgga-14`.
    - The 3 family-level crate artifacts ONLY: `crates/kernels/lda/Cargo.toml`, `crates/kernels/lda/src/lib.rs`, `crates/kernels/gga/Cargo.toml`, `crates/kernels/gga/src/lib.rs`, `crates/kernels/mgga/Cargo.toml`, `crates/kernels/mgga/src/lib.rs`. If `crates/kernels/{lda,gga,mgga}/src/` has other content, inspect it — under the old layout the family `src/` held only `lib.rs`; if there is more, it is stale and also removed. The `crates/kernels/{lda,gga,mgga}/` DIRECTORIES must survive (they become plain folders holding per-functional subcrates) — if `git rm` empties them, `mkdir -p` them back before STEP 2.
    - DO NOT delete `crates/kernels/math/`.

    Commit STEP 1 as its own atomic commit (`git commit --only -- <deleted paths>`) BEFORE running STEP 2 — this is the clean rollback point.

    STEP 2 — Regen all per-functional subcrates from Maple via splitter v2. Run the driver (`tools/maple_to_kernels.py` with the splitter-v2 tooling from plan 11-02) for all three families. Use the driver's full-regen entry point; if `all --family all` is not supported, run `--family lda`, `--family gga`, `--family mgga` sequentially. This produces ~264 per-functional subcrates under `crates/kernels/{family}/<func>/`. Run INLINE (D-07 — no worktree subagent). The regen itself is Python (cheap); it does not compile anything.

    STEP 3 — Verify the 5K cap holds: `python3 tools/audit_kernel_size.py --strict` MUST exit 0 (zero files >5000 lines).

    *** CHECKPOINT (mid-task, after STEP 3) ***
    The destructive delete (STEP 1) has already happened and been committed. If STEP 3 fails — any file still exceeds 5000 lines — the CSE pass from plan 11-02 has a gap. DO NOT proceed to STEP 4. Roll back the regen with `git checkout HEAD -- crates/kernels/` (this restores to the post-STEP-1 committed state — the old subcrates are gone, the working tree is clean) and return CHECKPOINT REACHED naming the offending file(s) and their line counts so plan 11-02's CSE heuristic can be retuned. This is the D-LOCK-B hard gate; do not proceed with oversized files. (The post-STEP-1 commit is the documented recovery point — the phase resumes from there once 11-02's CSE pass is fixed and re-run.)

    STEP 4 — Rewrite root `Cargo.toml`:
    - `[dependencies]`: KEEP `libxc-kernel-math`. DELETE every `libxc-kernel-{lda,gga,mgga}` family dep and every `libxc-kernel-{lda,gga,mgga}-N` numbered dep. ADD `libxc-kernel-<func> = { path = "crates/kernels/{family}/<func>" }` for each of the ~264 functionals (including the 7 deferred — they need a dep entry so `cargo build -p libxc-kernel-<func>` works on demand). Generate this list programmatically/deterministically (sorted) from the regenerated subcrate directories so D-LOCK-D idempotency holds.
    - `[workspace] default-members`: replace the numbered list with the ~257 ROUTED per-functional subcrate paths. EXCLUDE the 7 deferred kernels (`mgga_c_b94`, `mgga_x_br89`, `mgga_x_mbr`, `mgga_x_mbrxc_bg`, `mgga_x_mbrxh_bg`, `mgga_x_mggac`, `mgga_x_br89_explicit`) — per D-11 they are workspace members but NOT default-members, so `cargo build` skips them. KEEP `crates/kernels/math`. Sorted, deterministic.
    - `[workspace] members` (xtask, verify, libxc-sys): UNCHANGED.
    - Add a comment block above `default-members` noting the 7 D-11 exclusions and why.

    STEP 5 — Per-subcrate compile spot-check (D-12: NOT a whole-workspace gate). Pick ONE representative routed subcrate per family that the regen produced — recommend a small one (e.g. `lda_x`, a small GGA, a small MGGA) — and run `cargo build -p libxc-kernel-<func>` for each, INLINE, reading the build env from `.cargo/config.toml` (do NOT pass `--jobs`, do NOT set `CARGO_BUILD_JOBS`/`RUST_MIN_STACK`). Each must compile. Record the chosen subcrate names — Task 2's fallback ladder reuses these as the `--extern` rlib targets for the dispatch path-resolution check. If a representative subcrate fails to compile, it indicates an emit bug — STOP and return CHECKPOINT REACHED with the cargo error. (Dispatch-dependent compilation is verified in Task 2; this step only proves the kernel subcrates themselves compile.)

    Commit STEPS 2-5 as a second atomic commit (regen+manifest commit). Use `git commit --only -- <paths>` to avoid sweeping unrelated pre-staged files.
  </action>
  <verify>
    <automated>python3 tools/audit_kernel_size.py --strict && bash tools/audit_subcrate_collapse.sh</automated>
  </verify>
  <acceptance_criteria>
    - `find crates/kernels -maxdepth 1 -mindepth 1 -type d -printf '%f\n' | grep -cE '^(lda|gga|mgga)-[0-9]'` equals `0` (no numbered subcrates).
    - `ls crates/kernels/lda/Cargo.toml crates/kernels/gga/Cargo.toml crates/kernels/mgga/Cargo.toml 2>/dev/null | wc -l` equals `0` (no family-level crates).
    - `test -d crates/kernels/lda && test -d crates/kernels/gga && test -d crates/kernels/mgga && test -d crates/kernels/math` (family dirs survive as folders; math survives as a crate).
    - `python3 tools/audit_kernel_size.py --strict` exits `0` (zero files >5000 lines).
    - `bash tools/audit_subcrate_collapse.sh` exits `0`.
    - `find crates/kernels/lda crates/kernels/gga crates/kernels/mgga -maxdepth 2 -name Cargo.toml | wc -l` is at least `200` (~264 per-functional subcrates created).
    - `grep -cE 'libxc-kernel-(lda|gga|mgga)(-[0-9]| )' Cargo.toml` equals `0` (no numbered, no family-façade deps in root manifest).
    - `grep -c 'mgga_c_b94' Cargo.toml` is at least `1` (deferred kernel HAS a dep entry) but `default-members` does NOT list any of the 7 deferred kernels — verify `grep -A300 'default-members' Cargo.toml | grep -cE 'mgga_c_b94|mgga_x_br89|mgga_x_mbr|mgga_x_mbrxc_bg|mgga_x_mbrxh_bg|mgga_x_mggac|mgga_x_br89_explicit'` equals `0`.
    - The STEP-1 delete is a separate committed checkpoint from the STEP 2-5 regen commit (two commits in this task's session; `git log --oneline` shows the delete commit before the regen commit).
    - At least one representative `cargo build -p libxc-kernel-<func>` per family succeeded (recorded in the SUMMARY with the chosen subcrate names and build times).
    - No commit in this task's session passed `--jobs`, set `CARGO_BUILD_JOBS`, or modified `.cargo/config.toml`.
  </acceptance_criteria>
  <done>The old layout is gone, ~264 per-functional subcrates are regenerated from Maple with every file ≤5000 lines, the root manifest lists per-functional deps with the 7 deferred kernels excluded from default-members, and representative subcrates compile under the unchanged build envelope. The STEP-1 delete is a committed rollback point.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the dispatch tree against per-functional subcrates (close Blocker B1)</name>
  <files>tools/generate_gga_dispatch.py, tools/generate_mgga_dispatch.py, src/eval/gga_dispatch, src/eval/mgga_dispatch, src/kernel/mod.rs</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-DISPATCH-AUDIT.md (Blocker B1 full detail — the stale batchN references and the D-10b resolution path)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-10b — dispatch imports per-functional subcrates; drop batchN; recommended funcs/<func>.rs shape)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md (lines 234-271 — src/eval/{gga,mgga}_dispatch regeneration spec + generate_gga_dispatch.py modification scope; patterns to preserve verbatim: MggaLaunchCtx, the dispatch macros, zero_field!/readback!, the top-level match)
    - tools/generate_gga_dispatch.py (entire file — self-analog; L29 ROSTER path, L36 load_roster, L330 main, L344 pub mod batchN, L523-525 the outer match)
    - src/eval/gga_dispatch/mod.rs (the top-level dispatch_gga match + launch macro — patterns to preserve)
    - src/eval/mgga_dispatch/mod.rs (the top-level dispatch_mgga match + MggaLaunchCtx + macros — patterns to preserve)
    - src/eval/mgga_dispatch/batch17.rs (a representative per-batch dispatch helper — the path shape being rewritten: `crate::kernel::mgga::batch17::<func>::...`)
    - src/kernel/mod.rs (the root kernel re-export tree — `pub use libxc_kernel_lda as lda;` must become per-functional re-exports)
    - .planning/phases/04-bulk-kernel-translation/gga_roster.tsv and mgga_roster.tsv (the dispatch generator inputs — the `batch` column becomes degenerate)
    - tools/audit_dispatch_tree.sh (the B1 audit — must exit 0 after this task)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md is NOT yet written at Task-2 time — but the Task-1 SUMMARY notes / commit messages record the three spot-check subcrate names from STEP 5; reuse those as the `--extern` rlib targets in the fallback ladder below.
  </read_first>
  <action>
    Regenerate the dispatch tree so every dispatch path resolves against per-functional subcrates with NO `batchN::` segment (D-10b). This closes Blocker B1.

    --- `tools/generate_gga_dispatch.py` (modify) + `tools/generate_mgga_dispatch.py` (modify or create) ---

    First confirm whether `tools/generate_mgga_dispatch.py` exists. If it does NOT, create it by mirroring `generate_gga_dispatch.py` against `.planning/phases/04-bulk-kernel-translation/mgga_roster.tsv`, preserving the MGGA-specific patterns (`MggaLaunchCtx`, lapl/tau arrays, the `mgga_zero_scalar_unpol_dispatch!` macro). Document in the SUMMARY whether it was created or modified.

    Modification scope for BOTH generators:
    - STOP emitting `batchN.rs` files and `pub mod batchN;` lines in `mod.rs`.
    - Emit one dispatch helper per functional under `src/eval/{family}_dispatch/funcs/<func>.rs` (the D-10b recommended shape). Each `funcs/<func>.rs` contains the `dispatch_<func>` helper with kernel paths `crate::kernel::{family}::<func>::<output>::<func>_<output>` — NO `batchN::` segment.
    - The `mod.rs` top-level `match functional { ... }` stays — it delegates to `funcs::<func>::dispatch_<func>` instead of `batchN::dispatch_<func>`. Emit `pub mod funcs;` and `funcs/mod.rs` enumerating `pub mod <func>;`.
    - The `batch` column in the roster TSVs is now degenerate — the generator ignores it (do not require roster edits; just stop reading the column, or read-and-ignore).
    - PRESERVE VERBATIM (per 11-PATTERNS.md): `MggaLaunchCtx<'a>` struct, the `mgga_zero_scalar_unpol_dispatch!` / GGA-equivalent macros (including the `ArrayArg::from_raw_parts` arity — confirm 3-arg `from_raw_parts::<f64>(handle, len, 1)` vs 2-arg against cubecl 0.10 at each call site), `zero_field!` / `readback!` macros, the top-level entry-point match shape, and the `#![allow(unused_imports, unused_variables, clippy::too_many_arguments)]` dispatch-file header. Regenerate the "Auto-generated ... do not edit by hand" doc-comment.

    --- `src/kernel/mod.rs` (modify) ---

    The family façade crates no longer exist, so `pub use libxc_kernel_lda as lda;` (etc.) is broken. Replace each family re-export with a module that `pub use`s each per-functional subcrate under the family namespace, so `crate::kernel::{family}::<func>::...` still resolves. Generate this deterministically — recommend the dispatch generator (or a small helper) emits `src/kernel/{family}.rs` files each containing `pub use libxc_kernel_<func> as <func>;` for every functional in that family, and `src/kernel/mod.rs` declares `pub mod lda; pub mod gga; pub mod mgga;`. The 7 deferred kernels ARE re-exported here (runtime routing in `MggaFunctional::from_id` rejects them via `is_deferred`, but the path must still resolve for the crate to compile — or, if a deferred kernel is not in default-members and not depended-on by root, exclude it from the re-export too; verify which is correct by checking whether root `libxc_rs` depends on the deferred kernels — per Task 1 STEP 4 they DO get a root dep entry, so re-export them).

    --- Run the generators ---

    Run both dispatch generators INLINE. They emit `src/eval/{gga,mgga}_dispatch/funcs/*.rs`, regenerated `mod.rs`, and the `src/kernel/{family}.rs` re-export files (or wherever the helper writes them). Delete the old `src/eval/{gga,mgga}_dispatch/batch*.rs` files via `git rm` (they are superseded).

    --- Verify ---

    First: `bash tools/audit_dispatch_tree.sh` MUST exit 0. After the regen there are no `crate::kernel::{family}::batchN` references, so the audit's "referenced" set is empty and it passes trivially — that is the correct post-collapse state. BUT BE EXPLICIT: this trivial pass proves only that no `batchN` references survive; it does NOT prove the new `crate::kernel::{family}::<func>` re-export paths actually resolve. That proof is the SEPARATE load-bearing gate below.

    Second — the dispatch path-resolution gate (load-bearing): run `cargo check -p libxc_rs` INLINE (build env from `.cargo/config.toml`). This is the first time the root crate's dispatch tree is path-checked against the new layout. It is NOT a whole-workspace gate (D-12) — it is `-p libxc_rs` only, and it proves the dispatch paths resolve. If it OOMs before completing (the root crate pulls in ~257 kernel deps), that is the known D-12 risk — but the fallback MUST STILL type-check the `crate::kernel::{family}::<func>` re-export paths central to D-10b. "Skip verification" is NOT an acceptable fallback. Apply this fallback ladder, in order, taking the first rung that completes within the RAM envelope:

      RUNG 1 — `cargo check -p libxc_rs --no-default-features`: exercises the dispatch tree + the `src/kernel/{family}.rs` re-export paths against whatever subset of kernel deps the default-feature set drops. If `libxc_rs` has no feature-gated kernel deps this is equivalent to the full check; either way it still type-checks `src/kernel/{lda,gga,mgga}.rs`.

      RUNG 2 — narrowest rustc invocation that resolves the re-export paths: use the three spot-check subcrates Task 1 STEP 5 already built (their rlibs are in the cached target dir under `.cache/cargo-target`). For each family, run `rustc --edition 2024 --crate-type lib --extern libxc_kernel_<func>=<path-to-rlib> [more --extern flags] src/kernel/<family>.rs` so the per-family re-export file is type-checked with explicit `--extern` flags pointing at the already-built spot-check subcrate rlibs. This resolves `crate::kernel::{family}::<func>::...` for the spot-check functionals without pulling all ~264 kernel deps. (Locate each rlib with `find .cache/cargo-target -name 'liblibxc_kernel_<func>*.rlib'`.)

      RUNG 3 — if even RUNG 2 is infeasible, build a tiny throwaway `--test` or bin target that `use`s the dispatch funcs for the three spot-check functionals and `cargo check` it; this transitively forces resolution of the re-export paths for those functionals.

    The fallback must NOT be "skip verification". Whichever rung runs, the SUMMARY MUST record: (a) which rung ran and why (full check OOM'd / completed), (b) explicit confirmation it type-checked the `src/kernel/{family}.rs` re-export paths, (c) its result. The load-bearing gate for this task is: `audit_dispatch_tree.sh` exits 0 AND no `batchN` in the dispatch tree AND one fallback-ladder rung type-checked the re-export paths successfully.

    Commit as one atomic commit (`git commit --only -- <paths>`).
  </action>
  <verify>
    <automated>bash tools/audit_dispatch_tree.sh && ! grep -rqE 'batch[0-9]+[a-z]*::' src/eval/gga_dispatch src/eval/mgga_dispatch</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/audit_dispatch_tree.sh` exits `0` (Blocker B1 closed).
    - `grep -rE 'batch[0-9]' src/eval/gga_dispatch src/eval/mgga_dispatch | grep -v '^Binary' | wc -l` equals `0` (no batchN segment survives anywhere in the dispatch tree).
    - `find src/eval/gga_dispatch/funcs src/eval/mgga_dispatch/funcs -name '*.rs' 2>/dev/null | wc -l` is at least `150` (per-functional dispatch helpers emitted).
    - `ls src/eval/gga_dispatch/batch*.rs src/eval/mgga_dispatch/batch*.rs 2>/dev/null | wc -l` equals `0` (old per-batch files removed).
    - `grep -c 'libxc_kernel_' src/kernel/lda.rs src/kernel/gga.rs src/kernel/mgga.rs 2>/dev/null` reports per-functional re-exports in each (each at least `1`).
    - `grep -rcE 'crate::kernel::(gga|mgga)::[a-z][a-z0-9_]*::' src/eval/gga_dispatch src/eval/mgga_dispatch` shows per-functional paths present (sum at least `1`).
    - `tools/generate_mgga_dispatch.py` exists and is at least 100 lines (created or pre-existing-and-modified — stated in SUMMARY).
    - The dispatch generators preserved `MggaLaunchCtx` and the dispatch macros: `grep -c 'MggaLaunchCtx' src/eval/mgga_dispatch/mod.rs` is at least `1`.
    - The dispatch path-resolution gate ran: one fallback-ladder rung (`cargo check -p libxc_rs`, OR `cargo check -p libxc_rs --no-default-features`, OR the `rustc --extern` per-family check, OR the throwaway-target check) type-checked the `src/kernel/{lda,gga,mgga}.rs` re-export paths and SUCCEEDED — the SUMMARY records which rung ran, that it exercised the re-export paths, and its result. `audit_dispatch_tree.sh` passing alone does NOT satisfy this criterion.
    - No `--jobs`/`CARGO_BUILD_JOBS`/`.cargo/config.toml` override was used.
  </acceptance_criteria>
  <done>The dispatch tree is regenerated against per-functional subcrates with no batchN segment, `src/kernel/mod.rs` re-exports per-functional subcrates under family namespaces, both dispatch generators emit the new shape, `audit_dispatch_tree.sh` exits 0, AND a cargo/rustc path-resolution rung has type-checked the `crate::kernel::{family}::<func>` re-export paths — Blocker B1 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| splitter v2 tooling → emitted kernel tree → rustc | Build-tooling integrity. A mid-flight regen failure leaves a half-broken tree (D-10a acknowledged risk). The clean-slate delete is destructive; rollback safety is the primary concern. No network/auth/runtime-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering / Repudiation | clean-slate delete + regen | mitigate | A mid-flight failure between delete and regen leaves the workspace unbuildable with no clean recovery point. Mitigation: STEP 1 (delete) is committed as its own atomic commit BEFORE STEP 2 runs; the mid-task CHECKPOINT after STEP 3 documents the `git checkout HEAD -- crates/kernels/` rollback to that post-delete commit; STEPS 2-5 are a second atomic commit; per-subcrate `cargo build -p` spot-checks catch emit bugs before the regen commit. |
| T-11-03-02 | Spoofing (false-green) | `audit_kernel_size.py --strict` gate | mitigate | If the regen silently leaves an oversized file, the 5K-cap invariant (D-LOCK-B) is violated but later waves might not notice. Mitigation: STEP 3 makes `--strict` exit 0 a HARD gate — any oversized file STOPS the plan with a CHECKPOINT REACHED (with a documented rollback command), not a silent pass. |
| T-11-03-03 | Tampering | root `Cargo.toml` default-members | mitigate | If a deferred kernel (D-11) leaks into `default-members`, `cargo build` would try to compile a known-unbuildable/unrouted kernel and OOM or fail. Mitigation: explicit 7-kernel exclusion list; acceptance criterion greps `default-members` for all 7 names and requires `0` matches. |
| T-11-03-04 | Spoofing (false-green) | `audit_dispatch_tree.sh` post-regen | mitigate | After the regen the audit passes trivially (empty "referenced" set) — it no longer actively proves dispatch path resolution, only that no batchN references remain. Mitigation: Task 2 adds a SEPARATE load-bearing path-resolution gate (the cargo/rustc fallback ladder) that type-checks the `crate::kernel::{family}::<func>` re-export paths; "skip verification" is explicitly forbidden as a fallback; the acceptance criterion requires a rung to have SUCCEEDED, not just `audit_dispatch_tree.sh` to pass. |
| T-11-03-05 | Denial of Service (RAM) | `cargo check -p libxc_rs` with ~257 kernel deps | mitigate | The root crate now depends on ~257 kernel subcrates; a full `cargo check -p libxc_rs` may OOM (the exact D-12 risk). Mitigation: D-12 de-gates whole-workspace build; the fallback ladder degrades to `--no-default-features`, then a `rustc --extern` per-family check against the already-built spot-check rlibs, then a throwaway target — each rung still type-checks the re-export paths. Build envelope (`.cargo/config.toml` jobs=1, 64MB stack) is unchanged. |
</threat_model>

<verification>
End-of-plan checks (run after both tasks):
1. `python3 tools/audit_kernel_size.py --strict` exits `0` (zero files >5000 lines — P11-INV-2).
2. `bash tools/audit_subcrate_collapse.sh` exits `0` (no numbered subcrates, no family crates — P11-INV-1).
3. `bash tools/audit_dispatch_tree.sh` exits `0` (Blocker B1 closed).
4. `bash tools/audit_cube_launch.sh` exits `0` (launch_unchecked count ≤ baseline 23 — P11-INV-5; the regen must not introduce new launchable kernels).
5. `grep -rE 'batch[0-9]' src/eval/gga_dispatch src/eval/mgga_dispatch | wc -l` equals `0`.
6. Root `Cargo.toml` `default-members` excludes all 7 deferred kernels; per-functional deps present.
7. Representative `cargo build -p libxc-kernel-<func>` per family succeeded (recorded in SUMMARY).
8. A dispatch path-resolution rung (cargo/rustc) type-checked the `src/kernel/{family}.rs` re-export paths (recorded in SUMMARY) — `audit_dispatch_tree.sh` passing alone is insufficient.

Whole-workspace `cargo build` is NOT run as a gate (D-12). Verification is per-subcrate + audit-tool + path-resolution.
</verification>

<success_criteria>
- 27 numbered subcrates + 3 family-level crates deleted; family dirs survive as plain folders; math survives as a crate.
- ~264 per-functional subcrates regenerated from Maple, every file ≤5000 lines.
- Root `Cargo.toml` rewritten: per-functional deps, default-members excludes the 7 deferred kernels.
- Dispatch tree regenerated against per-functional paths, no batchN segment, Blocker B1 closed.
- All four audit tools (`audit_kernel_size.py --strict`, `audit_subcrate_collapse.sh`, `audit_dispatch_tree.sh`, `audit_cube_launch.sh`) exit 0, AND a cargo/rustc path-resolution rung type-checked the re-export paths.
- No `.cargo/config.toml` modification; no cargo jobs/env override.
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md` covering:
- The clean-slate delete: confirmation all 27 numbered + 3 family crates are gone, family dirs survive; the STEP-1 delete commit hash (the rollback point).
- Splitter v2 regen result: subcrate count produced, `audit_kernel_size.py --strict` result, any functionals that needed CSE-heuristic attention.
- Root `Cargo.toml` rewrite: dep count, default-members count, the 7 D-11 exclusions confirmed.
- Dispatch regen: whether `generate_mgga_dispatch.py` was created or modified; `audit_dispatch_tree.sh` result; which fallback-ladder rung ran for the path-resolution gate, confirmation it exercised the `src/kernel/{family}.rs` re-export paths, and its result.
- Representative per-subcrate `cargo build -p` results (subcrate names + build times) — these are the rlib targets the path-resolution rung may have reused.
- Any deviations (e.g., a deferred kernel that needed special handling, an oversized-file CHECKPOINT).
</output>
</output>
