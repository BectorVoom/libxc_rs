<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - tools/audit_cube_launch.sh
  - tools/generate_gga_dispatch.py
  - tools/generate_mgga_dispatch.py
  - tools/generate_kernel_reexports.py
  - src/eval/gga_dispatch
  - src/eval/mgga_dispatch
  - src/eval/dispatch.rs
  - src/kernel/mod.rs
  - src/kernel/lda.rs
  - src/kernel/gga.rs
  - src/kernel/mgga.rs
autonomous: false
requirements:
  - P11-INV-1
  - P11-INV-2
  - P11-INV-5
  - P11-INV-6

must_haves:
  truths:
    - "Plan 11-03 Task 1's committed state (clean-slate delete commit `95727cb36` + regen+manifest commit `97d6347be`) is re-confirmed by a NON-DESTRUCTIVE verify-only checkpoint: `tools/audit_subcrate_collapse.sh` exits 0, `tools/audit_kernel_size.py --strict` exits 0, the root `Cargo.toml` greps pass — without re-running any delete or regen."
    - "`tools/audit_cube_launch.sh` is rewritten to the D-13 per-design budget: it asserts (a) every ROUTED functional has exactly one `#[cube(launch_unchecked)]` entry kernel per emitted output module, (b) NO unrouted functional kernel is launchable, (c) `crates/kernels/math/src/` `#[cube(launch_unchecked)]` count is ≤22 — NOT a flat `≤23` count."
    - "The dispatch tree (`src/eval/{gga,mgga}_dispatch/`) is complete against per-functional subcrates building on WIP `c3fba8089` — no `batchN::` segment survives; `tools/audit_dispatch_tree.sh` exits 0 (Blocker B1 closed)."
    - "The `crate::kernel::{family}::<func>` re-export paths are type-checked by a cargo/rustc path-resolution gate (the fallback ladder), AND the rewritten `audit_cube_launch.sh` (D-13 form) passes as a gate."
  artifacts:
    - path: "tools/audit_cube_launch.sh"
      provides: "D-13 per-design launch-budget audit (routed-one-per-output, unrouted-zero, math ≤22)"
      min_lines: 40
    - path: "tools/generate_mgga_dispatch.py"
      provides: "MGGA dispatch generator emitting per-functional paths (D-10b)"
      min_lines: 100
    - path: "tools/generate_gga_dispatch.py"
      provides: "GGA dispatch generator emitting per-functional paths (no batchN segment)"
      min_lines: 100
  key_links:
    - from: "src/eval/mgga_dispatch"
      to: "crates/kernels/mgga/<func>"
      via: "crate::kernel::mgga::<func>::... re-export (no batchN segment)"
      pattern: "crate::kernel::(gga|mgga)::[a-z]"
    - from: "tools/audit_cube_launch.sh"
      to: "tools/kernel_routing.py"
      via: "routed-vs-unrouted gate drives the per-design launch budget"
      pattern: "kernel_routing|cached_routed_funcnames|routed"
---

<objective>
Finish Wave 2 under D-13. Plan 11-03 was paused mid-execution: Task 1 (clean-slate delete + 266-subcrate regen + root manifest rewrite) is COMMITTED and verified (`95727cb36`, `97d6347be`); Task 2 (dispatch-tree regen) has sound but INCOMPLETE WIP at `c3fba8089`. The pause cause was a plan-level contradiction — the original P11-INV-5 demanded a flat `≤23` `#[cube(launch_unchecked)]` count, but the D-10b dispatch macros (preserved verbatim) call `.launch_unchecked()` per `(functional × output module)`, so `audit_cube_launch.sh` reports 1677. D-13 resolves this: keep the macros, redefine the invariant, and rewrite `audit_cube_launch.sh` to a per-design budget.

This regenerated plan: (1) re-confirms Task 1's committed state with a verify-only checkpoint — NO destructive re-run; (2) rewrites `tools/audit_cube_launch.sh` to the D-13 per-design budget — a load-bearing task that MUST land before the dispatch-completion verification; (3) completes the dispatch-tree regeneration on top of WIP `c3fba8089`, runs the cargo/rustc path-resolution gate, and makes the D-13 `audit_cube_launch.sh` a passing gate.

Purpose: D-13 (user decision 2026-05-15) is the resolution of the P11-INV-5 contradiction. The per-functional subcrate restructure (D-10) is itself the structural mitigation the original flat-count invariant was guarding for — each subcrate holds only its own ~10 launch wrappers and compiles independently under `jobs = 1`; the ~1677 never expand in one rustc invocation. The audit must therefore assert the per-design budget, not a flat count.

Output: A re-confirmed Task-1 state, a rewritten `tools/audit_cube_launch.sh` (D-13 form), a complete dispatch tree with no batchN segment, and a cargo/rustc path-resolution rung that type-checked the `crate::kernel::{family}::<func>` re-export paths. Whole-workspace build is NOT a gate (D-12).
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-DISPATCH-AUDIT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/.continue-here.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-02-SUMMARY.md
@.cargo/config.toml

<interfaces>
<!-- The Wave-2 completion surface. Extracted from the codebase + git — do NOT re-explore. -->

GIT STATE (already committed — DO NOT re-run these):
  95727cb36  refactor(11-03): clean-slate delete 27 numbered subcrates + 3 family crates
  97d6347be  feat(11-03): regen 266 per-functional subcrates + rewrite root manifest
  c3fba8089  wip(11-03): regenerate dispatch tree per-functional — INCOMPLETE
  fe0db03f1  docs(11): pause phase 11 at 11-03 Task 2
  10127cea3  docs(11): revise P11-INV-5 per D-13
The verify-only checkpoint (Task 1 below) re-confirms `95727cb36`+`97d6347be`
WITHOUT re-running the destructive delete/regen. `.continue-here.md` Critical
Anti-Patterns: re-running Task 1's destructive delete/regen is BLOCKING-bad.

WIP `c3fba8089` already did (sound, builds on Task 1 — DO NOT redo, build ON it):
  - Relocated the `deferred` registry into crates/kernels/math/src/deferred.rs
    (lda + mgga submodules); rewired src/model/{lda,mgga}_functional.rs to
    libxc_kernel_math::deferred::{lda,mgga}::is_deferred. [carry-forward deviation]
  - tools/generate_gga_dispatch.py: emits per-functional funcs/<func>.rs (D-10b),
    drops the batchN layer, kernel paths via crate::kernel::gga::<func>.
  - tools/generate_mgga_dispatch.py: CREATED — MGGA mirror; preserves MggaLaunchCtx,
    mgga_zero_scalar_unpol_dispatch!, zero_field!, readback! VERBATIM.
  - tools/generate_kernel_reexports.py: CREATED — emits src/kernel/{lda,gga,mgga}.rs
    per-functional re-export façade (266 re-exports); src/kernel/mod.rs rewired.
  - src/eval/{gga,mgga}_dispatch/ regenerated — 106 GGA + 26 MGGA funcs/ helpers;
    old batch*.rs removed. src/eval/dispatch.rs rewired (36 LDA import sites).
  - audit_dispatch_tree.sh exits 0 (Blocker B1 closed); 0 batchN refs survive.
WIP `c3fba8089` did NOT do:
  - The cargo/rustc path-resolution gate was NEVER run (build env was uncapped).
    The dispatch + re-export paths are NOT yet type-checked.
  - 11-03-SUMMARY.md not written (this regenerated plan's executor writes it).
  - The D-13 audit rewrite (this plan's Task 2 — new, load-bearing).

CURRENT audit_cube_launch.sh (tools/audit_cube_launch.sh — being REWRITTEN by Task 2):
  - Flat count: `BASELINE=23`; fails if total `#[cube(launch_unchecked)]` under
    crates/kernels/ exceeds 23. Currently reports 1677 → fails. This flat form is
    the unsatisfiable invariant D-13 replaces.

Measured launch-surface facts (use these in the Task-2 rewrite):
  - Total `#[cube(launch_unchecked)]` under crates/kernels/ : 1677.
  - crates/kernels/math/src/ (excluding tests/) : exactly 22. The +1 (=23 total
    in math/) is crates/kernels/math/tests/spike_tuple_return_cube.rs — a TEST
    file. The D-13 math budget is `crates/kernels/math/src/` ≤22; the audit MUST
    exclude `crates/kernels/math/tests/`.
  - Routed kernel subcrate entry kernels carry `#[cube(launch_unchecked)]` exactly
    once per emitted output module (emit.py routing-aware launch policy, plan
    11-02 — correct as-is under D-13). Unrouted functional entry kernels carry
    plain `#[cube]` (the §4 anti-pattern fix from quick task 260512-q01).
    D-02 CSE chunk helpers (`partNN` / tuple-return `<F: Float>`) are plain
    `#[cube]`, never launchable.

tools/kernel_routing.py (the "is routed?" source of truth — KEEP UNCHANGED):
  cached_routed_funcnames(family: str) -> set[str]   # family in {lda,gga,mgga}
  collect_func_dirs(family: str) -> dict[str, list[Path]]   # on-disk func dir names
  CLI: `python3 tools/kernel_routing.py` prints "FAM: routed=N dirs=M unrouted=K"

tools/audit_dispatch_tree.sh (Blocker B1 audit — already exits 0 after WIP
  c3fba8089). After the collapse it greps for `crate::kernel::{family}::batchN`
  references; there are none, so the "referenced" set is empty and it passes
  TRIVIALLY. That trivial pass proves only that no batchN references survive —
  it does NOT prove the new `crate::kernel::{family}::<func>` re-export paths
  resolve. That proof is the SEPARATE load-bearing cargo/rustc path-resolution
  gate in Task 3.

src/kernel/ current shape (from WIP c3fba8089):
  src/kernel/mod.rs       — rewired; declares pub mod lda; pub mod gga; pub mod mgga;
  src/kernel/{lda,gga,mgga}.rs — per-functional re-export façade (266 `pub use
    libxc_kernel_<func> as <func>;` lines) emitted by generate_kernel_reexports.py.
  src/eval/{gga,mgga}_dispatch/{mod.rs,funcs/<func>.rs} — regenerated dispatch tree.
  src/eval/dispatch.rs    — LDA dispatch, rewired to crate::kernel::lda::<func>.

From .cargo/config.toml (D-08/D-09 — do NOT modify, do NOT duplicate):
  [build] jobs = 1, target-dir = .cache/cargo-target
  [env] RUST_MIN_STACK = "67108864"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify-only re-confirmation of Task 1's committed state (NO destructive re-run)</name>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/.continue-here.md (entire file — the pause record; Critical Anti-Patterns table: re-running Task 1's destructive delete/regen is BLOCKING-bad; Task 1 = commits 95727cb36 + 97d6347be, re-verified this session)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-10a clean-slate scope, D-11 deferred-kernel default-members exclusion — the invariants Task 1's output must still satisfy)
    - tools/audit_subcrate_collapse.sh (the collapse audit — must exit 0 against the committed tree)
    - tools/audit_kernel_size.py (the 5K-cap audit — `--strict` must exit 0)
    - Cargo.toml (root manifest — confirm per-functional deps + default-members with the 7 deferred kernels excluded)
  </read_first>
  <what-built>
    Plan 11-03 Task 1 (committed at `95727cb36` clean-slate delete + `97d6347be` regen+manifest) deleted all 27 numbered subcrates + 3 family-level crates, regenerated ~266 per-functional subcrates from Maple, and rewrote the root `Cargo.toml`. This was committed and its acceptance criteria re-verified during the paused session. This regenerated plan does NOT re-execute it — Task 1 here is a NON-DESTRUCTIVE re-confirmation that the committed state still holds, and documents the two commit hashes as the rollback/provenance point for the rest of Wave 2.
  </what-built>
  <action>
    Run ONLY read-only / audit commands. Do NOT run `git rm`, do NOT run the splitter / `maple_to_kernels.py`, do NOT regenerate any subcrate. If any check below fails, STOP and return CHECKPOINT REACHED — a failure means the committed tree drifted and the situation needs human triage, NOT a destructive re-run.

    Run and confirm each:
    1. `bash tools/audit_subcrate_collapse.sh` — MUST exit 0 (no numbered subcrates, no family-level crates).
    2. `python3 tools/audit_kernel_size.py --strict` — MUST exit 0 (zero files >5000 lines).
    3. `find crates/kernels/lda crates/kernels/gga crates/kernels/mgga -maxdepth 2 -name Cargo.toml | wc -l` — MUST be at least 200 (~266 per-functional subcrates present).
    4. `test -d crates/kernels/lda && test -d crates/kernels/gga && test -d crates/kernels/mgga && test -d crates/kernels/math` — family dirs survive as folders; math survives as a crate.
    5. `ls crates/kernels/lda/Cargo.toml crates/kernels/gga/Cargo.toml crates/kernels/mgga/Cargo.toml 2>/dev/null | wc -l` — MUST be 0 (no family-level crates).
    6. `grep -cE 'libxc-kernel-(lda|gga|mgga)(-[0-9]| )' Cargo.toml` — MUST be 0 (no numbered, no family-façade deps in root manifest).
    7. `grep -A400 'default-members' Cargo.toml | grep -cE 'mgga_c_b94|mgga_x_br89|mgga_x_mbr|mgga_x_mbrxc_bg|mgga_x_mbrxh_bg|mgga_x_mggac|mgga_x_br89_explicit'` — MUST be 0 (the 7 deferred kernels excluded from default-members per D-11).
    8. `git log --oneline -8` — confirm `95727cb36` and `97d6347be` are in history (the provenance/rollback point).

    Record all eight results in the eventual 11-03-SUMMARY.md as the "Task 1 re-confirmation" section, naming `95727cb36` + `97d6347be` as the documented provenance/rollback point for Wave 2.
  </action>
  <how-to-verify>
    Confirm: all eight checks above pass. The two checkpoint commits `95727cb36` and `97d6347be` are present in `git log`. No destructive command (`git rm`, splitter run, regen) was executed in this task. If any check fails, this is a CHECKPOINT REACHED for human triage — not a re-run.
  </how-to-verify>
  <acceptance_criteria>
    - `bash tools/audit_subcrate_collapse.sh` exits `0`.
    - `python3 tools/audit_kernel_size.py --strict` exits `0`.
    - `find crates/kernels/lda crates/kernels/gga crates/kernels/mgga -maxdepth 2 -name Cargo.toml | wc -l` is at least `200`.
    - `ls crates/kernels/lda/Cargo.toml crates/kernels/gga/Cargo.toml crates/kernels/mgga/Cargo.toml 2>/dev/null | wc -l` equals `0`.
    - `grep -cE 'libxc-kernel-(lda|gga|mgga)(-[0-9]| )' Cargo.toml` equals `0`.
    - `grep -A400 'default-members' Cargo.toml | grep -cE 'mgga_c_b94|mgga_x_br89|mgga_x_mbr|mgga_x_mbrxc_bg|mgga_x_mbrxh_bg|mgga_x_mggac|mgga_x_br89_explicit'` equals `0`.
    - `git log --oneline -12 | grep -c '95727cb36\|97d6347be'` equals `2` (both Task-1 commits present).
    - NO `git rm`, NO `maple_to_kernels.py` run, NO splitter/regen invocation occurred in this task — the SUMMARY explicitly confirms this.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all eight re-confirmation checks pass and the two provenance commits are confirmed; or describe the drift if any check failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Rewrite tools/audit_cube_launch.sh to the D-13 per-design launch budget</name>
  <files>tools/audit_cube_launch.sh</files>
  <read_first>
    - tools/audit_cube_launch.sh (entire file — the current flat `BASELINE=23` count check being REPLACED)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-13 in full — the three per-design assertions, the rationale, the rejected alternative; also D-11 for the deferred-kernel set)
    - .planning/phases/11-splitter-v2-unified-5k-cap/.continue-here.md ("The blocker — why a replan is needed" section — the 1677-vs-23 contradiction and why emit.py is correct as-is)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md (lines 200, 448, 582, 595 — the revised P11-INV-5 / audit_cube_launch.sh budget description)
    - tools/kernel_routing.py (entire file — `cached_routed_funcnames(family)`, `collect_func_dirs(family)`, the CLI `__main__` block; this is the "is routed?" source of truth the audit consumes)
    - tools/audit_subcrate_collapse.sh (sibling audit — copy its `set -euo pipefail` + `REPO_ROOT` derivation + `Build env source of truth: .cargo/config.toml` header-comment style VERBATIM for consistency)
  </read_first>
  <action>
    REPLACE the entire body of `tools/audit_cube_launch.sh`. The current script is a flat `BASELINE=23` count check — D-13 makes that unsatisfiable. The new script asserts a per-DESIGN budget, NOT a flat count. It MUST land before Task 3's verification, because Task 3's gate cites this rewritten audit.

    Keep the script header style consistent with `tools/audit_subcrate_collapse.sh`: `#!/usr/bin/env bash`, `set -euo pipefail`, `REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"`, and a header comment block that states this is the D-13 per-design launch-budget audit for P11-INV-5 and cites `.cargo/config.toml` as the build-env source of truth (the script itself runs no cargo — it is a filesystem + routing-table audit).

    The script invokes `tools/kernel_routing.py` to obtain the routed/unrouted partition. Recommended: add a small `__main__`-style invocation OR call `python3 -c "..."` importing `kernel_routing`. The simplest robust approach: a `python3` helper invocation that, given a family, prints the routed funcnames and the on-disk funcnames so bash can diff them — OR (cleaner) implement the whole audit logic as a `python3` block the bash script execs, since the routing partition is most naturally consumed in Python. Either is acceptable; spell out the chosen approach in the script's header comment. The THREE assertions the script MUST make, each with a clear `FAIL:` message and `exit 1` on violation:

    ASSERTION 1 — ROUTED functionals: exactly one `#[cube(launch_unchecked)]` entry kernel per emitted output module.
      For each family in {lda, gga, mgga}, for each ROUTED functional `<func>` (from `kernel_routing.cached_routed_funcnames(family)`):
        - Enumerate that functional's emitted output modules. An output module is each `.rs` file directly under `crates/kernels/{family}/<func>/src/` that is NOT `lib.rs` (e.g. `exc_unpol.rs`, `vxc_pol.rs`), PLUS each subdirectory under `crates/kernels/{family}/<func>/src/` that contains a `mod.rs` (the nested-by-output D-04 layout, e.g. `kxc_pol/`). Equivalently: the set of `pub mod <output>;` lines in that functional's `src/lib.rs` is the authoritative output-module list — read `src/lib.rs` and take each `pub mod` name.
        - For each output module, count `#[cube(launch_unchecked)]` occurrences (excluding `//`-comment lines) in the module's ENTRY file: `src/<output>.rs` for a single-file output, or `src/<output>/mod.rs` for a nested output. The `partNN.rs` chunk files and any `mod.rs`-declared `mod partNN;` helpers are NOT entry files — they must carry plain `#[cube]` and contribute ZERO launchables.
        - The count per output-module entry file MUST be exactly 1. FAIL if any routed `(functional, output)` entry file has 0 launchables (routed but not launchable — dispatch macro would not resolve) OR more than 1 (excess launch surface).
      Report the total routed `(functional, output)` pair count on PASS (this is the expected ~1677-class number — it is the per-design budget, and that is correct under D-13).

    ASSERTION 2 — UNROUTED functionals: zero launchable kernels.
      For each family, for each functional directory under `crates/kernels/{family}/` that is NOT in the routed set (the unrouted set = `collect_func_dirs(family)` keys minus `cached_routed_funcnames(family)` — this includes the deferred kernels and `mgga_x_br89_explicit`):
        - Count `#[cube(launch_unchecked)]` occurrences (excluding `//`-comment lines) across ALL `.rs` files under `crates/kernels/{family}/<func>/`.
        - The count MUST be 0. FAIL listing every unrouted functional that has ≥1 launchable kernel (the §4 anti-pattern — an unrouted entry kernel left launchable; quick task 260512-q01's routing-aware-emit fix must hold).

    ASSERTION 3 — math/ budget: `crates/kernels/math/src/` launch count ≤22.
      Count `#[cube(launch_unchecked)]` occurrences (excluding `//`-comment lines) in `crates/kernels/math/src/**/*.rs` — EXCLUDING `crates/kernels/math/tests/` (the Wave-0 spike `spike_tuple_return_cube.rs` lives under `tests/` and is a test fixture, not a production launch wrapper). The count MUST be ≤22. FAIL if >22 (Phase 11 MUST NOT add launchables to `math/`).

    On all three passing, print a PASS summary line reporting: total routed (functional, output) launch pairs, unrouted-launchable count (=0), and the `math/src/` count (≤22). `exit 0`.

    The script must be runnable as `bash tools/audit_cube_launch.sh` from anywhere (uses `REPO_ROOT`). It runs no cargo, sets no `CARGO_BUILD_JOBS`/`RUST_MIN_STACK`, passes no `--jobs`.

    Run `bash tools/audit_cube_launch.sh` against the current tree (post-WIP-`c3fba8089`). The EXPECTED outcome: it PASSES — assertion 1 holds because emit.py's routing-aware launch policy emits exactly one launch wrapper per routed output module; assertion 2 holds because unrouted entry kernels are plain `#[cube]`; assertion 3 holds because `math/src/` is exactly 22. If it FAILS, the failure is diagnostic, not a script bug to paper over: a failing assertion 1 or 2 means the regenerated kernel tree (Task 1's `97d6347be`) has a real launch-policy defect — STOP and return CHECKPOINT REACHED naming the offending `(functional, output)` pairs, because the fix routes back into plan 11-02's `emit.py`, not into this audit script. A failing assertion 3 means something added a launchable to `math/src/` — STOP and return CHECKPOINT REACHED.

    Commit `tools/audit_cube_launch.sh` as one atomic commit (`git commit --only -- tools/audit_cube_launch.sh`).
  </action>
  <verify>
    <automated>bash tools/audit_cube_launch.sh && ! grep -qE 'BASELINE=23|count.*-gt.*BASELINE' tools/audit_cube_launch.sh</automated>
  </verify>
  <acceptance_criteria>
    - `tools/audit_cube_launch.sh` no longer contains the flat-count form: `grep -cE 'BASELINE=23' tools/audit_cube_launch.sh` equals `0`.
    - The script references the routing source of truth: `grep -cE 'kernel_routing|cached_routed_funcnames|routed' tools/audit_cube_launch.sh` is at least `1`.
    - The script implements all three D-13 assertions — confirm by content: it checks routed `(functional, output)` entry files for exactly-one launchable, checks unrouted functionals for zero launchables, and checks `crates/kernels/math/src/` for ≤22 (excluding `tests/`). `grep -cE 'routed|unrouted|math' tools/audit_cube_launch.sh` is at least `3`.
    - The script excludes `crates/kernels/math/tests/` from the math budget count: `grep -c 'tests' tools/audit_cube_launch.sh` is at least `1` (the exclusion is explicit).
    - `bash tools/audit_cube_launch.sh` exits `0` against the current post-WIP tree, printing a PASS summary with the routed-launch-pair total, unrouted-launchable count `0`, and `math/src/` count ≤22.
    - The script header comment cites D-13 and `.cargo/config.toml` as the build-env source of truth.
    - `grep -E 'CARGO_BUILD_JOBS|RUST_MIN_STACK|--jobs ' tools/audit_cube_launch.sh` returns no matches.
  </acceptance_criteria>
  <done>`tools/audit_cube_launch.sh` is rewritten to the D-13 per-design launch budget — it asserts one launch wrapper per routed (functional, output), zero launchables for unrouted functionals, and `math/src/` ≤22 — and passes against the current tree. The flat `≤23` count check is gone. This is the gate Task 3's dispatch-completion verification cites.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the dispatch-tree regeneration + run the path-resolution gate (build on WIP c3fba8089)</name>
  <files>tools/generate_gga_dispatch.py, tools/generate_mgga_dispatch.py, tools/generate_kernel_reexports.py, src/eval/gga_dispatch, src/eval/mgga_dispatch, src/eval/dispatch.rs, src/kernel/mod.rs, src/kernel/lda.rs, src/kernel/gga.rs, src/kernel/mgga.rs</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/.continue-here.md ("Status of Plan 11-03" + "Task 2 — what is done / what is NOT done" — the exact WIP state at c3fba8089; the carry-forward deviations: deferred-registry relocation, pre-existing out-of-scope LSP errors)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-DISPATCH-AUDIT.md (Blocker B1 full detail — the stale batchN references and the D-10b resolution path)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-10b — dispatch imports per-functional subcrates; drop batchN; recommended funcs/<func>.rs shape; D-11 deferred kernels)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md (the src/eval/{gga,mgga}_dispatch regeneration spec; patterns to preserve verbatim: MggaLaunchCtx, the dispatch macros, zero_field!/readback!, the top-level match)
    - tools/generate_gga_dispatch.py (entire file — as modified in WIP c3fba8089; confirm it emits per-functional funcs/<func>.rs with no batchN)
    - tools/generate_mgga_dispatch.py (entire file — created in WIP c3fba8089; confirm MGGA mirror, MggaLaunchCtx + macros preserved)
    - tools/generate_kernel_reexports.py (entire file — created in WIP c3fba8089; emits src/kernel/{lda,gga,mgga}.rs)
    - src/eval/gga_dispatch/mod.rs and src/eval/mgga_dispatch/mod.rs (the regenerated top-level dispatch matches — confirm they delegate to funcs::<func>)
    - src/kernel/mod.rs and src/kernel/{lda,gga,mgga}.rs (the regenerated re-export façade)
    - tools/audit_dispatch_tree.sh (the B1 audit — must exit 0; note it passes trivially post-collapse)
    - tools/audit_cube_launch.sh (the D-13-rewritten audit from Task 2 — the gate this task cites)
    - .cargo/config.toml (D-08/D-09 invariant — build env source of truth, do NOT modify)
  </read_first>
  <action>
    Complete the dispatch-tree regeneration. WIP `c3fba8089` already did the bulk of this (generators modified/created, dispatch tree regenerated, `src/kernel/{family}.rs` re-export façade emitted, `audit_dispatch_tree.sh` exits 0). This task: (1) verifies the WIP regen is internally consistent and re-runnable; (2) runs the cargo/rustc path-resolution gate that WIP `c3fba8089` never ran; (3) makes the D-13 `audit_cube_launch.sh` a passing gate. The stale 11-03-PLAN.md's Task 2 `<action>` and fallback ladder remain the correct approach — reuse what is still right; only the verification citation changes from the old `≤23` to the D-13 audit.

    STEP 1 — Re-confirm the WIP dispatch regen is consistent and idempotent.
    - Re-run both dispatch generators INLINE: `python3 tools/generate_gga_dispatch.py` and `python3 tools/generate_mgga_dispatch.py`, and `python3 tools/generate_kernel_reexports.py`. These are Python — they compile nothing. After re-running, `git status --porcelain src/eval/gga_dispatch src/eval/mgga_dispatch src/kernel` MUST be empty (the WIP `c3fba8089` output is reproducible — the generators are deterministic). If there IS a diff, the generators are non-deterministic — STOP, return CHECKPOINT REACHED with the diff; the fix goes back into the generators (this means re-opening the dispatch generators, which is acceptable — idempotency is a hard success criterion).
    - `bash tools/audit_dispatch_tree.sh` MUST exit 0. After the collapse there are NO `crate::kernel::{family}::batchN` references, so the audit's "referenced" set is empty and it passes trivially — that is the correct post-collapse state; do NOT "fix" the audit to keep looking for batchN. BE EXPLICIT in the SUMMARY: this trivial pass proves only that no batchN references survive; it does NOT prove the new re-export paths resolve — that is STEP 3.
    - Confirm no `batchN` segment survives: `grep -rE 'batch[0-9]' src/eval/gga_dispatch src/eval/mgga_dispatch | grep -v '^Binary' | wc -l` MUST be 0.

    STEP 2 — Run the D-13 launch-budget gate.
    - `bash tools/audit_cube_launch.sh` (the Task-2 rewritten D-13 form) MUST exit 0. This is now a load-bearing gate for Wave 2: it asserts one launch wrapper per routed `(functional, output)`, zero unrouted launchables, `math/src/` ≤22. If it fails, STOP and return CHECKPOINT REACHED (the fix routes to emit.py per Task 2's note — not a hand-patch of the dispatch tree).

    STEP 3 — The dispatch path-resolution gate (LOAD-BEARING — WIP c3fba8089 never ran this).
    Run `cargo check -p libxc_rs` INLINE (build env from `.cargo/config.toml`; do NOT pass `--jobs`, do NOT set `CARGO_BUILD_JOBS`/`RUST_MIN_STACK`). This is the first time the root crate's dispatch tree + the `src/kernel/{lda,gga,mgga}.rs` re-export façade is type-checked against the per-functional layout. It is NOT a whole-workspace gate (D-12) — it is `-p libxc_rs` only, and it proves the dispatch paths resolve. If it OOMs before completing (the root crate pulls in ~257 kernel deps — the known D-12 risk), the fallback MUST STILL type-check the `crate::kernel::{family}::<func>` re-export paths. "Skip verification" is NOT an acceptable fallback. Apply this fallback ladder, in order, taking the first rung that completes within the RAM envelope:

      RUNG 1 — `cargo check -p libxc_rs --no-default-features`: exercises the dispatch tree + the `src/kernel/{family}.rs` re-export paths against whatever subset of kernel deps the default-feature set drops. Either way it still type-checks `src/kernel/{lda,gga,mgga}.rs`.

      RUNG 2 — narrowest rustc invocation that resolves the re-export paths: pick one small routed subcrate per family, `cargo build -p libxc-kernel-<func>` each (their rlibs land in `.cache/cargo-target`), then for each family run `rustc --edition 2024 --crate-type lib --extern libxc_kernel_<func>=<path-to-rlib> [more --extern flags] src/kernel/<family>.rs` so the per-family re-export file is type-checked with explicit `--extern` flags pointing at the built rlibs. Locate each rlib with `find .cache/cargo-target -name 'liblibxc_kernel_<func>*.rlib'`.

      RUNG 3 — if even RUNG 2 is infeasible, build a tiny throwaway `--test` or bin target that `use`s the dispatch funcs for three spot-check functionals (one per family) and `cargo check` it; this transitively forces resolution of the re-export paths.

    Whichever rung runs, the SUMMARY MUST record: (a) which rung ran and why (full check OOM'd / completed), (b) explicit confirmation it type-checked the `src/kernel/{lda,gga,mgga}.rs` re-export paths, (c) its result. NOTE: `.continue-here.md` flags pre-existing out-of-scope LSP compile errors (`xc_integrate`/`xc_E1_scaled` unresolved in some Task-1 regen subcrates; cubecl `from_raw_parts` arity in `crates/kernels/math` `#[cfg(test)]` code). If the path-resolution rung surfaces ONLY these pre-existing out-of-scope errors and the `crate::kernel::{family}::<func>` re-export paths themselves resolve, that is acceptable — document the pre-existing errors as carried-forward deviations and flag them for 11-04/11-05's per-subcrate gates. If the rung surfaces a NEW error in the dispatch tree or the re-export façade itself, STOP and return CHECKPOINT REACHED.

    The load-bearing gate for this task is: `audit_dispatch_tree.sh` exits 0 AND no `batchN` in the dispatch tree AND `audit_cube_launch.sh` (D-13 form) exits 0 AND one fallback-ladder rung type-checked the re-export paths successfully.

    Commit as one atomic commit (`git commit --only -- <paths>`). This commit supersedes WIP `c3fba8089` — note in the commit message that it completes the WIP and is verified under D-13.
  </action>
  <verify>
    <automated>bash tools/audit_dispatch_tree.sh && bash tools/audit_cube_launch.sh && ! grep -rqE 'batch[0-9]+[a-z]*::' src/eval/gga_dispatch src/eval/mgga_dispatch && test -z "$(git status --porcelain src/eval/gga_dispatch src/eval/mgga_dispatch src/kernel)"</automated>
  </verify>
  <acceptance_criteria>
    - Re-running the three generators produces zero diff: `git status --porcelain src/eval/gga_dispatch src/eval/mgga_dispatch src/kernel` is empty after `python3 tools/generate_gga_dispatch.py && python3 tools/generate_mgga_dispatch.py && python3 tools/generate_kernel_reexports.py`.
    - `bash tools/audit_dispatch_tree.sh` exits `0` (Blocker B1 closed).
    - `grep -rE 'batch[0-9]' src/eval/gga_dispatch src/eval/mgga_dispatch | grep -v '^Binary' | wc -l` equals `0` (no batchN segment survives).
    - `find src/eval/gga_dispatch/funcs src/eval/mgga_dispatch/funcs -name '*.rs' 2>/dev/null | wc -l` is at least `120` (per-functional dispatch helpers present — WIP c3fba8089 emitted 106 GGA + 26 MGGA).
    - `ls src/eval/gga_dispatch/batch*.rs src/eval/mgga_dispatch/batch*.rs 2>/dev/null | wc -l` equals `0` (old per-batch files gone).
    - `grep -c 'libxc_kernel_' src/kernel/lda.rs src/kernel/gga.rs src/kernel/mgga.rs 2>/dev/null` reports per-functional re-exports in each (each at least `1`).
    - `grep -c 'MggaLaunchCtx' src/eval/mgga_dispatch/mod.rs` is at least `1` (the preserved-verbatim dispatch pattern survived).
    - `bash tools/audit_cube_launch.sh` (D-13 form) exits `0` — the load-bearing P11-INV-5 gate.
    - The dispatch path-resolution gate ran: one fallback-ladder rung (`cargo check -p libxc_rs`, OR `--no-default-features`, OR the `rustc --extern` per-family check, OR the throwaway-target check) type-checked the `src/kernel/{lda,gga,mgga}.rs` re-export paths and SUCCEEDED (modulo the documented pre-existing out-of-scope LSP errors) — the SUMMARY records which rung ran, that it exercised the re-export paths, and its result. `audit_dispatch_tree.sh` passing alone does NOT satisfy this criterion.
    - No `--jobs`/`CARGO_BUILD_JOBS`/`RUST_MIN_STACK` override; `.cargo/config.toml` unchanged.
  </acceptance_criteria>
  <done>The dispatch tree is complete against per-functional subcrates with no batchN segment, the three generators are confirmed deterministic (zero diff on re-run), `audit_dispatch_tree.sh` exits 0, the D-13 `audit_cube_launch.sh` passes as a gate, and a cargo/rustc path-resolution rung type-checked the `crate::kernel::{family}::<func>` re-export paths — Blocker B1 is closed and Wave 2 is finished under D-13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed kernel tree (Task-1 commits) → dispatch generators → emitted dispatch tree → rustc | Build-tooling integrity. Task 1 here is verify-only; the destructive transformation already happened and is committed. The risk surface: a false-green audit (a flat-count audit replaced by a per-design audit that itself has a logic gap), a non-deterministic dispatch generator, or a dispatch tree that passes the trivial B1 audit but does not actually type-check. No network/auth/runtime-input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering / Repudiation | re-running Task 1's committed destructive work | mitigate | `.continue-here.md` marks re-running Task 1's delete/regen as BLOCKING-bad. Mitigation: Task 1 here is a verify-only checkpoint — read-only/audit commands only, explicitly forbids `git rm` / splitter / regen invocation; an acceptance criterion requires the SUMMARY to confirm no destructive command ran; the two Task-1 commit hashes are documented as the provenance/rollback point. |
| T-11-03-02 | Spoofing (false-green) | the D-13 audit_cube_launch.sh rewrite | mitigate | A flat-count audit was unsatisfiable; a per-design audit with a logic gap could pass while an invariant is actually violated. Mitigation: the rewrite implements three explicit assertions (routed one-per-output, unrouted-zero, math ≤22), each consuming the `kernel_routing.py` source of truth; the audit is run against the live tree and must PASS, and a failing assertion is routed to emit.py (not papered over); acceptance criteria grep for the three-assertion structure and the `tests/` exclusion. |
| T-11-03-03 | Spoofing (false-green) | `audit_dispatch_tree.sh` trivially passing post-collapse | mitigate | After the regen the B1 audit passes trivially (empty "referenced" set) — it proves only that no batchN references remain, NOT that the new re-export paths resolve. Mitigation: Task 3 STEP 3 adds a SEPARATE load-bearing cargo/rustc path-resolution gate (the fallback ladder); "skip verification" is explicitly forbidden; the acceptance criterion requires a rung to have SUCCEEDED, not just `audit_dispatch_tree.sh` to pass. |
| T-11-03-04 | Tampering | non-deterministic dispatch generators | mitigate | A non-deterministic generator silently mutates the dispatch tree on every regen, violating P11-INV-6. Mitigation: Task 3 STEP 1 re-runs all three generators and requires zero git diff against the committed WIP `c3fba8089` output; any diff STOPS the task and routes the fix into the generators. |
| T-11-03-05 | Denial of Service (RAM) | `cargo check -p libxc_rs` with ~257 kernel deps | mitigate | The root crate now depends on ~257 kernel subcrates; a full `cargo check -p libxc_rs` may OOM (the D-12 risk). Mitigation: D-12 de-gates whole-workspace build; the fallback ladder degrades to `--no-default-features`, then a `rustc --extern` per-family check against built spot-check rlibs, then a throwaway target — each rung still type-checks the re-export paths. Build envelope (`.cargo/config.toml` jobs=1, 64MB stack) is unchanged; no override is used. |
</threat_model>

<verification>
End-of-plan checks (run after all three tasks):
1. `bash tools/audit_subcrate_collapse.sh` exits `0` (Task 1 re-confirmation — P11-INV-1).
2. `python3 tools/audit_kernel_size.py --strict` exits `0` (Task 1 re-confirmation — P11-INV-2).
3. `bash tools/audit_cube_launch.sh` exits `0` — the D-13 per-design launch budget (revised P11-INV-5), NOT a flat ≤23 count.
4. `tools/audit_cube_launch.sh` no longer contains `BASELINE=23` (`grep -c 'BASELINE=23' tools/audit_cube_launch.sh` equals `0`).
5. `bash tools/audit_dispatch_tree.sh` exits `0` (Blocker B1 closed).
6. `grep -rE 'batch[0-9]' src/eval/gga_dispatch src/eval/mgga_dispatch | grep -v '^Binary' | wc -l` equals `0`.
7. Re-running the three dispatch/re-export generators produces zero git diff (P11-INV-6 for the dispatch tree).
8. A dispatch path-resolution rung (cargo/rustc) type-checked the `src/kernel/{family}.rs` re-export paths (recorded in SUMMARY) — `audit_dispatch_tree.sh` passing alone is insufficient.

Whole-workspace `cargo build` is NOT run as a gate (D-12). Verification is audit-tool + path-resolution.
</verification>

<success_criteria>
- Task 1's committed state (`95727cb36`, `97d6347be`) is re-confirmed by a non-destructive verify-only checkpoint — no destructive re-run.
- `tools/audit_cube_launch.sh` is rewritten to the D-13 per-design launch budget (routed one-per-output, unrouted-zero, `math/src/` ≤22); the flat `≤23` count check is gone.
- The dispatch tree is complete against per-functional subcrates with no batchN segment; Blocker B1 closed.
- The three dispatch/re-export generators are confirmed deterministic.
- `audit_cube_launch.sh` (D-13 form) and `audit_dispatch_tree.sh` both pass; a cargo/rustc path-resolution rung type-checked the re-export paths.
- No `.cargo/config.toml` modification; no cargo jobs/env override.
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md` covering:
- Task 1 re-confirmation: the eight verify-only check results; confirmation no destructive command ran; `95727cb36` + `97d6347be` named as the provenance/rollback point.
- The D-13 `audit_cube_launch.sh` rewrite: the three assertions implemented, how `kernel_routing.py` is consumed, the PASS summary against the current tree (routed launch-pair total, unrouted-launchable=0, math/src/ count).
- Dispatch-tree completion: the three generators' determinism re-confirmation (zero diff); `audit_dispatch_tree.sh` result; which path-resolution fallback-ladder rung ran, confirmation it exercised the `src/kernel/{family}.rs` re-export paths, and its result.
- Carry-forward deviations from WIP `c3fba8089`: the `deferred` registry relocation into `crates/kernels/math/src/deferred.rs`; the pre-existing out-of-scope LSP compile errors (`xc_integrate`/`xc_E1_scaled`, cubecl `from_raw_parts` arity) flagged for 11-04/11-05's per-subcrate gates.
- Any new deviations or CHECKPOINTs.
</output>
