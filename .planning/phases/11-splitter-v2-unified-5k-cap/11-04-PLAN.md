<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - crates/kernels/lda
  - crates/kernels/gga
  - verify/Cargo.toml
  - verify/tests/parity_phase11.rs
autonomous: false
requirements:
  - P11-INV-2
  - P11-INV-4
  - P11-INV-5
  - P11-INV-6

must_haves:
  truths:
    - "`verify/Cargo.toml` `[dev-dependencies]` depends on individual per-functional kernel subcrates (the ones `parity_phase11.rs` exercises), NEVER on umbrella/family kernel crates — the structural fix for the verify/ OOM (Wave 0 deviation D1, memory feedback_verify_crate_oom)."
    - "Every LDA per-functional subcrate (~41) and every GGA per-functional subcrate (~131) compiles via `cargo build -p <pkgname>` (package name read from each Cargo.toml) under the unchanged `.cargo/config.toml` envelope."
    - "`parity_phase11::phase11_smoke` passes at strict 1e-12 for the LDA + GGA smoke entries against the regenerated per-functional subcrates."
    - "Re-running splitter v2 against the LDA and GGA families produces zero git diff (LDA+GGA idempotency — D-LOCK-D, P11-INV-6)."
    - "`tools/audit_cube_launch.sh` (the D-13 per-design launch budget rewritten in plan 11-03) still passes after the LDA+GGA second splitter run — the regen did not introduce an extra launch wrapper on any routed (functional, output) and did not make an unrouted LDA/GGA functional launchable."
  artifacts:
    - path: "verify/Cargo.toml"
      provides: "Narrowed per-functional kernel dev-deps (D-05 verify-OOM fix)"
      contains: "libxc-kernel-"
    - path: "verify/tests/parity_phase11.rs"
      provides: "Phase 11 oracle gate — smoke set passing against the regenerated tree"
      contains: "STRICT_TOL"
  key_links:
    - from: "verify/tests/parity_phase11.rs"
      to: "crates/kernels/{lda,gga}/<func>"
      via: "dispatch_lda / dispatch_gga against regenerated per-functional subcrates"
      pattern: "dispatch_(lda|gga)"
    - from: "verify/Cargo.toml"
      to: "crates/kernels/{lda,gga}/<func>"
      via: "per-functional [dev-dependencies] path-deps (no umbrella crate)"
      pattern: "libxc-kernel-[a-z].*dev|path.*crates/kernels"
---

<objective>
Verify the regenerated LDA (~41) and GGA (~131) per-functional subcrates: narrow `verify/`'s dev-dependencies to individual functional subcrates (the D-05 structural fix for the verify/ OOM), compile every LDA and GGA subcrate per-`-p`, run `parity_phase11::phase11_smoke` at strict 1e-12 for the LDA+GGA entries, confirm LDA+GGA idempotency, and confirm the D-13 launch-budget audit still passes.

Purpose: Plan 11-03 finished Wave 2 (the structural restructure + the D-13 audit rewrite + the dispatch tree); this plan proves the LDA and GGA halves are numerically correct (D-05 oracle gate) and buildable. The verify/ dev-dep narrowing (D-05) is load-bearing — Wave 0 deviation D1 showed the umbrella `libxc-kernel-{lda,mgga}` dev-deps OOM-killed even a trivial test before it compiled. MGGA verification is plan 11-05 (it carries the worst-case offenders and the 207-file blast radius).

Output: A narrowed `verify/Cargo.toml`, a `parity_phase11.rs` that passes its LDA+GGA smoke entries against the regenerated tree, confirmed LDA+GGA idempotency, and a passing D-13 `audit_cube_launch.sh`. Per-subcrate compilation is verified incrementally (D-12 — no whole-workspace gate).
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md
@.cargo/config.toml
@verify/tests/parity_phase11.rs
@verify/Cargo.toml

<interfaces>
<!-- The verify surface. Extracted — do NOT re-explore. -->

From verify/tests/parity_phase11.rs (Wave 0 scaffold — already exists, passing smoke today):
  const STRICT_TOL: f64 = 1e-12;
  const REL_FLOOR: f64 = 1e-30;
  struct PhaseEntry { canonical: &'static str, family: Family, ids: &'static [i32] }
  const PHASE11_SMOKE: &[PhaseEntry] = &[ ... ]    — LDA (≥3) + GGA (≥3) + MGGA (≥4)
  const PHASE11_WORST_CASE: &[PhaseEntry] = &[ ... ] — MGGA worst offenders
  #[test] fn phase11_smoke()        — iterates PHASE11_SMOKE through dispatch_* vs oracle_* at STRICT_TOL
  #[test] fn phase11_worst_case()   — may be #[ignore]'d until plan 11-05

From verify/Cargo.toml (Wave 0 / pre-phase state):
  [dev-dependencies] currently includes umbrella kernel crates (libxc-kernel-lda,
  libxc-kernel-mgga, ...) — these crates NO LONGER EXIST after plan 11-03's
  clean-slate delete, so verify/Cargo.toml is currently BROKEN and must be
  rewritten to depend on per-functional subcrates instead.

D-05 verify-gate model (REVISED 2026-05-14):
  - verify/ dev-deps narrowed to individual per-functional subcrates, NEVER umbrella crates.
  - Per-iteration gate = representative smoke parity (a few functionals per family at 1e-12).
  - Full per-subcrate parity sweep runs at phase end (plan 11-06), NOT every iteration.
  - Tolerance: 1e-12 relative error on energy AND all routed derivatives, at f64.

Wave 0 deviation D1 (11-01-SUMMARY.md): the umbrella libxc-kernel-{lda,mgga}
dev-deps OOM-killed the spike before its own test compiled. The D-05 narrowing
is the structural fix.

D-13 launch-budget audit (tools/audit_cube_launch.sh — rewritten in plan 11-03):
  NOT a flat ≤23 count. It asserts: (a) every ROUTED functional has exactly one
  `#[cube(launch_unchecked)]` entry kernel per emitted output module, (b) NO
  unrouted functional kernel is launchable, (c) `crates/kernels/math/src/` launch
  count ≤22. This plan's LDA+GGA second splitter run (idempotency check) must not
  perturb that budget — re-run the D-13 audit after the second splitter run.

Cargo package-name caveat: `cargo build -p <name>` takes the PACKAGE name from
Cargo.toml `[package] name`, NOT the directory name. emit.py (plan 11-02) writes
`name = "libxc-kernel-<funcid>"` but this MUST be read from each Cargo.toml, not
assumed from the directory — see the verify loop below, which reads the actual
`name` field. Deriving `-p` flags from directory basenames is forbidden (it both
fails silently if `find` returns nothing AND produces wrong flags if emit.py's
naming ever deviates).

From .cargo/config.toml (D-08/D-09 — do NOT modify, do NOT duplicate):
  [build] jobs = 1, target-dir = .cache/cargo-target
  [env] RUST_MIN_STACK = "67108864"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow verify/ dev-deps to per-functional subcrates + compile all LDA + GGA subcrates</name>
  <files>verify/Cargo.toml</files>
  <read_first>
    - verify/Cargo.toml (entire file — the [dev-dependencies] block currently referencing now-deleted umbrella kernel crates)
    - verify/tests/parity_phase11.rs (the PHASE11_SMOKE / PHASE11_WORST_CASE entries — determines which per-functional subcrates verify/ must depend on)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-05 — verify deps narrowed to per-functional subcrates; the per-iteration smoke model)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md (the regenerated subcrate names + the root Cargo.toml dep shape — verify/ mirrors the per-functional dep style; also the package-naming emit.py uses)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-01-SUMMARY.md (deviation D1 — the umbrella-dev-dep OOM that D-05 fixes)
    - ~/.claude/projects/-home-user-Documents-workspace-libxc-rs/memory/feedback_verify_crate_oom.md (the verify/ OOM root cause)
    - .cargo/config.toml (D-08/D-09 invariant — build env source of truth)
  </read_first>
  <action>
    Two parts: rewrite `verify/Cargo.toml`, then compile every LDA and GGA subcrate.

    PART A — Rewrite `verify/Cargo.toml` `[dev-dependencies]`:
    - REMOVE every umbrella/family kernel dev-dep (`libxc-kernel-lda`, `libxc-kernel-gga`, `libxc-kernel-mgga`, and any numbered `libxc-kernel-{family}-N`) — these crates no longer exist after plan 11-03.
    - ADD per-functional `libxc-kernel-<func> = { path = "../crates/kernels/{family}/<func>" }` dev-deps for EXACTLY the functionals that `parity_phase11.rs` exercises (the union of `PHASE11_SMOKE` + `PHASE11_WORST_CASE` entries) PLUS whatever the existing `verify/tests/*.rs` parity harnesses (`parity_phase09.rs` etc.) need. Do NOT add all ~264 — D-05 explicitly narrows to individual subcrates the tests actually use, because depending on the whole tree re-creates the OOM.
    - If a verify test needs the family dispatch facade (`crate::kernel::{family}`), note that dispatch lives in the ROOT `libxc_rs` crate, not in verify/ — verify/ already dev-depends on `libxc_rs`; the per-functional kernel dev-deps are only needed where a test imports a kernel directly. Inspect `parity_phase11.rs` imports: if it goes through `dispatch_lda`/`dispatch_gga` (which it does — they live in `libxc_rs`), verify/ may need only the `libxc_rs` dev-dep plus `libxc-sys`. Confirm by reading the actual imports and add per-functional kernel deps ONLY where a test references `libxc_kernel_<func>::` directly.
    - The dep KEY in `verify/Cargo.toml` must match the PACKAGE name emit.py wrote into each subcrate's `Cargo.toml` (`[package] name`). Confirm against 11-03-SUMMARY / a sample subcrate Cargo.toml; do not assume the directory name.
    - Keep all non-kernel dev-deps (`libxc-sys`, `approx`, `anyhow`, `rayon`, etc.) unchanged.
    - Add a comment block above `[dev-dependencies]` citing D-05 and the verify-OOM rationale: umbrella kernel crates are forbidden as dev-deps; only per-functional subcrates the tests directly import.

    PART B — Compile every LDA and GGA per-functional subcrate:
    - Enumerate the LDA subcrates (`crates/kernels/lda/*/Cargo.toml`) and GGA subcrates (`crates/kernels/gga/*/Cargo.toml`).
    - For each, read the actual PACKAGE name from its `Cargo.toml` (`grep '^name' <toml> | cut -d'"' -f2`) and run `cargo build -p "<name>"` INLINE, sequentially, reading the build env from `.cargo/config.toml` (NO `--jobs`, NO `CARGO_BUILD_JOBS`/`RUST_MIN_STACK` override). Builds are incremental against the cached target dir (D-09 — do NOT clean it). Do NOT derive `-p` flags from directory basenames.
    - Before the loop, assert it is non-vacuous: the count of LDA Cargo.toml files must be ≥ the expected LDA minimum and the GGA count ≥ the expected GGA minimum (see the verify command below for the concrete guard) — a `find` that returns nothing must FAIL, not pass silently.
    - Record pass/fail per subcrate. The expectation is ALL pass — the splitter v2 emit + the proven D-02 ABI should produce compilable Rust. NOTE: 11-03-SUMMARY.md carries forward pre-existing out-of-scope LSP compile errors flagged for these per-subcrate gates (`xc_integrate`/`xc_E1_scaled` unresolved in some Task-1 regen subcrates). If a subcrate fails ONLY on one of those documented pre-existing errors, record it as a carried-forward deviation and continue — but if a NEW failure appears:
      * If it's a CSE-chunk / tuple-ABI error → STOP, return CHECKPOINT REACHED with the cargo error; plan 11-02's CSE pass or emit.py has a bug.
      * If it's a missing-import / detect_imports gap → STOP, return CHECKPOINT REACHED naming the missing primitive.
      * Do NOT hand-patch generated files — fixes go through the splitter (D-LOCK-D idempotency).
    - This is a long sequential run (~172 subcrates). It is per-subcrate incremental, NOT a whole-workspace build (D-12). Run it inline; it is the expensive part of this plan.

    Commit `verify/Cargo.toml` once Part A is done and at least the smoke-set subcrates compile. Use `git commit --only -- verify/Cargo.toml`.
  </action>
  <verify>
    <automated>n=$(find crates/kernels/lda crates/kernels/gga -maxdepth 2 -name Cargo.toml | wc -l); [ "$n" -ge 150 ] || { echo "VACUOUS: only $n LDA+GGA Cargo.toml found (expected >=150)"; exit 1; }; for toml in $(find crates/kernels/lda crates/kernels/gga -maxdepth 2 -name Cargo.toml); do name=$(grep '^name' "$toml" | head -1 | cut -d'"' -f2); cargo build -p "$name" --quiet || { echo "FAIL: $name"; exit 1; }; done && echo "ALL LDA+GGA SUBCRATES BUILD ($n crates)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'libxc-kernel-(lda|gga|mgga)( |-[0-9])' verify/Cargo.toml` equals `0` (no umbrella/numbered kernel dev-deps).
    - `verify/Cargo.toml` contains a comment citing `D-05` and the verify-OOM rationale (`grep -c 'D-05' verify/Cargo.toml` is at least `1`).
    - The build loop is non-vacuous: `find crates/kernels/lda crates/kernels/gga -maxdepth 2 -name Cargo.toml | wc -l` is at least `150` (guarded explicitly in the verify command — a vacuous `find` FAILS the gate).
    - Every LDA + GGA subcrate builds via package name read from Cargo.toml: the verify command's loop `for toml in $(find ...); do name=$(grep '^name' "$toml" | head -1 | cut -d'"' -f2); cargo build -p "$name" --quiet || exit 1; done` exits `0` (modulo any documented carried-forward pre-existing out-of-scope error from 11-03-SUMMARY, which the SUMMARY must explicitly name).
    - No `-p` flag in this task's session was derived from a directory basename — package names are read from `[package] name` in each Cargo.toml.
    - No `--jobs`/`CARGO_BUILD_JOBS`/`RUST_MIN_STACK` override or `.cargo/config.toml` edit in the session.
    - The SUMMARY records the per-family build time and any subcrate that needed a CHECKPOINT or carried a documented pre-existing error.
  </acceptance_criteria>
  <done>`verify/Cargo.toml` depends only on per-functional subcrates (no umbrella crates — D-05 OOM fix), and every LDA and GGA per-functional subcrate compiles (by package name read from Cargo.toml, with a non-vacuous count guard) under the unchanged build envelope.</done>
</task>

<task type="auto">
  <name>Task 2: LDA + GGA smoke parity at 1e-12 + LDA+GGA idempotency + D-13 launch-budget re-check</name>
  <files>verify/tests/parity_phase11.rs</files>
  <read_first>
    - verify/tests/parity_phase11.rs (entire file — the Wave 0 scaffold; PHASE11_SMOKE LDA+GGA entries, phase11_smoke test body, the rel_err_with_floor + classify_dispatch_err helpers)
    - verify/tests/parity_phase09.rs (the canonical analog — the dispatch-vs-oracle comparison pattern at STRICT_TOL)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-05 — per-iteration smoke parity is the load-bearing acceptance signal; tolerance 1e-12 on energy + routed derivatives at f64; D-13 — the launch-budget audit form)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md ("Sampling Rate" — per-wave parity = phase11_smoke; "Per-Task Verification Map")
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md (the regenerated dispatch tree state — confirms dispatch_lda/dispatch_gga resolve; the D-13 audit_cube_launch.sh rewrite is recorded here)
    - tools/test_idempotency.sh (the idempotency harness — Wave 0; runs the splitter twice and diffs)
    - tools/maple_to_kernels.py (the splitter driver invoked by the idempotency check)
    - tools/audit_cube_launch.sh (the D-13-rewritten launch-budget audit — re-run after the second splitter pass)
    - .cargo/config.toml (D-08/D-09 invariant)
  </read_first>
  <action>
    Three parts: smoke parity for LDA+GGA, LDA+GGA idempotency, then the D-13 launch-budget re-check.

    PART A — LDA + GGA smoke parity:
    - Run `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture` INLINE (build env from `.cargo/config.toml`; `--test-threads=1` is mandatory under the RAM constraint per RESEARCH).
    - The LDA and GGA entries in `PHASE11_SMOKE` MUST PASS at strict 1e-12 (energy + all routed derivatives) against the regenerated per-functional subcrates. The smoke set was pinned in Wave 0 against already-routed functionals — after the clean-slate regen the SAME functionals must still pass; if the regen preserved correctness (D-05 gate), they will.
    - The MGGA entries in `PHASE11_SMOKE` are NOT this plan's responsibility — plan 11-05 verifies MGGA. If the test as written also runs MGGA entries, either (a) the MGGA subcrates already build from 11-03 and the MGGA smoke entries pass too (great — note it), or (b) MGGA entries fail because MGGA verification/build hardening is still pending (plan 11-05) — in that case temporarily filter the test run to LDA+GGA entries (e.g. a `phase11_smoke_lda_gga` sub-filter or a `cfg`-free runtime family filter) and document that MGGA smoke is deferred to plan 11-05. Do NOT delete MGGA entries from `PHASE11_SMOKE` — plan 11-05 needs them.
    - If an LDA or GGA smoke entry FAILS at 1e-12: this is a real numerical regression from the regen. STOP and return CHECKPOINT REACHED with the functional, derivative order, spin, and the actual relative error. Do NOT narrow the smoke set to hide it — debug WHY (likely a CSE-chunk accumulation-order issue; D-05 accepts 1e-12 but not worse). The fix goes through the splitter (finer chunk granularity per RESEARCH risk table), not a hand-patch.

    PART B — LDA + GGA idempotency:
    - With a clean working tree (commit Part A first), run the splitter v2 against the LDA and GGA families a second time (`python3 tools/maple_to_kernels.py` for `--family lda` and `--family gga`, or via `tools/test_idempotency.sh` scoped to those families if it supports scoping).
    - `git status --porcelain crates/kernels/lda crates/kernels/gga` MUST be empty — re-running the splitter produces zero diff (D-LOCK-D, P11-INV-6).
    - If there IS a diff: the splitter has a non-determinism bug (set-order iteration, hash-based chunk ids, unsorted imports — see RESEARCH "Idempotency Contract"). STOP, return CHECKPOINT REACHED with the diff; the fix goes into plan 11-02's tooling (cse.py/emit.py) — note that this means re-opening 11-02's tooling, which is acceptable: idempotency is a hard success criterion.

    PART C — D-13 launch-budget re-check:
    - After the second splitter run, run `bash tools/audit_cube_launch.sh` (the D-13 per-design budget rewritten in plan 11-03). It MUST exit 0 — the LDA+GGA regen must not have added an extra `#[cube(launch_unchecked)]` to any routed `(functional, output)` entry file, must not have made an unrouted LDA/GGA functional launchable, and must not have touched `crates/kernels/math/src/`. This is NOT a flat ≤23 count — it is the per-design budget.
    - If it FAILS: a routed functional's regen emitted the wrong launch policy (or an unrouted one became launchable). STOP, return CHECKPOINT REACHED naming the offending `(functional, output)` pairs; the fix routes into plan 11-02's `emit.py` routing-aware launch policy, not a hand-patch.

    Commit any `parity_phase11.rs` changes (e.g. an added LDA+GGA sub-filter) with `git commit --only -- verify/tests/parity_phase11.rs`.
  </action>
  <verify>
    <automated>cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture && python3 tools/maple_to_kernels.py all --family lda && python3 tools/maple_to_kernels.py all --family gga && test -z "$(git status --porcelain crates/kernels/lda crates/kernels/gga)" && bash tools/audit_cube_launch.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture` exits `0` with the LDA and GGA smoke entries reported PASS at strict 1e-12 (the `--nocapture` output shows `PASS` lines for each LDA/GGA entry).
    - If MGGA smoke entries were filtered out, the SUMMARY explicitly states so and cites plan 11-05 as their owner; `PHASE11_SMOKE` still CONTAINS the MGGA entries (`grep -c 'mgga' verify/tests/parity_phase11.rs` unchanged from Wave 0).
    - Re-running splitter v2 on LDA + GGA produces zero diff: `git status --porcelain crates/kernels/lda crates/kernels/gga` is empty after a second splitter run.
    - `bash tools/audit_cube_launch.sh` (D-13 per-design budget) exits `0` after the second splitter run — NOT a flat ≤23 count; the per-design assertions (routed one-per-output, unrouted-zero, math/src/ ≤22) all hold.
    - No LDA or GGA smoke entry was silently dropped to make the test pass (the LDA-entry count and GGA-entry count in `PHASE11_SMOKE` are each still ≥3).
    - No `--jobs`/`CARGO_BUILD_JOBS`/`RUST_MIN_STACK` override; `.cargo/config.toml` unchanged.
  </acceptance_criteria>
  <done>The LDA and GGA per-functional subcrates pass oracle parity at strict 1e-12 (D-05 gate), the splitter is proven idempotent for the LDA and GGA families, and the D-13 launch-budget audit still passes after the LDA+GGA regen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated kernel subcrates → verify/ oracle harness → libxc 7.0.0 FFI | Verification integrity. The oracle (libxc-sys FFI) is trusted ground truth; the regenerated subcrates are under test. The risk surface is a false-green parity result or a verify/ dev-dep configuration that OOMs before the gate runs. No network/auth/runtime-input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Denial of Service (RAM) | `verify/Cargo.toml` dev-deps | mitigate | An umbrella kernel dev-dep re-creates the Wave-0-D1 OOM that kills the test before it compiles — a false build failure masking real parity results. Mitigation: Task 1 Part A removes ALL umbrella/numbered kernel dev-deps and depends only on per-functional subcrates the tests directly import (D-05); acceptance criterion greps for `0` umbrella deps. |
| T-11-04-02 | Spoofing (false-green) | per-subcrate build verify loop | mitigate | A loop that derives `-p` flags from directory basenames passes vacuously if `find` returns nothing and emits wrong flags if emit.py's package naming deviates from the directory name — a green gate against zero or wrong crates. Mitigation: the verify loop reads the actual `[package] name` from each Cargo.toml and is preceded by a non-vacuous count guard (`>=150` LDA+GGA Cargo.toml files) that FAILS on an empty `find`. |
| T-11-04-03 | Spoofing (false-green) | `phase11_smoke` smoke-set narrowing | mitigate | Silently dropping a failing LDA/GGA entry from `PHASE11_SMOKE` would hide a real numerical regression. Mitigation: the task explicitly forbids narrowing to hide failures — a failing entry STOPS the plan with a CHECKPOINT; acceptance criterion requires ≥3 LDA and ≥3 GGA entries retained. |
| T-11-04-04 | Tampering | splitter non-determinism | mitigate | A non-idempotent splitter silently mutates the kernel tree on every regen, corrupting it over time and violating P11-INV-6. Mitigation: Part B runs the splitter a second time and requires zero git diff; any diff STOPS the plan and routes the fix back into 11-02's tooling. |
| T-11-04-05 | Tampering | hand-patching generated kernels | mitigate | Hand-patching a generated subcrate to make it compile/pass would be silently overwritten by the next regen (idempotency) — false progress. Mitigation: the task explicitly forbids hand-patching generated files; compile/parity fixes go through the splitter. |
| T-11-04-06 | Spoofing (false-green) | D-13 launch-budget drift after regen | mitigate | The LDA+GGA second splitter run could perturb the per-design launch budget (an extra launch wrapper on a routed output, or an unrouted functional made launchable) and go unnoticed if the audit is not re-run. Mitigation: Part C re-runs the D-13 `audit_cube_launch.sh` after the second splitter pass; any violation STOPS the plan and routes the fix to emit.py's routing-aware launch policy. |
</threat_model>

<verification>
End-of-plan checks (run after both tasks):
1. `grep -cE 'libxc-kernel-(lda|gga|mgga)( |-[0-9])' verify/Cargo.toml` equals `0` (no umbrella kernel dev-deps).
2. Every LDA and GGA per-functional subcrate compiles via `cargo build -p <pkgname>` (package name read from Cargo.toml), with a non-vacuous count guard.
3. `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture` exits `0` with LDA+GGA entries PASS at 1e-12.
4. Re-running splitter v2 on LDA+GGA produces zero git diff (P11-INV-6 for LDA+GGA).
5. `python3 tools/audit_kernel_size.py --strict` still exits `0` (the second splitter run did not introduce oversized files).
6. `bash tools/audit_cube_launch.sh` exits `0` — the D-13 per-design launch budget (revised P11-INV-5), NOT a flat ≤23 count.
7. No `.cargo/config.toml` modification; no cargo jobs/env override.
</verification>

<success_criteria>
- `verify/Cargo.toml` depends only on per-functional subcrates (D-05 verify-OOM fix).
- Every LDA (~41) and GGA (~131) per-functional subcrate compiles under the unchanged envelope, verified by package name with a non-vacuous guard.
- `parity_phase11::phase11_smoke` passes its LDA+GGA entries at strict 1e-12.
- Splitter v2 is idempotent for the LDA and GGA families (zero diff on re-run).
- `tools/audit_cube_launch.sh` (D-13 per-design launch budget) passes after the LDA+GGA regen.
- No `.cargo/config.toml` touched; no cargo jobs/env override.
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-04-SUMMARY.md` covering:
- `verify/Cargo.toml` rewrite: which per-functional dev-deps were added, confirmation no umbrella crates remain.
- LDA + GGA per-subcrate build results: count (and the non-vacuous guard value), total time, any CHECKPOINT-triggering failures or documented carried-forward pre-existing errors; confirmation `-p` flags came from Cargo.toml `[package] name`.
- `phase11_smoke` LDA+GGA result at 1e-12; whether MGGA entries were filtered (and the deferral note to plan 11-05).
- LDA+GGA idempotency result (zero diff confirmed, or the non-determinism bug found and where the fix landed).
- The D-13 `audit_cube_launch.sh` re-check result after the second splitter run.
- Any deviations.
</output>
