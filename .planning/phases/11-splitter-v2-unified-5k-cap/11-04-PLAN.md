<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - tools/translate_mgga.py
  - crates/kernels/mgga-1/src/**/*.rs
  - crates/kernels/mgga-2/src/**/*.rs
  - crates/kernels/mgga-3/src/**/*.rs
  - crates/kernels/mgga-4/src/**/*.rs
  - crates/kernels/mgga-5/src/**/*.rs
  - crates/kernels/mgga-6/src/**/*.rs
  - crates/kernels/mgga-7/src/**/*.rs
  - crates/kernels/mgga-8a/src/**/*.rs
  - crates/kernels/mgga-8b/src/**/*.rs
  - crates/kernels/mgga-9a/src/**/*.rs
  - crates/kernels/mgga-9b/src/**/*.rs
  - crates/kernels/mgga-10/src/**/*.rs
  - crates/kernels/mgga-11a/src/**/*.rs
  - crates/kernels/mgga-11b/src/**/*.rs
  - crates/kernels/mgga-12/src/**/*.rs
  - crates/kernels/mgga-13/src/**/*.rs
  - crates/kernels/mgga-14/src/**/*.rs
autonomous: false
requirements:
  - P11-INV-2
  - P11-INV-3
  - P11-INV-4
  - P11-INV-5
  - P11-INV-6

must_haves:
  truths:
    - "tools/translate_mgga.py SPLIT_THRESHOLD is 5000 (D-LOCK-B for MGGA family)."
    - "tools/translate_mgga.py has a chunked-emission dispatch wedge mirroring the GGA wedge from plan 11-03, invoking chunk_single_output_d02 from tools/translate_v2/emit.py with family=\"mgga\" and the MGGA wrapper buffer inputs (rho + sigma + lapl + tau)."
    - "After regen, every .rs file under crates/kernels/mgga-* (all 17 numbered subcrates) is ≤5000 lines — including the worst-case offenders mgga_c_b94 kxc_pol (was 16703), mgga_c_kcisk lxc_pol_part15 (was 16138), mgga_c_ccalda lxc_pol (was 15378), mgga_c_kcis lxc_pol_part13 (was 14127), mgga_c_revtpss lxc_pol_part20 (was 12648), and the entire 25-file mgga_c_revtpss/lxc_pol_partNN set."
    - "After regen, cargo build -p libxc-kernel-mgga (and each numbered child) succeeds inline under the project envelope."
    - "After regen, parity_phase11::phase11_smoke and parity_phase11::phase11_worst_case both pass at strict 1e-12 (D-05)."
    - "Re-running translate_mgga.py on unchanged Maple input produces zero git diff under crates/kernels/mgga-* (MGGA portion of P11-INV-6)."
    - "Pre-existing deferral mgga_x_2d_prp10 (libxc id 211, missing Bessel I0/I1, from quick task 260510-q02) is preserved — the new translator does NOT regress this deferral."
    - "Per RESEARCH.md Q2: only files >5K are re-chunked; the 5 already-merged small revtpss splits (1K-5K range) are left untouched (idempotency-friendly)."
  artifacts:
    - path: "tools/translate_mgga.py"
      provides: "MGGA translator with D-02 chunked-emission wedge and SPLIT_THRESHOLD=5000"
      contains: "from translate_v2.emit import"
  key_links:
    - from: "tools/translate_mgga.py"
      to: "tools/translate_v2/emit.py"
      via: "import + call site at the new chunked-emission dispatch wedge"
      pattern: "chunk_single_output_d02"
    - from: "Each regenerated MGGA chunk file (especially mgga_c_b94, mgga_c_kcisk, mgga_c_revtpss)"
      to: "cubecl-macros 0.10 IR"
      via: "fn chunk_N<F: Float>(args: F, ...) -> (F, F, ...)"
      pattern: "fn .*_chunk[0-9]+<F: Float>"
---

<objective>
Wave 3: Port the D-02 chunked-emission wedge into `tools/translate_mgga.py` and regenerate the MGGA family at SPLIT_THRESHOLD=5000. This is the bulk of Phase 11's value — MGGA holds 207 of the 237 oversized files (87%), including all 11 files >12,000 lines (top offenders 16,703 / 16,138 / 15,378 / 14,127 / 13,913 / 13,719 / 13,238 / 12,648 / 12,598 / 12,596 / 12,164 lines per RESEARCH.md "Top 15 worst offenders" table).

Why a separate plan from GGA (despite structural similarity): MGGA is large enough that regen + verify can dominate context cost on its own. The wedge insertion (Task 1) is small but the regen + parity verification (Task 2) is the bulk-execution gate where most CSE-tuning work for the worst-case offenders will land.

Per RESEARCH.md Q2: re-chunk ONLY files >5K. Leave files ≤5K untouched (idempotency-friendly). The 25-file `mgga_c_revtpss/lxc_pol_*` set has 5 already-merged-small splits in the 1K-5K range — leave those alone; only re-chunk the 20+ that exceed 5K.

Output: MGGA translator with D-02 wedge; regenerated MGGA tree (the 207-file blast radius); verified parity on smoke + worst-case sets.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md
@.cargo/config.toml
@tools/translate_mgga.py
@tools/translate_gga.py
@tools/translate_v2/emit.py
@tools/translate_v2/cse.py

<interfaces>
MGGA translator landmarks:

- tools/translate_mgga.py line 553: `SPLIT_THRESHOLD = 6000`  → lower to 5000
- tools/translate_mgga.py line 798: `if est <= SPLIT_THRESHOLD:` (the no-split early-return)
- tools/translate_mgga.py line 818: `splits = merge_small_splits(splits, SPLIT_THRESHOLD)`
- tools/translate_mgga.py line 824: `if sub_est > SPLIT_THRESHOLD and len(sub_outputs) > 1:` (the per-component split fallback)
- The current MGGA translator has NO chunked-scratch fallback (RESEARCH.md "Tools Inventory"); Phase 11 ADDS the chunked wedge AFTER the per-component split

GGA reference (built in plan 11-03): `tools/translate_gga.py` has the wedge already; copy its SHAPE.

MGGA wrapper buffers (per spin):
- unpolarized: `rho` (1 component/grid), `sigma` (1), `lapl` (1), `tau` (1)
- polarized:   `rho` (2 components/grid), `sigma` (3), `lapl` (2), `tau` (2)

So `wrapper_buffer_inputs` for MGGA:

```python
components_rho   = 2 if spin == "pol" else 1
components_sigma = 3 if spin == "pol" else 1
components_lapl  = 2 if spin == "pol" else 1
components_tau   = 2 if spin == "pol" else 1
wrapper_buffer_inputs = [
    ("rho",   "F", components_rho),
    ("sigma", "F", components_sigma),
    ("lapl",  "F", components_lapl),
    ("tau",   "F", components_tau),
]
```

Verify the actual unpacking convention by reading any current MGGA wrapper file e.g. `crates/kernels/mgga-1/src/<func>/exc_unpol.rs`. The component counts above match RESEARCH.md "Standard Stack" — but the EMIT-side ordering in the wrapper body (rho first, sigma second, etc.) must match the existing `tools/translate_mgga.py` wrapper-emission ordering or the chunked output won't match the un-chunked output's input layout.

Worst-case offenders to gate parity on (PHASE11_WORST_CASE in parity_phase11.rs from plan 11-01):
- `mgga_c_b94` (kxc_pol = 16703)
- `mgga_c_kcisk` (lxc_pol_part15 = 16138; lxc_pol_part14 = 13719; lxc_pol_part16 = 13913)
- `mgga_c_ccalda` (lxc_pol = 15378)
- `mgga_c_kcis` (lxc_pol_part13 = 14127; lxc_pol_part14 = 12164; lxc_pol_part12 = 11988)
- `mgga_c_rppscan` (lxc_pol = 13238)
- `mgga_c_revtpss` (lxc_pol_part20 = 12648; ~30 files >5K total)
- `mgga_c_scan` (lxc_pol = 12598)
- `mgga_c_rregtm` (lxc_pol = 12596)
- `mgga_x_r4scan` (per CONTEXT.md D-LOCK-B)
- `mgga_x_br89_explicit` (per CONTEXT.md D-LOCK-B)
- `mbrxc` / `mgga_xc_b97m_v` (per CONTEXT.md — verify the exact MggaFunctional variant; if `mbrxc` is not routed, document)

mgga-7 has the kcis special-case currently handled by `tools/split_mgga_7_kcis.py` — Phase 11's CSE chunker should subsume this; the special-case helper becomes obsolete (deletion in plan 11-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add D-02 chunked dispatch wedge to tools/translate_mgga.py; lower SPLIT_THRESHOLD to 5000</name>
  <files>tools/translate_mgga.py</files>
  <read_first>
    - tools/translate_mgga.py (entire file — needed to find the exact dispatch site after per-component split, lines ~800-870 area)
    - tools/translate_gga.py (post-11-03 state — exact wedge shape to mirror, search for `chunk_single_output_d02(`)
    - tools/translate_v2/emit.py (the entry point being called)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md "tools/translate_{lda_v2,gga,mgga}.py — chunked-scratch path replacement" + the routing-aware `cube_attr` rule
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-02, D-03, D-04, D-LOCK-B)
    - crates/kernels/mgga-1/src/<any-functional>/exc_unpol.rs (one small file — confirm MGGA wrapper buffer-unpacking shape and component counts per spin)
  </read_first>
  <action>
    Three changes to `tools/translate_mgga.py` — same pattern as GGA wedge in plan 11-03.

    Change 1 — lower SPLIT_THRESHOLD:

    Replace line 553 `SPLIT_THRESHOLD = 6000` with `SPLIT_THRESHOLD = 5000  # Phase 11 D-LOCK-B hard cap`.

    Change 2 — add the import:

    Add (in the existing import block):

    ```python
    from translate_v2.emit import chunk_single_output_d02
    from translate_v2.cse import ChunkArityError
    ```

    Match the existing translator's `sys.path.insert(0, os.path.dirname(__file__))` idiom if needed.

    Change 3 — wedge the chunked-emission call into the dispatch tree.

    The current MGGA flow at lines ~795-870 is: `if est <= SPLIT_THRESHOLD: emit single`; otherwise `splits = split_by_output_array(...)`, `splits = merge_small_splits(splits, SPLIT_THRESHOLD)`, then `for sub in splits: if sub_est > SPLIT_THRESHOLD and len(sub_outputs) > 1: per-component split; else emit single (POSSIBLY OVERSIZED)`. The wedge inserts BEFORE the final "emit single (possibly oversized)" branch so the chunker activates only when (a) the split is single-output AND (b) it still exceeds 5000 lines.

    Read `tools/translate_mgga.py` lines 800-870 (verify exact line numbers — file may have shifted) to find the emit point. Insert:

    ```python
    # Phase 11 D-02: if a single-output split still exceeds SPLIT_THRESHOLD,
    # use the CSE-aware tuple-return chunker.
    if sub_est > SPLIT_THRESHOLD:
        is_unrouted = func_name not in cached_routed_funcnames("mgga")
        components_rho   = 2 if spin == "pol" else 1
        components_sigma = 3 if spin == "pol" else 1
        components_lapl  = 2 if spin == "pol" else 1
        components_tau   = 2 if spin == "pol" else 1
        wrapper_buffer_inputs = [
            ("rho",   "F", components_rho),
            ("sigma", "F", components_sigma),
            ("lapl",  "F", components_lapl),
            ("tau",   "F", components_tau),
        ]
        try:
            chunked = chunk_single_output_d02(
                func_name=func_name,
                level=level,
                spin=spin,
                fn_suffix=fn_suffix,
                compute=sub_compute,
                outputs=sub_outputs,
                bufs=sub_bufs,
                all_params=all_params,
                is_vxc_only=is_vxc_only,
                chunk_threshold=SPLIT_THRESHOLD,
                is_unrouted=is_unrouted,
                family="mgga",
                wrapper_buffer_inputs=wrapper_buffer_inputs,
            )
        except ChunkArityError as e:
            raise RuntimeError(
                f"MGGA chunked-emission arity overflow on {func_name}/{level}/{spin}{fn_suffix}: {e}"
            )
        for sub_name, text in chunked:
            path = os.path.join(subdir, f"{sub_name}.rs")
            with open(path, "w") as f:
                f.write(text)
            written.append(path)
            mod_entries.append(f"pub mod {sub_name};")
        continue
    ```

    Variable naming: verify `func_name`, `level`, `spin`, `fn_suffix`, `sub_compute`, `sub_outputs`, `sub_bufs`, `all_params`, `is_vxc_only`, `subdir`, `written`, `mod_entries` are all in scope at the wedge point in `tools/translate_mgga.py`. If any name differs (MGGA may use slightly different conventions; e.g., `sub_compute` may be `sub_lines`), use the MGGA name.

    `cached_routed_funcnames` for MGGA — should already exist per quick task 260512-q01; verify by grep. If named differently, use the MGGA name.

    mgga-7 kcis special-case handling: `tools/split_mgga_7_kcis.py` exists today as a one-off post-emit splitter. With the D-02 chunker handling this case during emission, the post-emit splitter becomes obsolete. Do NOT delete it in this plan (deletion lives in plan 11-06); just ensure the new emission path produces ≤5K files for kcis WITHOUT requiring the post-emit splitter.

    Self-check before declaring done:

    - `grep -c '^SPLIT_THRESHOLD = 5000' tools/translate_mgga.py` equals `1`.
    - `grep -c '^SPLIT_THRESHOLD = 6000' tools/translate_mgga.py` equals `0`.
    - `grep -c 'from translate_v2.emit import chunk_single_output_d02' tools/translate_mgga.py` equals `1`.
    - `grep -c 'chunk_single_output_d02(' tools/translate_mgga.py` is at least `1`.
    - `grep -c 'family="mgga"' tools/translate_mgga.py` equals `1`.
    - `grep -c 'lapl' tools/translate_mgga.py | head -1` shows the lapl wrapper buffer is in scope (sanity check that MGGA-specific buffers are recognized).
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0,'tools'); import translate_mgga; assert translate_mgga.SPLIT_THRESHOLD == 5000; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^SPLIT_THRESHOLD = 5000' tools/translate_mgga.py` equals `1`.
    - `grep -c '^SPLIT_THRESHOLD = 6000' tools/translate_mgga.py` equals `0`.
    - `grep -c 'from translate_v2.emit import chunk_single_output_d02' tools/translate_mgga.py` equals `1`.
    - `grep -c 'chunk_single_output_d02(' tools/translate_mgga.py` is at least `1`.
    - `grep -c 'family="mgga"' tools/translate_mgga.py` equals `1`.
    - `grep -c '"lapl"' tools/translate_mgga.py` is at least `1` (MGGA-specific buffer name appears in the wedge).
    - `grep -c '"tau"' tools/translate_mgga.py` is at least `1`.
    - Module imports cleanly: `python3 -c "import sys; sys.path.insert(0,'tools'); import translate_mgga"` exits `0`.
  </acceptance_criteria>
  <done>The MGGA translator now has a D-02 chunked-emission wedge that activates on single-output splits exceeding 5000 lines, using the same shared `chunk_single_output_d02` from `tools/translate_v2/emit.py` with MGGA-specific buffer inputs (rho + sigma + lapl + tau).</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate MGGA family at SPLIT_THRESHOLD=5000; verify size, build, parity (smoke + worst-case), idempotency</name>
  <files>crates/kernels/mgga-1/src/**/*.rs through crates/kernels/mgga-14/src/**/*.rs (regenerated; no manual edits)</files>
  <read_first>
    - tools/translate_mgga.py (post-Task-1 state)
    - tools/maple_to_kernels.py (the orchestrator — confirm MGGA invocation: `python3 tools/maple_to_kernels.py all --family mgga`)
    - tools/regen_phase09.py + tools/batch_translate_mgga.py (the underlying regen drivers if the unified driver doesn't support `regen --family mgga`)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-05 verify gate, D-07/D-08/D-09 build envelope, D-LOCK-B worst-case offenders)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "Pathological Functional Inventory" lines 346-401 (MGGA top offenders + per-functional aggregate)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "Per-functional (worst aggregate)" lines 379-393 (mgga_c_revtpss has 30 files >5K — the bulk of the work)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "Risks per success criterion" lines 540-552 (MEDIUM risk: oracle parity drift on chunked kernels — fall back to finer chunk granularity if 1e-12 violated; HIGH risk: cubecl-macros parser limits at 16-arity — drop cap to 8 if needed)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "Open Question 2" lines 723-727 (re-chunk only files >5K — leave already-merged small splits untouched)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md (pre-phase metric snapshot)
    - .cargo/config.toml (build envelope — cite, do NOT duplicate)
    - .planning/quick/260510-q02-restore-prp10-deferral/ (mgga_x_2d_prp10 deferral — must NOT be regressed)
  </read_first>
  <action>
    Three sequential operations, all run inline (D-07).

    Operation 1 — regen MGGA:

    ```bash
    python3 tools/maple_to_kernels.py all --family mgga
    ```

    Fall back to the underlying driver if needed (verify by reading `tools/maple_to_kernels.py`). Do NOT pass `--split-threshold` (D-09).

    Pre-existing deferral check: confirm `mgga_x_2d_prp10` (libxc id 211) is still deferred after regen — read `crates/kernels/mgga/src/deferred.rs` (or wherever the deferral lives, per quick task 260510-q02) and verify the file is unchanged. If the regen accidentally re-emitted prp10, restore the deferral inline before continuing (cherry-pick the relevant lines from quick task 260510-q02).

    After regen, audit MGGA-only:

    ```bash
    find crates/kernels/mgga-1 crates/kernels/mgga-2 crates/kernels/mgga-3 crates/kernels/mgga-4 \
         crates/kernels/mgga-5 crates/kernels/mgga-6 crates/kernels/mgga-7 \
         crates/kernels/mgga-8a crates/kernels/mgga-8b \
         crates/kernels/mgga-9a crates/kernels/mgga-9b \
         crates/kernels/mgga-10 \
         crates/kernels/mgga-11a crates/kernels/mgga-11b \
         crates/kernels/mgga-12 crates/kernels/mgga-13 crates/kernels/mgga-14 \
         -name '*.rs' -exec wc -l {} + | awk '$2 != "total" && $1 > 5000'
    ```

    MUST return zero rows. Iteration loop if any row remains:

    1. Identify the failing functional + file from the audit output.
    2. Confirm whether the failure is a CSE-arity overflow (cse.py raised ChunkArityError; emit.py wrapped + raised RuntimeError naming the functional). If yes, lower the input or output arity cap from 16 to 12 in the call site for that functional (per RESEARCH.md A2 mitigation), regen just that functional via `python3 tools/regen_phase09.py <functional>`, re-audit.
    3. Confirm whether the failure is a single chunk that's still >5K because the dep chain is too dense. If yes, lower `min_uses` in the call site (default 5 → 3), regen, re-audit.
    4. Per RESEARCH.md "Risks": "If specific functionals violate even 1e-12, fall back to a finer chunk granularity (preserve more named temporaries inline)." For these specific cases, document the per-functional override in `tools/translate_v2/emit.py` (or a sidecar `tools/translate_v2/per_functional_overrides.py` if the override list grows past 5 entries).
    5. After 3 retry rounds with no convergence, STOP and return CHECKPOINT REACHED naming the functional, the chunk arity, and the chosen mitigation strategy for user approval.

    Operation 2 — build + parity (each cargo command runs INLINE; no `--jobs`, no `CARGO_BUILD_JOBS`):

    ```bash
    for crate in mgga-1 mgga-2 mgga-3 mgga-4 mgga-5 mgga-6 mgga-7 \
                 mgga-8a mgga-8b mgga-9a mgga-9b mgga-10 \
                 mgga-11a mgga-11b mgga-12 mgga-13 mgga-14; do
        cargo build -p libxc-kernel-${crate}
    done
    cargo build -p libxc-kernel-mgga
    cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture
    cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case -- --test-threads=1 --nocapture
    ```

    `phase11_worst_case` is the 6-functional list from plan 11-01 — this is its first chance to actually pass (the test was scaffolded with `#[ignore]` markers in plan 11-01). REMOVE the `#[ignore]` attributes on the worst-case test entries that the regen successfully covers, KEEPING `#[ignore = "..."]` only on entries that still fail (with an updated reason string). Commit the test-file edit alongside the regen output.

    Both `phase11_smoke` and `phase11_worst_case` MUST exit 0 at strict 1e-12 (D-05). LDA + GGA from prior plans must remain green.

    If a worst-case functional shows oracle drift but stays within 10× of strict_tol (e.g., max rel err 5e-12 vs target 1e-12), the CSE-introduced reordering is the cause. Mitigations to try in order:
    1. Tighten min_uses (default 5 → 8) to reduce the number of CSE breakpoints, accepting larger but fewer chunks.
    2. Force the breakpoint to land at a `let t = a + b;` style line (additive accumulation reorders cause most drift) rather than a multiplicative line.
    3. Per-functional override: increase `chunk_max_lines` to 5500 (still under the hard cap with boilerplate room) to avoid the offending breakpoint.

    Operation 3 — MGGA idempotency:

    ```bash
    git status --porcelain crates/kernels/mgga-* > /tmp/mgga_status1.txt
    python3 tools/maple_to_kernels.py all --family mgga
    git status --porcelain crates/kernels/mgga-* > /tmp/mgga_status2.txt
    diff /tmp/mgga_status1.txt /tmp/mgga_status2.txt
    ```

    `diff` MUST exit 0. If not, fix non-determinism in cse.py / emit.py / per-functional overrides before re-running.

    Commit message: `refactor(phase-11/mgga): regen MGGA family under D-02 tuple-return ABI at SPLIT_THRESHOLD=5000 (closes 207-file blast radius)`. Stage `crates/kernels/mgga-*/`, `tools/translate_mgga.py`, and `verify/tests/parity_phase11.rs` (for the `#[ignore]` removal).

    Build envelope: every cargo command runs inline. The `.cargo/config.toml` provides jobs=1, target-dir, RUST_MIN_STACK=67108864 (D-07/D-08/D-09); these values are NOT to be passed on the command line.

    Per RESEARCH.md "Memory ceiling lessons": pre-collapse, MGGA still has 17 numbered subcrates so the per-crate compile RAM is not affected by the per-file cap reduction (each crate's HIR scales with total LOC, which goes DOWN slightly if anything). The OOM risk lives in plan 11-05's family collapse, not here. If `cargo build -p libxc-kernel-mgga-N` OOMs in this plan, that's a real regression — investigate before committing.
  </action>
  <verify>
    <automated>find crates/kernels -mindepth 2 -path '*/mgga-*/*' -name '*.rs' -type f -exec wc -l {} + | awk '$2 != "total" && $1 > 5000 {c++} END {exit c==0?0:1}' && cargo build -p libxc-kernel-mgga && cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture && cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case -- --test-threads=1 --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Zero MGGA files >5000 lines (Blocker B4 — single `find` with `-path` for shell portability, no brace/glob expansion): `find crates/kernels -mindepth 2 -path '*/mgga-*/*' -name '*.rs' -type f -exec wc -l {} + | awk '$2 != "total" && $1 > 5000' | wc -l` equals `0`.
    - Workspace-wide check: `python3 tools/audit_kernel_size.py --strict` exits `0` (LDA from 11-02, GGA from 11-03, MGGA from this plan all ≤ 5000 lines).
    - Each MGGA numbered crate builds: 17 commands of the form `cargo build -p libxc-kernel-mgga-N` exit `0`.
    - `cargo build -p libxc-kernel-mgga` exits `0`.
    - `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture` exits `0`.
    - `cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case -- --test-threads=1 --nocapture` exits `0` (with `#[ignore]` removed on the worst-case entries that now pass; `#[ignore]` retained ONLY on entries with documented blockers like `mbrxc` if it's not a routed variant).
    - LDA + GGA still pass (no regression from MGGA changes): `cargo build -p libxc-kernel-lda && cargo build -p libxc-kernel-gga` exits `0`.
    - `bash tools/audit_cube_launch.sh` exits `0` (count still ≤ 22; chunk helpers are plain `#[cube]`).
    - mgga_x_2d_prp10 deferral preserved (per quick task 260510-q02 — the deferral mechanism is a commented-out `pub mod mgga_x_2d_prp10;` line in the relevant subcrate's `lib.rs`, NOT an entry in `deferred.rs`; the 6-entry `deferred.rs` list is scoped to Brent-method-blocked deferrals only).
      Acceptance gates (BOTH must hold, AND use `find` for shell portability — no brace/glob expansion per Blocker B4):
        - Primary (build-level deferral): `find crates/kernels -mindepth 3 -maxdepth 4 -path '*/mgga-*/src/lib.rs' -type f -exec grep -lE '^[[:space:]]*//[[:space:]]*pub mod mgga_x_2d_prp10' {} +` returns ≥1 match (the `pub mod` line is commented out in the numbered subcrate's `lib.rs` that owns the prp10 directory).
        - Secondary (registry-level proof): `grep 'mgga_x_2d_prp10' src/model/mgga_functional.rs` returns ≥1 match (the registry doc still references prp10's exclusion rationale).
    - Idempotency: re-running `python3 tools/maple_to_kernels.py all --family mgga` produces zero `git status --porcelain` diff under `crates/kernels/mgga-*`.
    - At least one MGGA chunked file from the worst-case set matches `find crates/kernels -mindepth 2 -path '*/mgga-*/*' -name '*_chunk*.rs' -type f -exec grep -l 'fn .*_chunk[0-9]\\+<F: Float>' {} +` AND includes one of `mgga_c_b94`, `mgga_c_kcisk`, `mgga_c_revtpss`, `mgga_c_kcis`, `mgga_c_ccalda` (Blocker B4 — single `find` with `-path` glob is portable across sh/bash).
    - No `&mut Array<f64>` shared-scratch chunks in MGGA (Blocker B4 — single `find` with `-path` portable): `find crates/kernels -mindepth 2 -path '*/mgga-*/*' -name '*.rs' -type f -exec grep -lE 'fn .*_chunk[0-9]+\(.*&mut Array<f64>' {} +` returns no matches.
    - No `.cargo/config.toml` change: `git diff --name-only HEAD~1 HEAD .cargo/config.toml` returns empty.
  </acceptance_criteria>
  <done>The MGGA family is fully regenerated under the D-02 ABI at SPLIT_THRESHOLD=5000; every MGGA file is ≤5000 lines; the workspace-wide kernel-size audit is now CLEAN; cargo builds the family; oracle parity holds at 1e-12 on smoke + worst-case sets; the MGGA portion of the splitter is empirically idempotent; mgga_x_2d_prp10 deferral is preserved.</done>
  <scope_rationale>
    **W7 — task-monolith justification (acknowledged, retained):** This task has 17 numbered MGGA subcrates in `<files>`, ~150 lines of action prose, ~25 acceptance criteria, and 3 nested operation blocks (regen → build/parity → idempotency). The original draft kept it monolithic; the plan-checker flagged this as a scope concern.

    Decision: KEEP MONOLITHIC. Rationale:
    - The 207-file MGGA blast radius is a single regen invocation: splitting into "regen+audit / build+parity / idempotency" sub-tasks would require each sub-task to inherit + re-verify the prior's state, doubling the verification surface.
    - The three operations are sequentially dependent (regen produces the build inputs; build produces the parity inputs; idempotency requires both prior steps to have committed). Cross-task dependencies between sub-tasks within a single plan are forbidden by the GSD task model.
    - The action prose length is driven by the worst-case-offender mitigation knobs (CSE retry rounds, per-functional override paths). Splitting would just relocate that prose without reducing it.
    - Context budget for THIS task is bounded by the 17-subcrate cargo build + parity test wall-time, not by the prose. The executor reads the prose once and then iterates against the build/test loop until convergence.

    If this task's execution context exceeds 50% during run-time, the executor MAY split mid-execution by deferring the idempotency operation to a fresh follow-up plan (created via `/gsd-plan-phase 11 --gaps`). That escape hatch is the scope-overflow mitigation; up-front splitting is not.
  </scope_rationale>
</task>

</tasks>

<verification>
End-of-plan verification:

1. `python3 -c "import sys; sys.path.insert(0,'tools'); import translate_mgga; assert translate_mgga.SPLIT_THRESHOLD==5000"` exits `0`.
2. **Workspace-wide P11-INV-2 holds:** `python3 tools/audit_kernel_size.py --strict` exits `0`.
3. `cargo build -p libxc-kernel-lda && cargo build -p libxc-kernel-gga && cargo build -p libxc-kernel-mgga` all exit `0`.
4. `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture` exits `0`.
5. `cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case -- --test-threads=1 --nocapture` exits `0`.
6. `bash tools/audit_cube_launch.sh` exits `0` (count still 22).
7. `mgga_x_2d_prp10` deferral preserved: `find crates/kernels -mindepth 3 -maxdepth 4 -path '*/mgga-*/src/lib.rs' -type f -exec grep -lE '^[[:space:]]*//[[:space:]]*pub mod mgga_x_2d_prp10' {} +` returns ≥1 match (the commented-out `pub mod` line in the owning subcrate's lib.rs). Note: `deferred.rs` is for Brent-method-blocked functionals only, NOT for prp10 — see quick task 260510-q02.
8. MGGA splitter idempotent.

If verification 5 (worst-case parity) fails on any functional, STOP and return CHECKPOINT REACHED — D-05 may need re-discussion (e.g., per-functional tolerance relaxation) or per-functional CSE override required.
</verification>

<success_criteria>
- D-02 ABI active in MGGA translator (largest blast radius — 207 files).
- SPLIT_THRESHOLD = 5000 enforced for MGGA (D-LOCK-B contribution from MGGA).
- Every MGGA `.rs` file ≤5000 lines (P11-INV-2 fully holds workspace-wide).
- MGGA family builds and oracle-parity-passes for smoke + worst-case (P11-INV-3, P11-INV-4 contributions).
- MGGA splitter is empirically idempotent (P11-INV-6 contribution from MGGA).
- LDA + GGA from prior plans remain green.
- `#[cube(launch_unchecked)]` count unchanged at 22 (P11-INV-5).
- mgga_x_2d_prp10 pre-existing deferral preserved.
- No `.cargo/config.toml` modification (D-07/D-08/D-09 invariants).
- All cargo commands ran inline (D-07).
- Phase 11 success criterion 2 (every file ≤5K) is **MET** — the bulk technical milestone.
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-04-SUMMARY.md` covering:
- MGGA wedge insertion location (file + line) and the variables passed to `chunk_single_output_d02`.
- Per-functional chunk count for each PHASE11_WORST_CASE entry (mgga_c_b94, mgga_c_kcisk, mgga_c_ccalda, mgga_c_kcis, mgga_c_rppscan, mgga_c_revtpss, mgga_c_scan, mgga_x_r4scan, mgga_x_br89_explicit) — pre vs post (lines per file before, chunk count after).
- Per-functional CSE knob overrides (if any) — the exception list with rationale.
- Parity result on phase11_smoke + phase11_worst_case (max relative error per entry; specifically call out the 11 functionals that previously had files >12K lines).
- Idempotency result.
- mgga_x_2d_prp10 deferral preservation confirmation.
- Build envelope confirmation (cite `.cargo/config.toml`).
- The new workspace-wide kernel-size audit summary (target = 0 oversized; baseline was 237).
</output>
