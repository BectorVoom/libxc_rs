<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - crates/kernels/mgga
  - verify/Cargo.toml
  - verify/tests/parity_phase11.rs
autonomous: false
requirements:
  - P11-INV-2
  - P11-INV-4
  - P11-INV-6

must_haves:
  truths:
    - "Every routed MGGA per-functional subcrate (~85) compiles via `cargo build -p libxc-kernel-<func>`; the 7 deferred subcrates (mgga_c_b94, mgga_x_br89, mgga_x_mbr, mgga_x_mbrxc_bg, mgga_x_mbrxh_bg, mgga_x_mggac, mgga_x_br89_explicit) compile on demand via `cargo build -p` but are excluded from default-members (D-11)."
    - "The worst-case MGGA offenders (mgga_c_b94 kxc_pol, mgga_c_kcisk lxc_pol, mgga_c_ccalda lxc_pol, mgga_c_revtpss lxc_pol, mgga_x_r4scan, mgga_x_br89_explicit) are CSE-chunked to ≤5000 lines per file and `parity_phase11::phase11_worst_case` passes at strict 1e-12 for the routed ones."
    - "`parity_phase11::phase11_smoke` passes at strict 1e-12 for the MGGA smoke entries against the regenerated per-functional subcrates."
    - "Re-running splitter v2 against the MGGA family produces zero git diff (MGGA idempotency — D-LOCK-D, P11-INV-6)."
    - "The mgga_x_2d_prp10 deferral (libxc id 211, missing Bessel I0/I1) is preserved — it is not regressed by the regen."
  artifacts:
    - path: "verify/Cargo.toml"
      provides: "MGGA per-functional kernel dev-deps added for parity_phase11 MGGA entries"
      contains: "libxc-kernel-mgga"
    - path: "verify/tests/parity_phase11.rs"
      provides: "Phase 11 oracle gate — MGGA smoke + worst-case passing; phase11_worst_case un-ignored"
      contains: "PHASE11_WORST_CASE"
  key_links:
    - from: "verify/tests/parity_phase11.rs"
      to: "crates/kernels/mgga/<func>"
      via: "dispatch_mgga against regenerated per-functional subcrates"
      pattern: "dispatch_mgga"
    - from: "crates/kernels/mgga/<func>/src/<output>"
      to: "tools/translate_v2/cse.py"
      via: "CSE-chunked tuple-return helpers for >5K single outputs"
      pattern: "<F: Float>"
---

<objective>
Verify the regenerated MGGA per-functional subcrates — the family carrying the 207-file blast radius and every worst-case offender. Add MGGA per-functional dev-deps to `verify/`, compile every routed MGGA subcrate (and confirm the 7 deferred subcrates build on demand), un-ignore and pass `phase11_worst_case` at strict 1e-12, pass the MGGA smoke entries, confirm MGGA idempotency, and preserve the `mgga_x_2d_prp10` deferral.

Purpose: MGGA is where D-LOCK-B is hardest — the 16,703-line `mgga_c_b94/kxc_pol.rs`, the 13–16K `mgga_c_kcisk`/`mgga_c_ccalda`/`mgga_c_revtpss` `lxc_pol` monsters, and `mgga_x_r4scan`/`mgga_x_br89_explicit`. The CSE pass (D-01) and the proven D-02 tuple-return ABI must subdivide all of them to ≤5K. D-11 governs the 7 deferred kernels: per-functional subcrates excluded from default-members (superseding q02's reverted feature-gate approach). This is the plan that turns `phase11_worst_case` green (it was `#[ignore]`'d at Wave 0 — deviation D7).

Output: A `verify/Cargo.toml` with MGGA per-functional dev-deps, a `parity_phase11.rs` with `phase11_worst_case` un-ignored and passing, confirmed MGGA idempotency, and the `mgga_x_2d_prp10` deferral intact.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-04-SUMMARY.md
@.cargo/config.toml
@verify/tests/parity_phase11.rs
@verify/Cargo.toml

<interfaces>
<!-- The MGGA verification surface. Extracted — do NOT re-explore. -->

The 7 deferred MGGA kernels (D-11 — per-functional subcrates, EXCLUDED from
default-members, but build on demand via `cargo build -p`):
  mgga_c_b94, mgga_x_br89, mgga_x_mbr, mgga_x_mbrxc_bg, mgga_x_mbrxh_bg,
  mgga_x_mggac, mgga_x_br89_explicit
Runtime routing UNCHANGED: is_deferred(id) rejects these in MggaFunctional::from_id;
the oracle harness SKIPs them. They are NOT in PHASE11_WORST_CASE as PASS-required
entries — they appear there only as compile-on-demand checks, or are SKIP-classified.

mgga_x_2d_prp10 (libxc id 211): pre-existing deferral, missing Bessel I0/I1
(quick task 260510-q02). Phase 11 MUST NOT regress this — it stays deferred.

Worst-case MGGA offenders (from 11-RESEARCH.md "Top 15" + CONTEXT specifics) —
the CSE pass must subdivide each to ≤5000 lines:
  mgga_c_b94/kxc_pol (was 16,703)         — DEFERRED kernel: chunked + compiles, parity SKIP
  mgga_c_kcisk/lxc_pol (was 13-16K, 13 files)
  mgga_c_ccalda/lxc_pol (was 15,378)
  mgga_c_revtpss/lxc_pol (was 12-13K, 30 files)
  mgga_c_rppscan/lxc_pol (was 13,238)
  mgga_x_r4scan
  mgga_x_br89_explicit                    — UNROUTED: chunked + compiles, parity SKIP

From verify/tests/parity_phase11.rs:
  const PHASE11_WORST_CASE: &[PhaseEntry] = &[ mgga_c_revtpss, mgga_c_kcisk,
    mgga_c_b94, mgga_x_r4scan, mgga_x_br89_explicit, mgga_xc_b97m_v ]
  #[test] fn phase11_worst_case()  — #[ignore]'d at Wave 0 (deviation D7); UN-IGNORE here.
  Skip semantics: only UnsupportedFunctional / UnsupportedDerivativeOrder may skip.
  So mgga_c_b94 + mgga_x_br89_explicit (deferred/unrouted) are SKIP-classified, not FAIL.

From .cargo/config.toml (D-08/D-09 — do NOT modify, do NOT duplicate):
  [build] jobs = 1, target-dir = .cache/cargo-target
  [env] RUST_MIN_STACK = "67108864"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile all MGGA subcrates (routed + deferred-on-demand) + add MGGA dev-deps</name>
  <files>verify/Cargo.toml, crates/kernels/mgga</files>
  <read_first>
    - verify/Cargo.toml (the [dev-dependencies] block as rewritten by plan 11-04 — MGGA per-functional deps are added here)
    - verify/tests/parity_phase11.rs (PHASE11_SMOKE MGGA entries + PHASE11_WORST_CASE — determines which MGGA subcrates verify/ must dev-depend on)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-11 — deferred-kernel default-members exclusion; D-05 — per-functional dev-deps)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md (the regenerated MGGA subcrate names + which were the hardest; confirmation the 7 deferred subcrates exist and are excluded from default-members)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md (lines 346-401 "Pathological Functional Inventory" — the worst offenders and what makes them big; lines 529-552 the RAM-ceiling risk table)
    - ~/.claude/projects/-home-user-Documents-workspace-libxc-rs/memory/feedback_kernel_build_failure.md (kernel build failure → refactor per cubecl_macro_fanout_manual, do not declare a blocker prematurely)
    - .cargo/config.toml (D-08/D-09 invariant)
  </read_first>
  <action>
    Two parts: add MGGA dev-deps, then compile every MGGA subcrate.

    PART A — Add MGGA per-functional dev-deps to `verify/Cargo.toml`:
    - ADD `libxc-kernel-<func> = { path = "../crates/kernels/mgga/<func>" }` for EXACTLY the MGGA functionals that `parity_phase11.rs` exercises (the MGGA entries in `PHASE11_SMOKE` + all of `PHASE11_WORST_CASE`). Do NOT add all ~92 — D-05 narrows to what the tests import.
    - Same rule as plan 11-04: if `parity_phase11.rs` reaches MGGA kernels only via `dispatch_mgga` (which lives in root `libxc_rs`), verify/ may not need direct MGGA kernel dev-deps at all — confirm by reading the actual imports. Add direct kernel dev-deps ONLY where a test references `libxc_kernel_mgga_<func>::` directly.
    - The 7 deferred kernels: only add a dev-dep if a test directly imports one. They are SKIP-classified in parity, so likely no dev-dep needed — but if `phase11_worst_case` directly imports `mgga_c_b94` to do a compile-only check, add it.
    - Keep the comment block citing D-05.

    PART B — Compile every MGGA per-functional subcrate:
    - Enumerate MGGA subcrates (`crates/kernels/mgga/*/Cargo.toml`).
    - For each ROUTED subcrate (~85), run `cargo build -p libxc-kernel-<func>` INLINE, sequentially, build env from `.cargo/config.toml` (NO `--jobs`/`CARGO_BUILD_JOBS`/`RUST_MIN_STACK` override). Incremental against the cached target dir.
    - For each of the 7 DEFERRED subcrates, ALSO run `cargo build -p libxc-kernel-<func>` — D-11 requires they build ON DEMAND even though excluded from default-members. They must compile; they are just not in the default build set.
    - This is the heaviest run in the phase — ~92 subcrates, including the worst-case offenders whose CSE-chunked output is the densest. Per RESEARCH the MGGA family historically peaked 22.7 GB RSS as a single crate; per-functional subcrates are the structural mitigation — each subcrate is now small. Still, run inline and sequential (D-07).
    - On a compile failure:
      * CSE-chunk / tuple-ABI error → STOP, CHECKPOINT REACHED with the cargo error (plan 11-02 cse.py/emit.py bug).
      * SIGSEGV during proc-macro expansion → per D-08, the executor MAY raise `RUST_MIN_STACK` in `.cargo/config.toml` ONLY IF a specific chunk-graph still SIGSEGVs after splitting — document the new value and the failing chunk in the SUMMARY. This is the ONE sanctioned `.cargo/config.toml` edit in Phase 11, and only for a SIGSEGV that splitting did not resolve. Do NOT touch `jobs` or `target-dir`.
      * detect_imports gap → STOP, CHECKPOINT REACHED naming the missing primitive.
      * Do NOT hand-patch generated files — fixes go through the splitter (D-LOCK-D).
    - Record pass/fail + build time per subcrate; flag the worst-case offenders' build times specifically.

    Commit `verify/Cargo.toml` once Part A is done (`git commit --only -- verify/Cargo.toml`).
  </action>
  <verify>
    <automated>for f in $(find crates/kernels/mgga -maxdepth 2 -name Cargo.toml -exec dirname {} \; | xargs -n1 basename); do cargo build -p libxc-kernel-$f --quiet || { echo "FAIL: $f"; exit 1; }; done; echo "ALL MGGA SUBCRATES BUILD (routed + deferred)"</automated>
  </verify>
  <acceptance_criteria>
    - Every MGGA subcrate builds (routed + the 7 deferred): the verify loop over `crates/kernels/mgga` exits `0`.
    - The 7 deferred kernels each compiled via `cargo build -p` (recorded in SUMMARY) AND are confirmed absent from root `Cargo.toml` `default-members`: `grep -A300 'default-members' Cargo.toml | grep -cE 'mgga_c_b94|mgga_x_br89|mgga_x_mbr|mgga_x_mbrxc_bg|mgga_x_mbrxh_bg|mgga_x_mggac|mgga_x_br89_explicit'` equals `0`.
    - `python3 tools/audit_kernel_size.py --strict` exits `0` (all MGGA files ≤5000, including the former 16,703-line offender).
    - `grep -cE 'libxc-kernel-(lda|gga|mgga)( |-[0-9])' verify/Cargo.toml` equals `0` (still no umbrella deps after the MGGA additions).
    - `mgga_x_2d_prp10` is still deferred — `grep -rc 'mgga_x_2d_prp10' src/model/mgga_functional.rs` confirms it remains in the deferred handling (not newly routed); the SUMMARY states the deferral is preserved.
    - If `RUST_MIN_STACK` was raised, the SUMMARY documents the new value + the failing chunk-graph + confirms `jobs` and `target-dir` were untouched. Otherwise no `.cargo/config.toml` edit at all.
    - No `--jobs`/`CARGO_BUILD_JOBS` override.
  </acceptance_criteria>
  <done>Every routed MGGA subcrate compiles, the 7 deferred subcrates build on demand while staying out of default-members, every MGGA file is ≤5000 lines, and the mgga_x_2d_prp10 deferral is intact.</done>
</task>

<task type="auto">
  <name>Task 2: MGGA smoke + worst-case parity at 1e-12 (un-ignore phase11_worst_case) + MGGA idempotency</name>
  <files>verify/tests/parity_phase11.rs</files>
  <read_first>
    - verify/tests/parity_phase11.rs (entire file — PHASE11_SMOKE MGGA entries, PHASE11_WORST_CASE, the #[ignore]'d phase11_worst_case test, the classify_dispatch_err skip helper)
    - verify/tests/parity_phase09.rs (the canonical dispatch-vs-oracle pattern at STRICT_TOL)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-01-SUMMARY.md (deviation D6 — the W-D10-6 MGGA smoke-set selection rule; deviation D7 — why phase11_worst_case was #[ignore]'d at Wave 0)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-05 — 1e-12 on energy + routed derivatives; D-11 — deferred kernels SKIP, not FAIL)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md (lines 538-549 — oracle parity drift risk on chunked kernels; the finer-chunk-granularity fallback if 1e-12 is violated)
    - tools/test_idempotency.sh (the idempotency harness)
    - tools/maple_to_kernels.py (the splitter driver)
    - .cargo/config.toml (D-08/D-09 invariant)
  </read_first>
  <action>
    Two parts: MGGA parity (smoke + worst-case), then MGGA idempotency.

    PART A — MGGA smoke + worst-case parity:
    - Un-ignore `phase11_worst_case` — remove the `#[ignore = "..."]` attribute added at Wave 0 (deviation D7). The plan that turns it green is this one.
    - Run `cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture` INLINE (build env from `.cargo/config.toml`; `--test-threads=1` mandatory). This runs both `phase11_smoke` and `phase11_worst_case`.
    - `phase11_smoke` MGGA entries MUST PASS at strict 1e-12 (energy + routed derivatives). If plan 11-04 filtered MGGA smoke entries out, restore the full smoke run here.
    - `phase11_worst_case` acceptance:
      * Routed worst-case offenders (`mgga_c_revtpss`, `mgga_c_kcisk`, `mgga_x_r4scan`, `mgga_xc_b97m_v`) MUST PASS at strict 1e-12.
      * `mgga_c_b94` (DEFERRED per D-11) and `mgga_x_br89_explicit` (UNROUTED) are SKIP-classified — `classify_dispatch_err` returns `UnsupportedFunctional`, the test counts them as SKIP not FAIL. Confirm the skip classification is correct; if the test as written expects them to PASS, fix the classification (they are legitimately deferred/unrouted — the WORST_CASE list includes them as "must compile + must chunk to ≤5K", which Task 1 already proved, not as "must pass parity").
    - If a routed worst-case entry FAILS at 1e-12: per RESEARCH this is a chunked-kernel accumulation-order drift. D-05 accepts 1e-12 but not worse. STOP, return CHECKPOINT REACHED with the functional/order/spin/actual-error. The fix is finer CSE chunk granularity (preserve more named temporaries inline) — that goes into plan 11-02's `cse.py`, then re-regen MGGA. Do NOT hand-patch; do NOT relax the tolerance.
    - If a worst-case entry FAILS to compile at test time (vs the per-`-p` build in Task 1 passing): that indicates a dispatch-tree path mismatch — STOP, CHECKPOINT REACHED.

    PART B — MGGA idempotency:
    - Commit Part A first (clean working tree).
    - Run splitter v2 against the MGGA family a second time (`python3 tools/maple_to_kernels.py all --family mgga`).
    - `git status --porcelain crates/kernels/mgga` MUST be empty (D-LOCK-D, P11-INV-6).
    - If there is a diff: splitter non-determinism in the MGGA path. STOP, CHECKPOINT REACHED with the diff; fix goes into plan 11-02's tooling.

    Commit `parity_phase11.rs` changes (`git commit --only -- verify/tests/parity_phase11.rs`).
  </action>
  <verify>
    <automated>cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture && python3 tools/maple_to_kernels.py all --family mgga && test -z "$(git status --porcelain crates/kernels/mgga)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#\[ignore' verify/tests/parity_phase11.rs` equals `0` (phase11_worst_case un-ignored; no remaining ignored tests).
    - `cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture` exits `0` — BOTH `phase11_smoke` and `phase11_worst_case` pass.
    - The `--nocapture` output shows: routed worst-case offenders (`mgga_c_revtpss`, `mgga_c_kcisk`, `mgga_x_r4scan`, `mgga_xc_b97m_v`) reported PASS at 1e-12; `mgga_c_b94` and `mgga_x_br89_explicit` reported SKIP (not FAIL).
    - MGGA smoke entries report PASS at strict 1e-12.
    - Re-running splitter v2 on MGGA produces zero diff: `git status --porcelain crates/kernels/mgga` is empty.
    - `python3 tools/audit_kernel_size.py --strict` still exits `0` after the second splitter run.
    - No MGGA smoke or worst-case entry was silently dropped to make the test pass.
    - No `--jobs`/`CARGO_BUILD_JOBS` override; `.cargo/config.toml` unchanged (unless a documented Task-1 SIGSEGV `RUST_MIN_STACK` raise occurred, in which case `jobs`/`target-dir` are still untouched).
  </acceptance_criteria>
  <done>The MGGA per-functional subcrates pass oracle parity at strict 1e-12 (smoke + worst-case), `phase11_worst_case` is green and un-ignored, deferred/unrouted offenders are correctly SKIP-classified, and the splitter is proven idempotent for the MGGA family.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated MGGA kernel subcrates → verify/ oracle harness → libxc 7.0.0 FFI | Verification integrity for the densest family. The risk surface: a worst-case offender that chunks but drifts beyond 1e-12, a deferred kernel mis-classified as PASS-required, or a SIGSEGV during proc-macro expansion of a deep chunk graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Tampering | CSE chunking of worst-case offenders | mitigate | A worst-case offender whose CSE chunks reorder accumulation beyond 1e-12 produces a silently-wrong kernel that still compiles. Mitigation: `phase11_worst_case` at strict 1e-12 is the gate; a failing routed entry STOPS the plan and routes the fix to finer chunk granularity in 11-02's cse.py — the tolerance is never relaxed. |
| T-11-05-02 | Spoofing (false-green) | deferred-kernel skip classification | mitigate | If `mgga_c_b94` / `mgga_x_br89_explicit` are mis-counted as PASS instead of SKIP, the test would either falsely fail or (worse) a real regression in them would be hidden. Mitigation: explicit skip-classification check via `classify_dispatch_err` returning `UnsupportedFunctional`; acceptance criterion requires they report SKIP in the output. |
| T-11-05-03 | Tampering | `default-members` deferred-kernel leak | mitigate | A deferred kernel leaking into `default-members` makes `cargo build` attempt a known-problematic kernel. Mitigation: acceptance criterion re-greps `default-members` for all 7 deferred names requiring `0` matches (re-verified here even though set in 11-03). |
| T-11-05-04 | Tampering | splitter non-determinism (MGGA path) | mitigate | The MGGA path has the most chunk-heavy output; non-determinism here corrupts the largest subtree. Mitigation: Part B re-runs the splitter and requires zero diff; any diff STOPS the plan. |
| T-11-05-05 | Denial of Service (RAM) | proc-macro expansion of deep chunk graphs | mitigate | A deep CSE chunk graph can SIGSEGV rustc's macro-expansion thread despite the 64MB stack. Mitigation: D-08 sanctions raising `RUST_MIN_STACK` ONLY for a SIGSEGV that splitting did not resolve, documented in the SUMMARY, with `jobs`/`target-dir` untouched; per-functional subcrates keep each compilation unit small. |
</threat_model>

<verification>
End-of-plan checks (run after both tasks):
1. Every MGGA per-functional subcrate (routed + 7 deferred) compiles via `cargo build -p libxc-kernel-<func>`.
2. `python3 tools/audit_kernel_size.py --strict` exits `0` (all MGGA files ≤5000 — the former 16,703-line offender included).
3. `cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture` exits `0` — `phase11_smoke` AND `phase11_worst_case` both pass.
4. `grep -c '#\[ignore' verify/tests/parity_phase11.rs` equals `0`.
5. Re-running splitter v2 on MGGA produces zero git diff (P11-INV-6 for MGGA).
6. `bash tools/audit_cube_launch.sh` exits `0` (launch_unchecked count ≤ 23).
7. `default-members` excludes all 7 deferred kernels; `mgga_x_2d_prp10` deferral preserved.
8. No `.cargo/config.toml` modification except a documented Task-1 SIGSEGV `RUST_MIN_STACK` raise (jobs/target-dir untouched); no cargo jobs override.
</verification>

<success_criteria>
- Every routed MGGA subcrate compiles; the 7 deferred subcrates build on demand while excluded from default-members (D-11).
- Every MGGA file ≤5000 lines including all former worst-case offenders.
- `parity_phase11::phase11_smoke` and `phase11_worst_case` both pass at strict 1e-12; `phase11_worst_case` un-ignored.
- Splitter v2 is idempotent for the MGGA family.
- `mgga_x_2d_prp10` deferral preserved.
- No `.cargo/config.toml` jobs/target-dir change; no cargo jobs override.
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-05-SUMMARY.md` covering:
- MGGA per-subcrate build results: routed count, deferred count, total time, worst-case offenders' individual build times.
- Whether `RUST_MIN_STACK` was raised (and if so: new value, failing chunk-graph, confirmation jobs/target-dir untouched).
- `phase11_smoke` MGGA result + `phase11_worst_case` result at 1e-12 (which entries PASS, which SKIP).
- MGGA idempotency result (zero diff, or the non-determinism bug + where the fix landed).
- Confirmation the `mgga_x_2d_prp10` deferral is preserved.
- Any deviations or CHECKPOINTs.
</output>
