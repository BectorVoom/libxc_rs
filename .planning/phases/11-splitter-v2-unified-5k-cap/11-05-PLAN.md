<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
<!-- D-10/D-10a/D-10b architecture: splitter emits unified — write_dir = crates/kernels/mgga/src always. -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - crates/kernels/mgga/src/lib.rs
  - crates/kernels/mgga/src/**/*.rs           # populated from scratch by tools/regen_unified.py --family mgga
  - src/eval/mgga_dispatch/mod.rs              # regenerated against populated MGGA façade
autonomous: false   # cargo build under D-07 inline; bulk regen most likely site for OOM
requirements:
  - P11-INV-2
  - P11-INV-3
  - P11-INV-4
  - P11-INV-5
  - P11-INV-6

must_haves:
  truths:
    - "Per D-10: every MGGA functional (~92 today, sourced from libxc-master/src/maple2c/mgga_{exc,vxc}/*.c) is emitted under crates/kernels/mgga/src/<func>/."
    - "Per D-LOCK-B: every .rs file under crates/kernels/mgga/src/ is ≤5000 lines (P11-INV-2 holds for MGGA — closes the 207-file blast radius including mgga_c_b94 kxc_pol = 16703, mgga_c_kcisk lxc_pol_part15 = 16138, mgga_c_ccalda lxc_pol = 15378, mgga_c_kcis lxc_pol_part13 = 14127, mgga_c_revtpss lxc_pol_part20 = 12648 + ~30 files >5K total in revtpss, mgga_c_rppscan lxc_pol = 13238, mgga_c_scan lxc_pol = 12598, mgga_c_rregtm lxc_pol = 12596, mgga_x_r4scan, mgga_x_br89_explicit per D-LOCK-B)."
    - "crates/kernels/mgga/src/lib.rs has a `pub mod <func>;` line for every emitted MGGA functional, alphabetically sorted."
    - "Pre-existing deferral mgga_x_2d_prp10 (libxc id 211, missing Bessel I0/I1, from quick task 260510-q02) is preserved: crates/kernels/mgga/src/lib.rs contains a commented-out `// pub mod mgga_x_2d_prp10;` line (the deferral marker)."
    - "After regen, cargo build -p libxc-kernel-mgga and cargo build --workspace both succeed inline under the project envelope (P11-INV-3)."
    - "After regen, parity_phase11::phase11_smoke passes for all MGGA entries (≥4 per W8 floor: mgga_x_lta, mgga_x_tpss, mgga_x_pkzb, plus one more from the routed list) at strict 1e-12 (D-05)."
    - "After regen, parity_phase11::phase11_worst_case passes at strict 1e-12 for the 11 worst-case offenders (mgga_c_b94, mgga_c_kcisk, mgga_c_ccalda, mgga_c_kcis, mgga_c_rppscan, mgga_c_revtpss, mgga_c_scan, mgga_c_rregtm, mgga_x_r4scan, mgga_x_br89_explicit, mbrxc/mgga_xc_b97m_v if routed)."
    - "LDA + GGA from plans 11-03 + 11-04 remain green (no regression)."
    - "Re-running tools/regen_unified.py --family mgga produces zero git diff under crates/kernels/mgga/ (MGGA portion of P11-INV-6)."
    - "Dispatch tree (src/eval/mgga_dispatch/mod.rs) regenerated: every `crate::kernel::mgga::<func>::...` reference resolves; bash tools/audit_dispatch_tree.sh exits 0."
  artifacts:
    - path: "crates/kernels/mgga/src/lib.rs"
      provides: "Family façade with `pub mod <func>;` per MGGA functional, alphabetically sorted, plus the commented-out prp10 deferral marker (D-10 + quick task 260510-q02 preservation)"
      contains: "pub mod mgga_x_tpss"
  key_links:
    - from: "tools/regen_unified.py --family mgga"
      to: "crates/kernels/mgga/src/<func>/<level>_<spin>*.rs"
      via: "translate_mgga.py invoked with write_dir = crates/kernels/mgga/src for ALL MGGA functionals"
      pattern: "fn .*_chunk[0-9]+<F: Float>"
    - from: "src/eval/mgga_dispatch/mod.rs (regenerated)"
      to: "crates/kernels/mgga/src/<func>"
      via: "use crate::kernel::mgga::<func>::*; (D-10b — no batchN segment)"
      pattern: "crate::kernel::mgga::[a-z][a-z0-9_]+"
---

<objective>
Wave 4 — populate the unified MGGA façade. This is the bulk of Phase 11's value (207 of 237 oversized files = 87% live in MGGA per RESEARCH.md "Pathological Functional Inventory"). With LDA (plan 11-03) and GGA (plan 11-04) populated, this plan runs `tools/regen_unified.py --family mgga` to populate `crates/kernels/mgga/src/` from `libxc-master/src/maple2c/mgga_{exc,vxc}/*.c`, regenerates the MGGA dispatch tree, and verifies parity on both the smoke set and the worst-case offender list at strict 1e-12.

Per RESEARCH.md "Top 15 worst offenders" — the 11 functionals with files >12,000 lines pre-Phase-11 are the gating cases:
- mgga_c_b94 (kxc_pol = 16,703)
- mgga_c_kcisk (lxc_pol_part15 = 16,138; part14 = 13,719; part16 = 13,913)
- mgga_c_ccalda (lxc_pol = 15,378)
- mgga_c_kcis (lxc_pol_part13 = 14,127; part14 = 12,164; part12 = 11,988)
- mgga_c_rppscan (lxc_pol = 13,238)
- mgga_c_revtpss (lxc_pol_part20 = 12,648; ~30 files >5K total)
- mgga_c_scan (lxc_pol = 12,598)
- mgga_c_rregtm (lxc_pol = 12,596)
- mgga_x_r4scan (per D-LOCK-B)
- mgga_x_br89_explicit (per D-LOCK-B)

Per RESEARCH.md Q2: re-chunk ONLY files >5K. Under D-10 clean-slate the entire MGGA tree is being emitted from scratch — every functional starts blank. That's slightly different from Q2's pre-D-10 incremental-rechunk premise, but the outcome is the same: every emitted file ends up ≤5K because that's the SPLIT_THRESHOLD.

Output: populated MGGA family crate (the largest blast radius); verified parity on smoke + worst-case; regenerated MGGA dispatch.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-04-SUMMARY.md
@.cargo/config.toml
@tools/regen_unified.py
@tools/translate_mgga.py
@tools/translate_v2/cse.py
@tools/translate_v2/emit.py
@tools/generate_mgga_dispatch.py
@.planning/phases/04-bulk-kernel-translation/mgga_roster.tsv
@crates/kernels/mgga/src/lib.rs
@crates/kernels/mgga/Cargo.toml
@verify/tests/parity_phase11.rs
@.planning/quick/260510-q02-restore-prp10-deferral

<interfaces>
MGGA functional inventory: ~92 functionals across `libxc-master/src/maple2c/mgga_exc/` and `libxc-master/src/maple2c/mgga_vxc/`. Discovery via `tools/regen_unified.py --family mgga` walks both directories.

MGGA worst-case offenders (PHASE11_WORST_CASE in parity_phase11.rs from plan 11-01):
- mgga_c_b94, mgga_c_kcisk, mgga_c_ccalda, mgga_c_kcis, mgga_c_rppscan, mgga_c_revtpss, mgga_c_scan, mgga_c_rregtm, mgga_x_r4scan, mgga_x_br89_explicit, mbrxc/mgga_xc_b97m_v (the latter two: verify exact MggaFunctional variant name; the `mbrxc` analog may not be a routed variant — drop with documentation if so).

mgga-7 kcis special-case: pre-clean-slate this was handled by `tools/split_mgga_7_kcis.py` (a one-off post-emit splitter). Under D-10 + the D-02 chunker this case is subsumed: the chunker handles `mgga_c_kcis` and `mgga_c_kcisk` directly during emission. The post-emit splitter is now obsolete (deletion lives in plan 11-06).

mgga_x_2d_prp10 deferral (per quick task 260510-q02): the prp10 functional has a missing Bessel I0/I1 dependency. Pre-clean-slate the deferral was a commented-out `pub mod mgga_x_2d_prp10;` line in the owning numbered subcrate's `lib.rs`. Post-D-10 the deferral surface moves to `crates/kernels/mgga/src/lib.rs`.

CRITICAL deferral preservation logic: `tools/regen_unified.py:regenerate_family_libfile` (built in plan 11-02) MUST emit `// pub mod mgga_x_2d_prp10;  // DEFERRED — Bessel I0/I1 not implemented (libxc id 211, quick task 260510-q02)` instead of an active `pub mod mgga_x_2d_prp10;` line. Verify this logic is in place before this plan runs (per plan 11-02 acceptance criteria; if missing, fix in regen_unified.py before running this task).

The discovery (`discover_functionals` in regen_unified.py) DOES enumerate `mgga_x_2d_prp10` (its .c source exists in libxc-master/src/maple2c/mgga_exc/). The translator MAY emit a `crates/kernels/mgga/src/mgga_x_2d_prp10/` directory, BUT the lib.rs `pub mod` line for it is the commented-out deferral marker. The directory contents are tolerated (not used) but not referenced — this matches the pre-clean-slate behavior.

verify/tests/parity_phase11.rs PHASE11_SMOKE MGGA entries (per W8 floor: ≥4):
- mgga_x_lta (id 201), mgga_x_tpss (id 202), mgga_x_pkzb (id 213), plus one more (e.g. mgga_k_gea2 id ~627 or mgga_x_tau_hcth id 205)

PHASE11_WORST_CASE: 6+ entries (the 11 worst offenders above; mbrxc may be `#[ignore]`'d if not routed).

MGGA dispatch shape (post-D-10b, set up in plan 11-02 task 4):
- src/eval/mgga_dispatch/mod.rs: flat dispatch_mgga + per-functional dispatch_<func> helpers
- Paths: `crate::kernel::mgga::<func>::<level>_<spin>::*` (no batchN segment)
- Generator: tools/generate_mgga_dispatch.py (created in plan 11-02 task 4)

OOM risk (HIGHEST in this plan per RESEARCH.md "Memory ceiling lessons"): the unified MGGA crate could be ~6 MLOC of generated Rust pre-cap-reduction; with the 5K cap it's smaller but still the largest single crate in the workspace. The 5K-cap-first ordering (plans 11-03/04 already proved smaller families build cleanly) bounds the MGGA-specific RAM.

Build envelope (D-08, D-09 — read from .cargo/config.toml, NEVER inline):
- jobs = 1
- RUST_MIN_STACK = 67108864 (64 MB; load-bearing for proc-macro expansion of mgga br89 + mbrxc Brent-method root-finders per D-08; MUST NOT be reduced; MAY be raised if a specific chunk SIGSEGVs).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate MGGA family into unified layout — `tools/regen_unified.py --family mgga`; verify smoke + worst-case parity</name>
  <files>crates/kernels/mgga/src/lib.rs, crates/kernels/mgga/src/**/*.rs (~92 functional directories created from scratch under crates/kernels/mgga/src/), src/eval/mgga_dispatch/mod.rs (regenerated)</files>
  <read_first>
    - tools/regen_unified.py (post-plan-11-02 state — verify the prp10 deferral logic is in `regenerate_family_libfile`)
    - tools/translate_mgga.py (post-plan-11-02 state — SPLIT_THRESHOLD=5000 + D-02 wedge with rho+sigma+lapl+tau wrapper buffers)
    - tools/translate_v2/cse.py + tools/translate_v2/emit.py (the partitioner + emitter; check PER_FUNCTIONAL_CSE_OVERRIDES dict if it was added in 11-03/04)
    - tools/generate_mgga_dispatch.py (the dispatch generator)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-05, D-07/D-08/D-09, D-10, D-LOCK-B worst-case offenders)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "Pathological Functional Inventory" + "Per-functional (worst aggregate)" — mgga_c_revtpss has 30 files >5K
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "Risks per success criterion" — MEDIUM oracle parity drift risk; HIGH cubecl-macros parser limits at 16-arity
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "Open Question 2" — re-chunk ONLY files >5K (under D-10 clean-slate this is automatic)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md (the metric snapshot we beat)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERNS.md "Splitter emission target redirect"
    - .cargo/config.toml (build envelope — cite, do NOT duplicate)
    - .planning/quick/260510-q02-restore-prp10-deferral/ (mgga_x_2d_prp10 deferral context — must NOT be regressed)
    - libxc-master/src/maple2c/mgga_exc/ + mgga_vxc/ (verify the MGGA functional source files exist)
  </read_first>
  <action>
    Three sequential operations, all inline (D-07).

    --- Operation 1: pre-flight + regen MGGA ---

    Pre-flight:

    ```bash
    grep -cE '^pub mod ' crates/kernels/mgga/src/lib.rs              # MUST be 0 (post-clean-slate state)
    grep -cE '^pub use libxc_kernel_' crates/kernels/mgga/src/lib.rs   # MUST be 0
    git status --porcelain crates/kernels/mgga                       # MUST be empty
    # LDA + GGA regression check
    cargo build -p libxc-kernel-lda
    cargo build -p libxc-kernel-gga
    # Verify regen_unified.py has prp10 deferral logic
    grep -c 'mgga_x_2d_prp10' tools/regen_unified.py                 # MUST be ≥ 1
    ```

    All five must hold; if not, STOP and return CHECKPOINT REACHED (in particular, fix `regen_unified.py` if it lacks the prp10 deferral logic — that's a regression of plan 11-02 acceptance).

    Pre-MGGA peak RSS measurement (for OOM regression comparison):

    ```bash
    /usr/bin/time -v cargo build -p libxc-kernel-mgga 2>&1 | tail -25 | grep 'Maximum resident' | tee /tmp/mgga_rss_pre.txt
    ```

    (Pre-MGGA-populate the MGGA crate is empty, so this is a tiny baseline. Recorded for completeness.)

    Regen:

    ```bash
    python3 tools/regen_unified.py --family mgga
    ```

    The driver:
    1. Walks `libxc-master/src/maple2c/mgga_exc/*.c` + `mgga_vxc/*.c`, enumerates ~92 functionals.
    2. For each, invokes `tools/translate_mgga.py` with `--write-to <staging> --split --incremental` (and `--vxc-only` if applicable).
    3. Atomically copies into `crates/kernels/mgga/src/<func>/`.
    4. Rewrites `crates/kernels/mgga/src/lib.rs` with `pub mod <func>;` lines, alphabetically sorted, with the commented-out `// pub mod mgga_x_2d_prp10;  // DEFERRED — ...` line as the prp10 deferral marker.

    Iteration loop if any MGGA file >5000 lines:

    ```bash
    find crates/kernels/mgga/src -name '*.rs' -exec wc -l {} + | awk '$2 != "total" && $1 > 5000'
    ```

    MUST return zero rows. If non-empty (most likely cases: mgga_c_b94, mgga_c_kcisk, mgga_c_revtpss):

    1. Identify the failing functional + file from the audit output.
    2. If CSE-arity overflow (RuntimeError raised by emit.py wrapping ChunkArityError), lower the input or output arity cap from 16 to 12 in the emit.py PER_FUNCTIONAL_CSE_OVERRIDES dict for that functional (per RESEARCH.md A2 mitigation).
    3. If a single chunk is >5K because the dep chain is too dense, lower `min_uses` in the override (default 5 → 3).
    4. Per RESEARCH.md "Risks": "If specific functionals violate even 1e-12, fall back to a finer chunk granularity (preserve more named temporaries inline)." **W-D10-5: EXTEND** the existing module-level `PER_FUNCTIONAL_CSE_OVERRIDES: dict[str, dict] = {}` constant in `tools/translate_v2/emit.py` (created in plan 11-02 Task 2; possibly populated by plans 11-03/04) with the per-functional override entry. Do NOT introduce the dict; it already exists. If the override list grows past 5 entries, MOVE (not split) the dict to a sidecar `tools/translate_v2/per_functional_overrides.py` and re-import.
    5. Re-run regen for just that functional via direct translator invocation.
    6. Re-audit. After 3 retries with no convergence, STOP and return CHECKPOINT REACHED naming the functional + the chunk arity + the chosen mitigation strategy.

    Post-regen sanity checks:

    ```bash
    # prp10 deferral preservation
    grep -E '^[[:space:]]*//[[:space:]]*pub mod mgga_x_2d_prp10' crates/kernels/mgga/src/lib.rs
    # MUST return ≥ 1 match.
    grep -E '^[[:space:]]*pub mod mgga_x_2d_prp10' crates/kernels/mgga/src/lib.rs
    # MUST return 0 matches (no live `pub mod` for prp10).
    ```

    --- Operation 2: regen MGGA dispatch + build + parity verify (inline) ---

    ```bash
    python3 tools/generate_mgga_dispatch.py
    bash tools/audit_dispatch_tree.sh    # MUST exit 0

    cargo build -p libxc-kernel-mgga
    cargo build --workspace

    cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture
    cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case -- --test-threads=1 --nocapture
    ```

    Each must exit 0. `phase11_worst_case` is the 6+ entry list from plan 11-01; this is its first chance to actually pass (the test was scaffolded with `#[ignore]` markers in plan 11-01). REMOVE the `#[ignore]` attributes on the worst-case test entries that the regen successfully covers, KEEPING `#[ignore = "..."]` only on entries that still fail (with an updated reason string). Commit the test-file edit alongside the regen output.

    Both `phase11_smoke` and `phase11_worst_case` MUST exit 0 at strict 1e-12 (D-05). LDA + GGA from prior plans must remain green.

    Per RESEARCH.md "Risks": if a worst-case functional shows oracle drift but stays within 10× of strict_tol (e.g., max rel err 5e-12 vs target 1e-12), the CSE-introduced reordering is the cause. Mitigations to try in order:
    1. Tighten min_uses (default 5 → 8) to reduce CSE breakpoints.
    2. Force breakpoint to land at additive vs multiplicative line (additive accumulation reorders cause most drift).
    3. Per-functional `chunk_max_lines` increase to 5500 (still under hard cap with boilerplate room).

    Post-MGGA peak RSS measurement:

    ```bash
    /usr/bin/time -v cargo build -p libxc-kernel-mgga 2>&1 | tail -25 | grep 'Maximum resident' | tee /tmp/mgga_rss_post.txt
    ```

    OOM contingency: if `cargo build -p libxc-kernel-mgga` OOMs (RAM peak exceeds the user's hardware budget — per memory `feedback_ram_constraints.md`), the unified MGGA crate is too large. Mitigations in order:
    1. Per-functional CSE tuning to produce smaller per-file outputs (lower `chunk_max_lines` for the offending functionals).
    2. RUST_MIN_STACK can be RAISED (D-08 permits raising; document the new value and the failing chunk in the SUMMARY). DO NOT lower below 67108864.
    3. STOP and return CHECKPOINT REACHED. Possible (rare) mitigation: split MGGA into 2+ family façades — but this would re-introduce the very subcrate proliferation D-10 forbids; user approval required.

    --- Operation 3: MGGA-only idempotency check ---

    ```bash
    git status --porcelain crates/kernels/mgga > /tmp/mgga_status1.txt
    python3 tools/regen_unified.py --family mgga
    git status --porcelain crates/kernels/mgga > /tmp/mgga_status2.txt
    diff /tmp/mgga_status1.txt /tmp/mgga_status2.txt
    ```

    `diff` MUST exit 0.

    Path-scoped commit:

    ```bash
    git commit --only -m "feat(phase-11/mgga): regen MGGA family into unified façade (D-10) at SPLIT_THRESHOLD=5000 — closes 207-file blast radius

    - Populated crates/kernels/mgga/src/ with ~92 functional subdirs from libxc-master/src/maple2c/mgga_{exc,vxc}/.
    - Every MGGA .rs file ≤5000 lines (P11-INV-2 fully met workspace-wide — closes the 207-file blast radius including mgga_c_b94 16703→<N>, mgga_c_kcisk 16138→<N>, mgga_c_ccalda 15378→<N>, mgga_c_kcis 14127→<N>, mgga_c_revtpss 12648→<N>, mgga_c_rppscan 13238→<N>, mgga_c_scan 12598→<N>, mgga_c_rregtm 12596→<N>).
    - MGGA chunked emission uses D-02 tuple-return <F: Float> ABI with rho+sigma+lapl+tau wrapper buffers.
    - Smoke + worst-case parity green at strict 1e-12 (D-05).
    - mgga_x_2d_prp10 deferral preserved (commented-out pub mod line in mgga/src/lib.rs per quick task 260510-q02).
    - MGGA splitter is empirically idempotent.
    - Dispatch regenerated against populated MGGA façade." -- \
      crates/kernels/mgga \
      src/eval/mgga_dispatch \
      verify/tests/parity_phase11.rs
    # Include emit.py edit if per-functional CSE overrides were added/extended.
    ```
  </action>
  <verify>
    <automated>find crates/kernels/mgga/src -name '*.rs' -exec wc -l {} + | awk '$2 != "total" && $1 > 5000 {c++} END {exit c==0?0:1}' && python3 tools/audit_kernel_size.py --strict && cargo build --workspace && cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture && cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case -- --test-threads=1 --nocapture && bash tools/audit_dispatch_tree.sh</automated>
  </verify>
  <acceptance_criteria>
    - Zero MGGA files >5000 lines: `find crates/kernels/mgga/src -name '*.rs' -exec wc -l {} + | awk '$2 != "total" && $1 > 5000' | wc -l` equals `0`.
    - Workspace-wide cap holds: `python3 tools/audit_kernel_size.py --strict` exits `0` (P11-INV-2 fully met).
    - `crates/kernels/mgga/src/lib.rs` contains ≥80 active `pub mod` lines: `grep -cE '^pub mod ' crates/kernels/mgga/src/lib.rs` is at least `80` (~92 functionals minus the prp10 deferred + a margin).
    - prp10 deferral preserved (BOTH gates must hold per Blocker B4 — use `find` not glob expansion, and the `find ... -path` pattern is portable across sh/bash):
        - Primary (deferral marker present): `find crates/kernels/mgga -maxdepth 3 -name 'lib.rs' -type f -exec grep -lE '^[[:space:]]*//[[:space:]]*pub mod mgga_x_2d_prp10' {} +` returns ≥1 match.
        - Secondary (no live pub mod): `find crates/kernels/mgga -maxdepth 3 -name 'lib.rs' -type f -exec grep -E '^[[:space:]]*pub mod mgga_x_2d_prp10[[:space:]]*;[[:space:]]*$' {} +` returns no matches (live pub mod absent).
    - The previously-pathological functionals exist as subdirs and are ≤5000 lines per file: for each of `mgga_c_b94`, `mgga_c_kcisk`, `mgga_c_ccalda`, `mgga_c_kcis`, `mgga_c_revtpss`, `mgga_c_rppscan`, `mgga_c_scan`, `mgga_c_rregtm`, `mgga_x_r4scan`, `mgga_x_br89_explicit`, `test -d crates/kernels/mgga/src/<name>` exits `0` AND `find crates/kernels/mgga/src/<name> -name '*.rs' -exec wc -l {} + | awk '$2 != "total" && $1 > 5000' | wc -l` equals `0`.
    - Specific smoke entries exist as subdirs: `test -d crates/kernels/mgga/src/mgga_x_lta && test -d crates/kernels/mgga/src/mgga_x_tpss && test -d crates/kernels/mgga/src/mgga_x_pkzb` exits `0`.
    - `cargo build -p libxc-kernel-mgga` exits `0`.
    - `cargo build --workspace` exits `0` (P11-INV-3).
    - `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture` exits `0` (per W8 floor: ≥4 MGGA entries pass — assert `grep -E '"mgga_' verify/tests/parity_phase11.rs | grep -c PHASE11_SMOKE` is at least `4`).
    - `cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case -- --test-threads=1 --nocapture` exits `0` (with `#[ignore]` removed on now-passing worst-case entries; `#[ignore = "..."]` retained ONLY on entries with documented blockers like mbrxc if it is not a routed variant).
    - LDA + GGA regression check: `cargo build -p libxc-kernel-lda && cargo build -p libxc-kernel-gga` exits `0` AND LDA + GGA portions of phase11_smoke still pass.
    - `bash tools/audit_dispatch_tree.sh` exits `0`. **W-D10-4: substantive (NOT trivial-pass) PASS required** — the script's stdout MUST start with `PASS: every dispatch ref resolves (substantive ...` (no `TRIVIALLY PASS` marker; both REFERENCED and EXPOSED sets are non-empty post-population).
    - **W-D10-4 acceptance — REAL MGGA dispatch (no stub bodies):** every routed MGGA functional has a real `ten_arm_dispatch_mgga!` body in the regenerated `src/eval/mgga_dispatch/mod.rs` (or per-functional helper file), NOT a `TODO Phase 12` stub. The count of `UnsupportedFunctional` returns in `src/eval/mgga_dispatch/*.rs` MUST equal the count of explicitly-deferred-functionals + vxc-only functionals + scalar-bearing functionals not yet wired (mirroring `tools/generate_gga_dispatch.py`'s classification). Specifically:
        - `grep -c 'UnsupportedFunctional' src/eval/mgga_dispatch/mod.rs` is at most `N + V + S` where N = `len(EXPLICITLY_DEFERRED)` (currently 1: mgga_x_2d_prp10 — see `tools/generate_mgga_dispatch.py`), V = vxc-only MGGA functional count (sourced from mgga_roster.tsv `has_exc=0` rows), S = scalar-bearing MGGA functional count (sourced from mgga_roster.tsv non-empty `scalars` column where the wiring is not yet done — verify count by `awk -F'\t' 'NR > 1 && $6 != "" {c++} END {print c}' .planning/phases/04-bulk-kernel-translation/mgga_roster.tsv`).
        - `grep -c 'TODO Phase 12' src/eval/mgga_dispatch/*.rs` MUST equal `0` (no stub TODOs remain — phase 11 owns this; deferral comments cite the explicit deferral source per W-D10-4, e.g. `// DEFERRED: Bessel I0/I1 not implemented (libxc id 211, quick task 260510-q02)`).
        - `grep -c 'ten_arm_dispatch_mgga!' src/eval/mgga_dispatch/*.rs` is at least `(R - N - V - S)` where R = total routed MGGA functional count (~92 minus the explicitly-deferred set).
    - `bash tools/audit_cube_launch.sh` exits `0` (count still ≤ 22).
    - At least one MGGA chunked file from the worst-case set matches D-02 ABI: `find crates/kernels/mgga/src/mgga_c_b94 crates/kernels/mgga/src/mgga_c_kcisk crates/kernels/mgga/src/mgga_c_revtpss crates/kernels/mgga/src/mgga_c_kcis crates/kernels/mgga/src/mgga_c_ccalda -name '*_chunk*.rs' -exec grep -l 'fn .*_chunk[0-9]\+<F: Float>' {} + 2>/dev/null | head -1 | grep -c .` is at least `1` (Blocker B4 — single `find` with explicit subdirs is portable).
    - No `&mut Array<f64>` shared-scratch chunks in MGGA: `find crates/kernels/mgga/src -name '*.rs' -exec grep -lE 'fn .*_chunk[0-9]+\\(.*&mut Array<f64>' {} +` returns no matches.
    - Idempotency: re-running `python3 tools/regen_unified.py --family mgga` produces zero `git status --porcelain crates/kernels/mgga` diff.
    - No `.cargo/config.toml` change: `git diff --name-only HEAD~1 HEAD .cargo/config.toml` returns empty.
  </acceptance_criteria>
  <done>The MGGA family is fully populated under D-10 at SPLIT_THRESHOLD=5000; every MGGA file is ≤5000 lines (workspace-wide kernel-size audit is now CLEAN); cargo builds the workspace; oracle parity holds at 1e-12 on smoke + worst-case sets; the MGGA portion of the splitter is empirically idempotent; mgga_x_2d_prp10 deferral preserved; the MGGA dispatch tree resolves against the populated façade; LDA + GGA from prior plans remain unbroken.</done>
  <scope_rationale>
    **W7 — task-monolith justification (acknowledged, retained):** This task has the largest action prose in the phase (the bulk MGGA regen with worst-case-offender mitigation knobs), ~25 acceptance criteria, and 3 sequential operation blocks (regen+audit / dispatch+build+parity / idempotency).

    Decision: KEEP MONOLITHIC. Rationale:
    - The 207-file MGGA blast radius is a single regen invocation: splitting into "regen+audit / dispatch+build+parity / idempotency" sub-tasks would require each sub-task to inherit + re-verify the prior's state, doubling the verification surface.
    - The three operations are sequentially dependent (regen produces dispatch inputs; dispatch + build produce parity inputs; idempotency requires both prior steps committed).
    - Action prose length is driven by worst-case-offender mitigation knobs (CSE retry rounds, per-functional override paths). Splitting would relocate prose without reducing it.
    - Context budget bounded by the cargo build wall-time + parity test runtime, not prose length. The executor reads the prose once then iterates the build/test loop.

    If this task's execution context exceeds 50% during run-time, the executor MAY split mid-execution by deferring the idempotency operation to a fresh follow-up plan (created via `/gsd-plan-phase 11 --gaps`).
  </scope_rationale>
</task>

</tasks>

<verification>
End-of-plan verification:

1. P11-INV-2 fully workspace-wide: `python3 tools/audit_kernel_size.py --strict` exits `0`.
2. P11-INV-3: `cargo build --workspace` exits `0`.
3. P11-INV-4 (smoke): `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke -- --test-threads=1 --nocapture` exits `0`.
4. P11-INV-4 (worst-case): `cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case -- --test-threads=1 --nocapture` exits `0`.
5. P11-INV-5: `bash tools/audit_cube_launch.sh` exits `0`.
6. P11-INV-6 (MGGA portion): MGGA regen is idempotent.
7. Blocker B1 closed: `bash tools/audit_dispatch_tree.sh` exits `0`.
8. mgga_x_2d_prp10 deferral preserved: commented-out `// pub mod mgga_x_2d_prp10;` line in `crates/kernels/mgga/src/lib.rs`.
9. crates/kernels/mgga/src/lib.rs has ≥80 active `pub mod` lines.

If verification 4 (worst-case parity) fails on any functional, STOP and return CHECKPOINT REACHED — D-05 may need per-functional CSE override or tolerance discussion.
</verification>

<success_criteria>
- D-10 unified emit proven on the largest family (MGGA — 92 functionals + 207 oversized files pre-Phase-11).
- SPLIT_THRESHOLD=5000 enforced for MGGA (D-LOCK-B contribution from MGGA).
- Every MGGA `.rs` file ≤5000 lines (P11-INV-2 fully workspace-wide).
- MGGA family builds + workspace builds + MGGA oracle parity holds at strict 1e-12 on smoke + worst-case (P11-INV-3, P11-INV-4 contributions).
- MGGA splitter is empirically idempotent (P11-INV-6 contribution from MGGA).
- LDA + GGA from prior plans unbroken.
- `#[cube(launch_unchecked)]` count unchanged at 22 (P11-INV-5).
- mgga_x_2d_prp10 pre-existing deferral preserved.
- Dispatch tree resolves against the populated MGGA façade.
- No `.cargo/config.toml` modification (D-07/D-08/D-09 invariants).
- All cargo commands ran inline (D-07).
- Phase 11 success criteria 1, 2, 3, 4, 5, 7 are MET (only criterion 6 — full end-to-end idempotency — remains for plan 11-06).
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-05-SUMMARY.md` covering:
- Per-functional emit count (~92 MGGA functionals).
- Worst-case results: per-functional chunk count + max line count for each PHASE11_WORST_CASE entry (mgga_c_b94, mgga_c_kcisk, mgga_c_ccalda, mgga_c_kcis, mgga_c_rppscan, mgga_c_revtpss, mgga_c_scan, mgga_c_rregtm, mgga_x_r4scan, mgga_x_br89_explicit). Specifically call out the 11 functionals that previously had files >12K lines.
- Per-functional CSE knob overrides (the exception list with rationale).
- Parity result on phase11_smoke + phase11_worst_case (max relative error per entry).
- Idempotency result.
- mgga_x_2d_prp10 deferral preservation confirmation (grep evidence).
- Peak RSS for `cargo build -p libxc-kernel-mgga` post-population (compared to pre-Phase-11 sum across 17 numbered subcrates).
- Build envelope confirmation.
- The new workspace-wide kernel-size audit summary (target = 0 oversized; baseline was 237).
- Pointer to next plan: `/gsd-execute-phase 11` resumes at plan 11-06 (CLAUDE.md update + obsolete-tool deletion + idempotency + phase gate).
</output>
