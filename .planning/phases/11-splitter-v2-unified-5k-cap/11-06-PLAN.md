---
phase: 11-splitter-v2-unified-5k-cap
plan: 06
type: execute
wave: 3
depends_on: [05]
files_modified: [
  "tools/translate_v2/cse.py",
  "tools/translate_v2/per_functional.py",
  "crates/kernels/math/src/",
  "crates/kernels/lda/",
  "crates/kernels/gga/",
  "crates/kernels/mgga/",
  "Cargo.toml",
  ".planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md"
]
autonomous: true
requirements: [KERN-03, KERN-04, KERN-05, VERIFY-02, VERIFY-03, VERIFY-04]
user_setup: []

must_haves:
  truths:
    - "D-16 AST emit pass lands in cse.py consolidating literal-wrap + helper-call-wrap per locked D-02 ABI"
    - "Full kernel tree regenerates from Maple: ~266 per-functional subcrates with all files ≤5K"
    - "Representative canaries (lda_x, gga_x_lb, mgga_c_b94) compile per-`-p` under D-15 entry gate"
    - "All three canaries pass oracle parity at 1e-12 (energy + routed derivatives)"
    - "Idempotency confirmed on full tree: splitter re-run produces zero diff"
    - "D-13 per-design launch budget audit passes (routed 1-per-output, unrouted 0, math ≤22)"
  artifacts:
    - path: "crates/kernels/lda/lda_x/"
      provides: "LDA canary subcrate, ≤5K per file"
      contains: "src/lib.rs, src/exc_unpol.rs, src/vxc_unpol.rs"
    - path: "crates/kernels/gga/gga_x_lb/"
      provides: "GGA canary subcrate, ≤5K per file"
      contains: "src/lib.rs, src/exc_unpol.rs"
    - path: "crates/kernels/mgga/mgga_c_b94/"
      provides: "MGGA canary subcrate, ≤5K per file, from D-14 spike"
      contains: "src/lib.rs, src/kxc_pol/, src/kxc_unpol/"
    - path: "tools/translate_v2/cse.py"
      provides: "Unified AST emit pass per D-16, incorporating D-02 locked ABI"
      contains: "AST visitor for literal-wrap + helper-call-wrap consolidation"
    - path: "Cargo.toml"
      provides: "Root manifest with ~266 per-functional subcrate deps + default-members"
      contains: "libxc-kernel-<func> path-deps, deferred excluded"
    - path: ".planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md"
      provides: "Full tree regen results, canary gate verification, audit outcomes"
      contains: "Per-functional subcrate count, max file size, audit results"
  key_links:
    - from: "D-16 AST emit pass"
      to: "cse.py"
      via: "consolidates literal-wrap + helper-call-wrap"
      pattern: "CallExpr|F::new|F::cast_into"
    - from: "canary compile gates"
      to: "per-functional subcrates"
      via: "`cargo build -p libxc-kernel-<func>`"
      pattern: "libxc-kernel-lda_x|gga_x_lb|mgga_c_b94"
    - from: "audit_cube_launch.sh (D-13)"
      to: "per-design budget"
      via: "routed 1-per-output, unrouted 0, math ≤22"
      pattern: "1654 routed pairs"
---

<objective>
Regen full kernel tree from Maple with D-16 unified AST emit pass (D-02 ABI locked from 11-05). Emit ~266 per-functional subcrates with all files ≤5K. Verify three representative canaries (lda_x, gga_x_lb, mgga_c_b94) compile per-`-p` and pass oracle parity at 1e-12 under D-15 compile-first entry gate. Confirm D-13 per-design launch budget audit passes. Verify idempotency.

Purpose: Prove that the full tree can be generated and that representative samples from each family (LDA, GGA, MGGA) meet the 5K cap, compile, and pass parity. This is the proof-of-concept for the per-functional structure before the per-`-p` sweep (11-07).
Output: Full regen'd tree, canary gates passing, audit clean, idempotency verified.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-16, D-15)
@.planning/phases/11-splitter-v2-unified-5k-cap/11-05-SUMMARY.md (locked D-02 ABI)
@tools/translate_v2/cse.py
@tools/maple_to_kernels.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate D-16 unified AST emit pass in cse.py per locked D-02 ABI</name>
  <files>tools/translate_v2/cse.py, tools/translate_v2/per_functional.py</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-05-SUMMARY.md (locked D-02 ABI choice)
    - tools/translate_v2/cse.py (current state with MAX_TUPLE_ARITY=12)
    - tools/translate_v2/per_functional.py (current emit shape)
    - crates/kernels/math/tests/spike_cse_emit_q01.rs (reference for q01 fixes incorporated)
  </read_first>
  <action>
Per D-16, consolidate emit logic into a single AST pass in cse.py:

1. **Literal-wrap integration (subsuming q01's _wrap_f64_literals regex):**
   - AST visitor scans all `f64` literals: integer-mantissa style (`2e-21`), decimal (`3.14`), named constants (`M_PI`, `M_CBRT3`, etc.)
   - Wraps all with `F::new(...)` in a single deterministic pass
   - Retires the fragile multi-regex approach that q01 partially fixed

2. **D-02 ABI-specific emit:**
   - **If Option A (generic helpers):** Chunk bodies emit `<F: Float>` directly; helper calls are direct (no wrapping)
   - **If Option C (cast-at-call-site):** Chunk bodies emit `<F: Float>`; helper calls wrapped with `F::cast_into(...)` args + `F::new(...)` return per the 38-helper allowlist

3. **Single-output scalar-return shape (from q01):**
   - Single-output chunks emit `-> F` (scalar), not `-> (F,)` (1-tuple)
   - Retains q01's fix; can stay in per_functional.py or migrate to cse.py (planner's call)

4. **Idempotency guarantee:**
   - Pass is deterministic (no hash-order iteration, no random ordering)
   - Multi-run produces identical output

5. **Test reference:**
   - Verify against crates/kernels/math/tests/spike_cse_emit_q01.rs (from q01 commit 5c379dc25) — confirm the three emit fixes (arity cap, literal wrap, scalar return) still apply

6. **Commit:**
   - Format: `feat(11-06): D-16 unified AST emit pass in cse.py per D-02 Option [A|C]`
  </action>
  <verify>
    <automated>grep -q "def ast_visitor" tools/translate_v2/cse.py || grep -q "def emit_wrapped" tools/translate_v2/cse.py</automated>
  </verify>
  <done>D-16 AST emit pass consolidated in cse.py, literal-wrap subsumed, Option A/C ABI logic integrated, idempotent</done>
</task>

<task type="auto">
  <name>Task 2: Fix math/src/ #[cfg(test)] API drift in ArrayArg::from_raw_parts (CubeCL 0.10)</name>
  <files>crates/kernels/math/src/ (all files with #[cfg(test)] blocks)</files>
  <read_first>
    - crates/kernels/math/src/powers.rs (lines with #[cfg(test)])
    - crates/kernels/math/src/dft_quantities.rs (same)
    - crates/kernels/math/src/piecewise.rs (same)
  </read_first>
  <action>
Fix the 165 `from_raw_parts` API drift errors from quick-task q01's carry-forward section (.continue-here.md):

CubeCL 0.10 changed the `ArrayArg::from_raw_parts` signature:
- Old: `::<f64>(&handle, n, 1)` (generic turbo-fish, 3 args)
- New: `(handle: Handle, length: usize)` (non-generic, 2 args)

Files affected (per .continue-here.md): `dft_quantities.rs`, `powers.rs`, `polynomials.rs`, `bspline.rs`, `erf.rs`, and others with `#[cfg(test)]` blocks.

1. Scan each file's `#[cfg(test)]` section
2. Replace `ArrayArg::from_raw_parts::<f64>(&handle, n, 1)` → `ArrayArg::from_raw_parts(handle, n)`
3. Verify tests compile: `cargo test -p libxc-kernel-math`
4. Commit: `fix(11-06): update ArrayArg::from_raw_parts calls for CubeCL 0.10 API (165 fixes)`

After this fix, spike_cse_emit_q01.rs and other math-crate tests can run to validate D-14/D-16 emit changes.
  </action>
  <verify>
    <automated>cargo test -p libxc-kernel-math 2>&1 | grep -q "test result:" && cargo test -p libxc-kernel-math 2>&1 | grep -q "ok"</automated>
  </verify>
  <done>165 API drift fixes applied, math-crate tests compile and pass</done>
</task>

<task type="auto">
  <name>Task 3: Full kernel tree regeneration from Maple via tools/maple_to_kernels.py</name>
  <files>crates/kernels/lda/, crates/kernels/gga/, crates/kernels/mgga/, Cargo.toml</files>
  <read_first>
    - tools/maple_to_kernels.py (current state with D-13 per-design launch budget audit)
    - tools/translate_lda_v2.py, tools/translate_gga.py, tools/translate_mgga.py (SPLIT_THRESHOLD tuned for 5K per D-LOCK-B)
    - tools/kernel_routing.py (routing table, deferred list per D-11)
  </read_first>
  <action>
Run full kernel tree regen with locked D-02 ABI from D-16 cse.py:

1. **Prepare splitter environment:**
   - Verify cse.py has D-16 AST pass integrated (Task 1 complete)
   - Verify math/src/ tests compile (Task 2 complete) so helper validation works post-regen
   - SPLIT_THRESHOLD should be tuned to hit ≤5K per file (per D-LOCK-B hard cap)

2. **Run regen:**
   ```bash
   cd /home/user/Documents/workspace/libxc_rs
   rm -rf crates/kernels/lda crates/kernels/gga crates/kernels/mgga
   python tools/maple_to_kernels.py \
     --lda --gga --mgga \
     --splitter-version 2 \
     --output-format per-functional \
     --per-design-budget  # D-13 audit flag
   ```
   (Exact command depends on maple_to_kernels.py's interface; adjust per its --help)

3. **Validate output structure:**
   - ~266 per-functional subcrates created (41 LDA + 131 GGA + 94 MGGA estimate)
   - Each subcrate: `crates/kernels/{lda,gga,mgga}/<func>/Cargo.toml` + `src/lib.rs` + output modules
   - No numbered subcrates (lda-1, gga-N, mgga-N)
   - No per-family crates (lda/Cargo.toml, gga/Cargo.toml, mgga/Cargo.toml)

4. **Root Cargo.toml update:**
   - Rewrite `[dependencies]` to list all ~266 per-functional subcrates as path-deps
   - Rewrite `[workspace] default-members` to list ~260 routed + 6 deferred (D-11) per-functional subcrates
   - Deferred subcrates (mgga_c_b94, mgga_x_br89, mgga_x_mbr, mgga_x_mbrxc_bg, mgga_x_mbrxh_bg, mgga_x_mggac) omitted from default-members but remain dependencies for manual test

5. **Commit:**
   - Format: `feat(11-06): full tree regen via maple_to_kernels.py per-functional ~266 subcrates, ≤5K files`
  </action>
  <verify>
    <automated>find crates/kernels/lda crates/kernels/gga crates/kernels/mgga -maxdepth 2 -name "Cargo.toml" | wc -l | grep -E "26[0-9]|27[0-9]"</automated>
  </verify>
  <done>~266 per-functional subcrates generated, root Cargo.toml updated, no numbered subcrates, no per-family crates</done>
</task>

<task type="auto">
  <name>Task 4: Canary compile gates — lda_x, gga_x_lb, mgga_c_b94 per-`-p` validation</name>
  <files>crates/kernels/lda/lda_x/, crates/kernels/gga/gga_x_lb/, crates/kernels/mgga/mgga_c_b94/</files>
  <read_first>
    - crates/kernels/lda/lda_x/Cargo.toml (verify from regen Task 3)
    - crates/kernels/gga/gga_x_lb/Cargo.toml (verify from regen Task 3)
    - crates/kernels/mgga/mgga_c_b94/Cargo.toml (from D-14 spike 11-05)
  </read_first>
  <action>
D-15 compile-first entry gate: verify three representative canaries (one per family) compile per-`-p` under CubeCL 0.10, jobs=1 envelope:

1. **LDA canary (lda_x):**
   ```bash
   cargo build -p libxc-kernel-lda_x 2>&1 | tee /tmp/lda_x_build.log
   ```
   Expected: `Finished release [optimized] target(s)` (or equivalent success)
   File inspection: verify all lda_x source files ≤5K lines

2. **GGA canary (gga_x_lb):**
   ```bash
   cargo build -p libxc-kernel-gga_x_lb 2>&1 | tee /tmp/gga_x_lb_build.log
   ```
   Expected: success
   File inspection: verify all gga_x_lb source files ≤5K lines

3. **MGGA canary (mgga_c_b94, from D-14 spike):**
   ```bash
   cargo build -p libxc-kernel-mgga_c_b94 2>&1 | tee /tmp/mgga_c_b94_build.log
   ```
   Expected: success (already compiled in 11-05, but re-verify here)
   File inspection: verify all mgga_c_b94 source files ≤5K lines

4. **Dispatch macros validate against canaries:**
   ```bash
   cargo build -p libxc_rs 2>&1 | tee /tmp/libxc_rs_build.log
   ```
   Expected: success (dispatch macros at src/kernel/{lda,gga,mgga}/ + src/eval/{gga,mgga}_dispatch/ expand cleanly against regen'd canaries under D-10b)

5. **File size audit (per D-LOCK-B):**
   ```bash
   tools/audit_kernel_size.py crates/kernels/
   ```
   Expected: all files ≤5000 lines (zero oversized files)

6. **Commit:**
   - Format: `feat(11-06): three-canary compile gates PASS — lda_x, gga_x_lb, mgga_c_b94 per-`-p`, dispatch GREEN`
  </action>
  <verify>
    <automated>cargo build -p libxc-kernel-lda_x &amp;&amp; cargo build -p libxc-kernel-gga_x_lb &amp;&amp; cargo build -p libxc-kernel-mgga_c_b94 &amp;&amp; cargo build -p libxc_rs &amp;&amp; python tools/audit_kernel_size.py crates/kernels/ | grep -q "Max.*5000"</automated>
  </verify>
  <done>All three canaries compile, dispatch GREEN, files ≤5K audit passes</done>
</task>

<task type="auto">
  <name>Task 5: Canary oracle parity gates — 1e-12 energy + routed derivatives</name>
  <files>verify/tests/parity_phase11.rs</files>
  <read_first>
    - verify/tests/parity_phase11.rs (scaffold from 11-01, to be exercised here)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md (baseline metrics)
  </read_first>
  <action>
Run oracle parity verification on the three canaries at 1e-12 energy + routed derivatives:

1. **LDA parity (lda_x canary):**
   - Run ad-hoc oracle test: invoke lda_x functional via the regen'd per-functional subcrate
   - Sample grid points (recommend: dense, polarized, unpolarized)
   - Compare energy output to libxc C oracle at 1e-12 relative error
   - Expected: PASS

2. **GGA parity (gga_x_lb canary):**
   - Same pattern: regen'd gga_x_lb subcrate
   - Energy + vxc (1st derivative)
   - Expected: PASS

3. **MGGA parity (mgga_c_b94 canary, deferred kernel):**
   - One-shot `is_deferred(id)` bypass for parity step only (per D-15 gate scope); D-11 deferred status preserved after
   - Energy + all routed derivatives (vxc, fxc, kxc if computed)
   - Expected: PASS

4. **Verify harness location:**
   - Use verify/tests/parity_phase11.rs or inline ad-hoc test if harness not yet complete
   - Log results in `.planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md`

5. **Commit:**
   - Format: `feat(11-06): canary parity gates PASS — lda_x, gga_x_lb, mgga_c_b94 at 1e-12 energy + routed`
  </action>
  <verify>
    <automated>cargo test -p libxc_rs --test parity_phase11 -- --nocapture 2>&1 | grep -E "(phase11_smoke|phase11_worst_case)" | grep -q "ok"</automated>
  </verify>
  <done>All three canaries pass 1e-12 parity on energy + routed derivatives</done>
</task>

<task type="auto">
  <name>Task 6: Idempotency and audit gates</name>
  <files>crates/kernels/, tools/audit_cube_launch.sh, tools/audit_subcrate_collapse.sh</files>
  <read_first>
    - tools/audit_cube_launch.sh (D-13 per-design budget rewrite)
    - tools/audit_subcrate_collapse.sh (to be updated for per-functional invariant)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md (prior audit results)
  </read_first>
  <action>
Verify idempotency and run full audit suite:

1. **Idempotency check:**
   ```bash
   git status  # should show clean, with new crates/kernels/ content from Task 3
   python tools/maple_to_kernels.py ... (same as Task 3)
   git diff crates/kernels/ | wc -l  # should be zero
   ```
   Expected: zero diff (pipeline is deterministic, idempotent per D-LOCK-D)
   Commit: `test(11-06): idempotency verified — splitter re-run produces zero diff`

2. **D-13 per-design launch budget audit (audit_cube_launch.sh):**
   - Per per-functional subcrate structure (D-10), each subcrate holds ~10 entry kernels
   - Run audit: `tools/audit_cube_launch.sh`
   - Expected results:
     * Routed functionals: exactly 1 `#[cube(launch_unchecked)]` per (functional, output) pair
     * Unrouted functionals: zero launchables (entry kernels demoted to plain `#[cube]` per kernel_routing.py)
     * `crates/kernels/math/src/` launchables: ≤22 (from Wave 0 baseline)
   - Output: audit log showing all three conditions PASS
   - Commit: `test(11-06): D-13 per-design launch budget audit PASS`

3. **Subcrate collapse audit (audit_subcrate_collapse.sh update):**
   - Per-functional invariant (D-10a): no per-family crates (lda/Cargo.toml, gga/Cargo.toml, mgga/Cargo.toml)
   - Per-functional invariant: no numbered subcrates (lda-N, gga-N, mgga-N)
   - Update script to check:
     * `find crates/kernels/{lda,gga,mgga}/ -maxdepth 1 -name "Cargo.toml" | wc -l` should be 0 (no per-family crates)
     * `find crates/kernels/ -name "*-[0-9]" -type d | wc -l` should be 0 (no numbered dirs)
     * `find crates/kernels/{lda,gga,mgga}/ -mindepth 2 -name "Cargo.toml" | wc -l` should be ~266 (per-functional)
   - Run updated script: `tools/audit_subcrate_collapse.sh`
   - Expected: PASS on all invariants
   - Commit: `test(11-06): audit_subcrate_collapse.sh updated for per-functional invariants — PASS`

4. **Summary log:**
   - Collect audit results, idempotency check, metrics (file count, max lines, etc.)
   - Log in `.planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md`
  </action>
  <verify>
    <automated>python tools/audit_kernel_size.py crates/kernels/ | grep "All.*within 5000" &amp;&amp; tools/audit_cube_launch.sh &amp;&amp; tools/audit_subcrate_collapse.sh</automated>
  </verify>
  <done>Idempotency verified (zero diff), D-13 audit PASS, D-10a collapse invariants PASS, all audits logged</done>
</task>

<task type="auto">
  <name>Task 7: Create 11-06-SUMMARY.md and update ROADMAP.md success criteria</name>
  <files>.planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md, ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 11 success criteria section, lines ~250–260)
    - All prior SUMMARY files (11-01..05) for structure reference
  </read_first>
  <action>
1. **Create 11-06-SUMMARY.md:**
   - Status: COMPLETE — Full tree regen, canary gates all PASS
   - Task breakdown: D-16 emit consolidation, API drift fixes, full regen, 3 canary compiles, parity gates, audits
   - Metrics: per-functional subcrate count (~266), max file size (should be ≤5K), compile times per canary, audit results
   - Gates: all three canaries compile, dispatch GREEN, parity 1e-12 PASS, idempotency zero diff, audits PASS
   - Forward dependency: plan 11-07 (per-`-p` sweep) can proceed; plan 11-08 (close) depends on 11-07

2. **Update ROADMAP.md success criteria:**
   - Original criterion #1: "only per-functional subcrates; no numbered subcrates; family is plain directory" → VERIFY ACHIEVED (Task 6 audit confirms)
   - Original criterion #4: "`cargo build --workspace` succeeds" → REINTERPRETED per D-12 (per-`-p` incremental gates, whole-workspace not a gate) — UPDATE ROADMAP.md to reflect this reinterpretation
   - Original criterion #7: "CubeCL macro fan-out audit clean (no `#[cube(launch)]` increase)" → VERIFY ACHIEVED (Task 6 D-13 audit confirms routed 1-per-output, unrouted 0)
   - Add new criterion per D-15: "Compile-first entry gate: representative canaries compile per-`-p` before per-`-p` sweep" → ACHIEVED in this plan (Task 4)

3. **ROADMAP.md edits:**
   - Section "Phase 11: Splitter v2 — Unified Kernels with 5K Line Cap"
   - Locate "Success Criteria" subsection (currently numbered 0–7)
   - Update criterion 1: "only family-level crates" → "only per-functional subcrates; family level is a plain directory; no numbered subcrates"
   - Update criterion 4: "`cargo build --workspace` succeeds" → reinterpreted per D-12 as "per-`-p` incremental verification; whole-workspace build is not a per-iteration gate"
   - Add after 7: "8. Compile-first entry gate (per D-15): representative canaries (lda_x, gga_x_lb, mgga_c_b94) compile per-`-p` GREEN before per-`-p` sweep"

4. **Commit:**
   - Format: `docs(11-06): SUMMARY + ROADMAP success criteria updates (D-12, D-15, per-functional invariants)`
  </action>
  <verify>
    <automated>test -f .planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md &amp;&amp; grep -q "per-functional" ROADMAP.md</automated>
  </verify>
  <done>SUMMARY created, ROADMAP updated, success criteria reflect per-functional structure and D-15 entry gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Splitter emit logic | D-16 consolidation reduces regex fragility; AST pass is deterministic |
| Per-functional subcrate count | ~266 expected; audit verifies exact count |
| Canary selection | lda_x, gga_x_lb, mgga_c_b94 are representatives; gga_x_lb is smallest GGA, reasonable proof-of-concept |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering | Idempotency | mitigate | `git diff` baseline resets between runs; re-run confirmed zero diff |
| T-11-06-02 | Repudiation | Canary selection | mitigate | Three canaries document representative families; SUMMARY records selection |

</threat_model>

<verification>
D-16 consolidated, full tree regen'd, three canaries compile and pass parity, idempotency zero diff, D-13 and D-10a audits pass, ROADMAP success criteria updated.
</verification>

<success_criteria>
- [ ] Task 1: D-16 AST emit pass in cse.py integrated per locked D-02 ABI
- [ ] Task 2: 165 ArrayArg::from_raw_parts API drift fixes applied, math tests compile
- [ ] Task 3: Full tree regen'd, ~266 per-functional subcrates, root Cargo.toml updated, no numbered subcrates
- [ ] Task 4: Three canaries (lda_x, gga_x_lb, mgga_c_b94) compile per-`-p`, files ≤5K, dispatch GREEN
- [ ] Task 5: Canaries pass oracle parity at 1e-12 energy + routed derivatives
- [ ] Task 6: Idempotency zero diff, D-13 audit PASS, D-10a collapse audit PASS
- [ ] Task 7: SUMMARY created, ROADMAP updated with success criteria corrections

</success_criteria>

<output>
After all tasks, major commit:
```bash
git commit -m "feat(11-06): full tree regen per-functional ~266 subcrates, canaries + audits all PASS, D-15 entry gate GREEN"
```
</output>
