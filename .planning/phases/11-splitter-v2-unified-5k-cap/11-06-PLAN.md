<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 06
type: execute
wave: 5
depends_on: [04, 05]
files_modified:
  - CLAUDE.md
  - tools/maple_to_kernels.py
  - tools/split_oversized_kernel.py
  - tools/split_oversized_mgga.py
  - tools/split_mgga_7_kcis.py
  - tools/rebatch_mgga.py
  - tools/shrink_part_fanout.py
  - tools/translate_lda.py
  - .planning/ROADMAP.md
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md
autonomous: false
requirements:
  - P11-INV-1
  - P11-INV-2
  - P11-INV-5
  - P11-INV-6

must_haves:
  truths:
    - "`CLAUDE.md` § Constraints reflects D-03a: 'f64 only' is replaced with 'f64 by default and for oracle gating; f32 opt-in at launch with no correctness gate', and the bit-level operation-order rule is softened to the D-05 1e-12 relative gate."
    - "The 7 obsolete numbered-subcrate-layout splitter tools (split_oversized_kernel, split_oversized_mgga, split_mgga_7_kcis, rebatch_mgga, shrink_part_fanout, the legacy translate_lda, plus regen_phase09 IF superseded) are deleted; `tools/maple_to_kernels.py` no longer references them."
    - "End-to-end idempotency holds: running splitter v2 across all three families produces zero git diff (P11-INV-6, the phase-final gate)."
    - "All five Phase 11 audit gates pass simultaneously: `audit_kernel_size.py --strict`=0, `audit_subcrate_collapse.sh`=0, `audit_cube_launch.sh`=0, `audit_dispatch_tree.sh`=0, `test_idempotency.sh`=0."
    - "ROADMAP Phase 11 success criteria #1 and #4 are corrected for the per-functional-subcrate target and the D-12 build-gate reinterpretation; `11-FINAL-METRICS.md` records the post-phase metric snapshot vs 11-BASELINE.md."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Precision + operation-order policy updated per D-03a"
      contains: "f64 by default"
    - path: ".planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md"
      provides: "Post-phase metric snapshot (oversized=0, max line count, subcrate count, launch count) vs baseline"
      min_lines: 30
    - path: "tools/maple_to_kernels.py"
      provides: "Driver cleaned of obsolete split_*.py references; rewired to splitter v2"
      min_lines: 80
  key_links:
    - from: "tools/maple_to_kernels.py"
      to: "tools/translate_v2"
      via: "driver delegates to splitter v2, not the deleted split_*.py helpers"
      pattern: "translate_v2|translate_(lda_v2|gga|mgga)"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md"
      via: "Phase 11 success-criteria correction + metrics cross-link"
      pattern: "per-functional subcrate|FINAL-METRICS"
---

<objective>
Close Phase 11: update `CLAUDE.md` for the D-03a precision/operation-order policy shift, delete the 7 obsolete numbered-subcrate-layout splitter tools and clean up `maple_to_kernels.py`, run the end-to-end idempotency gate (P11-INV-6) across all three families, confirm all five Phase 11 audit gates pass simultaneously, correct the ROADMAP success criteria for the per-functional-subcrate target, and snapshot final metrics against `11-BASELINE.md`.

Purpose: Plans 11-02..05 built the splitter v2, restructured the tree, and verified per-family correctness. This plan lands the policy documentation that Phase 11 amends (D-03a), removes the dead tooling that assumed the numbered-subcrate layout, proves whole-pipeline idempotency (the D-LOCK-D success criterion), and finalizes the phase record.

Output: An updated `CLAUDE.md`, a cleaned `tools/` directory, a passing end-to-end idempotency gate, a corrected ROADMAP, and `11-FINAL-METRICS.md`.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-04-SUMMARY.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-05-SUMMARY.md
@.cargo/config.toml
@CLAUDE.md

<interfaces>
<!-- Phase-close surface. Extracted — do NOT re-explore. -->

CLAUDE.md § "## Project" > "### Constraints" — the lines to amend (D-03a):
  "Precision: f64 only; energy relative error <= 10^-12 vs libxc oracle"
    → "Precision: f64 by default and for oracle gating; f32 opt-in at launch
       with no correctness gate; energy relative error <= 10^-12 vs libxc oracle"
  "Operation order: Maple2c formula translations must preserve floating-point
   operation order for bit-level equivalence"
    → soften: CSE-aware subdivision (Phase 11 D-01) introduces named temporaries
       that may legitimately reorder accumulation; the gate is 1e-12 relative
       error (D-05), not bit-exact.

Obsolete splitter tools to delete (CONTEXT canonical_refs + Claude's Discretion —
all assume the numbered-subcrate layout, now gone):
  tools/split_oversized_kernel.py
  tools/split_oversized_mgga.py
  tools/split_mgga_7_kcis.py
  tools/rebatch_mgga.py
  tools/shrink_part_fanout.py        (per RESEARCH: already integrated into translators since 260512-q01)
  tools/translate_lda.py             (LEGACY — translate_lda_v2.py is the live one; confirm translate_lda.py is unused)
  CONFIRM before deleting: tools/split_lda_subcrates.py, tools/resplit_gga.py,
  tools/regen_phase09.py — read CONTEXT D-10a and 11-03-SUMMARY to determine
  whether the splitter v2 driver still delegates to regen_phase09.py (if 11-02/11-03
  replaced it, delete it; if it is still the per-functional iteration driver, KEEP).

tools/maple_to_kernels.py — the `split` subcommand and split_*.py references
must be removed/no-op'd (plan 11-02 left them intact "for now"; this plan removes them).

ROADMAP Phase 11 success criteria needing correction (per CONTEXT re-plan note):
  #1: "only family-level crates" → "only per-functional subcrates; family level
       is a plain directory; no numbered subcrates"
  #4: "cargo build --workspace succeeds" → reinterpret per D-12 (per-subcrate
       incremental verification; whole-workspace build is not a per-iteration gate)
  #7: CubeCL macro fan-out audit — still applies, no change.

11-BASELINE.md metrics to compare against in 11-FINAL-METRICS.md:
  oversized files: 235 (q03-stash-adjusted) / 237 (plan projection) → target 0
  max line count: 16,703 → target ≤5000
  numbered subcrates: 27 → target 0
  #[cube(launch_unchecked)]: 23 (post-W0) → target ≤23 (MUST NOT increase)
  workspace member count: 35 → new count (root + libxc-sys + verify + xtask +
    math + ~264 per-functional subcrates)

The 5 Phase 11 audit gates (all must pass simultaneously at phase close):
  python3 tools/audit_kernel_size.py --strict     (P11-INV-2)
  bash tools/audit_subcrate_collapse.sh           (P11-INV-1)
  bash tools/audit_cube_launch.sh                 (P11-INV-5)
  bash tools/audit_dispatch_tree.sh               (B1 / dispatch criterion)
  bash tools/test_idempotency.sh                  (P11-INV-6)

From .cargo/config.toml (D-08/D-09 — do NOT modify, do NOT duplicate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md (D-03a) + delete obsolete splitter tools + clean maple_to_kernels.py</name>
  <files>CLAUDE.md, tools/maple_to_kernels.py, tools/split_oversized_kernel.py, tools/split_oversized_mgga.py, tools/split_mgga_7_kcis.py, tools/rebatch_mgga.py, tools/shrink_part_fanout.py, tools/translate_lda.py</files>
  <read_first>
    - CLAUDE.md (§ "## Project" > "### Constraints" — the f64-only and operation-order lines to amend per D-03a)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-03, D-03a, D-05 — the precision/operation-order policy shift; canonical_refs "Superseded work" + Claude's Discretion on tool deletion)
    - tools/maple_to_kernels.py (entire file — the `split` subcommand + split_*.py subprocess calls + the stale DEFAULT_* knobs; plan 11-02 retuned the threshold but left split wiring intact)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-03-SUMMARY.md (confirm whether regen_phase09.py is still used by the splitter v2 driver — determines if it is safe to delete)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-02-SUMMARY.md (what splitter v2's driver entry point actually is)
    - tools/translate_lda.py (first ~20 lines — confirm it is the legacy translator superseded by translate_lda_v2.py, not still referenced)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md (lines 113-127 "Tools Inventory" — which tools are explicitly marked obsolete/deletable post-collapse)
  </read_first>
  <action>
    Three parts.

    PART A — Update `CLAUDE.md` § Constraints (D-03a):
    - Replace the precision line. Current: `**Precision**: f64 only; energy relative error <= 10^-12 vs libxc oracle`. New: `**Precision**: f64 by default and for oracle gating; f32 opt-in at launch with no correctness gate; energy relative error <= 10^-12 vs libxc oracle (f64)`.
    - Replace the operation-order line. Current: `**Operation order**: Maple2c formula translations must preserve floating-point operation order for bit-level equivalence`. New: `**Operation order**: Maple2c formula translations preserve floating-point operation order where practical; CSE-aware kernel subdivision (Phase 11 D-01) introduces named temporaries that may legitimately reorder accumulation — the correctness gate is 1e-12 relative error vs the libxc oracle (Phase 11 D-05), not bit-exact equivalence`.
    - Make NO other edits to CLAUDE.md.

    PART B — Delete obsolete splitter tools. Confirm each is unreferenced (grep `tools/` and the codebase for imports/subprocess calls) BEFORE deleting, then `git rm`:
    - `tools/split_oversized_kernel.py` — assumes numbered subcrates; obsolete.
    - `tools/split_oversized_mgga.py` — assumes numbered subcrates; obsolete (latent rmtree bug noted in 260510-q01).
    - `tools/split_mgga_7_kcis.py` — one-off for mgga-7's kcis; obsolete.
    - `tools/rebatch_mgga.py` — first-fit-decreasing bin-pack into numbered subcrates; obsolete.
    - `tools/shrink_part_fanout.py` — per RESEARCH already integrated into translators since 260512-q01; obsolete.
    - `tools/translate_lda.py` — legacy LDA translator superseded by `translate_lda_v2.py`; delete ONLY after confirming nothing references it (grep `translate_lda\b` excluding `translate_lda_v2`).
    - For `tools/split_lda_subcrates.py`, `tools/resplit_gga.py`, `tools/regen_phase09.py`: read 11-02-SUMMARY + 11-03-SUMMARY first. If the splitter v2 driver no longer delegates to them, `git rm` them too and note it. If `regen_phase09.py` is still the per-functional iteration driver the splitter v2 uses, KEEP it. Document the decision per tool in the SUMMARY.

    PART C — Clean `tools/maple_to_kernels.py`:
    - Remove the `split` subcommand and ALL `split_*.py` subprocess invocations (`split_cmd`, the `split` branch of the CLI, the `--target-max` plumbing if not already removed in 11-02).
    - The driver's only remaining job is `translate` (delegating to the splitter v2 translators / `regen_phase09.py` if kept). Update the module docstring to describe the splitter-v2-only model: per-functional subcrates, SPLIT_THRESHOLD=4500, no per-subcrate cap, no post-split bin-packing step.
    - Confirm `tools/test_idempotency.sh` still invokes the driver correctly (it calls `python3 tools/maple_to_kernels.py all --family all` per the Wave 0 plan) — if the `all` subcommand was the `translate`+`split` combo and `split` is now gone, `all` should just mean `translate`. Adjust the driver so `test_idempotency.sh`'s invocation still works, OR update `test_idempotency.sh` to call the correct post-cleanup entry point (whichever is cleaner — document in SUMMARY).

    Commit the three parts together (`git commit --only -- <paths>`).
  </action>
  <verify>
    <automated>grep -c 'f64 by default' CLAUDE.md && ! ls tools/split_oversized_kernel.py tools/split_oversized_mgga.py tools/split_mgga_7_kcis.py tools/rebatch_mgga.py tools/shrink_part_fanout.py 2>/dev/null && python3 -c "import ast; ast.parse(open('tools/maple_to_kernels.py').read()); print('maple_to_kernels.py parses')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'f64 by default and for oracle gating' CLAUDE.md` equals `1`.
    - `grep -c 'f64 only' CLAUDE.md` equals `0` (old phrasing fully replaced).
    - `grep -c 'bit-exact\|1e-12 relative error vs the libxc oracle' CLAUDE.md` confirms the operation-order line was softened (the new phrasing present).
    - These files do NOT exist: `tools/split_oversized_kernel.py`, `tools/split_oversized_mgga.py`, `tools/split_mgga_7_kcis.py`, `tools/rebatch_mgga.py`, `tools/shrink_part_fanout.py` (`ls` returns nothing for all five).
    - `tools/translate_lda.py` deletion: either gone (if confirmed unreferenced) or KEPT with a documented reason in the SUMMARY.
    - `grep -c 'split_oversized\|rebatch_mgga\|split_mgga_7_kcis\|shrink_part_fanout' tools/maple_to_kernels.py` equals `0` (driver no longer references deleted tools).
    - `python3 -c "import ast; ast.parse(open('tools/maple_to_kernels.py').read())"` exits `0` (driver still parses after cleanup).
    - `grep -rE 'CARGO_BUILD_JOBS|RUST_MIN_STACK|--jobs ' tools/maple_to_kernels.py` returns no matches.
  </acceptance_criteria>
  <done>CLAUDE.md reflects the D-03a precision/operation-order policy, the 7 obsolete numbered-subcrate-layout splitter tools are deleted, and `maple_to_kernels.py` is cleaned to a splitter-v2-only driver.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end idempotency gate + 5-audit sweep + ROADMAP correction + 11-FINAL-METRICS.md</name>
  <files>.planning/ROADMAP.md, .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md, .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md (the pre-phase metric snapshot 11-FINAL-METRICS.md compares against)
    - .planning/ROADMAP.md (§ Phase 11 — success criteria #1, #4, #7 + the plan list to mark complete)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (the re-plan note at end — the exact success-criteria corrections required; D-12 build-gate reinterpretation)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md (the Per-Task Verification Map with placeholder {N}-XX-XX rows + the `nyquist_compliant: false` frontmatter to flip)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-04-SUMMARY.md and 11-05-SUMMARY.md (the per-family verification results to roll up)
    - tools/test_idempotency.sh (the end-to-end idempotency harness)
    - tools/audit_kernel_size.py, tools/audit_subcrate_collapse.sh, tools/audit_cube_launch.sh, tools/audit_dispatch_tree.sh (the 5 audit gates)
    - .cargo/config.toml (D-08/D-09 invariant)
  </read_first>
  <action>
    Four parts.

    PART A — End-to-end idempotency gate (P11-INV-6, the phase-final gate):
    - With a clean working tree, run `bash tools/test_idempotency.sh`. It runs splitter v2 across all three families twice and diffs the emitted tree. It MUST exit 0.
    - This is the phase-final idempotency proof — distinct from the per-family idempotency checks in plans 11-04/11-05. If it FAILS: a cross-family non-determinism remains (e.g. the root `Cargo.toml` `default-members` ordering, or a shared-state bug). STOP, return CHECKPOINT REACHED with the diff; the fix goes into 11-02's tooling or 11-03's manifest generator.

    PART B — 5-audit simultaneous sweep:
    - Run all five Phase 11 audit gates and confirm ALL exit 0 SIMULTANEOUSLY on the final tree:
      1. `python3 tools/audit_kernel_size.py --strict` → 0 (zero files >5000 — P11-INV-2)
      2. `bash tools/audit_subcrate_collapse.sh` → 0 (no numbered subcrates, no family crates — P11-INV-1)
      3. `bash tools/audit_cube_launch.sh` → 0 (launch_unchecked count ≤ 23 — P11-INV-5)
      4. `bash tools/audit_dispatch_tree.sh` → 0 (B1 closed / dispatch criterion)
      5. `bash tools/test_idempotency.sh` → 0 (P11-INV-6)
    - If any fails, STOP — a regression slipped in. Do NOT mark the phase complete with a red gate.

    PART C — Correct ROADMAP Phase 11:
    - Success criterion #1: change to "`find crates/kernels -maxdepth 1 -type d` shows only per-functional subcrates grouped under plain `{lda,gga,mgga}/` directories — no `lda-N`/`gga-N`/`mgga-N` numbered children AND no family-level crate (`crates/kernels/{family}/Cargo.toml`)".
    - Success criterion #4: change to "Per-subcrate `cargo build -p libxc-kernel-<func>` succeeds for every routed functional under the unchanged `.cargo/config.toml` envelope; whole-workspace `cargo build --workspace` is NOT a per-iteration gate (D-12 — currently OOMing; the per-functional subcrate isolation IS the structural fix, verified incrementally)".
    - Success criterion #7: leave unchanged (CubeCL macro fan-out audit still applies).
    - Mark all six Phase 11 plans complete in the plan list: `- [x] 11-01-PLAN.md` ... `- [x] 11-06-PLAN.md`. Update the plan-list descriptions if the regenerated plans (11-02..06) diverged from the old descriptions.
    - Add a cross-link line pointing to `11-FINAL-METRICS.md`.

    PART D — Write `11-FINAL-METRICS.md` + flip 11-VALIDATION.md frontmatter:
    - Create `.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md` — the post-phase metric snapshot. Run each command, paste the literal output, compare to `11-BASELINE.md`:
      * Oversized files (>5000 lines): command `python3 tools/audit_kernel_size.py` → expect 0 (baseline 235/237).
      * Max file size: → expect ≤5000 (baseline 16,703).
      * Numbered subcrates: `bash tools/audit_subcrate_collapse.sh` → expect 0 (baseline 27).
      * Family-level crates: → expect 0 (baseline 3).
      * `#[cube(launch_unchecked)]` count: `bash tools/audit_cube_launch.sh` → expect ≤23 (baseline 23, MUST NOT increase).
      * Workspace member count: `cargo metadata --format-version 1 --no-deps | python3 -c "import sys,json; print(len(json.load(sys.stdin)['workspace_members']))"` → new count (baseline 35; now root + libxc-sys + verify + xtask + math + ~264 per-functional subcrates).
      * Per-functional subcrate count: `find crates/kernels/{lda,gga,mgga} -maxdepth 2 -name Cargo.toml | wc -l` → ~264.
      * Roll up the parity results from 11-04/11-05 SUMMARYs: smoke PASS count, worst-case PASS/SKIP breakdown.
      * Idempotency: `test_idempotency.sh` result.
      Replace every placeholder with literal command output before committing.
    - In `11-VALIDATION.md`: replace the placeholder `{N}-XX-XX` rows in the Per-Task Verification Map with the concrete task IDs from plans 11-02..06 (each implementation task → its P11-INV-* requirement → its automated command). Flip `nyquist_compliant: false` → `true` in the frontmatter once the map is fully populated. Tick the Wave 0 Requirements checkboxes (all satisfied) and the Validation Sign-Off checkboxes.

    Commit all four parts (`git commit --only -- <paths>`).
  </action>
  <verify>
    <automated>python3 tools/audit_kernel_size.py --strict && bash tools/audit_subcrate_collapse.sh && bash tools/audit_cube_launch.sh && bash tools/audit_dispatch_tree.sh && bash tools/test_idempotency.sh && test -s .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/test_idempotency.sh` exits `0` (end-to-end idempotency — P11-INV-6).
    - All five audit gates exit `0` in a single sweep: `python3 tools/audit_kernel_size.py --strict && bash tools/audit_subcrate_collapse.sh && bash tools/audit_cube_launch.sh && bash tools/audit_dispatch_tree.sh && bash tools/test_idempotency.sh` exits `0`.
    - ROADMAP Phase 11 success criterion #1 mentions "per-functional subcrates" and "no family-level crate" (`grep -c 'per-functional subcrate' .planning/ROADMAP.md` is at least `1`).
    - ROADMAP Phase 11 success criterion #4 mentions "D-12" or "not a per-iteration gate" (the build-gate reinterpretation is recorded).
    - All six Phase 11 plans marked `- [x]` in the ROADMAP plan list (`grep -cE '^\- \[x\] 11-0[1-6]-PLAN' .planning/ROADMAP.md` equals `6`).
    - `.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md` exists, ≥30 lines, contains the literal `0` for oversized-file count and the post-phase max-line-count ≤5000, with explicit before/after comparison to `11-BASELINE.md`.
    - `11-VALIDATION.md` frontmatter has `nyquist_compliant: true` and no `{N}-XX-XX` placeholder rows remain (`grep -c '{N}-XX-XX' .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md` equals `0`).
    - No `.cargo/config.toml` modification; no cargo jobs/env override.
  </acceptance_criteria>
  <done>End-to-end idempotency passes, all five Phase 11 audit gates pass simultaneously, the ROADMAP success criteria are corrected for the per-functional-subcrate target and D-12 build-gate reinterpretation, and 11-FINAL-METRICS.md records the post-phase snapshot against the baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| splitter v2 pipeline (whole) → emitted tree → audit gates | Phase-close integrity. The risk surface: a false-green phase gate (an audit that passes while an invariant is actually violated), or a cross-family idempotency bug that the per-family checks missed. No network/auth/runtime-input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering | cross-family splitter non-determinism | mitigate | Per-family idempotency (11-04/11-05) can pass while a cross-family artifact (root `Cargo.toml` `default-members` ordering, shared roster state) still varies between runs. Mitigation: `test_idempotency.sh` runs ALL THREE families twice and diffs the whole tree — the phase-final gate; any diff STOPS the phase. |
| T-11-06-02 | Spoofing (false-green) | the 5-audit sweep | mitigate | Running audits at different times against a drifting tree could let each pass individually while no single tree satisfies all five. Mitigation: Part B runs all five in a single chained sweep against one final tree; the acceptance criterion is the chained `&&` command exiting 0. |
| T-11-06-03 | Repudiation | ROADMAP / metrics not updated | mitigate | If the ROADMAP keeps the stale success criteria, a future reader believes the phase delivered family-level crates and a passing `cargo build --workspace`. Mitigation: Part C corrects criteria #1/#4 per the CONTEXT re-plan note; Part D's 11-FINAL-METRICS.md gives a verifiable before/after snapshot. |
| T-11-06-04 | Tampering | deleting a still-referenced tool | mitigate | Deleting `regen_phase09.py` or `translate_lda.py` while the splitter v2 driver still depends on it would silently break the pipeline (not caught until the next regen). Mitigation: Task 1 Part B requires grep-confirmation of non-reference BEFORE deletion, with the keep/delete decision documented per tool; `test_idempotency.sh` (Part A) exercises the full driver and would fail if a needed tool was removed. |
| T-11-06-05 | Information disclosure | CLAUDE.md policy drift | accept | CLAUDE.md is a project-internal doc; the D-03a edit relaxes a constraint. There is no disclosure risk — the change is intentional, reviewed, and traceable to D-03a. No mitigation needed; recorded for completeness. |
</threat_model>

<verification>
End-of-plan checks (run after both tasks):
1. `grep -c 'f64 by default and for oracle gating' CLAUDE.md` equals `1`; `grep -c 'f64 only' CLAUDE.md` equals `0`.
2. The 5 obsolete tools are deleted; `maple_to_kernels.py` no longer references them and still parses.
3. `bash tools/test_idempotency.sh` exits `0` (end-to-end idempotency).
4. All five audit gates exit `0` in a single chained sweep.
5. ROADMAP Phase 11 criteria #1/#4 corrected; all six plans marked `- [x]`.
6. `11-FINAL-METRICS.md` exists with the post-phase snapshot (oversized=0, max ≤5000) vs baseline.
7. `11-VALIDATION.md` `nyquist_compliant: true`, no placeholder rows.
8. No `.cargo/config.toml` modification; no cargo jobs/env override.
</verification>

<success_criteria>
- `CLAUDE.md` reflects the D-03a precision + operation-order policy shift.
- The 7 obsolete numbered-subcrate-layout splitter tools are deleted; `maple_to_kernels.py` is a splitter-v2-only driver.
- End-to-end idempotency (P11-INV-6) passes across all three families.
- All five Phase 11 audit gates pass simultaneously.
- ROADMAP Phase 11 success criteria #1 and #4 corrected; `11-FINAL-METRICS.md` records the post-phase snapshot.
- No `.cargo/config.toml` touched; no cargo jobs/env override.
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md` covering:
- CLAUDE.md edit confirmation (the exact before/after of both constraint lines).
- Tool-deletion log: which tools deleted, which kept and why (especially regen_phase09.py / translate_lda.py / split_lda_subcrates.py / resplit_gga.py decisions).
- `maple_to_kernels.py` cleanup: what was removed; how test_idempotency.sh's invocation was kept working.
- End-to-end idempotency result; the 5-audit sweep result (all five exit codes).
- ROADMAP corrections made; 11-FINAL-METRICS.md before/after table.
- Phase 11 close-out: confirmation all P11-INV-* invariants hold (A1 from Wave 0, 1/2/5/6 from this plan, 4 from 11-04/11-05, the dispatch criterion from 11-03).
- Any deviations.
</output>
