<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
<!-- D-10/D-10a/D-10b architecture: phase gate against the unified-emission post-state. -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - CLAUDE.md
  - tools/split_oversized_kernel.py     # DELETE (obsolete post-D-10 — splitter emits ≤5K directly)
  - tools/split_oversized_mgga.py       # DELETE (obsolete + has known rmtree bug)
  - tools/split_mgga_7_kcis.py          # DELETE (one-off, subsumed by D-02 chunker)
  - tools/rebatch_mgga.py               # DELETE (numbered-subcrate bin-packer; obsolete under D-10)
  - tools/shrink_part_fanout.py         # DELETE (already integrated into translators per 260512-q01)
  - tools/regen_phase09.py              # DELETE (filesystem-discovery via numbered-subcrate scan; replaced by tools/regen_unified.py per D-10)
  - tools/translate_lda.py              # DELETE (legacy translator; replaced by translate_lda_v2.py)
  - tools/maple_to_kernels.py           # update stale defaults per RESEARCH.md Q5 + rewire to regen_unified.py
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md
  - .planning/ROADMAP.md
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
  - .planning/STATE.md
autonomous: false   # cargo build under D-07 inline
requirements:
  - P11-INV-1
  - P11-INV-2
  - P11-INV-3
  - P11-INV-4
  - P11-INV-5
  - P11-INV-6

must_haves:
  truths:
    - "CLAUDE.md § Constraints updated per D-03a: line 13 'Precision: f64 only; energy relative error <= 10^-12 vs libxc oracle' becomes the new generic-over-<F: Float> wording; line 16 'Operation order: ...bit-level equivalence' becomes the chunk-internal preservation + 1e-12 gate wording; line 17 GPU precision becomes the launch-time-opt-in wording."
    - "Obsolete splitter scaffolding deleted: tools/{split_oversized_kernel, split_oversized_mgga, split_mgga_7_kcis, rebatch_mgga, shrink_part_fanout, regen_phase09, translate_lda}.py — 7 obsolete tools removed (D-10 subsumes the splitters; the new regen_unified.py replaces regen_phase09.py; translate_lda.py is the v1 successor of translate_lda_v2.py)."
    - "tools/maple_to_kernels.py stale defaults DEFAULT_SPLIT_THRESHOLD=100_000 and DEFAULT_TARGET_MAX=500_000 are removed per RESEARCH.md Q5 — translators own SPLIT_THRESHOLD=5000 (D-09); driver invokes tools/regen_unified.py (was regen_phase09.py)."
    - "tools/test_idempotency.sh exits 0: running the unified-driver pipeline twice produces zero diff under crates/kernels/ (P11-INV-6 / Phase 11 success criterion 6)."
    - "Per D-LOCK-D: pipeline iterated through Wave 0..5 until BOTH invariants (no numbered subcrates AND 5K cap) hold AND cargo build --workspace passes AND D-05 oracle gate passes; idempotency verified."
    - "Final phase-gate sweep: cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke + phase11_worst_case both pass at strict 1e-12 (D-05; success criterion 5)."
    - "All 5 audit gates green simultaneously: tools/audit_subcrate_collapse.sh (P11-INV-1), tools/audit_kernel_size.py --strict (P11-INV-2), cargo build --workspace (P11-INV-3), tools/audit_cube_launch.sh (P11-INV-5), tools/test_idempotency.sh (P11-INV-6)."
    - "11-FINAL-METRICS.md captures the post-phase metrics side-by-side vs 11-BASELINE.md: oversized=0 (was 237), max line ≤5000 (was 16703), numbered subcrates=0 (was 22), launch_unchecked=22 (unchanged), workspace member count post-D-10, peak RSS for cargo build --workspace."
    - "ROADMAP.md Phase 11 entry marked complete: leading checkbox `[ ]` becomes `[x]` and Phase 11 Plans listing has all 6 plans flipped to `[x]`."
    - "VALIDATION.md per-task verification map placeholders ({N}-XX-XX) replaced with concrete task IDs from plans 11-01..06; nyquist_compliant: true set in frontmatter."
    - "STATE.md advanced past Phase 11: next-phase pointer set to /gsd-plan-phase 10 per D-06 ordering."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated precision policy reflecting D-03a + operation-order policy reflecting D-05"
      contains: "f32 is.*launch-time opt-in"
    - path: ".planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md"
      provides: "Post-phase metric snapshot vs 11-BASELINE.md (side-by-side comparison)"
      min_lines: 30
  key_links:
    - from: "tools/test_idempotency.sh"
      to: "crates/kernels/ tree (post-Phase-11)"
      via: "two runs of tools/regen_unified.py --family all; git status diff"
      pattern: "regen_unified.py.*--family all"
    - from: "11-FINAL-METRICS.md"
      to: "11-BASELINE.md"
      via: "side-by-side metric comparison"
      pattern: "237"
    - from: "ROADMAP.md Phase 11 entry"
      to: "this plan's commits"
      via: "checkbox flip + plan list flip"
      pattern: "Phase 11"
---

<objective>
Wave 5 — phase gate. Five operations small enough that combined they fit one ~50% context plan:

1. **Update CLAUDE.md per D-03a** — bring the project Constraints section in line with the generic-over-<F: Float> kernel chunks and the 1e-12 (not bit-exact) verification gate.
2. **Delete obsolete splitter scaffolding** — 7 Python tools subsumed by the D-02 chunker (plans 11-02..05) and the D-10 unified architecture: split_oversized_kernel.py, split_oversized_mgga.py, split_mgga_7_kcis.py, rebatch_mgga.py, shrink_part_fanout.py, regen_phase09.py (replaced by regen_unified.py), translate_lda.py (legacy v1, replaced by translate_lda_v2.py).
3. **Clean up tools/maple_to_kernels.py** — remove stale DEFAULT_SPLIT_THRESHOLD/DEFAULT_TARGET_MAX defaults (RESEARCH.md Q5); rewire to call regen_unified.py instead of the deleted regen_phase09.py.
4. **Run end-to-end idempotency test** — Phase 11 success criterion 6 (tools/test_idempotency.sh).
5. **Final phase gate** — full sweep + 11-FINAL-METRICS.md + ROADMAP.md + VALIDATION.md + STATE.md updates; ROADMAP success criterion 8 reworded for D-10b path shape.

Output: Final phase artifacts; ROADMAP.md flips Phase 11 to complete; project ready for Phase 10 (per D-06 ordering).
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-05-SUMMARY.md
@CLAUDE.md
@.cargo/config.toml
@tools/maple_to_kernels.py
@tools/split_oversized_kernel.py
@tools/split_oversized_mgga.py
@tools/split_mgga_7_kcis.py
@tools/rebatch_mgga.py
@tools/shrink_part_fanout.py
@tools/regen_phase09.py
@tools/translate_lda.py
@tools/test_idempotency.sh
@tools/regen_unified.py

<interfaces>
CLAUDE.md target lines (verified per cat output, lines 11-17):

```
### Constraints

- **Tech stack**: Pure Rust + CubeCL 0.9.0; no C/Fortran in production path
- **Precision**: f64 only; energy relative error <= 10^-12 vs libxc oracle
- **Dependencies**: cubecl 0.9.0, thiserror 2.0, bitflags 2.10, bytemuck 1.25 (production); ...
- **Compatibility**: Must provide extern "C" layer for drop-in replacement in C/Fortran DFT codes
- **Operation order**: Maple2c formula translations must preserve floating-point operation order for bit-level equivalence
- **GPU precision**: No silent f32 fallback; typed error if device lacks f64 support
```

Notes:
- This section may be wrapped in GSD `<!-- GSD:project-start --> ... <!-- GSD:project-end -->` markers sourced from .planning/PROJECT.md. The CLAUDE.md edit MUST propagate to PROJECT.md if PROJECT.md is the source of truth.
- cubecl version "0.9.0" mentions on lines 13/27 are stale (Cargo.toml has 0.10.0). Drive-by fix recommended.

Tools to delete (verified):
- tools/split_oversized_kernel.py (obsolete under D-10)
- tools/split_oversized_mgga.py (obsolete + has known rmtree bug per quick task 260510-q01)
- tools/split_mgga_7_kcis.py (one-off; subsumed by D-02 chunker)
- tools/rebatch_mgga.py (numbered-subcrate bin-packer; obsolete under D-10)
- tools/shrink_part_fanout.py (already integrated into translators per 260512-q01)
- tools/regen_phase09.py (filesystem-discovery via numbered subcrate scan — broken under D-10a; replaced by regen_unified.py from plan 11-02)
- tools/translate_lda.py (legacy v1 translator; verified unused by `grep -rE translate_lda.py tools/ src/ verify/ | grep -v translate_lda_v2`)

tools/maple_to_kernels.py landmarks (per RESEARCH.md):
- Lines 89-90: DEFAULT_SPLIT_THRESHOLD = 100_000, DEFAULT_TARGET_MAX = 500_000 — REMOVE.
- Line 104: cmd = [sys.executable, str(TOOLS / "regen_phase09.py"), "--family", family] — UPDATE to regen_unified.py.

ROADMAP.md targets (verified):
- Line 22: `- [ ] **Phase 11: Splitter v2 — Unified Kernels with 5K Line Cap** *(INSERTED)* - ...` → `[x]`.
- Lines 264-270: Plans listing — flip 6 `[ ]` to `[x]`.
- Line 260 (success criterion 8) — currently reads about `crate::kernel::{family}::batchN::...` references; under D-10b paths drop the batchN segment. Reword to: "every `crate::kernel::{family}::<func>::...` reference (no batchN segment under D-10b) resolves against the unified façade."
- Progress table (around line 159): add or update Phase 11 row to Complete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md per D-03a + delete 7 obsolete tools + clean up maple_to_kernels.py</name>
  <files>CLAUDE.md (or .planning/PROJECT.md if it is the GSD source), tools/maple_to_kernels.py, 7 tool files deleted</files>
  <read_first>
    - CLAUDE.md (lines 1-30 — verify exact wording; identify GSD-block markers)
    - .planning/PROJECT.md (verify whether it is source of truth: `grep -l "Maple2c formula translations" .planning/PROJECT.md`)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-03, D-03a, D-05 — exact policy text)
    - tools/maple_to_kernels.py (lines 80-110)
    - Each of the 7 tools to delete (header — confirm role; verify no other consumer): tools/{split_oversized_kernel, split_oversized_mgga, split_mgga_7_kcis, rebatch_mgga, shrink_part_fanout, regen_phase09, translate_lda}.py
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "State of the Art" deprecated list + Q5
  </read_first>
  <action>
    Three independent edits in one commit.

    --- Edit 1: CLAUDE.md (or PROJECT.md) — D-03a + D-05 ---

    Determine authoritative file:
    - If .planning/PROJECT.md exists AND CLAUDE.md Constraints block sits inside `<!-- GSD:project-start source:PROJECT.md -->` markers, edit PROJECT.md.
    - Otherwise edit CLAUDE.md directly.

    Three line replacements (W10 — negative checks first to ensure stale text is REMOVED before adding new):

    Line 13 (Precision, D-03a). Replace:
    ```
    - **Precision**: f64 only; energy relative error <= 10^-12 vs libxc oracle
    ```
    With:
    ```
    - **Precision**: f64 by default and for oracle gating (energy relative error <= 10^-12 vs libxc oracle); kernel chunks are generic over `<F: Float>` per Phase 11 D-10/D-02; f32 is a launch-time opt-in for performance with no correctness guarantee
    ```

    Line 16 (Operation order, D-05). Replace:
    ```
    - **Operation order**: Maple2c formula translations must preserve floating-point operation order for bit-level equivalence
    ```
    With:
    ```
    - **Operation order**: Maple2c formula translations preserve floating-point operation order at the chunk-internal level; CSE-aware chunk boundaries (Phase 11 D-01/D-02) introduce named temporaries that may reorder cross-chunk accumulation. Verification gate is 1e-12 relative error on energy + routed derivatives at f64 (per Phase 11 D-05), not bit-exact.
    ```

    Line 17 (GPU precision, D-03a). Replace:
    ```
    - **GPU precision**: No silent f32 fallback; typed error if device lacks f64 support
    ```
    With:
    ```
    - **GPU precision**: No silent f32 fallback at launch — f32 is an explicit launch-time opt-in for performance with no correctness guarantee. Typed error if the user selects f64 and the device lacks f64 support.
    ```

    Drive-by fix (do unless trivial; document if skipped): cubecl version "0.9.0" mentions on lines 13/27 are stale per Cargo.toml line 9 (`cubecl = "0.10.0"`). Update to "0.10.0".

    If editing PROJECT.md, re-run any GSD sync command (check for `gsd-sdk run` or Makefile target). If no sync exists, edit CLAUDE.md by hand to match (commit BOTH files).

    --- Edit 2: Delete 7 obsolete tools ---

    Pre-deletion safety check:

    ```bash
    grep -rE "(split_oversized_kernel|split_oversized_mgga|split_mgga_7_kcis|rebatch_mgga|shrink_part_fanout|regen_phase09|translate_lda)\.py" tools/ src/ verify/ xtask/ scripts/ Makefile* 2>/dev/null | grep -v "\.planning/" | grep -v "translate_lda_v2"
    ```

    Output MUST be empty (besides the deleted files themselves and the maple_to_kernels.py call to regen_phase09.py — handled in Edit 3 below). If anything else references these scripts, STOP and either update the referencing site (preferred) or keep the script as a deprecated stub.

    Note on translate_lda.py: verify via `grep -rE "translate_lda\.py" tools/ src/ verify/ xtask/ | grep -v "translate_lda_v2"` is empty before deletion.

    Delete:

    ```bash
    git rm tools/split_oversized_kernel.py
    git rm tools/split_oversized_mgga.py
    git rm tools/split_mgga_7_kcis.py
    git rm tools/rebatch_mgga.py
    git rm tools/shrink_part_fanout.py
    git rm tools/regen_phase09.py
    git rm tools/translate_lda.py
    ```

    --- Edit 3: tools/maple_to_kernels.py — Q5 cleanup + regen_unified.py wiring ---

    Three changes:

    1. Remove stale defaults at lines 89-90:
       - DEFAULT_SPLIT_THRESHOLD = 100_000 → DELETE
       - DEFAULT_TARGET_MAX = 500_000 → DELETE
       Also remove every reference to these constants downstream (likely argparse defaults + a "warning: divergence from translator constant" log line).

    2. Replace the regen_phase09.py invocation at line 104:
       Before: `cmd = [sys.executable, str(TOOLS / "regen_phase09.py"), "--family", family]`
       After:  `cmd = [sys.executable, str(TOOLS / "regen_unified.py"), "--family", family]`

    3. If argparse exposes `--split-threshold`, either remove the flag entirely (translators own the constant per D-09) or leave it with a comment noting it has no effect.

    Re-test driver:

    ```bash
    python3 tools/maple_to_kernels.py --help
    python3 tools/maple_to_kernels.py all --family lda
    git status --porcelain crates/kernels/lda
    ```

    Both python commands must exit 0; the `git status` must be empty (idempotent on the post-Phase-11 tree).

    Path-scoped commit:

    ```bash
    git commit --only -m "docs(phase-11): update CLAUDE.md per D-03a/D-05; remove 7 obsolete splitter tools; rewire maple_to_kernels.py to regen_unified.py" -- \
      CLAUDE.md \
      .planning/PROJECT.md \
      tools/maple_to_kernels.py \
      tools/split_oversized_kernel.py tools/split_oversized_mgga.py \
      tools/split_mgga_7_kcis.py tools/rebatch_mgga.py tools/shrink_part_fanout.py \
      tools/regen_phase09.py tools/translate_lda.py
    ```

    (.planning/PROJECT.md only included if the edit was made there; otherwise omit.)
  </action>
  <verify>
    <automated>test "$(grep -c 'f64 only;' CLAUDE.md)" -eq 0 &amp;&amp; grep -q 'f32 is.*launch-time opt-in' CLAUDE.md &amp;&amp; ! ls tools/split_oversized_kernel.py tools/split_oversized_mgga.py tools/split_mgga_7_kcis.py tools/rebatch_mgga.py tools/shrink_part_fanout.py tools/regen_phase09.py tools/translate_lda.py 2&gt;/dev/null &amp;&amp; ! grep -E 'DEFAULT_SPLIT_THRESHOLD = 100_000|DEFAULT_TARGET_MAX = 500_000' tools/maple_to_kernels.py &amp;&amp; grep -q 'regen_unified' tools/maple_to_kernels.py &amp;&amp; python3 tools/maple_to_kernels.py --help</automated>
  </verify>
  <acceptance_criteria>
    - **W10 — negative checks first (order matters)**: `grep -c 'f64 only;' CLAUDE.md` equals `0` (the old language MUST BE REMOVED — checked first because adding the new sentence without removing the old would silently pass the positive check below).
    - `grep -c 'f32 is.*launch-time opt-in' CLAUDE.md` is at least `1` (D-03a wording present).
    - `grep -c 'CSE-aware chunk boundaries' CLAUDE.md` is at least `1` (D-05 wording present).
    - **W10 — combined back-stop**: `test "$(grep -c 'f64 only;' CLAUDE.md)" -eq 0 && test "$(grep -c 'f32 is.*launch-time opt-in' CLAUDE.md)" -ge 1` exits `0`.
    - All 7 deleted tools absent: `! test -e tools/split_oversized_kernel.py && ! test -e tools/split_oversized_mgga.py && ! test -e tools/split_mgga_7_kcis.py && ! test -e tools/rebatch_mgga.py && ! test -e tools/shrink_part_fanout.py && ! test -e tools/regen_phase09.py && ! test -e tools/translate_lda.py` exits `0`.
    - `grep -cE 'DEFAULT_SPLIT_THRESHOLD = 100_000|DEFAULT_TARGET_MAX = 500_000' tools/maple_to_kernels.py` equals `0`.
    - `grep -c 'regen_unified' tools/maple_to_kernels.py` is at least `1` (driver rewired).
    - `grep -c 'regen_phase09' tools/maple_to_kernels.py` equals `0` (old reference removed).
    - `python3 tools/maple_to_kernels.py --help` exits `0`.
    - `python3 tools/maple_to_kernels.py all --family lda` exits `0` AND produces ZERO `git status --porcelain crates/kernels/lda` diff (driver still functional + idempotent on post-Phase-11 tree).
    - No other source file references the deleted tools: `grep -rE '(split_oversized_kernel|split_oversized_mgga|split_mgga_7_kcis|rebatch_mgga|shrink_part_fanout|regen_phase09)\.py' tools/ src/ verify/ xtask/ scripts/ Makefile* 2>/dev/null | grep -v 'Binary' | grep -v '\.planning/'` returns empty.
    - For translate_lda.py specifically: `grep -rE 'translate_lda\.py' tools/ src/ verify/ xtask/ 2>/dev/null | grep -v 'translate_lda_v2' | grep -v '\.planning/'` returns empty.
    - No `.cargo/config.toml` change: `git diff --name-only HEAD~1 HEAD .cargo/config.toml` returns empty.
  </acceptance_criteria>
  <done>CLAUDE.md reflects the post-Phase-11 precision and operation-order policy; 7 obsolete splitter tools are gone; the unified driver is rewired to call regen_unified.py and no longer carries stale defaults that lie about the splitter constant.</done>
</task>

<task type="auto">
  <name>Task 2: Run end-to-end idempotency test; capture 11-FINAL-METRICS.md; close the phase (ROADMAP + VALIDATION + STATE)</name>
  <files>.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md, .planning/ROADMAP.md, .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md, .planning/STATE.md</files>
  <read_first>
    - tools/test_idempotency.sh (built in plan 11-01; verify it now invokes regen_unified.py since regen_phase09.py is gone — if not, update it as part of this task)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md (the baseline to compare against)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md (the per-task map to populate, frontmatter to flip)
    - .planning/ROADMAP.md (Phase 11 entry to flip + Plans listing to flip + criterion 8 to reword for D-10b + Progress table)
    - .planning/STATE.md (Current Position, progress counters)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-{01..05}-SUMMARY.md (per-plan summaries)
    - .cargo/config.toml (build envelope — cite, do NOT duplicate)
  </read_first>
  <action>
    Six sequential operations.

    --- Operation 1: end-to-end idempotency (P11-INV-6 / success criterion 6) ---

    First, verify tools/test_idempotency.sh references the post-Phase-11 driver. The original (per plan 11-01) invokes `python3 tools/maple_to_kernels.py all --family all`. With Task 1 having rewired maple_to_kernels.py to call regen_unified.py, the test should still work — but verify by reading the script. If it directly references regen_phase09.py (which now doesn't exist), update it to use regen_unified.py.

    Run:

    ```bash
    bash tools/test_idempotency.sh
    ```

    MUST exit 0. The script:
    1. Confirms crates/kernels/ is clean.
    2. Runs `python3 tools/maple_to_kernels.py all --family all` (which now invokes regen_unified.py).
    3. Snapshots `git status --porcelain crates/kernels`.
    4. Runs the regen again.
    5. Snapshots again.
    6. Diffs the two snapshots → must be empty.

    If non-zero, identify the first non-deterministic file via the `git status --porcelain` output and trace to the responsible component (cse.py / emit.py / regen_unified.py / a translator). Fix the determinism bug, recommit the relevant tool, re-run.

    --- Operation 2: full final phase-gate sweep ---

    All gates simultaneously:

    ```bash
    bash tools/audit_subcrate_collapse.sh                 # P11-INV-1, success criterion 1
    python3 tools/audit_kernel_size.py --strict           # P11-INV-2, success criterion 2
    cargo build --workspace                                # P11-INV-3, success criterion 4
    bash tools/audit_cube_launch.sh                        # P11-INV-5, success criterion 7
    bash tools/audit_dispatch_tree.sh                      # success criterion 8 (D-10b reworded — see Operation 4)
    cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture  # P11-INV-4 + D-05, criterion 5
    bash tools/test_idempotency.sh                         # P11-INV-6, criterion 6
    ```

    Every command MUST exit 0. If any fail, the phase is not complete — STOP and return CHECKPOINT REACHED with the failing command and its output.

    --- Operation 3: 11-FINAL-METRICS.md ---

    Write `.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md` mirroring 11-BASELINE.md structure with side-by-side comparison:

    ```markdown
    # Phase 11 — Final Metrics Snapshot

    **Captured:** <YYYY-MM-DD> at phase gate.
    **Baseline reference:** .planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md
    **Build envelope:** see .cargo/config.toml (D-08, D-09 invariants — values NOT duplicated).

    | Metric | Pre-Phase-11 (baseline) | Post-Phase-11 (now) | Δ | Success criterion |
    |--------|------------------------:|--------------------:|----:|-------------------|
    | Oversized kernel files (>5K lines) | 237 | 0 | -237 | SC2 (P11-INV-2) |
    | Maximum file line count | 16,703 | <paste actual> | -<paste> | SC2 |
    | Numbered subcrates | 22 | 0 | -22 | SC1 (P11-INV-1) |
    | #[cube(launch_unchecked)] count | 22 | <paste actual> | <paste Δ> | SC7 (P11-INV-5; must be ≤22) |
    | Workspace member count | <paste pre> | <paste post> | <paste Δ> | — |
    | cargo build --workspace peak RSS | <paste pre> | <paste post> | <paste Δ> | RAM-survival (D-07) |

    ## Detailed verification

    | Gate | Command | Result |
    |------|---------|--------|
    | P11-INV-1 | `bash tools/audit_subcrate_collapse.sh` | <PASS/FAIL + exit code> |
    | P11-INV-2 | `python3 tools/audit_kernel_size.py --strict` | <PASS/FAIL + exit code> |
    | P11-INV-3 | `cargo build --workspace` | <PASS/FAIL + exit code> |
    | P11-INV-4 (smoke) | `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke` | <PASS/FAIL + max rel err> |
    | P11-INV-4 (worst-case) | `cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case` | <PASS/FAIL + max rel err> |
    | P11-INV-5 | `bash tools/audit_cube_launch.sh` | <PASS/FAIL + count> |
    | P11-INV-6 | `bash tools/test_idempotency.sh` | <PASS/FAIL + diff bytes> |
    | SC8 (D-10b) | `bash tools/audit_dispatch_tree.sh` | <PASS/FAIL + unresolved count> |

    ## Success criteria sign-off

    - [x] SC0: P11-INV-A1 (D-02 ABI spike) green (plan 11-01)
    - [x] SC1: no numbered children
    - [x] SC2: no files >5000 lines
    - [x] SC3: splitter capable of subdividing single output expressions (cse.py + emit.py + worst-case offenders all ≤5K)
    - [x] SC4: cargo build --workspace succeeds inline
    - [x] SC5: oracle parity preserved at 1e-12 (D-05)
    - [x] SC6: pipeline idempotent
    - [x] SC7: #[cube(launch_unchecked)] count not increased from baseline 22
    - [x] SC8 (reworded for D-10b): every `crate::kernel::{family}::<func>::...` reference resolves against the unified façade
    ```

    Replace every `<paste ...>` placeholder with literal output from the gates. If any gate FAILED, change the box to `[ ]`, write the failure mode, and STOP THE PLAN.

    --- Operation 4: ROADMAP.md flip + criterion 8 reword ---

    Edit `.planning/ROADMAP.md`:

    1. Find line 22: `- [ ] **Phase 11: Splitter v2 — Unified Kernels with 5K Line Cap** *(INSERTED)* - ...` → `[x]`.

    2. Reword success criterion 8 (line 260) per D-10b:
       Before:
       ```
       8. Dispatch tree (`src/eval/{gga,mgga}_dispatch/batch*.rs`) resolves cleanly post-phase: every `crate::kernel::{family}::batchN::...` reference resolves against the post-collapse façade. ...
       ```
       After:
       ```
       8. Dispatch tree (`src/eval/{gga,mgga}_dispatch/mod.rs`) resolves cleanly post-phase: every `crate::kernel::{family}::<func>::...` reference (no `batchN::` segment under D-10b) resolves against the unified façade's `pub mod <func>;` listing. (Pre-Phase-11 the dispatch tree referenced stale batch IDs that the façade did not expose; Phase 11 plan 11-02 task 4 closed B1 by regenerating dispatch against the per-functional unified shape introduced by D-10b.)
       ```

    3. In the Plans listing (lines 264-270), flip 6 `[ ]` to `[x]` and update the descriptions to match the new architecture:
       ```
       Plans:
       - [x] 11-01-PLAN.md — Wave 0: D-02 ABI spike + audit tools + parity_phase11 + baseline snapshot + dispatch audit
       - [x] 11-02-PLAN.md — Wave 1: clean-slate (D-10a) — delete 22 numbered subcrates + strip root Cargo.toml + reset family façades + build splitter v2 emit infra (cse.py/emit.py/regen_unified.py) + modify 3 family translators + regen dispatch against empty unified façade
       - [x] 11-03-PLAN.md — Wave 2: populate unified LDA façade (~41 functionals) at SPLIT_THRESHOLD=5000; verify smoke parity at 1e-12
       - [x] 11-04-PLAN.md — Wave 3: populate unified GGA façade (~131 functionals; closes gga_c_acgga 11K + gga_c_ft97 10K); verify smoke parity
       - [x] 11-05-PLAN.md — Wave 4: populate unified MGGA façade (~92 functionals; closes 207-file blast radius including mgga_c_b94 16K + mgga_c_kcisk 16K); verify smoke + worst-case parity
       - [x] 11-06-PLAN.md — Wave 5: CLAUDE.md per D-03a + delete 7 obsolete tools + maple_to_kernels.py cleanup + idempotency + phase gate (FINAL-METRICS, ROADMAP/STATE/VALIDATION updates)
       ```

    4. In the Progress table (around line 159), add or update the Phase 11 row:
       ```
       | 11. Splitter v2 — Unified Kernels with 5K Line Cap | 6/6 | Complete | <YYYY-MM-DD> |
       ```

    --- Operation 5: VALIDATION.md per-task map population + nyquist flip ---

    Edit `.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md`:

    1. Replace placeholder `{N}-XX-XX` rows in the Per-Task Verification Map with concrete task IDs:
       - 11-01-01 (spike), 11-01-02 (audit tools), 11-01-03 (parity_phase11 + baseline), 11-01-04 (dispatch audit)
       - 11-02-01 (clean-slate delete + façade reset), 11-02-02 (cse.py + emit.py + regen_unified.py), 11-02-03 (translator wiring), 11-02-04 (dispatch regen)
       - 11-03-01 (LDA populate)
       - 11-04-01 (GGA populate)
       - 11-05-01 (MGGA populate + worst-case parity)
       - 11-06-01 (CLAUDE.md + obsolete-tool deletion), 11-06-02 (idempotency + phase gate, this task)

       Map every task to its primary P11-INV-* requirement and the automated command from its `<verify><automated>` block.

    2. In the frontmatter, set `nyquist_compliant: true` and verify `wave_0_complete: true` (should already be flipped since plan 11-01 landed).

    --- Operation 6: STATE.md update ---

    Edit `.planning/STATE.md` to advance the project position past Phase 11:

    - status: "Phase 11 complete (Splitter v2 — D-10 unified emission + 5K cap). Ready to plan Phase 10 (workspace modular split — sequenced after Phase 11 per D-06)."
    - stopped_at: "Phase 11 complete (<YYYY-MM-DD>); next: /gsd-plan-phase 10"
    - last_updated: today's ISO timestamp.
    - last_activity: one-line summary of Phase 11 outcomes.
    - progress.completed_phases: increment by 1; progress.total_plans: += 6; progress.completed_plans: += 6; recompute progress.percent.
    - Add a new "Recent decisions affecting current work" entry: "[Phase 11]: D-10 splitter emits directly into unified per-family layout (no numbered subcrates ever); D-10a clean-slate (22 numbered subcrates deleted upfront); D-10b dispatch paths drop batchN segment; kernel chunks generic over <F: Float> (D-03); chunked-emission ABI uses tuple returns (D-02); 5K hard cap per file (D-LOCK-B); 1e-12 verify gate at f64 (D-05)."

    --- Final commit ---

    Path-scoped:

    ```bash
    git commit --only -m "docs(phase-11): close phase — final metrics, ROADMAP flip (criterion 8 reworded for D-10b), VALIDATION populated, STATE advanced" -- \
      .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md \
      .planning/ROADMAP.md \
      .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md \
      .planning/STATE.md \
      tools/test_idempotency.sh
    ```

    (test_idempotency.sh only included if it was updated to reference regen_unified.py.)
  </action>
  <verify>
    <automated>bash tools/test_idempotency.sh &amp;&amp; bash tools/audit_subcrate_collapse.sh &amp;&amp; python3 tools/audit_kernel_size.py --strict &amp;&amp; bash tools/audit_cube_launch.sh &amp;&amp; bash tools/audit_dispatch_tree.sh &amp;&amp; cargo build --workspace &amp;&amp; cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture &amp;&amp; grep -q '\[x\] \*\*Phase 11' .planning/ROADMAP.md &amp;&amp; grep -q 'nyquist_compliant: true' .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/test_idempotency.sh` exits `0` (P11-INV-6, success criterion 6).
    - `bash tools/audit_subcrate_collapse.sh` exits `0` (P11-INV-1, success criterion 1).
    - `python3 tools/audit_kernel_size.py --strict` exits `0` (P11-INV-2, success criterion 2).
    - `cargo build --workspace` exits `0` (P11-INV-3, success criterion 4).
    - `bash tools/audit_cube_launch.sh` exits `0` (P11-INV-5, success criterion 7).
    - `bash tools/audit_dispatch_tree.sh` exits `0` (success criterion 8 under D-10b reword).
    - `cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture` exits `0` (P11-INV-4 / D-05, success criterion 5).
    - `.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md` exists, ≥30 lines, contains the literal `237` (the pre-Phase-11 oversized count) AND `0` in adjacent cells (the post-Phase-11 count).
    - ROADMAP.md flip: `grep -c '\[x\] \*\*Phase 11' .planning/ROADMAP.md` is at least `1`.
    - ROADMAP.md plan listing: 6 `[x]` lines for `11-0[1-6]-PLAN.md`: `grep -cE '\[x\] 11-0[1-6]-PLAN.md' .planning/ROADMAP.md` equals `6`.
    - ROADMAP.md criterion 8 reworded: `grep -c 'no batchN.* segment under D-10b' .planning/ROADMAP.md` is at least `1` AND `grep -c 'crate::kernel::{family}::batchN::' .planning/ROADMAP.md` equals `0` (the old wording with batchN in the success criterion is gone).
    - ROADMAP.md Progress table contains the row: `grep -cE 'Splitter v2.*6/6.*Complete' .planning/ROADMAP.md` is at least `1`.
    - VALIDATION.md placeholders gone: `grep -c '{N}-XX-XX' .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md` equals `0`.
    - VALIDATION.md cites all 6 plans: every `11-0[1-6]` plan number appears at least once.
    - VALIDATION.md `grep -c 'nyquist_compliant: true' .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md` is at least `1`.
    - STATE.md advances: `grep -c 'Phase 11 complete' .planning/STATE.md` is at least `1`; `progress.completed_phases` increments.
    - No `.cargo/config.toml` change in this commit: `git diff --name-only HEAD~1 HEAD .cargo/config.toml` returns empty.
  </acceptance_criteria>
  <done>Phase 11 fully closed: every invariant verified, every success criterion met (8 criteria including D-10b-reworded SC8), every ROADMAP/STATE/VALIDATION artifact updated. Project ready for `/gsd-plan-phase 10` (workspace modular split, next phase per D-06 ordering).</done>
</task>

</tasks>

<verification>
End-of-plan verification (the phase gate itself):

1. P11-INV-6: `bash tools/test_idempotency.sh` exits `0`.
2. P11-INV-1: `bash tools/audit_subcrate_collapse.sh` exits `0`.
3. P11-INV-2: `python3 tools/audit_kernel_size.py --strict` exits `0`.
4. P11-INV-3: `cargo build --workspace` exits `0`.
5. P11-INV-5: `bash tools/audit_cube_launch.sh` exits `0`.
6. P11-INV-4 + D-05: `cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture` exits `0`.
7. SC8 (D-10b reword): `bash tools/audit_dispatch_tree.sh` exits `0`.
8. CLAUDE.md updated per D-03a + D-05.
9. 7 obsolete tools deleted.
10. tools/maple_to_kernels.py rewired to regen_unified.py + stale defaults removed.
11. 11-FINAL-METRICS.md captures side-by-side comparison.
12. ROADMAP.md Phase 11 marked complete + criterion 8 reworded.
13. STATE.md advanced past Phase 11.

If any of 1-7 fail, STOP and return CHECKPOINT REACHED — Phase 11 is not complete.
</verification>

<success_criteria>
**Phase 11 success criteria 0-8 from ROADMAP.md, all sign-off here:**

- [x] SC0 — P11-INV-A1 D-02 ABI spike green (plan 11-01)
- [x] SC1 — no numbered children (P11-INV-1)
- [x] SC2 — no files >5000 lines (P11-INV-2)
- [x] SC3 — splitter capable of subdividing single output expressions
- [x] SC4 — cargo build --workspace succeeds inline (P11-INV-3)
- [x] SC5 — oracle parity preserved at 1e-12 (P11-INV-4 / D-05)
- [x] SC6 — pipeline idempotent (P11-INV-6)
- [x] SC7 — `#[cube(launch_unchecked)]` count unchanged from baseline 22 (P11-INV-5)
- [x] SC8 (reworded for D-10b) — dispatch references resolve against unified façade

Plus:
- D-03a CLAUDE.md update landed.
- 7 obsolete tools deleted.
- Driver stale defaults cleaned up + rewired to regen_unified.py (RESEARCH Q5).
- All 6 plan SUMMARYs exist; all VALIDATION map rows populated; ROADMAP / STATE updated.
- No `.cargo/config.toml` modification across the entire phase (D-07/D-08/D-09 invariants).
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md` covering:
- CLAUDE.md edit summary (which file was authoritative; PROJECT.md sync if used).
- The 7 deleted tools (with confirmation no other code referenced them).
- maple_to_kernels.py cleanup diff (which lines went, what stayed; regen_phase09 → regen_unified rewire).
- Idempotency test result.
- Final phase-gate sweep results (each command's exit code + relevant output).
- 11-FINAL-METRICS.md summary (one-line side-by-side: 237→0, 16703→<actual>, 22→0, 22→22).
- ROADMAP / VALIDATION / STATE update summary (criterion 8 reword to D-10b shape highlighted).
- Pointer to next phase: `/gsd-plan-phase 10` per D-06 ordering.
</output>
