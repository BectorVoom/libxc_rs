<!-- D-07/D-08/D-09 invariant: do NOT change .cargo/config.toml. -->
<!-- Source of truth for build env: .cargo/config.toml (jobs=1, target-dir=.cache/cargo-target, RUST_MIN_STACK=67108864). -->
---
phase: 11-splitter-v2-unified-5k-cap
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - CLAUDE.md
  - tools/split_oversized_kernel.py    # DELETE (obsolete post-collapse)
  - tools/split_oversized_mgga.py      # DELETE (obsolete + has known rmtree bug)
  - tools/split_mgga_7_kcis.py         # DELETE (one-off, subsumed by D-02 chunker)
  - tools/rebatch_mgga.py              # DELETE (irrelevant post-collapse)
  - tools/shrink_part_fanout.py        # DELETE (already integrated into translators per 260512-q01)
  - tools/maple_to_kernels.py          # update stale defaults per RESEARCH Q5
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md
autonomous: false
requirements:
  - P11-INV-1
  - P11-INV-2
  - P11-INV-3
  - P11-INV-4
  - P11-INV-5
  - P11-INV-6

must_haves:
  truths:
    - "CLAUDE.md § Constraints is updated per D-03a: line 13 'Precision: f64 only; energy relative error <= 10^-12 vs libxc oracle' becomes 'Precision: f64 by default and for oracle gating (energy relative error <= 10^-12 vs libxc oracle); kernel chunks are generic over <F: Float> per Phase 11; f32 is launch-time opt-in with no correctness gate' AND line 17 'GPU precision: No silent f32 fallback; typed error if device lacks f64 support' is updated to read 'GPU precision: No silent f32 fallback at launch — f32 is an explicit launch-time opt-in for performance with no correctness guarantee; typed error if user selects f64 and device lacks f64 support'."
    - "CLAUDE.md § Constraints line 16 'Operation order: Maple2c formula translations must preserve floating-point operation order for bit-level equivalence' is updated to acknowledge D-05's relaxation: 'Operation order: Maple2c formula translations preserve floating-point operation order at the chunk-internal level; CSE-aware chunk boundaries (Phase 11) introduce named temporaries that may reorder cross-chunk accumulation. Verification gate is 1e-12 relative error on energy + routed derivatives at f64 (D-05), not bit-exact.'"
    - "tools/split_oversized_kernel.py, tools/split_oversized_mgga.py, tools/split_mgga_7_kcis.py, tools/rebatch_mgga.py, tools/shrink_part_fanout.py are DELETED (their roles are subsumed by the D-02 chunker + subcrate collapse)."
    - "tools/maple_to_kernels.py stale defaults DEFAULT_SPLIT_THRESHOLD=100_000 and DEFAULT_TARGET_MAX=500_000 are removed per RESEARCH.md Q5 — translators now own the constant (5000); driver no longer pretends to."
    - "tools/test_idempotency.sh exits 0: running `python3 tools/maple_to_kernels.py all --family all` (or the per-family loop fallback) twice produces zero diff under crates/kernels/ (P11-INV-6 success criterion 6)."
    - "Per D-LOCK-D: the splitter pipeline has been iterated through Wave 0..5 until BOTH invariants (collapsed subcrates AND 5K cap) hold AND `cargo build --workspace` passes AND the D-05 oracle gate passes; idempotency (re-running the pipeline produces no diff) is verified by tools/test_idempotency.sh."
    - "Final phase-gate sweep passes: cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 (BOTH phase11_smoke AND phase11_worst_case) at strict 1e-12 (D-05; success criterion 5)."
    - "tools/audit_subcrate_collapse.sh exits 0 (P11-INV-1 / success criterion 1)."
    - "tools/audit_kernel_size.py --strict exits 0 (P11-INV-2 / success criterion 2)."
    - "tools/audit_cube_launch.sh exits 0 (P11-INV-5 / success criterion 7 — count not increased from baseline 22)."
    - "cargo build --workspace exits 0 inline under .cargo/config.toml jobs=1 (P11-INV-3 / success criterion 4)."
    - "11-FINAL-METRICS.md captures the post-phase metrics: oversized=0 (was 237), max line count ≤5000 (was 16703), numbered subcrates=0 (was 22), launch_unchecked=22 (unchanged), workspace member count post-collapse, peak RSS for cargo build --workspace."
    - "ROADMAP.md Phase 11 entry is marked complete: leading checkbox `[ ]` becomes `[x]` and the phase row in the Progress table moves to 'Complete'."
    - "VALIDATION.md per-task verification map has all placeholder {N}-XX-XX rows replaced with the concrete task IDs from plans 11-01..06; nyquist_compliant: true is set in frontmatter."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated precision policy reflecting D-03a + operation-order policy reflecting D-05"
      contains: "f32 is launch-time opt-in"
    - path: ".planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md"
      provides: "Post-phase metric snapshot vs 11-BASELINE.md"
      min_lines: 30
  key_links:
    - from: "tools/test_idempotency.sh"
      to: "crates/kernels/ tree (post-Phase-11)"
      via: "two runs of the splitter; git status diff"
      pattern: "git status --porcelain"
    - from: "11-FINAL-METRICS.md"
      to: "11-BASELINE.md"
      via: "side-by-side metric comparison"
      pattern: "237 → 0|16703 → "
    - from: "ROADMAP.md Phase 11 entry"
      to: "this plan's commits"
      via: "checkbox flip + Progress table update"
      pattern: "Phase 11.*Complete"
---

<objective>
Wave 5 — phase gate. This plan does five things, each small enough that combined they fit one ~50% context plan:

1. **Update CLAUDE.md per D-03a** — bring the project Constraints section in line with the new generic-over-`<F: Float>` kernel chunks and the 1e-12 (not bit-exact) verification gate.
2. **Delete obsolete splitter scaffolding** — five Python tools that are subsumed by the D-02 chunker (plans 11-02..04) and subcrate collapse (plan 11-05).
3. **Clean up tools/maple_to_kernels.py** — remove stale `DEFAULT_SPLIT_THRESHOLD=100_000` / `DEFAULT_TARGET_MAX=500_000` defaults (RESEARCH.md Q5).
4. **Run the end-to-end idempotency test** — Phase 11 success criterion 6 (`tools/test_idempotency.sh`).
5. **Final phase gate** — full sweep + 11-FINAL-METRICS.md + ROADMAP.md update + VALIDATION.md per-task map population.

Output: Final phase artifacts; ROADMAP.md flips Phase 11 to complete; the project is ready for Phase 10 (which D-06 sequenced after Phase 11).
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-05-SUMMARY.md
@CLAUDE.md
@.cargo/config.toml
@tools/maple_to_kernels.py
@tools/split_oversized_kernel.py
@tools/split_oversized_mgga.py
@tools/split_mgga_7_kcis.py
@tools/rebatch_mgga.py
@tools/shrink_part_fanout.py
@tools/test_idempotency.sh

<interfaces>
CLAUDE.md target lines (verified verbatim):

Pre-Phase-11 (lines 11-17):
```
### Constraints

- **Tech stack**: Pure Rust + CubeCL 0.9.0; no C/Fortran in production path
- **Precision**: f64 only; energy relative error <= 10^-12 vs libxc oracle
- **Dependencies**: cubecl 0.9.0, thiserror 2.0, bitflags 2.10, bytemuck 1.25 (production); bindgen, anyhow, criterion, rayon (verification/benchmark only)
- **Compatibility**: Must provide extern "C" layer for drop-in replacement in C/Fortran DFT codes
- **Operation order**: Maple2c formula translations must preserve floating-point operation order for bit-level equivalence
- **GPU precision**: No silent f32 fallback; typed error if device lacks f64 support
```

Note 1: this section is wrapped in the GSD `<!-- GSD:project-start --> ... <!-- GSD:project-end -->` block sourced from PROJECT.md. The CLAUDE.md edit must propagate to PROJECT.md if PROJECT.md is the source of truth (verify via `grep -l "Maple2c formula translations" .planning/PROJECT.md`). If PROJECT.md is the source, edit there and let the GSD sync regenerate CLAUDE.md; otherwise edit CLAUDE.md directly.

Note 2: the cubecl version on the line 13 / 27 mentions "0.9.0" — but Cargo.toml shows `cubecl = "0.10.0"`. That's a stale CLAUDE.md note (not a Phase 11 fix), but if the executor is already editing CLAUDE.md, fix this hand-in-hand to avoid drift. Document as a "drive-by".

Tools to delete (per RESEARCH.md "State of the Art" deprecated list):
- tools/split_oversized_kernel.py
- tools/split_oversized_mgga.py
- tools/split_mgga_7_kcis.py
- tools/rebatch_mgga.py
- tools/shrink_part_fanout.py

Verify before delete: each tool's role is documented as "obsolete post-collapse" or "already integrated"; no other tool imports from these.

tools/maple_to_kernels.py landmarks (per RESEARCH.md):
- Lines 89-90: `DEFAULT_SPLIT_THRESHOLD = 100_000` and `DEFAULT_TARGET_MAX = 500_000` — stale (translators own the constant per D-09); REMOVE.
- The rest of the driver is the orchestrator that subagents have been calling — keep functional behavior intact.

ROADMAP.md target (line 22):
```
- [ ] **Phase 11: Splitter v2 — Unified Kernels with 5K Line Cap** *(INSERTED)* - ...
```
Becomes:
```
- [x] **Phase 11: Splitter v2 — Unified Kernels with 5K Line Cap** *(INSERTED)* - ...
```

ROADMAP.md Progress table (around line 159) should add Phase 11 row with `Complete` status.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md per D-03a; delete obsolete splitter scaffolding; clean up maple_to_kernels.py</name>
  <files>CLAUDE.md (or .planning/PROJECT.md if it's the GSD source), tools/maple_to_kernels.py, tools/split_oversized_kernel.py (deleted), tools/split_oversized_mgga.py (deleted), tools/split_mgga_7_kcis.py (deleted), tools/rebatch_mgga.py (deleted), tools/shrink_part_fanout.py (deleted)</files>
  <read_first>
    - CLAUDE.md (lines 1-30 — verify exact wording of the Constraints section; identify the GSD-block markers)
    - .planning/PROJECT.md (verify whether it's the source of truth for the GSD-managed CLAUDE.md block — `grep -l "Maple2c formula translations" .planning/PROJECT.md`)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-CONTEXT.md (D-03, D-03a, D-05 — exact policy text to write)
    - tools/maple_to_kernels.py (lines 80-100 — stale defaults to remove)
    - tools/split_oversized_kernel.py (header — confirm role is "post-emit per-file bin-pack within oversized functional → letter-suffix subcrates" → obsolete post-collapse)
    - tools/split_oversized_mgga.py (header — confirm same role + the rmtree bug noted in 260510-q01)
    - tools/split_mgga_7_kcis.py (header — confirm one-off for kcis)
    - tools/rebatch_mgga.py (header — confirm "first-fit-decreasing bin-pack for MGGA functionals → numbered subcrates" → obsolete post-collapse)
    - tools/shrink_part_fanout.py (header — confirm "already integrated into translators per 260512-q01")
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md "State of the Art" lines 803-808 (deprecated list)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-RESEARCH.md Q5 lines 738-741 (driver defaults cleanup)
    - Use `grep -r 'split_oversized_kernel\\|split_oversized_mgga\\|split_mgga_7_kcis\\|rebatch_mgga\\|shrink_part_fanout' tools/` to confirm no other tool imports from these (they are CLI-only utilities; if no imports, deletion is safe).
  </read_first>
  <action>
    Three independent edits.

    --- Edit 1: CLAUDE.md (or PROJECT.md) — D-03a + D-05 ---

    Determine which file is authoritative:
    - If `.planning/PROJECT.md` exists AND CLAUDE.md's Constraints block sits inside `<!-- GSD:project-start source:PROJECT.md -->` markers, edit `.planning/PROJECT.md`.
    - Otherwise edit CLAUDE.md directly.

    Line 13 — Precision constraint (D-03a):

    Replace:
    ```
    - **Precision**: f64 only; energy relative error <= 10^-12 vs libxc oracle
    ```
    With:
    ```
    - **Precision**: f64 by default and for oracle gating (energy relative error <= 10^-12 vs libxc oracle); kernel chunks are generic over `<F: Float>` per Phase 11; f32 is a launch-time opt-in for performance with no correctness guarantee
    ```

    Line 16 — Operation order constraint (D-05):

    Replace:
    ```
    - **Operation order**: Maple2c formula translations must preserve floating-point operation order for bit-level equivalence
    ```
    With:
    ```
    - **Operation order**: Maple2c formula translations preserve floating-point operation order at the chunk-internal level; CSE-aware chunk boundaries (Phase 11) introduce named temporaries that may reorder cross-chunk accumulation. Verification gate is 1e-12 relative error on energy + routed derivatives at f64 (per Phase 11 D-05), not bit-exact.
    ```

    Line 17 — GPU precision constraint (D-03a):

    Replace:
    ```
    - **GPU precision**: No silent f32 fallback; typed error if device lacks f64 support
    ```
    With:
    ```
    - **GPU precision**: No silent f32 fallback at launch — f32 is an explicit launch-time opt-in for performance with no correctness guarantee. Typed error if the user selects f64 and the device lacks f64 support.
    ```

    Drive-by (do NOT do unless trivial — note in summary if skipped): cubecl version "0.9.0" mentions on lines 13 / 27 are stale (Cargo.toml has 0.10.0). If the lines are simple version numbers, update; if they reference broader stack, leave for a separate quick task.

    If editing PROJECT.md: after the edit, re-run the GSD sync command (likely `gsd-sdk run` or a CLAUDE.md regeneration script — check for a Makefile target or note in CLAUDE.md headers) to propagate to CLAUDE.md. If no sync command is documented, edit CLAUDE.md by hand to match (but ALSO commit PROJECT.md so future syncs preserve the edit).

    --- Edit 2: Delete five obsolete tools ---

    Pre-deletion safety check (run BEFORE removing):

    ```bash
    grep -rE 'import (split_oversized_kernel|split_oversized_mgga|split_mgga_7_kcis|rebatch_mgga|shrink_part_fanout)' tools/ src/ verify/ xtask/
    grep -rE 'tools/(split_oversized_kernel|split_oversized_mgga|split_mgga_7_kcis|rebatch_mgga|shrink_part_fanout)' tools/ src/ verify/ xtask/ scripts/ Makefile* 2>/dev/null
    ```

    Both commands MUST return empty (besides the deleted files themselves). If anything else references these scripts (e.g., a Makefile target, a CI config, the unified driver's command list), STOP and either update the referencing site (preferred) or keep the script as a deprecated stub.

    If safe, delete:
    ```bash
    git rm tools/split_oversized_kernel.py
    git rm tools/split_oversized_mgga.py
    git rm tools/split_mgga_7_kcis.py
    git rm tools/rebatch_mgga.py
    git rm tools/shrink_part_fanout.py
    ```

    --- Edit 3: tools/maple_to_kernels.py — Q5 cleanup ---

    Remove the stale defaults (lines 89-90 per RESEARCH.md):
    - `DEFAULT_SPLIT_THRESHOLD = 100_000` → DELETE.
    - `DEFAULT_TARGET_MAX = 500_000` → DELETE.

    Remove all references to these constants from the rest of the file (probably the argparse defaults and any "warning: divergence from translator constant" log line). Replace argparse `--split-threshold` (if present) with a comment that translators own the constant (per D-09), OR remove the argv entirely if it's never honored (the most truthful change per RESEARCH.md Q5).

    Re-test the driver's primary entry point still works:
    ```bash
    python3 tools/maple_to_kernels.py --help
    python3 tools/maple_to_kernels.py all --family lda  # should regen LDA from current Maple inputs
    ```

    Both commands must exit 0. The second command's output must be IDENTICAL to its output before this edit (unchanged tree → idempotent regen).

    Final commit message: `docs(phase-11): update CLAUDE.md per D-03a/D-05; remove obsolete splitter scaffolding (5 tools); clean up maple_to_kernels.py stale defaults`. Stage all the edits + 5 deletions in one commit.
  </action>
  <verify>
    <automated>test "$(grep -c 'f64 only;' CLAUDE.md)" -eq 0 && grep -c 'f32 is.*launch-time opt-in' CLAUDE.md > /dev/null && ! ls tools/split_oversized_kernel.py tools/split_oversized_mgga.py tools/split_mgga_7_kcis.py tools/rebatch_mgga.py tools/shrink_part_fanout.py 2>/dev/null && ! grep -E 'DEFAULT_SPLIT_THRESHOLD = 100_000|DEFAULT_TARGET_MAX = 500_000' tools/maple_to_kernels.py && python3 tools/maple_to_kernels.py --help</automated>
  </verify>
  <acceptance_criteria>
    - **W10 — negative checks first (order matters for early-exit semantics):** CLAUDE.md `grep -c 'f64 only;' CLAUDE.md` equals `0` (the old language MUST BE REMOVED — checked first because adding the new sentence without removing the old would otherwise silently pass the positive check below).
    - CLAUDE.md (or PROJECT.md if source) `grep -c 'f32 is.*launch-time opt-in' CLAUDE.md` is at least `1` (the new D-03a language is present).
    - CLAUDE.md `grep -c 'CSE-aware chunk boundaries' CLAUDE.md` is at least `1` (D-05 reference; the new operation-order language is present).
    - **W10 — combined single-shot check (back-stop)**: `test "$(grep -c 'f64 only;' CLAUDE.md)" -eq 0 && test "$(grep -c 'f32 is.*launch-time opt-in' CLAUDE.md)" -ge 1` exits `0` (both conditions atomically — neither stale language remains AND new language is present).
    - `! test -e tools/split_oversized_kernel.py` exits `0`.
    - `! test -e tools/split_oversized_mgga.py` exits `0`.
    - `! test -e tools/split_mgga_7_kcis.py` exits `0`.
    - `! test -e tools/rebatch_mgga.py` exits `0`.
    - `! test -e tools/shrink_part_fanout.py` exits `0`.
    - `grep -cE 'DEFAULT_SPLIT_THRESHOLD = 100_000|DEFAULT_TARGET_MAX = 500_000' tools/maple_to_kernels.py` equals `0`.
    - `python3 tools/maple_to_kernels.py --help` exits `0`.
    - `python3 tools/maple_to_kernels.py all --family lda` exits `0` and produces ZERO `git status --porcelain` diff under `crates/kernels/lda` (driver still functional + idempotent on the post-Phase-11 tree).
    - No other source file references the deleted tools: `grep -rE '(split_oversized_kernel|split_oversized_mgga|split_mgga_7_kcis|rebatch_mgga|shrink_part_fanout)\\.py' tools/ src/ verify/ xtask/ scripts/ Makefile* 2>/dev/null | grep -v '^Binary' | grep -v '\\.planning/'` returns empty (or only matches in `.planning/` historical docs which are allowed).
  </acceptance_criteria>
  <done>CLAUDE.md reflects the post-Phase-11 precision and operation-order policy; obsolete splitter scaffolding is gone; the unified driver no longer carries stale defaults that lie about the splitter constant.</done>
</task>

<task type="auto">
  <name>Task 2: Run end-to-end idempotency test; capture 11-FINAL-METRICS.md; close the phase</name>
  <files>.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md, .planning/ROADMAP.md, .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md, .planning/STATE.md</files>
  <read_first>
    - tools/test_idempotency.sh (built in plan 11-01)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md (the baseline to compare against)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md (the per-task map to populate, frontmatter to flip)
    - .planning/ROADMAP.md (Phase 11 entry to flip + Progress table to update)
    - .planning/STATE.md (Current Position, progress counters)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-{01..05}-SUMMARY.md (the per-plan summaries already written by execute-phase)
    - .cargo/config.toml (build envelope — cite, do NOT duplicate)
  </read_first>
  <action>
    Five sequential operations.

    --- Operation 1: end-to-end idempotency (P11-INV-6 / success criterion 6) ---

    Run the idempotency test:

    ```bash
    bash tools/test_idempotency.sh
    ```

    MUST exit 0. The script:
    1. Confirms `crates/kernels/` is clean.
    2. Runs `python3 tools/maple_to_kernels.py all --family all` (or the per-family loop fallback documented in plan 11-01).
    3. Snapshots `git status --porcelain crates/kernels`.
    4. Runs the regen again.
    5. Snapshots again.
    6. Diffs the two snapshots → must be empty.

    If non-zero, identify the first non-deterministic file and trace it back to the responsible component (cse.py / emit.py / collapse_subcrates.py / a translator). Fix the determinism bug, recommit the relevant tool, re-run. The test_idempotency.sh script's output names the offending files via `git status --porcelain`; that's the diagnostic surface.

    --- Operation 2: full final phase-gate sweep ---

    All five gates simultaneously:

    ```bash
    bash tools/audit_subcrate_collapse.sh                 # P11-INV-1, success criterion 1
    python3 tools/audit_kernel_size.py --strict           # P11-INV-2, success criterion 2
    cargo build --workspace                                # P11-INV-3, success criterion 4
    bash tools/audit_cube_launch.sh                        # P11-INV-5, success criterion 7
    cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture  # P11-INV-4 + D-05, success criterion 5
    bash tools/test_idempotency.sh                         # P11-INV-6, success criterion 6
    ```

    Every command MUST exit 0. If any fail, the phase is not yet complete — STOP and return CHECKPOINT REACHED with the failing command and its output.

    --- Operation 3: 11-FINAL-METRICS.md ---

    Write `.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md` mirroring the structure of 11-BASELINE.md, with side-by-side comparison:

    ```markdown
    # Phase 11 — Final Metrics Snapshot

    **Captured:** <YYYY-MM-DD> at phase gate.
    **Baseline reference:** .planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md
    **Build envelope:** see .cargo/config.toml (D-08, D-09 invariants — values NOT duplicated).

    | Metric | Pre-Phase-11 (baseline) | Post-Phase-11 (now) | Δ | Success criterion |
    |--------|------------------------:|--------------------:|----:|-------------------|
    | Oversized kernel files (>5K lines) | 237 | 0 | -237 | SC2 (P11-INV-2) |
    | Maximum file line count | 16,703 | <paste actual> | -<paste> | SC2 (P11-INV-2) |
    | Numbered subcrates | 22 | 0 | -22 | SC1 (P11-INV-1) |
    | #[cube(launch_unchecked)] count | 22 | <paste actual> | <paste Δ> | SC7 (P11-INV-5; must be ≤22) |
    | Workspace member count | <paste pre> | <paste post> | <paste Δ> | — |
    | cargo build --workspace peak RSS | <paste pre> | <paste post> | <paste Δ> | RAM-survival (D-07) |

    ## Detailed verification

    | Gate | Command | Result |
    |------|---------|--------|
    | P11-INV-1 | `bash tools/audit_subcrate_collapse.sh` | <PASS/FAIL + exit code> |
    | P11-INV-2 | `python3 tools/audit_kernel_size.py --strict` | <PASS/FAIL + exit code> |
    | P11-INV-3 | `cargo build --workspace` | <PASS/FAIL + exit code> |
    | P11-INV-4 (smoke) | `cargo test -p libxc_rs-verify --test parity_phase11 phase11_smoke` | <PASS/FAIL + max rel err> |
    | P11-INV-4 (worst-case) | `cargo test -p libxc_rs-verify --test parity_phase11 phase11_worst_case` | <PASS/FAIL + max rel err> |
    | P11-INV-5 | `bash tools/audit_cube_launch.sh` | <PASS/FAIL + count> |
    | P11-INV-6 | `bash tools/test_idempotency.sh` | <PASS/FAIL + diff bytes> |

    ## Success criteria sign-off

    - [x] SC1: no numbered children
    - [x] SC2: no files >5000 lines
    - [x] SC3: splitter capable of subdividing single output expressions
    - [x] SC4: cargo build --workspace succeeds
    - [x] SC5: oracle parity preserved at 1e-12 (D-05)
    - [x] SC6: pipeline idempotent
    - [x] SC7: #[cube(launch_unchecked)] count not increased
    ```

    Replace every `<paste ...>` placeholder with the literal output from the gates above before committing. If any gate FAILED, change the box to `[ ]`, write the failure mode, and STOP THE PLAN — the phase is not complete.

    --- Operation 4: ROADMAP.md flip ---

    Edit `.planning/ROADMAP.md`:

    1. Find line `- [ ] **Phase 11: Splitter v2 — Unified Kernels with 5K Line Cap** *(INSERTED)* - ...` and change `[ ]` to `[x]`.
    2. In the Progress table (around line 159), add or update the Phase 11 row to read:
       ```
       | 11. Splitter v2 — Unified Kernels with 5K Line Cap | 6/6 | Complete | <YYYY-MM-DD> |
       ```
       The plan count is 6 (this plan + the 5 prior). The completion date is the date this task runs.
    3. Find the Phase 11 detailed section (around line 237) and update the **Plans:** line:
       ```
       **Plans:** 6 plans

       Plans:
       - [x] 11-01-PLAN.md — Wave 0: spike (D-02 ABI proof) + audit tools + parity_phase11 + baseline snapshot
       - [x] 11-02-PLAN.md — Wave 1: CSE partitioner (tools/translate_v2/cse.py) + LDA D-02 ABI port + LDA regen at SPLIT_THRESHOLD=5000
       - [x] 11-03-PLAN.md — Wave 2: factor chunk_single_output_d02 into translate_v2/emit.py + GGA D-02 wedge + GGA regen at SPLIT_THRESHOLD=5000
       - [x] 11-04-PLAN.md — Wave 3: MGGA D-02 wedge + MGGA regen at SPLIT_THRESHOLD=5000 (closes the 207-file blast radius — worst-case parity green)
       - [x] 11-05-PLAN.md — Wave 4: subcrate collapse (LDA → GGA → MGGA) via tools/collapse_subcrates.py (Strategy 1, dispatch paths preserved)
       - [x] 11-06-PLAN.md — Wave 5: CLAUDE.md per D-03a + obsolete-tool deletions + idempotency test + phase gate
       ```

    --- Operation 5: VALIDATION.md per-task map population + nyquist flip ---

    Edit `.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md`:

    1. Replace every placeholder `{N}-XX-XX` row in the Per-Task Verification Map with concrete task IDs from plans 11-01..06. Concrete task IDs:
       - `11-01-01` (spike), `11-01-02` (audit tools), `11-01-03` (parity_phase11 + baseline)
       - `11-02-01` (cse.py), `11-02-02` (LDA D-02 emitter), `11-02-03` (LDA regen)
       - `11-03-01` (emit.py factor-out), `11-03-02` (GGA wedge), `11-03-03` (GGA regen)
       - `11-04-01` (MGGA wedge), `11-04-02` (MGGA regen)
       - `11-05-01` (collapse_subcrates.py), `11-05-02` (LDA→GGA→MGGA collapse)
       - `11-06-01` (CLAUDE.md + tool deletions), `11-06-02` (idempotency + phase gate, this task)

       Map every task to its primary P11-INV-* requirement and the automated command from its `<verify><automated>` block.

    2. In the frontmatter, set `nyquist_compliant: true` and `wave_0_complete: true` (the latter has been true since plan 11-01 landed; should already be flipped; verify).

    --- Operation 6: STATE.md update ---

    Edit `.planning/STATE.md` to advance the project position past Phase 11:

    - `status:` becomes "Phase 11 complete (Splitter v2 — 5K cap + subcrate collapse). Ready to plan Phase 10 (workspace modular split — sequenced after Phase 11 per D-06)."
    - `stopped_at:` becomes "Phase 11 complete (<YYYY-MM-DD>); next: /gsd-plan-phase 10"
    - `last_updated:` to today's ISO timestamp.
    - `last_activity:` to a one-line summary of Phase 11 outcomes.
    - `progress.completed_phases:` increment by 1; `progress.total_plans:` += 6; `progress.completed_plans:` += 6; recompute `progress.percent`.
    - Add a new "Recent decisions affecting current work" entry: "[Phase 11]: Kernel chunks generic over <F: Float> (D-03); chunked-emission ABI uses tuple returns (D-02); 5K hard cap per file (D-LOCK-B); 22 numbered subcrates collapsed into 3 family façades (D-LOCK-A); 1e-12 verify gate at f64 (D-05)."

    --- Final commit ---

    `docs(phase-11): close phase — final metrics, ROADMAP flip, VALIDATION populated, STATE advanced`. Stage:
    - `.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md` (new)
    - `.planning/ROADMAP.md` (modified)
    - `.planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md` (modified)
    - `.planning/STATE.md` (modified)
  </action>
  <verify>
    <automated>bash tools/test_idempotency.sh && bash tools/audit_subcrate_collapse.sh && python3 tools/audit_kernel_size.py --strict && bash tools/audit_cube_launch.sh && cargo build --workspace && cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture && grep -q '\[x\] \*\*Phase 11' .planning/ROADMAP.md && grep -q 'nyquist_compliant: true' .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/test_idempotency.sh` exits `0` (P11-INV-6, success criterion 6).
    - `bash tools/audit_subcrate_collapse.sh` exits `0` (P11-INV-1, success criterion 1).
    - `python3 tools/audit_kernel_size.py --strict` exits `0` (P11-INV-2, success criterion 2).
    - `cargo build --workspace` exits `0` (P11-INV-3, success criterion 4).
    - `bash tools/audit_cube_launch.sh` exits `0` (P11-INV-5, success criterion 7).
    - `cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture` exits `0` (P11-INV-4 / D-05, success criterion 5).
    - `.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md` exists, ≥30 lines, contains literal `237 | 0` (or equivalent showing the oversized count went to zero).
    - `grep -c '\\[x\\] \\*\\*Phase 11' .planning/ROADMAP.md` is at least `1`.
    - ROADMAP Progress table contains the row `11. Splitter v2 — Unified Kernels with 5K Line Cap | 6/6 | Complete`: `grep -c 'Splitter v2.*6/6.*Complete' .planning/ROADMAP.md` is at least `1`.
    - `grep -c 'nyquist_compliant: true' .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md` is at least `1`.
    - VALIDATION.md placeholders gone: `grep -c '{N}-XX-XX' .planning/phases/11-splitter-v2-unified-5k-cap/11-VALIDATION.md` equals `0`.
    - VALIDATION.md per-task map cites all 6 plans: every `11-0[1-6]` plan number appears at least once in the per-task map.
    - STATE.md advances past Phase 11: `grep -c 'Phase 11 complete' .planning/STATE.md` is at least `1`; `progress.completed_phases` increments.
    - No `.cargo/config.toml` change in this commit: `git diff --name-only HEAD~1 HEAD .cargo/config.toml` returns empty.
  </acceptance_criteria>
  <done>Phase 11 is fully closed: every invariant verified, every success criterion met, every ROADMAP/STATE/VALIDATION artifact updated. The project is ready for `/gsd-plan-phase 10` (workspace modular split, the next phase per D-06 ordering).</done>
</task>

</tasks>

<verification>
End-of-plan verification (the phase gate itself):

1. `bash tools/test_idempotency.sh` exits `0` (success criterion 6).
2. `bash tools/audit_subcrate_collapse.sh` exits `0` (success criterion 1).
3. `python3 tools/audit_kernel_size.py --strict` exits `0` (success criterion 2).
4. `cargo build --workspace` exits `0` (success criterion 4).
5. `bash tools/audit_cube_launch.sh` exits `0` (success criterion 7).
6. `cargo test -p libxc_rs-verify --test parity_phase11 -- --test-threads=1 --nocapture` exits `0` (success criterion 5).
7. CLAUDE.md updated per D-03a (success criterion 3 prereq — splitter capability + ABI documented).
8. 11-FINAL-METRICS.md captures the side-by-side comparison.
9. ROADMAP.md Phase 11 marked complete.
10. STATE.md advanced.

If any of 1-6 fail, STOP and return CHECKPOINT REACHED — Phase 11 is not complete.
</verification>

<success_criteria>
**Phase 11 success criteria 1-7 from ROADMAP.md, all sign-off here:**

- [x] SC1 — `find crates/kernels -maxdepth 1 -type d` shows no numbered children (P11-INV-1).
- [x] SC2 — `find crates/kernels -name '*.rs' -exec wc -l {} +` shows zero files >5000 lines (P11-INV-2).
- [x] SC3 — splitter capable of subdividing single output expressions: cse.py + emit.py exist; r4scan, br89_explicit, kcis/kcisk all fit ≤5K via the new chunker.
- [x] SC4 — `cargo build --workspace` succeeds inline under jobs=1 (P11-INV-3).
- [x] SC5 — oracle parity preserved at 1e-12 on energy + routed derivatives at f64 (P11-INV-4 / D-05).
- [x] SC6 — pipeline idempotent: re-run produces no diff (P11-INV-6).
- [x] SC7 — `#[cube(launch_unchecked)]` count unchanged from baseline 22 (P11-INV-5).

Plus:
- D-03a CLAUDE.md update landed.
- 5 obsolete tools deleted.
- Driver stale defaults cleaned up (RESEARCH Q5).
- All 6 plan SUMMARYs exist; all VALIDATION map rows populated; ROADMAP / STATE updated.
- No `.cargo/config.toml` modification across the entire phase (D-07/D-08/D-09 invariants).
</success_criteria>

<output>
After completion, create `.planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md` covering:
- CLAUDE.md edit summary (which file was authoritative; PROJECT.md sync if used).
- The 5 deleted tools (with confirmation no other code referenced them).
- maple_to_kernels.py cleanup diff (which lines went, what stayed).
- Idempotency test result.
- Final phase-gate sweep results (each command's exit code + relevant output).
- 11-FINAL-METRICS.md summary (one-line side-by-side: 237→0, 16703→<actual>, 22→0, 22→22).
- ROADMAP / VALIDATION / STATE update summary.
- Pointer to next phase: `/gsd-plan-phase 10` per D-06 ordering.
</output>
