---
phase: 11-splitter-v2-unified-5k-cap
plan: 08
type: execute
wave: 5
depends_on: [07]
files_modified: [
  "tools/audit_kernel_size.py",
  "tools/audit_cube_launch.sh",
  "tools/audit_subcrate_collapse.sh",
  "tools/split_oversized_kernel.py",
  "tools/split_oversized_mgga.py",
  "tools/split_mgga_7_kcis.py",
  "tools/rebatch_mgga.py",
  "CLAUDE.md",
  "ROADMAP.md",
  "Cargo.toml",
  ".planning/phases/11-splitter-v2-unified-5k-cap/11-08-SUMMARY.md",
  ".planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md"
]
autonomous: true
requirements: [KERN-03, KERN-04, KERN-05, VERIFY-02, VERIFY-03, VERIFY-04]
user_setup: []

must_haves:
  truths:
    - "CLAUDE.md § Constraints updated per D-03a (f64 by default + oracle gating; f32 opt-in at launch no correctness gate)"
    - "Obsolete numbered-subcrate splitter tools deleted (split_oversized_*, rebatch_mgga.py, split_mgga_7_kcis.py)"
    - "All audit tools finalized and passing (D-13 per-design budget, D-10a per-functional invariants, 5K size cap)"
    - "ROADMAP.md success criteria amended per D-12 (per-`-p` incremental gates, not whole-workspace build)"
    - "root Cargo.toml default-members list finalized (~260 routed, 6 deferred, correct subset)"
    - "Phase 11 FINAL-METRICS.md documents pre→post deltas and validates all invariants"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated precision policy (f64 default, f32 opt-in)"
      contains: "f64 by default"
    - path: "ROADMAP.md"
      provides: "Amended success criteria reflecting per-functional structure and D-12"
      contains: "per-functional subcrates"
    - path: "Cargo.toml"
      provides: "Final root manifest with ~266 per-functional deps"
      contains: "default-members"
    - path: ".planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md"
      provides: "Pre/post metrics, invariant validation, phase deliverables"
      contains: "P11-INV-1 through P11-INV-6"
  key_links:
    - from: "D-03a amendment"
      to: "CLAUDE.md Constraints"
      via: "precision policy revision"
      pattern: "f64.*f32"
    - from: "Audit tools"
      to: "crates/kernels/"
      via: "5K cap + per-functional invariants"
      pattern: "audit_kernel_size|audit_subcrate_collapse"
---

<objective>
Phase 11 close: Update project documentation per D-03a (CLAUDE.md precision policy), delete obsolete splitter tooling, finalize all audits, amend ROADMAP success criteria per D-12 reinterpretation, validate final Cargo.toml, and document phase deliverables + invariant validation in FINAL-METRICS.md.

Purpose: Conclude Phase 11 with clean documentation, tooling, and a comprehensive metrics report.
Output: Updated CLAUDE.md, ROADMAP.md, cleaned tooling, FINAL-METRICS.md, ready for Phase 10 or future work.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@ROADMAP.md
@Cargo.toml
@tools/
@.planning/phases/11-splitter-v2-unified-5k-cap/11-01-SUMMARY.md (carry-forward)
@.planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md (metrics starting point)
@.planning/phases/11-splitter-v2-unified-5k-cap/11-07-SUMMARY.md (final sweep results)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md § Constraints per D-03a (precision policy)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines covering "f64 only; no silent f32 fallback" constraint)
  </read_first>
  <action>
Revise precision policy in CLAUDE.md § Constraints:

**Old text to replace:**
```
- **Precision**: f64 only; no silent f32 fallback; typed error if device lacks f64 support
```

**New text:**
```
- **Precision**: f64 is the default and the sole correctness target for oracle verification (energy relative error ≤ 10^-12 vs libxc oracle). f32 is available as a launch-time opt-in for performance-only mode with no correctness guarantee. Typed error returned if user selects f64 and device lacks f64 support.
```

**Rationale:** Phase 11 D-03a reflects that kernel chunks are generic `<F: Float>` (per locked D-02 ABI) and can launch with either f64 or f32. The oracle gate (1e-12) runs f64 only. f32 is explicitly user-selected at launch time and documented as having no correctness guarantee, NOT a silent fallback.

**Also update:**
- Operation order constraint: "Maple2c formula translations must preserve floating-point operation order for bit-level equivalence" → amend to "Translations must preserve floating-point operation order for functional correctness; CSE-aware chunking (Phase 11) introduces named temporaries that may reorder accumulation within acceptable tolerance (1e-12 relative error for energy + routed derivatives)."

**Commit:**
- Format: `docs(11-08): CLAUDE.md D-03a precision policy update (f64 default, f32 opt-in launch)`
  </action>
  <verify>
    <automated>grep -q "f64.*default\|f32.*opt-in" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md § Constraints updated per D-03a, committed</done>
</task>

<task type="auto">
  <name>Task 2: Amend ROADMAP.md Phase 11 success criteria per D-12 reinterpretation</name>
  <files>ROADMAP.md</files>
  <read_first>
    - ROADMAP.md (Phase 11 section, lines ~240–260, success criteria 0–7)
  </read_first>
  <action>
Locate Phase 11 section in ROADMAP.md and amend success criteria:

**Criterion #1 (existing):**
- Old: "Only per-family crates (lda, gga, mgga facades); no numbered subcrates"
- New: "Only per-functional subcrates (one per routed functional, ~260 total); family level (crates/kernels/{lda,gga,mgga}/) is a plain directory with no Cargo.toml; no numbered subcrates (lda-N, gga-N, mgga-N)"

**Criterion #4 (existing):**
- Old: "`cargo build --workspace` succeeds on the user's RAM-constrained machine"
- New: "Per-`-p` incremental compilation verified (all ~260 routed + 6 deferred subcrates compile independently without OOM under `jobs=1`; whole-workspace build is not a per-iteration gate per D-12 due to ambient dependency complexity)"

**Criterion #7 (existing):**
- No change needed; CubeCL macro fan-out audit clean per D-13 budget is still valid.

**Add new criterion after #7:**
- **#8 (NEW per D-15):** "Compile-first entry gate (D-15): representative canaries (lda_x, gga_x_lb, mgga_c_b94) compile per-`-p` and pass oracle parity at 1e-12 energy + routed derivatives before per-`-p` sweep"

**Commit:**
- Format: `docs(11-08): ROADMAP Phase 11 success criteria amended (D-12, D-15, per-functional structure)`
  </action>
  <verify>
    <automated>grep -q "per-functional subcrates\|per-\`-p\` incremental\|compile-first entry gate" ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md Phase 11 success criteria updated, committed</done>
</task>

<task type="auto">
  <name>Task 3: Delete obsolete numbered-subcrate splitter tools</name>
  <files>tools/split_oversized_kernel.py, tools/split_oversized_mgga.py, tools/split_mgga_7_kcis.py, tools/rebatch_mgga.py</files>
  <read_first>
    - tools/ (directory listing of obsolete tools)
  </read_first>
  <action>
Delete tools that assume the old numbered-subcrate layout (now obsolete under D-10a per-functional structure):

1. `tools/split_oversized_kernel.py` — post-split helper for numbered-subcrate layout
2. `tools/split_oversized_mgga.py` — post-split helper for numbered-subcrate MGGA layout
3. `tools/split_mgga_7_kcis.py` — MGGA-specific splitter helper (numbered layout)
4. `tools/rebatch_mgga.py` — re-batching helper for numbered MGGA sub-crates

Each of these tools operates on the assumption of numbered subdirectories (`lda-1`, `gga-3`, `mgga-5a`, etc.) which no longer exist post-11-03 D-10a clean-slate.

Keep: `split_lda_subcrates.py` (if it's used as a general splitting utility), `audit_*` tools (audit suite), `generate_*_dispatch.py` (dispatch generation — used in 11-06), `translate_*.py` (core translators — still used).

**Command:**
```bash
cd /home/user/Documents/workspace/libxc_rs
git rm tools/split_oversized_kernel.py tools/split_oversized_mgga.py tools/split_mgga_7_kcis.py tools/rebatch_mgga.py
```

**Commit:**
- Format: `refactor(11-08): delete obsolete numbered-subcrate splitter tools (D-10a per-functional cleanup)`
  </action>
  <verify>
    <automated>! test -f tools/split_oversized_kernel.py &amp;&amp; ! test -f tools/rebatch_mgga.py &amp;&amp; git log --oneline -1 | grep -q "obsolete"</automated>
  </verify>
  <done>Obsolete tools deleted, cleanup committed</done>
</task>

<task type="auto">
  <name>Task 4: Finalize audit suite — verify all passing, document in SUMMARY</name>
  <files>tools/audit_kernel_size.py, tools/audit_cube_launch.sh, tools/audit_subcrate_collapse.sh, tools/test_idempotency.sh</files>
  <read_first>
    - tools/audit_kernel_size.py
    - tools/audit_cube_launch.sh (D-13 rewrite from 11-03)
    - tools/audit_subcrate_collapse.sh (updated for per-functional invariant)
    - tools/test_idempotency.sh
  </read_first>
  <action>
Run final audit suite and document results:

1. **Audit suite execution:**
   ```bash
   # 5K cap audit
   tools/audit_kernel_size.py crates/kernels/ 2>&1 | tee /tmp/audit_kernel_size.log
   
   # Per-design launch budget (D-13)
   tools/audit_cube_launch.sh 2>&1 | tee /tmp/audit_cube_launch.log
   
   # Per-functional structure invariants (D-10a)
   tools/audit_subcrate_collapse.sh 2>&1 | tee /tmp/audit_subcrate_collapse.log
   
   # Idempotency check
   tools/test_idempotency.sh 2>&1 | tee /tmp/test_idempotency.log
   ```

2. **Expected results:**
   - `audit_kernel_size.py`: All files ≤5000 lines (zero oversized)
   - `audit_cube_launch.sh`: Per-design budget (routed 1-per-output, unrouted 0, math ≤22)
   - `audit_subcrate_collapse.sh`: No per-family crates, no numbered subcrates, ~266 per-functional
   - `test_idempotency.sh`: Zero diff on splitter re-run

3. **If any audit fails:**
   - Investigate root cause (likely a stray file not covered by Task 3 cleanup, or regen drift)
   - Fix via minimal splitter adjustment or manual file cleanup
   - Re-run audit to confirm PASS
   - Document failure / fix in SUMMARY

4. **Collect results in SUMMARY (Task 5)**
  </action>
  <verify>
    <automated>tools/audit_kernel_size.py crates/kernels/ 2>&1 | grep -q "All.*within 5000" &amp;&amp; tools/audit_cube_launch.sh 2>&1 | grep -q "PASS" &amp;&amp; tools/audit_subcrate_collapse.sh 2>&1 | grep -q "PASS"</automated>
  </verify>
  <done>All audits run and passing; results documented</done>
</task>

<task type="auto">
  <name>Task 5: Validate final Cargo.toml root manifest default-members list</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (root, lines ~20–60, [workspace] default-members section)
  </read_first>
  <action>
Verify root Cargo.toml default-members is correct post-Phase 11:

1. **Count routed:**
   ```bash
   grep "default-members" Cargo.toml | grep -o '"libxc-kernel-[^"]*"' | wc -l
   ```
   Expected: ~260 routed functional subcrates

2. **Verify deferred are excluded:**
   ```bash
   deferred=("mgga_c_b94" "mgga_x_br89" "mgga_x_mbr" "mgga_x_mbrxc_bg" "mgga_x_mbrxh_bg" "mgga_x_mggac")
   for func in "${deferred[@]}"; do
     if grep "default-members" Cargo.toml | grep -q "libxc-kernel-mgga_$func"; then
       echo "ERROR: $func should not be in default-members"
       exit 1
     fi
   done
   ```
   Expected: all deferred excluded from default-members (but still in [dependencies])

3. **Verify no numbered subcrates:**
   ```bash
   grep "default-members" Cargo.toml | grep -q "libxc-kernel-lda-[0-9]" && echo "ERROR: numbered LDA found" || true
   grep "default-members" Cargo.toml | grep -q "libxc-kernel-gga-[0-9]" && echo "ERROR: numbered GGA found" || true
   grep "default-members" Cargo.toml | grep -q "libxc-kernel-mgga-[0-9]" && echo "ERROR: numbered MGGA found" || true
   ```
   Expected: zero numbered subcrates

4. **Verify [dependencies] includes all routed + deferred for manual build:**
   ```bash
   total=$(grep "libxc-kernel-" Cargo.toml | grep -c "path.*crates/kernels")
   echo "Total per-functional deps in Cargo.toml: $total (expect ~266)"
   ```
   Expected: ~266 per-functional deps total (260 routed + 6 deferred)

5. **No commit needed unless fixes required**
  </action>
  <verify>
    <automated>grep "default-members" Cargo.toml | wc -l | grep -q "1" &amp;&amp; grep "libxc-kernel-" Cargo.toml | grep -c "path.*crates/kernels" | grep -E "26[0-9]"</automated>
  </verify>
  <done>Cargo.toml default-members verified: ~260 routed, 6 deferred excluded, no numbered subcrates</done>
</task>

<task type="auto">
  <name>Task 6: Create 11-08-SUMMARY.md and 11-FINAL-METRICS.md</name>
  <files>.planning/phases/11-splitter-v2-unified-5k-cap/11-08-SUMMARY.md, .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-BASELINE.md (pre-phase metrics)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md (canary results)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-07-SUMMARY.md (sweep results)
  </read_first>
  <action>
Create two summary documents:

**11-08-SUMMARY.md (Task 6 closure):**
1. Status: COMPLETE — Phase 11 close
2. Tasks completed:
   - CLAUDE.md D-03a amendment
   - ROADMAP.md success criteria amended per D-12, D-15
   - Obsolete splitter tools deleted
   - Audit suite all PASS
   - Cargo.toml verified (~260 routed + 6 deferred)
3. No code changes in this plan; documentation + cleanup only
4. Forward dependency: Phase 10 (workspace modular split) can proceed per D-06

**11-FINAL-METRICS.md (comprehensive phase report):**
1. **Phase goal recap:**
   - Per-functional subcrates, hard 5K cap, oracle parity 1e-12, idempotency
2. **Pre→post deltas:**
   - Numbered subcrates: 27 → 0 (deleted)
   - Per-functional subcrates: 0 → ~266 (emitted)
   - Oversized files (>5K): 235 → 0 (D-LOCK-B achieved)
   - Max line count: 16,703 → ≤5,000 (per file)
   - Routed functionals passing parity: TBD (from 11-07) → ~260/260 (100%)
3. **Invariant validation:**
   - P11-INV-1: Per-functional structure confirmed (audit_subcrate_collapse.sh)
   - P11-INV-2 (historical, may be N/A): LDA+GGA narrowed verify/ deps
   - P11-INV-3 (historical, may be N/A): MGGA narrowed verify/ deps
   - P11-INV-4 (historical, may be N/A): Per-subcrate compile per-`-p` (11-07 Task 1)
   - P11-INV-5 (D-13 redef): Per-design launch budget (audit_cube_launch.sh PASS)
   - P11-INV-6 (D-LOCK-D): Idempotency zero diff (11-07 Task 3)
4. **Tooling state:**
   - Splitter v2: `translate_v2/cse.py` (D-16 consolidated AST pass), `translate_v2/per_functional.py` finalized
   - Obsolete tools: deleted (split_oversized_*, rebatch_mgga.py)
   - Audits finalized: all passing
5. **Compile times (from 11-07):**
   - LDA: {record}
   - GGA: {record}
   - MGGA routed: {record}
   - MGGA deferred: {record}
6. **Oracle parity results (from 11-07):**
   - Routed functionals at 1e-12: {count} / {total}
   - Failures: {count} (expected 0)
7. **Next phase:**
   - D-06 lock: Phase 11 complete, Phase 10 (workspace modular split) unblocked per D-06 ordering
  </action>
  <verify>
    <automated>test -f .planning/phases/11-splitter-v2-unified-5k-cap/11-08-SUMMARY.md &amp;&amp; test -f .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md</automated>
  </verify>
  <done>11-08-SUMMARY.md and 11-FINAL-METRICS.md created and committed</done>
</task>

<task type="auto">
  <name>Task 7: Final commit and Phase 11 state record</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current phase status section)
  </read_first>
  <action>
Record Phase 11 completion in STATE.md:

1. **Update STATE.md "Current Position" section:**
   - Change: "Phase 11 (splitter-v2-unified-5k-cap) — [status]"
   - New status: "COMPLETE (2026-05-17 replan: 11-04 retroactive + 11-05..08 executed)"
   - Remove "Paused at" and "Replan structure locked" entries

2. **Record final metrics:**
   - Plans: 11-01..08 all complete (Wave 0 + Wave 1..5)
   - Per-functional subcrates: 266 (41 LDA + 131 GGA + 94 MGGA estimate)
   - Routed functionals: ~260 passing 1e-12 parity
   - Files ≤5K: 100% (hard cap achieved)

3. **Commit:**
   - Format: `docs(phase-11): Phase 11 COMPLETE — per-functional 266 subcrates, ≤5K files, 1e-12 parity, idempotency verified`

**Cumulative Phase 11 commits:**
- 11-04 retroactive SUMMARY
- 11-05 D-02 ABI spike locked
- 11-06 full tree regen, canaries GREEN
- 11-07 per-`-p` sweep, parity PASS
- 11-08 (this task): CLAUDE.md, ROADMAP.md, cleanup, close

After this commit, Phase 11 is locked; Phase 10 (workspace modular split) next per D-06.
  </action>
  <verify>
    <automated>grep -q "COMPLETE" .planning/STATE.md &amp;&amp; git log --oneline -1 | grep -q "Phase 11"</automated>
  </verify>
  <done>STATE.md updated, Phase 11 marked COMPLETE, final commit recorded</done>
</task>

</tasks>

<verification>
All audits passing, CLAUDE.md and ROADMAP.md updated, obsolete tools deleted, Cargo.toml verified, final metrics documented, Phase 11 marked complete.
</verification>

<success_criteria>
- [ ] Task 1: CLAUDE.md D-03a precision policy updated and committed
- [ ] Task 2: ROADMAP.md Phase 11 success criteria amended (criteria #1, #4, +#8)
- [ ] Task 3: Obsolete numbered-subcrate tools deleted (4 files)
- [ ] Task 4: All audits run and passing (5K cap, per-design budget, per-functional structure, idempotency)
- [ ] Task 5: Cargo.toml default-members verified (~260 routed, 6 deferred excluded)
- [ ] Task 6: 11-08-SUMMARY.md and 11-FINAL-METRICS.md created
- [ ] Task 7: STATE.md updated, Phase 11 marked COMPLETE

</success_criteria>

<output>
Final cumulative commit:
```bash
git commit -m "docs(phase-11): Phase 11 COMPLETE — 266 per-functional subcrates, ≤5K files, 1e-12 parity, full audit suite PASS"
```

Phase 11 is now complete and ready for Phase 10 (workspace modular split) per D-06 ordering.
</output>
