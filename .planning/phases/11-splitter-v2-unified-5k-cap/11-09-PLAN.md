---
phase: 11-splitter-v2-unified-5k-cap
plan: 09
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/eval/mgga_dispatch/prepare.rs
  - src/eval/mgga_dispatch/mod.rs
  - verify-canary/tests/g1_tau_clamp_dispatch_parity.rs
  - verify-canary/Cargo.toml
autonomous: false
requirements: []
tags: [g1, work_mgga, tau-clamp, von-weizsacker, mgga, systemic, dispatch]

must_haves:
  truths:
    - "The libxc work_mgga von Weizsacker tau-clamp (tau >= sigma/(8 rho)) is applied in the PRODUCTION MGGA path (src/eval/mgga_dispatch), not just the verify-canary host driver — every functional dispatched through dispatch_mgga regularizes tau before the kernel launch."
    - "A direct-call single-kernel parity check (verify-canary pattern, builds ONE kernel, NOT the libxc_rs umbrella) proves a sub-vW tau point now matches the libxc id=397 oracle at 1e-12, exercising the SAME clamp formula production uses."
    - "The clamp is raise-only (no-op on tau >= tau_W) and guarded at rho < dens_threshold to mirror libxc."
    - "No monolithic all-281-kernel build is performed; the parity check builds exactly one kernel via verify-canary (gap G-1 Done-when + machine constraint 2)."
  artifacts:
    - path: "src/eval/mgga_dispatch/prepare.rs"
      provides: "tau_von_weizsacker regularization helper applied to input tau before buffer creation in dispatch_mgga"
      contains: "fn tau_von_weizsacker"
    - path: "verify-canary/tests/g1_tau_clamp_dispatch_parity.rs"
      provides: "Direct-call single-kernel parity check on a sub-vW tau point exercising the production clamp formula"
      contains: "397"
  key_links:
    - from: "src/eval/mgga_dispatch/mod.rs (dispatch_mgga step 5)"
      to: "src/eval/mgga_dispatch/prepare.rs (tau_von_weizsacker)"
      via: "call regularization on input.tau() before create_input_buffer with the clamped tau"
      pattern: "tau_von_weizsacker"
    - from: "verify-canary/tests/g1_tau_clamp_dispatch_parity.rs"
      to: "the production clamp formula tau.max(sigma/(8 rho))"
      via: "same raise-only clamp on a sub-vW point vs libxc id=397 oracle at 1e-12"
      pattern: "8.0"
---

<objective>
Close gap **G-1** (CRITICAL PATH): add libxc work_mgga input regularization — the von Weizsacker
tau-clamp tau >= sigma/(8 rho) — to the PRODUCTION MGGA path. Today src/eval/mgga_dispatch passes raw
tau; libxc's work_mgga driver clamps it. Any MGGA point with tau < tau_W diverges at the percent-level
(NOT an f32/tolerance issue). The fix was proven in the verify-canary host driver only (Phase 11.1 G3
root-cause); this plan moves it into production and proves it with a direct-call single-kernel parity
check on a tau < tau_W point. Per gap index, G-1 gates a meaningful G-2.

Source-of-truth formula: verify-canary/tests/g3_mgga_c_b94_parity.rs::tau_von_weizsacker (lines 86-100)
— tau[i].max(sigma[i] / (8.0 * rho[i])), confirmed ~5e-13 vs libxc id=397 on 4 points. See memory
project_translator_missing_workmgga_tau_clamp.

Output: production tau-clamp in mgga_dispatch; a new canary test proving parity at a tau < tau_W point;
both build exactly one kernel.

Why autonomous: false — the tau-clamp PLACEMENT is a design decision (Task 1 checkpoint).
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VERIFICATION.md
@.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-03-G4-DEFERRAL.md
@CLAUDE.md
@verify-canary/tests/g3_mgga_c_b94_parity.rs
@verify-canary/Cargo.toml
@src/eval/mgga_dispatch/mod.rs

### Interfaces (executor uses these directly)

dispatch_mgga (mod.rs step 5, ~lines 273-282) builds tau_handle from create_input_buffer(&client,
input.tau()) — RAW tau. input: &MggaInput; input.rho()/sigma()/tau() return &[f64].
create_input_buffer(&client, &[f64]) -> Handle. thresholds.density (f64) is in scope (dt =
thresholds.density).

Proven clamp (copy verbatim): tau[i].max(sigma[i] / (8.0 * rho[i])) per point.

Direct-kernel-call ABI (g3 test, builds ONE kernel): use
libxc_kernel_mgga_c_b94::exc_unpol::mgga_c_b94_exc_unpol (concrete-f64, no turbofish); thin
#[cube(launch_unchecked)] wrapper -> launch_unchecked::<CpuRuntime>(...) with
ArrayArg::from_raw_parts(handle, np) (cubecl 0.10, handle MOVED); oracle via xc_func_init(func, 397, 1)
then xc_mgga_exc(func, np, rho, sigma, lapl, tau, zk).

### Machine constraints (NON-NEGOTIABLE)
- .cargo/config.toml hand-pinned jobs = 1. DO NOT edit. No parallel/multi-job cargo.
- NO monolithic all-kernel build. Single-functional checks use verify-canary (cargo test -p
  libxc_rs-verify-canary), NEVER -p libxc_rs-verify or dispatch_*.
- Per-`-p` compile is an ENTRY gate. Prefer cargo check -p libxc_rs --lib; if it OOMs (D-12) DO NOT
  force it — rely on Task 3's canary build and document the production-crate compile as gated by the
  G-3 full sweep (11-10). HALT and surface if neither path works.
</context>

<threat_model>
Pure-Rust numerical library + local FFI parity test. NO network/auth/untrusted input. Real risk is
SILENT NUMERICAL INCORRECTNESS and regularization-scope error (mitigated by the oracle parity gate);
codegen non-determinism is out of scope here (G-4).

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-11-09-01 | Tampering | Clamp lowers a compliant tau | mitigate | raise-only tau.max(sigma/(8 rho)); parity check asserts a tau>=tau_W point unchanged |
| T-11-09-02 | Repudiation | Clamp lands in canary only (repeats 11.1 partial fix) | mitigate | Production edit in prepare.rs; acceptance greps the production file; canary exercises the identical formula |
| T-11-09-03 | Info Disclosure | Div-by-zero / NaN at rho->0 | mitigate | Guard with thresholds.density; documented in prepare.rs header |
| T-11-09-04 | DoS | Parity check pulls 281-kernel umbrella (OOM) | mitigate | Test lives in verify-canary (one kernel + libxc-sys); never -p libxc_rs-verify |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide tau-clamp placement and confirm regularization invariants</name>
  <read_first>
    - src/eval/mgga_dispatch/mod.rs step 5 (the raw-tau buffer site, ~line 281)
    - verify-canary/tests/g3_mgga_c_b94_parity.rs lines 86-100 (proven clamp + no-op-on-compliant note)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-03-G4-DEFERRAL.md Additional-finding section
  </read_first>
  <decision>Where does the tau-clamp (tau >= sigma/(8 rho)) live in the production MGGA path, and how is the rho-to-zero edge guarded?</decision>
  <context>libxc's work_mgga driver regularizes tau before the maple2c expression. Rust production passes raw tau. The formula is proven; only PLACEMENT is open (per gap G-1 + plan_authoring_rules: a tau-clamp placement design choice is the legitimate non-autonomous reason). Clamp must be raise-only; rho->0 must mirror libxc's density-threshold gate.</context>
  <options>
    <option id="prepare-rs">
      <name>Option A: Regularize in src/eval/mgga_dispatch/prepare.rs before buffer creation (RECOMMENDED)</name>
      <pros>Single chokepoint for every MGGA functional; matches libxc's driver-level regularization; one test site; no per-kernel regen (no AP-3 edits).</pros>
      <cons>Per-call O(np) tau allocation; negligible vs launch cost.</cons>
    </option>
    <option id="per-kernel">
      <name>Option B: Inside each generated kernel (translator-side, all 92 MGGA)</name>
      <pros>Clamp travels with the kernel (GPU + future direct callers).</pros>
      <cons>Translator change + full-tree MGGA regen (heavy; AP-3 blast radius); the clamp is a DRIVER concern, not a maple2c-expression concern. Rejected unless A cannot mirror libxc.</cons>
    </option>
  </options>
  <acceptance_criteria>User selects a placement and confirms: (1) raise-only tau.max(sigma/(8 rho)); (2) rho->0 guarded via thresholds.density; (3) the parity check exercises the SAME clamp formula production uses.</acceptance_criteria>
  <resume-signal>Select: prepare-rs, per-kernel, or describe alternative; confirm the three invariants.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the tau-clamp in the production MGGA dispatch path</name>
  <files>src/eval/mgga_dispatch/prepare.rs, src/eval/mgga_dispatch/mod.rs</files>
  <read_first>
    - src/eval/mgga_dispatch/mod.rs (dispatch_mgga step 5, ~lines 273-282; AND surrounding ctx build / launch)
    - src/eval/mgga_dispatch/prepare.rs (read in full; if absent, create it and add pub(crate) mod prepare; to mod.rs)
    - verify-canary/tests/g3_mgga_c_b94_parity.rs lines 86-100 (source-of-truth clamp)
    - CLAUDE.md (f64 only; 1e-12; operation order matters)
  </read_first>
  <action>
Per Task 1 (default Option A — prepare.rs):

Step 1 — Add to prepare.rs a helper with the EXACT canary arithmetic tau[i].max(sigma[i] / (8.0 *
rho[i])) wrapped in a per-point guard: if rho[i] < dens_threshold keep tau[i], else clamp. Signature:
pub(crate) fn tau_von_weizsacker(rho: &[f64], sigma: &[f64], tau: &[f64], dens_threshold: f64) ->
Vec<f64>. Header doc-comment cites gap G-1 + memory project_translator_missing_workmgga_tau_clamp and
the raise-only / rho-below-dt semantics.

Step 2 — Wire into dispatch_mgga step 5 (mod.rs): replace let tau_handle =
create_input_buffer(&client, input.tau()); with a tau_von_weizsacker(...) call yielding tau_clamped,
then let tau_handle = create_input_buffer(&client, &tau_clamped); and let tau_len = tau_clamped.len();.
Declare pub(crate) mod prepare;. Change NO other input handle. Keep tau_clamped alive until after the
launch.

Step 3 — Compile-gate (ENTRY gate): cargo check -p libxc_rs --lib. If it OOMs (D-12) DO NOT force it;
document the production-crate compile as gated by the G-3 sweep (11-10) and use Task 3's canary build
as the immediate signal. HALT/surface if neither works.

BANS: no .cargo/config.toml edits; no --jobs over 1 / CARGO_BUILD_JOBS; no hand-edit of
crates/kernels/{lda,gga,mgga}/* (AP-3); no cargo build --workspace or -p libxc_rs-verify.
  </action>
  <acceptance_criteria>prepare.rs defines fn tau_von_weizsacker and contains the substring 8.0 * rho[i]; mod.rs references tau_von_weizsacker and tau_clamped (raw input.tau() no longer reaches create_input_buffer); the prepare module is declared.</acceptance_criteria>
  <verify><automated>grep -q 'fn tau_von_weizsacker' src/eval/mgga_dispatch/prepare.rs</automated></verify>
  <done>tau_von_weizsacker exists in prepare.rs with the exact raise-only formula + rho-below-dt guard; dispatch_mgga creates the tau buffer from the clamped vec; dispatch edit compile-gated (or gate deferred to 11-10 with rationale in SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 3: Direct-call single-kernel parity check on a sub-vW tau point (verify-canary; builds ONE kernel)</name>
  <files>verify-canary/tests/g1_tau_clamp_dispatch_parity.rs, verify-canary/Cargo.toml</files>
  <read_first>
    - verify-canary/tests/g3_mgga_c_b94_parity.rs (FULL existing canary — copy its launch ABI, oracle FFI, rel_err_with_floor verbatim; the new test is a sibling)
    - verify-canary/Cargo.toml (single-kernel dep pattern: libxc-kernel-mgga_c_b94 + libxc-sys + cubecl cpu + bytemuck)
    - src/eval/mgga_dispatch/prepare.rs (the production clamp — the test must exercise the SAME formula)
  </read_first>
  <action>
Create verify-canary/tests/g1_tau_clamp_dispatch_parity.rs — a sibling of g3 that specifically
exercises a sub-vW tau point and proves the production clamp brings it to parity.

Step 1 — Build a tau < tau_W grid: at least 2 points where tau < sigma/(8 rho) (clamp active) AND at
least 1 point where tau >= sigma/(8 rho) (clamp no-op). Example active point: rho=1.0, sigma=2.0,
tau=0.01 -> tau_W = 0.25 > 0.01. Comment each point's tau vs tau_W so the no-op invariant is visible.

Step 2 — Exercise the SAME clamp production uses: copy the EXACT formula tau[i].max(sigma[i] / (8.0 *
rho[i])) (with the rho<dt guard) and assert in a comment it is byte-identical to
prepare.rs::tau_von_weizsacker. Apply the clamp, launch mgga_c_b94_exc_unpol via the thin
#[cube(launch_unchecked)] wrapper (copy g3's launch ABI verbatim), compare zk to the libxc id=397
unpolarized oracle.

Step 3 — Assert parity at 1e-12 on the clamped grid AND assert the clamp was active: add an assertion
that the sub-vW points have tau_clamped[i] > tau_raw[i] (so the test cannot pass vacuously / would
fail against raw tau). Emit a G1_SUMMARY line with max_rel_err + verdict.

Step 4 — Register the test (add a [[test]] entry named g1_tau_clamp_dispatch_parity if needed). Confirm
verify-canary still depends ONLY on libxc-kernel-mgga_c_b94, libxc-sys, cubecl(cpu), bytemuck — NO
libxc_rs umbrella dep.

Step 5 — Run (builds ONE kernel): cargo test -p libxc_rs-verify-canary --test
g1_tau_clamp_dispatch_parity -- --nocapture (tee /tmp/11-09-g1-parity.log). EXPECTED: test result ok,
1 passed, and G1_SUMMARY verdict=PASS. If the clamped grid diverges > 1e-12, HALT and surface (clamp
formula or placement is wrong, not a tolerance issue).

BANS: NEVER cargo test -p libxc_rs-verify (281-kernel pull -> OOM); no .cargo/config.toml edits; no
--jobs over 1.
  </action>
  <acceptance_criteria>verify-canary/tests/g1_tau_clamp_dispatch_parity.rs exists, contains 8.0 (the clamp denominator) and 397 (the libxc id), and verify-canary/Cargo.toml has NO libxc_rs umbrella dep. The single-kernel test run prints test result: ok and G1_SUMMARY verdict=PASS.</acceptance_criteria>
  <verify><automated>test -f verify-canary/tests/g1_tau_clamp_dispatch_parity.rs</automated></verify>
  <done>The new canary test exists, builds exactly one kernel, and PASSES at 1e-12 on a sub-vW tau point while asserting the clamp was active; verify-canary remains umbrella-free.</done>
</task>

</tasks>

<verification>
- prepare.rs has tau_von_weizsacker with the exact raise-only formula + rho<dt guard (grep)
- dispatch_mgga creates the tau buffer from the clamped vec (grep tau_clamped in mod.rs)
- verify-canary g1 test exists, umbrella-free, PASSES at 1e-12 on a sub-vW point (single-kernel run)
- No monolithic build was performed; no .cargo/config.toml edit; no AP-3 generated-tree edit
</verification>

<success_criteria>
G-1 closed per 11-VERIFICATION.md Done-when: the von Weizsacker tau-clamp is applied in the production
MGGA evaluation path (not just the canary driver), and a direct-call single-kernel parity check proves
a tau < tau_W point now matches libxc id=397 at 1e-12.
</success_criteria>

<output>
After completion, create .planning/phases/11-splitter-v2-unified-5k-cap/11-09-SUMMARY.md (record the
chosen tau-clamp placement, the dispatch-edit compile-gate disposition, the g1 parity max_rel_err, and
note that production-crate full compile is confirmed downstream by 11-10's G-3 sweep).
</output>
