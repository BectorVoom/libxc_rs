---
phase: 11-splitter-v2-unified-5k-cap
plan: 10
type: execute
wave: 2
gap_closure: true
depends_on:
  - 11-15
files_modified:
  - .cache/sweep-lda-manifest.json
  - .cache/sweep-gga-manifest.json
  - .cache/sweep-mgga-manifest.json
  - .cache/batched-compile-sweep-manifest.json
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-10-SWEEP-LOG.md
autonomous: true
requirements: []
tags: [g3, compile-sweep, per-p-entry-gate, resumable, chunked, f64, SPEC-11-R4]

must_haves:
  truths:
    - "tools/batched_compile_sweep.py is driven to VERDICT: ALL_OK across ALL on-disk subcrates the roster enumerates — record count equals len(build_roster()) (currently 280 = LDA 43 + GGA 131 + MGGA 106, where MGGA 106 includes the 14 mgga_c_tpssloc/mgga_c_revtpss _pK shard crates + their 2 facades) — at f64 (11.1 verified only a 50-subcrate sample) — SPEC-11-R4."
    - "The sweep is f64-ONLY. There is NO f32 sweep: the kernels are f64-concrete by design (2491 files &Array<f64>, 0 generic &Array<F>; memory project_kernels_f64_concrete_f32_milestone) so LIBXC_RS_F32=1 monomorphizes nothing different and a 'pass' would be a false f64-vs-f64 result (threat T-11-12-01, user-rejected 2026-05-23). f32 is a milestone-scale follow-up, not a Phase-11 gate."
    - "The sweep was run RESUMABLE/CHUNKED (per-family segments to distinct manifest paths, merged) so it can be paced across overnight segments — NOT one unbroken multi-day session."
    - "jobs policy honored: the sweep tool's own --jobs override is the ONLY parallel cargo; .cargo/config.toml is untouched; no cargo build --workspace; no -p libxc_rs umbrella build."
    - "The 11-15 math test-module 0.10 migration landed first (depends_on 11-15) so the sweep's cargo-test path does not break on the 0.9 launch-ABI drift."
  artifacts:
    - path: ".cache/batched-compile-sweep-manifest.json"
      provides: "Merged NDJSON manifest, one record per subcrate (count == len(build_roster()), currently 280 incl the 14 _pK shard crates), all result=ok, zero pass=-1, at f64 — the SPEC-11-R4 evidence consumed by 11-13 (G-5)"
      contains: "result"
    - path: ".planning/phases/11-splitter-v2-unified-5k-cap/11-10-SWEEP-LOG.md"
      provides: "Per-segment sweep log: family, range, verdict, peak-RSS, wall-clock, any documented exception"
      contains: "VERDICT"
  key_links:
    - from: "tools/batched_compile_sweep.py (--families / --start-after / --manifest)"
      to: "the roster-enumerated on-disk subcrates (currently 280) under crates/kernels/{lda,gga,mgga}"
      via: "per-`-p` cargo build per subcrate, 2-pass (jobs=3 then jobs=1 retry), NDJSON manifest per record"
      pattern: "batched_compile_sweep"
    - from: ".cache/sweep-{lda,gga,mgga}-manifest.json (per-family segment outputs)"
      to: ".cache/batched-compile-sweep-manifest.json (merged)"
      via: "concatenate the per-family NDJSON segments into the canonical manifest path after all segments PASS"
      pattern: "manifest"
---

<objective>
Close gap **G-3**: drive tools/batched_compile_sweep.py to VERDICT: ALL_OK across ALL roster-enumerated
on-disk subcrates (currently 280 = LDA 43 + GGA 131 + MGGA 106) at **f64 ONLY**. Phase 11.1 verified only
a 50-subcrate representative sample; this plan completes the remaining subcrates. This IS the per-`-p`
compile ENTRY-GATE codification (memory project_phase11_structural_without_compile) — SPEC-11-R4 /
ROADMAP SC #4. Note: P1-grep still shows ~1114 hits concentrated in gga_c_gaploc (now concrete-f64 ->
likely benign but UNVERIFIED at full-tree scope); this sweep is what confirms it.

NO f32 SWEEP (this plan was revised 2026-05-24 to DROP the former Task 2). The kernels are f64-CONCRETE
by design — 2491 files take `&Array<f64>` with f64 scalars; ZERO are generic `&Array<F>`. There is no
f32 kernel path. Running `LIBXC_RS_F32=1` monomorphizes nothing different; its only reader
(parity_phase11.rs) flips a tolerance but still computes in f64, so an f32 "pass" would be a false
f64-vs-f64 result at a relaxed tolerance — that is threat T-11-12-01, already rejected by the user
(2026-05-23, 11-12-SUMMARY). Real f32 support is a translator re-architecture + full ~2491-file regen +
FP-order reconciliation = a MILESTONE-scale follow-up, NOT a Phase-11 gate (memory
project_kernels_f64_concrete_f32_milestone; CLAUDE.md "Precision: f64 only").

IMPORTANT — the authoritative subcrate count is len(build_roster()), NOT a hardcoded magic number.
build_roster() (tools/batched_compile_sweep.py lines 81-100) enumerates EVERY subdir-with-Cargo.toml
under crates/kernels/{lda,gga,mgga}. On the live tree that is 280 records: LDA 43 + GGA 131 + MGGA 106.
MGGA is 106 (not the stale 92/266 figures) because mgga_c_tpssloc and mgga_c_revtpss are each a facade
crate + 7 _pK shard crates — i.e. mgga_c_tpssloc + mgga_c_tpssloc_p0..p6 (8 crates) and mgga_c_revtpss +
mgga_c_revtpss_p0..p6 (8 crates) = 16 on-disk packages for 2 logical functionals. The sweep builds the
on-disk PACKAGES, so all 14 _pK shard records (plus the 2 facades) MUST appear and MUST NOT be dropped
as "unexpected". (The old "266 = 43+131+92" figure counted LOGICAL functionals per 11-FINAL-METRICS.md;
the sweep counts on-disk packages — they are different and the gate must use the package count. Confirmed
on-disk 2026-05-24: 43 + 131 + 106 = 280.)

The sweep is ~22-44h wall-clock at jobs=1, so it MUST be RESUMABLE/CHUNKED (per gap index machine
constraint 4) — run per-family (or sub-range) segments to distinct manifest paths, paced across overnight
runs, then merge + validate.

PRE-REQ: 11-15 (the crates/kernels/math/src test-module cubecl-0.10 launch-ABI migration) MUST have
landed first — otherwise the sweep's cargo-test path breaks on the residual 0.9 drift. depends_on: 11-15.

Output: a merged .cache/batched-compile-sweep-manifest.json whose record count equals len(build_roster())
(currently 280), all result=ok at f64, plus a per-segment sweep log.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VERIFICATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-15-PLAN.md
@CLAUDE.md
@tools/batched_compile_sweep.py

### Sweep tool CLI (from tools/batched_compile_sweep.py main(), verified)

- --batch-size N (default 20)
- --jobs N (default 3 — the D-32 per-invocation override; the ONLY legitimate >1 cargo path)
- --start-after PKG (resume after a given package name)
- --families lda,gga,mgga (comma-separated subset; default all three)
- --roster PATH (override the on-disk roster discovery)
- --manifest PATH (default .cache/batched-compile-sweep-manifest.json — NDJSON, one record per pkg)
- --summary PATH ; --continue-here PATH ; --dry-run

IMPORTANT BEHAVIOR (lines 359-364): a real (non-dry-run) sweep CLEARS its --manifest at start. So
chunked resume across families MUST use a DISTINCT --manifest per segment (e.g. --families mgga
--manifest .cache/sweep-mgga-manifest.json), then merge segments into the canonical path at the end.
A 2-pass strategy is built in: pass 1 jobs=3 per pkg, on first batch failure pass 2 retries the whole
batch at jobs=1; failures record pass=-1, result=fail with stderr_tail.

ROSTER COUNT (authoritative): the expected record count is len(build_roster()) — derive it, do NOT
trust the stale "266" comment baked into the tool's docstring (lines 38-45, 86-89). On the live tree
len(build_roster()) == 280 (LDA 43 + GGA 131 + MGGA 106; confirmed on-disk 2026-05-24). MGGA 106 includes
the 14 mgga_c_tpssloc / mgga_c_revtpss _pK shard crates + their 2 facade crates. To confirm the live
count before asserting, run: `python3 tools/batched_compile_sweep.py --dry-run` and read total_packages,
or count subdir-with-Cargo.toml under crates/kernels/{lda,gga,mgga}.

f64 ONLY: do NOT set LIBXC_RS_F32. Every sweep invocation in this plan runs at f64 (the kernels'
only real precision). See the objective for why an f32 sweep is rejected.

### Machine constraints (NON-NEGOTIABLE)
- USER-RUN, multi-day. The ASSISTANT runs only `cargo tree` / `--dry-run`; the USER runs every real
  per-`-p` compile (OOM-prone on the 30 GB box). Mark the sweep invocations as user-run.
- .cargo/config.toml hand-pinned jobs = 1. DO NOT edit. The sweep tool's own --jobs is the sole
  exception (D-32) and is passed on the COMMAND LINE, not via config.
- Long job MUST be resumable/chunked. Run families separately; persist each segment manifest; the user
  paces with --wave N / overnight runs. A single unbroken multi-day session is a planning defect.
- NO cargo build --workspace. NO -p libxc_rs (umbrella). The sweep builds per-`-p` per subcrate.
- Scope every commit with `git commit --only -- <path>` (memory feedback_path_scoped_commits).
</context>

<threat_model>
Local codegen-compile tooling. NO network/auth/untrusted input. Real risk is a SILENTLY PARTIAL sweep
masquerading as ALL_OK (mitigated by record-count + zero-fail assertions) and OOM from over-wide jobs.

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-11-10-01 | Repudiation | Merged manifest accepted with < full roster (partial sweep) — including silently dropping the 14 _pK shard records | mitigate | Final assert: record count == len(build_roster()) (currently 280; >= 280 if the tree grows), zero result=fail, zero pass=-1. A count BELOW the roster means a partial sweep and MUST FAIL. |
| T-11-10-02 | DoS | jobs=3 batch OOMs on the 30 GB box | mitigate | Tool measures per-batch peak-RSS; if a batch exceeds the ~21 GB envelope, drop --jobs to 2 then 1 for that family and document. NEVER edit .cargo/config.toml. |
| T-11-10-03 | Tampering | A segment manifest clobbers a prior family's records (tool clears --manifest) | mitigate | Distinct --manifest per family segment; merge only after each segment PASSES |
| T-11-10-04 | Repudiation | A real translator defect (e.g. gga_c_gaploc P1 residue) is glossed as a splitter-floor exception | mitigate | Any fail is root-caused in tools/translate_v2/ + re-emitted per-functional; a documented exception requires an explicit splitter-floor rationale, not a compile error |
| T-11-10-05 | Repudiation | An f32 sweep is run "to be thorough" and a false f64-vs-f64 pass is recorded | mitigate | This plan is f64-ONLY by construction; LIBXC_RS_F32 is never set; f32 is a documented milestone follow-up (project_kernels_f64_concrete_f32_milestone), not a gate |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: f64 full-roster sweep, chunked per family, to distinct segment manifests, then merge + validate</name>
  <files>.cache/sweep-lda-manifest.json, .cache/sweep-gga-manifest.json, .cache/sweep-mgga-manifest.json, .cache/batched-compile-sweep-manifest.json, .planning/phases/11-splitter-v2-unified-5k-cap/11-10-SWEEP-LOG.md</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-15-SUMMARY.md (confirm the math test-module 0.10 migration landed — depends_on; the sweep's cargo-test path depends on it)
    - tools/batched_compile_sweep.py (main() arg parsing + run_sweep manifest-clear behavior at lines 359-364 + the 2-pass loop at 380-459 + build_roster at 81-100; note the docstring "266" comment is STALE — the runtime roster is 280)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md (11-07 LDA partial sweep: peak-of-peak 17.3 GB at jobs=3; lda_c_pk09 P2 failure — now Rule-3-fixed by 11.1)
    - .cargo/config.toml (read-only; confirm jobs=1 unchanged — do NOT edit)
  </read_first>
  <action>
Pre-flight (ASSISTANT-runnable, cheap):
- Confirm 11-15 landed: 11-15-SUMMARY.md exists and reports the `cargo build -p libxc-kernel-math --tests`
  gate green. If not, STOP — the sweep's cargo-test path will break on the residual 0.9 drift.
- Confirm .cargo/config.toml still has jobs = 1 (read-only; do NOT edit).
- Derive the authoritative roster count: run `python3 tools/batched_compile_sweep.py --dry-run` and
  capture total_packages (currently 280) — this is the target record count, NOT the stale "266" in the
  tool's docstring. Confirm the dry-run roster includes the mgga_c_tpssloc_p0..p6 and
  mgga_c_revtpss_p0..p6 shard packages (14) + the 2 facades; if any are missing the sweep is mis-scoped
  — surface it.

Step 1 — f64 sweep (USER-RUN, multi-day, paced). Run THREE INDEPENDENT, RESUMABLE per-family segments,
each to its own manifest path so a later segment does not clobber an earlier one (the tool clears
--manifest on start). f64 only — do NOT set LIBXC_RS_F32:

  python3 tools/batched_compile_sweep.py --families lda  --manifest .cache/sweep-lda-manifest.json  --summary .cache/sweep-lda-summary.md
  python3 tools/batched_compile_sweep.py --families gga  --manifest .cache/sweep-gga-manifest.json  --summary .cache/sweep-gga-summary.md
  python3 tools/batched_compile_sweep.py --families mgga --manifest .cache/sweep-mgga-manifest.json --summary .cache/sweep-mgga-summary.md

Pacing: these are designed to be run in separate sessions / overnight (user paces with --wave N). Within
a family, if a session is interrupted, resume with --start-after <last-ok-package> against the SAME
segment manifest. Record per-segment in 11-10-SWEEP-LOG.md: family, package range, VERDICT, peak-of-peak
peak_rss_mb, wall-clock.

OOM handling: if a jobs=3 batch's reported peak_rss_mb approaches ~21 GB, re-run that family with
--jobs 2 (then --jobs 1 if still tight) and note it. Do NOT edit .cargo/config.toml; pass --jobs on the
command line only (D-32).

FAILURE handling: any result=fail / pass=-1 is a real codegen defect. Root-cause in tools/translate_v2/
(P1 named-const-in-F-arithmetic / P2 bare-f64-tuple-member residue) and re-emit the affected functional
only via `python3 tools/maple_to_kernels.py translate --family <fam>` (or the per-func entry point), then
re-sweep that segment. Do NOT hand-edit crates/kernels/{lda,gga,mgga}/* (AP-3). Do NOT mark a compile
error as a splitter-floor exception.

Step 2 — Merge the f64 per-family segments into the canonical manifest path that 11-13 (G-5) consumes:
concatenate .cache/sweep-lda-manifest.json + .cache/sweep-gga-manifest.json +
.cache/sweep-mgga-manifest.json into .cache/batched-compile-sweep-manifest.json (NDJSON append order
LDA, GGA, MGGA). Use a small python3 script (read each segment line-by-line, write to the merged path).

Step 3 — Validate the merged manifest: record count == len(build_roster()) (currently 280; re-derive
via the --dry-run total_packages, do NOT hardcode), zero result=fail, zero pass=-1, and the 14 _pK shard
records (mgga_c_tpssloc_p0..p6, mgga_c_revtpss_p0..p6) all present. If validation fails, do NOT proceed —
a count below the roster means a partial/clobbered sweep; a failing package is a real defect (re-emit +
re-sweep) or a documented splitter-floor exception (explicit rationale required, not a compile error).

BANS: no .cargo/config.toml edits; no cargo build --workspace; no -p libxc_rs umbrella build; no
LIBXC_RS_F32; scope any commit with git commit --only -- on the .cache/* + 11-10-SWEEP-LOG.md paths.
  </action>
  <acceptance_criteria>All three per-family segment manifests exist; each segment's summary shows VERDICT: ALL_OK (or any non-ok package is root-caused + re-emitted + re-swept to ok); .cache/batched-compile-sweep-manifest.json exists with record count EQUALS len(build_roster()) (currently 280 = LDA 43 + GGA 131 + MGGA 106, where MGGA 106 includes the 14 mgga_c_tpssloc/mgga_c_revtpss _pK shard crates + 2 facades) with zero result=fail and zero pass=-1; the 14 _pK shard records are present (not dropped); LIBXC_RS_F32 was never set (f64-only); 11-10-SWEEP-LOG.md records each segment's verdict + peak-RSS.</acceptance_criteria>
  <verify><automated>test -f .cache/batched-compile-sweep-manifest.json</automated></verify>
  <done>f64 sweep ALL_OK across all roster-enumerated subcrates (currently 280) via chunked per-family segments; failures (if any) root-caused in the translator and re-swept; merged canonical manifest written + validated (record count == len(build_roster()), zero fail, zero pass=-1) for 11-13's SPEC-11-R4 ingest; per-segment log written. NO f32 sweep.</done>
</task>

</tasks>

<verification>
- f64 sweep VERDICT: ALL_OK across all roster-enumerated subcrates (per-family segment summaries)
- merged .cache/batched-compile-sweep-manifest.json record count == len(build_roster()) (currently 280, incl the 14 _pK shard records), zero fail, zero pass=-1, at f64
- NO f32 sweep ran (LIBXC_RS_F32 never set); f32 is a documented milestone follow-up, not a gate
- sweep was chunked/resumable per family; .cargo/config.toml untouched; no workspace/umbrella build
- 11-15 (math test-module 0.10 migration) landed first (depends_on)
</verification>

<success_criteria>
G-3 closed per 11-VERIFICATION.md Done-when (re-scoped f64-only): batched_compile_sweep.py manifest shows
all roster-enumerated subcrates (record count == len(build_roster()), currently 280) PASS at f64 (or any
exception explicitly excepted with splitter-floor rationale). No f32 sweep — f32 is a milestone-scale
follow-up per the 2026-05-23 f64-concrete decision.
</success_criteria>

<output>
After completion, create .planning/phases/11-splitter-v2-unified-5k-cap/11-10-SUMMARY.md (record the f64
verdict, per-family record counts, the live len(build_roster()) count used, peak-RSS envelope, any
re-emitted functionals, any documented exceptions, the merged manifest path for 11-13, and an explicit
note that NO f32 sweep was run because the kernels are f64-concrete).
</output>
