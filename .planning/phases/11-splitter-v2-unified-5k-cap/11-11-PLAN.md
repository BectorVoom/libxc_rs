---
phase: 11-splitter-v2-unified-5k-cap
plan: 11
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-11-IDEMPOTENCY-PROOF.md
autonomous: true
requirements: []
tags: [g4, d-lock-d, idempotency, translator, sharded, tpssloc, revtpss, hier-cse]

must_haves:
  truths:
    - "Translator idempotency is PROVEN for the 264 non-sharded functionals: a fresh translate --family all (vanilla, hier-CSE OFF) produces git diff --stat crates/kernels/ ZERO diff for those 264 (D-LOCK-D / ROADMAP SC #6)."
    - "mgga_c_tpssloc + mgga_c_revtpss (hier-CSE + split_per_functional_subcrate.py shards: facade + 7 _pK shards each) are handled SEPARATELY — either excluded-and-documented as a deterministic post-process, OR proven zero-diff via the full split pipeline replay — WITHOUT clobbering the OOM-fix layout."
    - "The proof never re-emits the two sharded functionals FLAT (a vanilla translate --family all would, clobbering project_tpssloc_oom_resolution); the OOM-fix facade/shard layout is preserved verbatim."
    - "The proof is translator-only (no cargo build) — low OOM risk; no monolithic build performed."
  artifacts:
    - path: ".planning/phases/11-splitter-v2-unified-5k-cap/11-11-IDEMPOTENCY-PROOF.md"
      provides: "D-LOCK-D proof: 264 zero-diff evidence + the tpssloc/revtpss disposition (exclude-and-document OR pipeline-replay) with the chosen option's evidence"
      contains: "SATISFIED"
  key_links:
    - from: "python3 tools/maple_to_kernels.py translate --family all (vanilla, hier-CSE OFF)"
      to: "crates/kernels/{lda,gga,mgga}/<func>/ (264 non-sharded functionals)"
      via: "re-emit then git diff --stat crates/kernels/ — expect zero diff for the 264"
      pattern: "translate --family all"
    - from: "tpssloc/revtpss split pipeline (LIBXC_RS_HIERARCHICAL_CSE=1 regen + split_per_functional_subcrate.py)"
      to: "the facade + 7 _pK shard layout per functional"
      via: "either exclude-and-document the split as a deterministic post-process, or replay it and prove zero-diff including shards"
      pattern: "split_per_functional_subcrate"
---

<objective>
Close gap **G-4**: prove translator idempotency (D-LOCK-D / ROADMAP SC #6). A vanilla python3
tools/maple_to_kernels.py translate --family all must produce a ZERO git diff for the 264 NON-sharded
functionals. The two sharded functionals — mgga_c_tpssloc (260520-eem) and mgga_c_revtpss (260520-k1q)
— were converted to a hierarchical-CSE + split_per_functional_subcrate.py layout (facade + 7 _pK
shards each) to clear a per-functional proc-macro OOM. A vanilla translate re-emits them FLAT, which
would CLOBBER that OOM fix (memory project_tpssloc_oom_resolution). So the proof must respect the split
pipeline.

Per 11.1-02-IDEMPOTENCY-DEFERRAL.md the executor chooses one of two dispositions for the sharded pair:
(a) exclude-and-document the split as a separate deterministic post-process and prove the other 264, OR
(b) make the split pipeline reproducible end-to-end and prove zero-diff including the shards.

This plan is translator-only (no cargo build) — low OOM risk. Independent of G-1/G-2/G-3 (no hard dep)
but easiest after G-3 (11-10) confirms the tree compiles. Output: 11-11-IDEMPOTENCY-PROOF.md with a
SATISFIED verdict.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VERIFICATION.md
@.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-02-IDEMPOTENCY-DEFERRAL.md
@CLAUDE.md
@tools/split_per_functional_subcrate.py

### Interfaces (translator + split pipeline — executor uses these directly)

- Vanilla regen: python3 tools/maple_to_kernels.py translate --family all (hier-CSE default OFF).
- tpssloc/revtpss split recipe (from STATE quick tasks 260520-eem / 260520-k1q):
  LIBXC_RS_HIERARCHICAL_CSE=1 regen of the functional, THEN python3
  tools/split_per_functional_subcrate.py shards the oversized output into facade + 7 _pK shards at a
  budget (~10000) keeping the public crate name. tpssloc was deferred from default-members; revtpss
  STAYS in default-members.
- Idempotency tool note: the legacy test_idempotency.sh FAILS on tool staleness
  (split_lda_subcrates.py references the pre-D-10a path crates/kernels/lda/src deleted in 11-03). Do
  NOT rely on that stale tool; perform the proof directly via translate + git diff --stat.

### Machine constraints (NON-NEGOTIABLE)
- .cargo/config.toml hand-pinned jobs = 1. DO NOT edit. This plan runs NO cargo build (translator-only).
- NO monolithic build. NO re-emit of tpssloc/revtpss FLAT (would clobber the OOM fix).
- Work on a clean tree; the proof is git diff --stat after a regen — commit/stash any unrelated dirty
  files first (per memory feedback_path_scoped_commits, scope commits with --only).
</context>

<threat_model>
Local codegen determinism proof. NO network/auth/untrusted input. Real risk is CODEGEN
NON-DETERMINISM (the thing being proven) and accidental clobber of the tpssloc/revtpss OOM fix.

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-11-11-01 | Tampering | Vanilla translate --family all re-emits tpssloc/revtpss FLAT, clobbering the facade/shard OOM fix | mitigate | EXCLUDE the two from the vanilla diff scope (option a) OR replay the split pipeline (option b); never commit a flat re-emit; restore via git checkout -- if a flat emit lands |
| T-11-11-02 | Repudiation | Zero-diff claimed without actually re-running the translator | mitigate | Proof = git diff --stat output captured AFTER a real translate run; the proof doc embeds the diff stat |
| T-11-11-03 | Tampering | Regen leaves the working tree dirty and contaminates a later plan's commit scope | mitigate | After capturing the diff, git checkout -- crates/kernels/ (or commit if zero-diff, which is a no-op) to restore a clean tree |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Prove zero-diff idempotency for the 264 non-sharded functionals</name>
  <files>.planning/phases/11-splitter-v2-unified-5k-cap/11-11-IDEMPOTENCY-PROOF.md</files>
  <read_first>
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-02-IDEMPOTENCY-DEFERRAL.md (the two options + why vanilla translate re-emits the sharded pair flat)
    - tools/maple_to_kernels.py (translate --family driver entry; confirm hier-CSE default OFF)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md (test_idempotency.sh A5 tool-staleness note — do not use that stale tool)
  </read_first>
  <action>
Step 0 — Ensure a clean working tree (git status --porcelain empty for crates/kernels/; stash/commit
unrelated dirty files first, scoping with --only per memory feedback_path_scoped_commits).

Step 1 — Run a vanilla full regen (hier-CSE OFF):
  python3 tools/maple_to_kernels.py translate --family all
(tee the output). This re-emits ALL functionals from Maple, including — problematically — tpssloc and
revtpss FLAT. That flat re-emit is EXPECTED and will be excluded from the 264-scope diff in Task 2.

Step 2 — Capture the diff, EXCLUDING the two sharded functionals' directories:
  git diff --stat -- crates/kernels/ ':(exclude)crates/kernels/mgga/mgga_c_tpssloc/**' ':(exclude)crates/kernels/mgga/mgga_c_revtpss/**'
The 264 non-sharded functionals MUST show ZERO diff. If any non-sharded functional shows a diff, that
is real codegen non-determinism — root-cause in tools/translate_v2/ (e.g. dict ordering, set
iteration, timestamp) and fix until the re-emit is byte-stable. Document the root cause if found.

Step 3 — Record the 264 zero-diff evidence (the git diff --stat output) in
11-11-IDEMPOTENCY-PROOF.md.

Step 4 — Restore the clean tree: git checkout -- crates/kernels/ (discards the regen; for the 264 it
is a no-op since zero-diff, for the sharded pair it discards the unwanted flat re-emit). Confirm
git status --porcelain is clean for crates/kernels/ afterward.

BANS: no cargo build of any kind; no commit of a flat tpssloc/revtpss re-emit; no .cargo/config.toml edits.
  </action>
  <acceptance_criteria>11-11-IDEMPOTENCY-PROOF.md exists and embeds the git diff --stat output showing ZERO diff for the 264 non-sharded functionals (the sharded pair excluded); any non-determinism found was root-caused + fixed; the working tree is restored clean afterward.</acceptance_criteria>
  <verify><automated>test -f .planning/phases/11-splitter-v2-unified-5k-cap/11-11-IDEMPOTENCY-PROOF.md</automated></verify>
  <done>264 non-sharded functionals proven byte-stable under re-translate; evidence captured; tree restored.</done>
</task>

<task type="auto">
  <name>Task 2: Disposition mgga_c_tpssloc + mgga_c_revtpss (sharded pair) without clobbering the OOM fix</name>
  <files>.planning/phases/11-splitter-v2-unified-5k-cap/11-11-IDEMPOTENCY-PROOF.md</files>
  <read_first>
    - tools/split_per_functional_subcrate.py (the sharder — facade + _pK shards; budget arg)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-02-IDEMPOTENCY-DEFERRAL.md (options a/b)
    - STATE.md quick tasks 260520-eem (tpssloc) + 260520-k1q (revtpss): the split recipe (LIBXC_RS_HIERARCHICAL_CSE=1 regen + split at budget ~10000; facade + 7 shards; <=70 parts/shard heuristic)
  </read_first>
  <action>
Choose and document ONE disposition for the sharded pair:

Option A (exclude-and-document — RECOMMENDED, lower risk): document in 11-11-IDEMPOTENCY-PROOF.md that
mgga_c_tpssloc + mgga_c_revtpss are produced by a DETERMINISTIC two-stage post-process (vanilla
hier-CSE regen + split_per_functional_subcrate.py at a fixed budget), and that vanilla translate
--family all intentionally does NOT reproduce their facade/shard layout. Demonstrate determinism by
re-running ONLY the split stage on an already-hier-regen'd input and showing the SHARD outputs are
byte-stable (git diff --stat on the shard dirs == zero after a repeat split). Do NOT run a flat
translate that would overwrite the committed facade/shard layout.

Option B (full pipeline replay — higher effort): replay LIBXC_RS_HIERARCHICAL_CSE=1 regen +
split_per_functional_subcrate.py end-to-end for BOTH functionals at the committed budget, then git
diff --stat the two functionals' dirs and prove ZERO diff including shards. This requires the regen +
split steps to be deterministic end-to-end; if they are not, fix the non-determinism or fall back to
Option A.

Whichever option: the committed facade + 7 _pK shard layout (project_tpssloc_oom_resolution) MUST be
preserved verbatim — never leave a flat re-emit in the tree. If a flat emit landed during Task 1, it
was discarded by Task 1 Step 4's git checkout.

Write the chosen disposition + its evidence into 11-11-IDEMPOTENCY-PROOF.md and conclude the file with
an explicit SATISFIED verdict line (D-LOCK-D: 264 zero-diff + sharded pair handled per chosen option).
Restore a clean tree at the end.

BANS: no cargo build; no flat re-emit committed; no .cargo/config.toml edits.
  </action>
  <acceptance_criteria>11-11-IDEMPOTENCY-PROOF.md documents the chosen disposition (A or B) with evidence for the sharded pair, the facade/shard layout is preserved (git status clean, no flat re-emit), and the file contains a SATISFIED verdict line.</acceptance_criteria>
  <verify><automated>grep -q 'SATISFIED' .planning/phases/11-splitter-v2-unified-5k-cap/11-11-IDEMPOTENCY-PROOF.md</automated></verify>
  <done>D-LOCK-D proven: 264 zero-diff + tpssloc/revtpss handled per chosen option with the OOM-fix layout preserved; SATISFIED verdict recorded.</done>
</task>

</tasks>

<verification>
- 264 non-sharded functionals show zero git diff under a vanilla re-translate (proof doc embeds the stat)
- tpssloc + revtpss dispositioned per chosen option without clobbering the facade/shard OOM-fix layout
- no flat re-emit committed; working tree restored clean; no cargo build performed
- 11-11-IDEMPOTENCY-PROOF.md ends with a SATISFIED verdict
</verification>

<success_criteria>
G-4 closed per 11-VERIFICATION.md Done-when: zero-diff demonstrated for the 264, and tpssloc/revtpss
handled per chosen option with the OOM-fix layout preserved.
</success_criteria>

<output>
After completion, create .planning/phases/11-splitter-v2-unified-5k-cap/11-11-SUMMARY.md (record the
264 zero-diff result, any non-determinism root-caused/fixed, the chosen sharded-pair disposition, and
the SATISFIED verdict).
</output>
