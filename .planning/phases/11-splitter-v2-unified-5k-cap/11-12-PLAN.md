---
phase: 11-splitter-v2-unified-5k-cap
plan: 12
type: execute
wave: 2
gap_closure: true
depends_on:
  - 11-09
  - 11-14
files_modified:
  - Cargo.toml
  - verify/Cargo.toml
  - verify/tests/lda_oracle.rs
  - verify/tests/gga_oracle.rs
  - verify/tests/mgga_oracle.rs
  - crates/kernels/math/tests/f32_tolerance_overrides.toml
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-12-ORACLE-F32-LOG.md
autonomous: false
requirements: []
tags: [g2, g4, full-649-oracle, f32, family-chunked, memory-safe, tolerance-override, D-12, D-24]

must_haves:
  truths:
    - "A memory-safe full-649 oracle path exists that does NOT require the OOMing monolithic all-281-kernel build: the oracle is family-chunked / feature-gated so each family's oracle runs against only its kernels, in segments runnable under jobs=1 on the 30 GB box."
    - "The full-649 f32 oracle sweep (LIBXC_RS_F32=1) runs to COMPLETION via the chunked path with the von Weizsacker tau-clamp from G-1 (11-09) applied — so MGGA failures reflect real f32 codegen, not the missing regularization."
    - "All functionals pass within per-functional tolerance bounded by the 1e-3 HARD CEILING (D-12/D-24); any residual failure is ATTRIBUTED (not silently passed); any per-functional override is recorded with rationale and stays under 1e-3."
    - "No single monolithic build/test invocation is used; the path is chunked + jobs=1 + resumable."
  artifacts:
    - path: ".planning/phases/11-splitter-v2-unified-5k-cap/11-12-ORACLE-F32-LOG.md"
      provides: "The G4 f32 oracle outcome: per-family pass/fail counts, per-functional overrides + rationale, residual-failure attribution, peak-RSS, the chunking design used"
      contains: "1e-3"
    - path: "crates/kernels/math/tests/f32_tolerance_overrides.toml"
      provides: "Updated per-functional f32 tolerance map (default 1e-6; Brent-class 1e-4; any G4-discovered overrides), hard ceiling 1e-3"
      contains: "default"
  key_links:
    - from: "the family-chunked oracle path (feature-gated kernel deps per family)"
      to: "verify/tests/{lda,gga,mgga}_oracle.rs"
      via: "run each family's oracle against only its kernels (feature gate or per-family verify build), in segments"
      pattern: "_oracle"
    - from: "src/eval/mgga_dispatch/prepare.rs::tau_von_weizsacker (G-1 / 11-09)"
      to: "the f32 MGGA oracle run"
      via: "production dispatch already applies the clamp; the f32 oracle inherits it (no canary host-driver special-casing)"
      pattern: "tau_von_weizsacker"
---

<objective>
Close gap **G-2**: (a) build a MEMORY-SAFE way to evaluate the full-649 oracle WITHOUT the OOMing
monolithic all-281-kernel build, and (b) run G4 (LIBXC_RS_F32=1 oracle sweep) with per-functional
tolerance overrides bounded by the 1e-3 HARD CEILING (D-12/D-24). G4 is ROADMAP SC #5 / 11-08 Task 3.

Blocker (11.1-03-G4-DEFERRAL.md): the full oracle exercises every functional through
libxc_rs::eval::dispatch_*, which statically links the whole kernel tree via the libxc_rs umbrella ->
OOMs at jobs=3, multi-hour + OOM-risk at jobs=1 on the 30 GB box. The deferral PROPOSES the unblocking
design: feature-gate libxc_rs kernel deps by family so each family's oracle runs against only its
kernels, in chunks.

DEPENDS ON G-1 (11-09): without the production von Weizsacker tau-clamp, the MGGA oracle fails widely
for non-precision reasons. 11-09 lands first.

DEPENDS ON G-6 (11-14): the family-chunked oracle runs through libxc_rs::eval::dispatch_*, so the
umbrella libxc_rs lib must compile under cubecl 0.10 first. That umbrella-compile prerequisite
is satisfied by 11-14 (cubecl-0.10 launch-ABI migration); 11-14 lands before this plan.

Output: a family-chunked oracle path + a completed G4 f32 sweep with attributed results and a
1e-3-ceilinged tolerance map.

Why autonomous: false — ACCEPTING per-functional tolerance-override VALUES (within the 1e-3 ceiling)
and attributing residual failures is human judgment (plan_authoring_rules: "accepting
tolerance-override values in G-2"). Task 3 is a checkpoint.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VERIFICATION.md
@.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-03-G4-DEFERRAL.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-09-PLAN.md
@CLAUDE.md

### Interfaces (extracted)

f32 tolerance infra (11-FINAL-METRICS.md D-19c table + verify/tests/parity_phase11.rs):
- crates/kernels/math/tests/f32_tolerance_overrides.toml: default 1e-6; Brent-class (mgga_c_b94,
  mgga_x_br89, mgga_x_br89_explicit, mgga_x_mbr, mgga_x_mbrxc_bg, mgga_x_mbrxh_bg, mgga_x_mggac) 1e-4;
  HARD CEILING 1e-3 asserted in f32_tolerance_for.
- env gate LIBXC_RS_F32=1 enables f32; f64 is default.

Oracle harness: verify/tests/lda_oracle.rs, gga_oracle.rs, mgga_oracle.rs (the *_oracle.rs form —
confirm exact names via ls verify/tests/) cover routed functionals at f64 1e-12; they need the f32
env-gate + per-functional tolerance pattern to opt into f32.

Kernel deps: root Cargo.toml + verify/Cargo.toml pull the libxc_rs umbrella (all 281). The
family-chunked design feature-gates kernel deps by family so a single family's oracle compiles only
its kernels.

G-1 dependency: src/eval/mgga_dispatch/prepare.rs::tau_von_weizsacker (landed in 11-09) is applied in
production dispatch — the f32 MGGA oracle inherits it automatically.

### Machine constraints (NON-NEGOTIABLE)
- .cargo/config.toml hand-pinned jobs = 1. DO NOT edit. No parallel/multi-job cargo.
- NO monolithic all-281-kernel build — the whole point of G-2 is to AVOID it. Each family's oracle
  builds only its family's kernels (feature-gated).
- HIGH OOM risk: chunked + jobs=1 + resumable; families in separate segments / overnight; user paces
  with --wave N. A single unbroken multi-day session is a planning defect.
- Per-`-p` compile is an ENTRY gate: the family-gated verify build must compile before its oracle runs.
</context>

<threat_model>
Local oracle parity testing + cargo feature plumbing. NO network/auth/untrusted input. Real risk is
SILENT NUMERICAL INCORRECTNESS (a failing functional masked by an over-wide tolerance) and OOM from the
umbrella build.

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-11-12-01 | Repudiation | Real f32 codegen failure hidden by ratcheting tolerance past 1e-3 | mitigate | f32_tolerance_for asserts the 1e-3 HARD CEILING; entries above it panic; Task 3 checkpoint requires explicit acceptance of each override |
| T-11-12-02 | DoS | Family-gated build still pulls the full umbrella -> OOM | mitigate | cargo tree spot-check confirms a per-family build pulls only that family's kernels; jobs=1; narrow to sub-family chunks if OOM. Task 2 RE-CONFIRMS this with cargo tree before each f32 segment build starts. |
| T-11-12-03 | Repudiation | Full-649 sweep reported complete with families silently skipped | mitigate | 11-12-ORACLE-F32-LOG.md records per-family functional counts summing to the routed total; residual failures attributed, not dropped |
| T-11-12-04 | Tampering | MGGA f32 failures attributed to f32 when the cause is the missing tau-clamp | mitigate | depends_on 11-09 (G-1) ensures the production clamp is present; MGGA oracle inherits it |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the memory-safe family-chunked oracle path (feature-gated kernel deps)</name>
  <files>Cargo.toml, verify/Cargo.toml, verify/tests/lda_oracle.rs, verify/tests/gga_oracle.rs, verify/tests/mgga_oracle.rs</files>
  <read_first>
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-03-G4-DEFERRAL.md (proposed feature-gate-by-family design + why the umbrella OOMs)
    - root Cargo.toml [dependencies] + [features] (the 281 kernel path-deps; default-members)
    - verify/Cargo.toml (how verify depends on libxc_rs / kernels)
    - verify/tests/lda_oracle.rs, gga_oracle.rs, mgga_oracle.rs (existing 649-functional harness shape; confirm filenames via ls verify/tests/ — the real files are the *_oracle.rs form, NOT oracle_*.rs)
  </read_first>
  <action>
Pre-flight: ls verify/tests/ and confirm the oracle harness files are lda_oracle.rs, gga_oracle.rs,
mgga_oracle.rs (the *_oracle.rs form). Edit THOSE files — do NOT create phantom oracle_*.rs files.

Design a family-chunked oracle path so each family's oracle compiles only its family's kernels.
RECORD which of the two paths below you landed (feature-gate vs per-family target) in
11-12-ORACLE-F32-LOG.md — Task 2 keys its run commands off this:

Step 1 — PREFERRED (Path A, feature-gate): Add per-family feature sets to root Cargo.toml +
verify/Cargo.toml (e.g. oracle-lda / oracle-gga / oracle-mgga) gating the kernel deps + dispatch arms
so a build with only oracle-mgga pulls only the MGGA kernel crates. Confirm via cargo tree -e features
that a single-family feature build does NOT pull the other families' kernels.

  FALLBACK (Path B, per-family target): If feature-gating the umbrella is infeasible, create per-family
  verify targets (e.g. distinct [[test]] entries or a thin per-family verify sub-crate) that dep ONLY
  that family's kernel crates directly, bypassing the umbrella.

Step 2 — Wire the f32 env-gate (LIBXC_RS_F32=1) + per-functional tolerance lookup from
parity_phase11.rs into {lda,gga,mgga}_oracle.rs (the deferral noted this as a Phase-12 follow-up; this
gap pulls it forward as the G-2 vehicle). f64 stays default; LIBXC_RS_F32=1 opts into f32 with the
1e-3-ceilinged per-functional tolerance.

Step 3 — Compile-gate each family oracle target (ENTRY gate, jobs=1). For Path A:
  cargo build -p libxc_rs-verify --features oracle-mgga --no-default-features
For Path B: cargo build the per-family target. Either MUST compile WITHOUT pulling all 281 kernels. If
a family build still OOMs, narrow to sub-family chunks (e.g. MGGA alphabetical halves) and document.

BANS: no .cargo/config.toml edits; no --jobs over 1; no cargo build --workspace; no all-family build in
one invocation.
  </action>
  <acceptance_criteria>Per-family oracle feature sets (Path A) OR per-family verify targets (Path B) exist, and the chosen path is RECORDED in 11-12-ORACLE-F32-LOG.md; cargo tree confirms a single-family build pulls only that family's kernels; the real {lda,gga,mgga}_oracle.rs files (not phantom oracle_*.rs) honor LIBXC_RS_F32=1 + the per-functional tolerance lookup; each family oracle target compiles under jobs=1 without the 281-kernel umbrella.</acceptance_criteria>
  <verify><automated>grep -q oracle verify/Cargo.toml</automated></verify>
  <done>A memory-safe family-chunked oracle path exists and compiles per-family under jobs=1 without the umbrella build; the landed path (A or B) is recorded; oracle harness honors the f32 env-gate + tolerance lookup.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full-649 f32 oracle sweep, chunked per family, with tau-clamp inherited from G-1</name>
  <files>.planning/phases/11-splitter-v2-unified-5k-cap/11-12-ORACLE-F32-LOG.md</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-09-PLAN.md (confirm G-1 tau-clamp landed in src/eval/mgga_dispatch/prepare.rs before running MGGA)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-12-ORACLE-F32-LOG.md (Task 1 recorded which path landed — Path A feature-gate vs Path B per-family target — this keys the run commands below)
    - crates/kernels/math/tests/f32_tolerance_overrides.toml (current default 1e-6 / Brent 1e-4 / ceiling 1e-3)
    - verify/tests/{lda,gga,mgga}_oracle.rs (the f32-gated harness from Task 1)
  </read_first>
  <action>
Confirm the production tau-clamp from 11-09 is present (grep tau_von_weizsacker in
src/eval/mgga_dispatch/prepare.rs). If absent, HALT — G-2's MGGA results are meaningless without it.

The exact run command depends on which path Task 1 landed (read it from 11-12-ORACLE-F32-LOG.md).
BEFORE each family segment build starts, RE-CONFIRM memory-safety with cargo tree so a regression to
the all-kernel graph is caught BEFORE the (potentially OOMing) build kicks off:

  cargo tree -e features <chosen-invocation-flags-for-this-family> | grep -E 'libxc-kernel-(lda|gga|mgga)' | sort -u

This MUST list ONLY the current family's kernels. If it lists another family's kernels (or the full
281), HALT — the feature-gate/target regressed; do NOT start the build (it would OOM).

Then run the f32 oracle per family, each as a separate jobs=1 segment (LIBXC_RS_F32=1), pacing across
overnight runs. Use the form matching the landed path:

  Path A (feature-gate, PREFERRED):
    LIBXC_RS_F32=1 cargo test -p libxc_rs-verify --features oracle-lda  --no-default-features -- --test-threads=1 --nocapture
    LIBXC_RS_F32=1 cargo test -p libxc_rs-verify --features oracle-gga  --no-default-features -- --test-threads=1 --nocapture
    LIBXC_RS_F32=1 cargo test -p libxc_rs-verify --features oracle-mgga --no-default-features -- --test-threads=1 --nocapture

  Path B (per-family target, FALLBACK — use the exact target names Task 1 created):
    LIBXC_RS_F32=1 cargo test -p <verify-lda-target>  --test lda_oracle  -- --test-threads=1 --nocapture
    LIBXC_RS_F32=1 cargo test -p <verify-gga-target>  --test gga_oracle  -- --test-threads=1 --nocapture
    LIBXC_RS_F32=1 cargo test -p <verify-mgga-target> --test mgga_oracle -- --test-threads=1 --nocapture

(Use ONLY the form for the path Task 1 actually landed; the other is dead. Tee each to
.cache/11-12-oracle-f32-<fam>.log.)

For each family record in 11-12-ORACLE-F32-LOG.md: the cargo tree re-confirmation result, functionals
run, pass count, fail count with per-functional max_rel_err, and peak-RSS. The sum of functionals run
across families MUST equal the routed total (the full-649 / routed subset) — no silent family skips.

For each functional failing at the current tolerance: capture its actual max_rel_err. Classify as (i)
f32 convergence noise (candidate for a per-functional override up to but not exceeding 1e-3), or (ii)
a real f32 codegen defect (override would exceed 1e-3 -> NOT acceptable). Do NOT yet edit the TOML —
collect candidates for the Task 3 checkpoint.

BANS: no .cargo/config.toml edits; no --jobs over 1; no all-family build in one invocation; no
auto-fixing a failing functional by silently widening tolerance past 1e-3; do NOT start a family build
whose cargo tree shows cross-family/all-281 kernels.
  </action>
  <acceptance_criteria>For each family the cargo tree re-confirmation (only that family's kernels) was recorded BEFORE its build; the run command used matches Task 1's landed path (A or B); 11-12-ORACLE-F32-LOG.md records per-family functional counts summing to the routed total, pass/fail counts, and per-functional max_rel_err for every failure; the tau-clamp presence was confirmed before the MGGA run; every family sweep completed under jobs=1 without the umbrella build.</acceptance_criteria>
  <verify><automated>test -f .planning/phases/11-splitter-v2-unified-5k-cap/11-12-ORACLE-F32-LOG.md</automated></verify>
  <done>The full-649 f32 oracle ran to completion via the chunked path (run command keyed to Task 1's landed path, cargo tree re-confirmed per family) with the production tau-clamp applied; per-functional max_rel_err collected for all failures; override candidates staged for Task 3.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Accept per-functional f32 tolerance overrides (within 1e-3) and attribute residual failures</name>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-12-ORACLE-F32-LOG.md (Task 2's per-functional max_rel_err table + override candidates)
    - crates/kernels/math/tests/f32_tolerance_overrides.toml (the map being amended; 1e-3 hard ceiling)
  </read_first>
  <decision>Which per-functional f32 tolerance overrides (each strictly under the 1e-3 hard ceiling) are ACCEPTED, and how is each residual failure attributed?</decision>
  <context>
G4 / D-24 requires the full-649 f32 sweep to pass within per-functional tolerance bounded by the 1e-3
HARD CEILING. Per plan_authoring_rules, ACCEPTING tolerance-override values is human judgment — the
executor presents Task 2's measured max_rel_err per failing functional and proposes overrides (set to
the smallest power of 10 >= measured error, capped under 1e-3). Any functional whose error exceeds 1e-3
is a real f32 codegen defect, NOT an override candidate — it must be attributed (root-caused or flagged
as a known limitation), not silently passed.
  </context>
  <options>
    <option id="accept-overrides">
      <name>Accept the proposed per-functional overrides (each under 1e-3) and attribute any >1e-3 failures</name>
      <pros>Closes G4 within the D-24 ceiling; documents the f32 convergence behavior of ill-conditioned (Brent-class) functionals per D-19c.</pros>
      <cons>Each override widens the f32 gate for that functional — must be justified by genuine convergence noise, not a masked codegen bug.</cons>
    </option>
    <option id="defer-residuals">
      <name>Accept overrides for clear convergence cases; defer/flag any ambiguous &gt;1e-3 functionals as documented known-limitations</name>
      <pros>Avoids passing a masked codegen bug; keeps the 1e-3 ceiling meaningful.</pros>
      <cons>G4 closes with explicitly-attributed residuals rather than a clean 100% pass.</cons>
    </option>
  </options>
  <acceptance_criteria>User reviews the per-functional max_rel_err table and approves: (1) the set of overrides to write into f32_tolerance_overrides.toml (each strictly under 1e-3, with rationale); (2) the attribution for any functional exceeding 1e-3. After approval, the executor writes the approved overrides to the TOML, re-runs the affected family segment to confirm green, and records the final disposition in 11-12-ORACLE-F32-LOG.md.</acceptance_criteria>
  <resume-signal>Approve the override set + residual attributions, or request changes. Confirm no override exceeds the 1e-3 hard ceiling.</resume-signal>
</task>

</tasks>

<verification>
- family-chunked oracle path compiles per-family under jobs=1 without the 281-kernel umbrella (cargo tree spot-check)
- full-649 f32 oracle ran to completion via the chunked path with the G-1 tau-clamp applied
- all functionals within per-functional tolerance <= 1e-3 ceiling; residual failures attributed, not silently passed
- f32_tolerance_overrides.toml updated with approved overrides (each under 1e-3); 11-12-ORACLE-F32-LOG.md records the outcome
- no monolithic build; .cargo/config.toml untouched
</verification>

<success_criteria>
G-2 closed per 11-VERIFICATION.md Done-when: full-649 f32 oracle runs to completion via the chunked
path; all functionals within per-functional tolerance <= 1e-3 ceiling, with any residual failures
attributed (not silently passed).
</success_criteria>

<output>
After completion, create .planning/phases/11-splitter-v2-unified-5k-cap/11-12-SUMMARY.md (record the
family-chunked oracle design + landed path (A/B), per-family f32 pass/fail counts, the approved override
set + rationale, residual-failure attribution, and peak-RSS envelope).
</output>
</output>
