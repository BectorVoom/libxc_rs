---
phase: 11-splitter-v2-unified-5k-cap
plan: 13
type: execute
wave: 3
gap_closure: true
depends_on:
  - 11-09
  - 11-10
  - 11-11
  - 11-12
files_modified:
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md
  - .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/model/mgga_functional.rs
  - tools/split_oversized_kernel.py
  - tools/split_oversized_mgga.py
  - tools/split_mgga_7_kcis.py
  - tools/rebatch_mgga.py
  - tools/regen_phase09.py
autonomous: false
requirements: []
tags: [g5, closure, phase-complete, 11-06-legs, 11-08-task2, cleanup, f64-only, f32-milestone, mgga-parity, D-11]

must_haves:
  truths:
    - "11-06-SUMMARY.md is rewritten PARTIAL -> COMPLETE honestly, reflecting that G-1..G-4 (11-09/10/11/12) landed at f64: Gate 3 Legs 2/4 (parity f64 + idempotency) + Task 8 close against real f64 evidence. Leg 3 (parity f32) is DISPOSITIONED as a milestone-scale follow-up (kernels f64-concrete), NOT claimed as passed. The FAILED-iteration history is PRESERVED (AP-5)."
    - "11-08 Task 2 cleanup executed: CLAUDE.md KEEPS 'Precision: f64 only; energy relative error <= 10^-12 vs libxc oracle' and ADDS an f32-is-a-milestone-follow-up note (the OLD 'f32 secondary 1e-6' string is INVERTED away); ROADMAP SC #4 -> manifest record count == len(build_roster()) = 280, 'NOT a phase gate'; ROADMAP SC #5 + G4 -> 'full-649 f32 oracle is a MILESTONE-scale follow-up, NOT a Phase-11 gate'; the 5 obsolete numbered-subcrate-era tools deleted; LIBXC_RS_BYPASS_DEFERRED removed from src/model/mgga_functional.rs (D-11 production semantics restored)."
    - "A NEW dedicated ROADMAP entry (Phase 12 — MGGA f64 Parity) is ADDED recording the 6 routed exc functionals that fail vs the C oracle at f64 (mgga_x_th 2.0e-1, mgga_x_2d_js17 1.1e-2, mgga_c_cs 9.2e-3, mgga_x_pkzb 3.7e-3, mgga_x_pbe_gx 1.5e-3, mgga_x_tm 9.2e-4); root cause = per-functional translation bug (mgga_x_th likely) + residual work_mgga regularization beyond the G-1 tau-clamp (the tau-clamp IS applied @ mgga_dispatch/mod.rs:280-282 and is NOT the cause). This is RECORDED as out-of-Phase-11 follow-up work, NOT implemented in 11-13."
    - "tools/batched_compile_sweep.py + tools/refactor_helpers_generic.py + tools/refactor_test_fixtures/symbol_class_matrix.rs are RETAINED (NOT deleted) per D-28/D-31."
    - "Phase 11 is closed ROADMAP/STATE-effective: ROADMAP marks all gap plans [x] (incl 11-10/11-12/11-13/11-15) + flips the stale checklist wording + adds a Progress row; STATE.md records Phase 11 complete. (gsd-sdk phase.complete handler is unimplemented here — closure is manual, see commit c4480dd881.)"
    - "All G-5 work is gated on G-1..G-4 actually passing at f64 — the honest SUMMARY can only be written after 11-09..12 land (Task 1 checkpoint)."
  artifacts:
    - path: ".planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md"
      provides: "Rewritten COMPLETE summary: Gate 3 Legs 2/4 + Task 8 closed against 11-09/11-11/11-12 f64 evidence; Leg 3 (f32) dispositioned as a milestone follow-up; FAILED-iteration history preserved"
      contains: "COMPLETE"
    - path: "CLAUDE.md"
      provides: "Precision policy KEPT at 'f64 only / 1e-12' + f32-milestone-follow-up note added"
      contains: "Precision"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11 SC #4 (280, NOT a phase gate) + SC #5/G4 (f32 = milestone follow-up) corrected; all gap plans [x] + Progress row; NEW Phase 12 MGGA f64 Parity entry"
      contains: "NOT a phase gate"
  key_links:
    - from: "11-09/11-11/11-12 f64 evidence (tau-clamp + g1 parity + idempotency proof + family-chunked f64 oracle log) + 11-10 merged manifest"
      to: ".planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md (rewritten COMPLETE)"
      via: "the honest rewrite cites each gap's closing artifact at f64; f32 dispositioned as milestone"
      pattern: "COMPLETE"
    - from: "src/model/mgga_functional.rs (LIBXC_RS_BYPASS_DEFERRED env-gate, line 132)"
      to: "D-11 production deferred-rejection semantics"
      via: "remove the bypass branch so is_deferred IDs are unconditionally rejected"
      pattern: "LIBXC_RS_BYPASS_DEFERRED"
    - from: "11-12-SUMMARY.md (6 MGGA f64 parity failures attributed + routed)"
      to: ".planning/ROADMAP.md new Phase 12 — MGGA f64 Parity entry"
      via: "record the 6 functionals + root-cause as a dedicated follow-up phase (RECORDED, not implemented)"
      pattern: "MGGA f64 Parity"
---

<objective>
Close gap **G-5** (GATED on G-1..G-4 at f64): the final phase-close work, with the stale f32 wording
INVERTED to match the 2026-05-23 f64-concrete / f32-defer decision (11-12-SUMMARY). (a) Rewrite
11-06-SUMMARY.md PARTIAL -> COMPLETE honestly against 11-09/11-11/11-12 f64 evidence (Leg 3 f32
dispositioned as a milestone follow-up, NOT claimed passed). (b) Execute 11-08 Task 2 with the f32
wording INVERTED: KEEP CLAUDE.md "Precision: f64 only / 1e-12" + add an f32-milestone note; fix ROADMAP
SC #4 (manifest record count == len(build_roster()) = 280, "NOT a phase gate") + SC #5/G4 (f32 oracle =
milestone follow-up, not a gate); delete the 5 obsolete tools; remove the LIBXC_RS_BYPASS_DEFERRED
env-gate (D-11 restored). (c) ADD a new ROADMAP Phase 12 — MGGA f64 Parity entry recording the 6 routed
exc functionals that fail vs the C oracle at f64 (RECORDED, not implemented). (d) Close Phase 11
ROADMAP/STATE-effective (gsd-sdk phase.complete handler unimplemented — manual, see commit c4480dd881).

WHY THE INVERSION: the old 11-13 (and the 11-08 Task-2 FROM/TO strings it copied) encode the OPPOSITE of
the locked decision — they would replace "f64 only" with "f32 secondary 1e-6 env-gated" and treat a
"full-649 f32 oracle within 1e-3" as a Phase-11 gate. That is now WRONG: the kernels are f64-CONCRETE
(2491 files &Array<f64>, 0 generic), so an f32 oracle would be a false f64-vs-f64 pass (threat
T-11-12-01, user-rejected). f32 support is a MILESTONE-scale follow-up (translator re-arch + full regen).
The checkpoint and the SUMMARY rewrite verify the **f64** evidence actually produced, NOT a re-deferred
f32 oracle.

Per gap index G-5: an HONEST SUMMARY can only be written AFTER G-1..G-4 land — hence depends_on
11-09/10/11/12, Wave 3.

Output: rewritten 11-06-SUMMARY.md, updated CLAUDE.md + ROADMAP.md (incl new Phase 12 entry) + STATE.md,
5 tools deleted, bypass var removed, Phase 11 closed.

Why autonomous: false — the FINAL honesty check (does the rewritten COMPLETE SUMMARY truthfully reflect
G-1..G-4 f64 evidence? is Phase 11 genuinely closeable?) is a human gate. Task 1 is a checkpoint.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-VERIFICATION.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-08-PLAN.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-12-SUMMARY.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md
@CLAUDE.md
@src/model/mgga_functional.rs

### Interfaces (11-08 Task 2 edits — with the f32 wording INVERTED per the 2026-05-23 decision)

CLAUDE.md "### Constraints" — the f32 inversion:
- KEEP the precision bullet essentially as-is: `- **Precision**: f64 only; energy relative error <=
  10^-12 vs libxc oracle`. Do NOT replace it with the OLD (now WRONG) 11-08 string "f64 is the primary
  correctness target at 1e-12; f32 is a secondary correctness target at 1e-6 ... LIBXC_RS_F32=1".
- ADD one new bullet immediately after the precision bullet:
  `- **f32 support**: a documented MILESTONE-scale follow-up (translator re-architecture to emit
  float-generic kernels + full ~2491-file regen + FP-order reconciliation), NOT a current target — the
  kernels are f64-concrete by design (2491 files &Array<f64>, 0 generic). See memory
  project_kernels_f64_concrete_f32_milestone.`
- The "Operation order" bullet: LEAVE AS-IS. Do NOT apply the old D-05 softening ("oracle gate at 1e-12
  replaces the prior bit-exact f64 requirement"). Evidence (11-12-ORACLE-F32-LOG.md line 27) states the
  translator STILL "preserves exact maple2c FP operation order" — bit-exact operation order was NOT
  abandoned, so the softening is not supported. Keep the existing wording.
- Preserve all other constraint bullets verbatim. (Note: the "Tech stack: CubeCL 0.9.0" / "Dependencies:
  cubecl 0.9.0" bullets are stale vs the 0.10 bump but are OUT OF SCOPE for this plan — do NOT touch.)

ROADMAP Phase 11 Success Criteria corrections (section starts at ROADMAP line 252):
- #4 (currently "cargo build --workspace succeeds on the user's RAM-constrained machine") -> "Per-functional
  subcrate compile coverage is empirically verified via batched_compile_sweep.py's manifest evidence;
  cargo build --workspace is NOT a phase gate (manifest at .cache/batched-compile-sweep-manifest.json is
  the canonical evidence — record count == len(build_roster()), currently 280 on-disk packages at f64,
  NOT the 266 logical-functional figure)".
- #5 (currently "Oracle parity preserved at the gate level chosen during discussion") -> append: "...
  at f64 (energy relative error <= 1e-12 vs libxc oracle). A full-649 f32 oracle is a MILESTONE-scale
  follow-up, NOT a Phase-11 gate (kernels are f64-concrete; see Phase 12 — MGGA f64 Parity for the
  routed f64 residuals)".
- #7 -> "CubeCL macro fan-out audit clean per D-13 per-design budget: (a) each routed (functional,
  output) module has exactly one #[cube(launch_unchecked)]; (b) unrouted functionals carry zero
  launchables; (c) crates/kernels/math/src/ count <=22".
- Any remaining "full-649 f32 oracle" / "G4 ... within 1e-3" line in the Phase 11 plan checklist or
  closure notes -> re-scope to "f64 oracle (f32/G4 re-deferred milestone-scale)".

ROADMAP Phase 11 plan-checklist FLIPS (the stale-wording corrections):
- 11-12 line: `[ ]` -> `[x]`; correct "G4 f32 sweep within the 1e-3 hard ceiling" -> "memory-safe
  family-chunked f64 oracle (LDA ✓ GGA ✓; MGGA 6/12 exc routed to Phase 12 — f32/G4 re-deferred
  milestone-scale)".
- 11-10 line: `[ ]` -> `[x]`; correct "all 266 subcrates at f64+f32" -> "280 on-disk packages at f64
  (f32 dropped — kernels f64-concrete)".
- 11-13 line: `[ ]` -> `[x]`. 11-15 line: add `[x] 11-15-PLAN.md — math test-module cubecl-0.10
  launch-ABI migration (G-3 pre-req)`.

NEW ROADMAP entry — Phase 12 — MGGA f64 Parity (planner's reasonable default placement — see note below):
- Add a top-level checkbox in the "## Phases" list (after the Phase 11.1 line at ROADMAP line 23):
  `- [ ] **Phase 12: MGGA f64 Parity** - Fix the 6 routed MGGA exc functionals that fail vs the libxc C
  oracle at f64 (per-functional translation debug + residual work_mgga regularization beyond the
  von Weizsäcker tau-clamp)`.
- Add a "### Phase 12: MGGA f64 Parity" detail section AFTER the Phase 11.1 detail section (after ROADMAP
  line 337), with: Goal (bring the 6 functionals within 1e-12 of the libxc f64 oracle); Depends on
  (Phase 11 closure — the family-chunked f64 oracle that surfaced them); Success Criteria (each of
  mgga_x_th, mgga_x_2d_js17, mgga_c_cs, mgga_x_pkzb, mgga_x_pbe_gx, mgga_x_tm passes the MGGA oracle at
  the f64 tier); Driver (the 6 failures + rel_err: mgga_x_th 2.0e-1 · mgga_x_2d_js17 1.1e-2 · mgga_c_cs
  9.2e-3 · mgga_x_pkzb 3.7e-3 · mgga_x_pbe_gx 1.5e-3 · mgga_x_tm 9.2e-4; root cause = per-functional
  translation bug (mgga_x_th most likely) + residual work_mgga regularization beyond the G-1 tau-clamp
  which IS applied @ mgga_dispatch/mod.rs:280-282 and is NOT the cause; source = 11-12-SUMMARY.md). Plans:
  TBD (will be decomposed during /gsd-plan-phase). This is RECORDED ONLY — do NOT implement any fix here.

  PLACEMENT NOTE (flag for the user at the Task 1 checkpoint): the ROADMAP has no backlog/Phase-12 section
  today, so the planner's reasonable default is a new dedicated Phase 12 entry. If the user prefers a
  backlog note or a different home for the MGGA-parity work, redirect at the checkpoint before writing.

Obsolete tools to DELETE (5, all confirmed present 2026-05-24): tools/split_oversized_kernel.py,
tools/split_oversized_mgga.py, tools/split_mgga_7_kcis.py, tools/rebatch_mgga.py, tools/regen_phase09.py.
RETAIN (do NOT delete, all confirmed present 2026-05-24): tools/batched_compile_sweep.py,
tools/refactor_helpers_generic.py, tools/refactor_test_fixtures/symbol_class_matrix.rs, all
tools/translate_v2/*, all audit tools, the dispatch generators, the family translators,
tools/maple_to_kernels.py, tools/kernel_routing.py, tools/split_per_functional_subcrate.py.

LIBXC_RS_BYPASS_DEFERRED removal (src/model/mgga_functional.rs:132): the env-var bypass branch wraps the
is_deferred rejection — remove it so is_deferred IDs are unconditionally rejected (D-11). The current code
(lines 127-140) reads `if is_deferred_mgga(id.raw()) { if std::env::var("LIBXC_RS_BYPASS_DEFERRED")... !=
Ok("1") { return Err(...UnsupportedFunctional...) } }`. After removal it must unconditionally
`return Err(...UnsupportedFunctional...)` for deferred IDs (drop the inner env-var `if` + the
fall-through comment). Grep src/ for LIBXC_RS_BYPASS_DEFERRED -> zero references.

phase.complete: gsd-sdk handler unimplemented; closure is ROADMAP/STATE-effective + manual (see commit
c4480dd881). Mark ROADMAP gap plans [x] + add Progress row; update STATE.md status to Phase 11 complete.

### Machine constraints (NON-NEGOTIABLE)
- .cargo/config.toml hand-pinned jobs = 1. DO NOT edit. This plan is docs/cleanup — minimal cargo
  (only a re-confirm that bypass removal compiles, jobs=1, via the LIGHT model-crate path — NO umbrella
  build).
- NO monolithic build. NO -p libxc_rs umbrella build. NO LIBXC_RS_F32.
- Scope EVERY commit with `git commit --only -- <path>` (memory feedback_path_scoped_commits; sessions
  open with thousands of pre-staged files).
- Do NOT delete batched_compile_sweep.py / refactor_helpers_generic.py / symbol_class_matrix.rs.
</context>

<threat_model>
Docs + cleanup + manual phase close. NO network/auth/untrusted input. Real risk is a DISHONEST CLOSE
(SUMMARY claims COMPLETE without G-1..G-4 f64 evidence, or claims an f32 pass that never happened),
re-introducing the inverted f32 wording, deleting a still-needed tool, and a bypass-removal that misses a
caller.

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-11-13-01 | Repudiation | Phase closed COMPLETE while a gap (G-1..G-4) is still red at f64, or an f32 oracle pass is fabricated | mitigate | Task 1 checkpoint requires the f64 gap-evidence artifacts to exist + pass before any close edit; NO f32-within-1e-3 requirement is asserted; depends_on 11-09..12 |
| T-11-13-02 | Tampering | A RETAINED tool accidentally deleted | mitigate | Explicit DELETE list of exactly 5; post-delete test -f confirms the 3 retained files survive |
| T-11-13-03 | Elevation of Privilege | LIBXC_RS_BYPASS_DEFERRED removal misses a caller -> deferred kernel reachable in production | mitigate | grep src/ for the env-var after removal confirms zero references; D-11 rejection restored |
| T-11-13-04 | Tampering | Path-scoped commit sweeps thousands of pre-staged files into the close commit | mitigate | git commit --only -- <path> for every commit (memory feedback_path_scoped_commits) |
| T-11-13-05 | Repudiation | The INVERTED (wrong) f32 wording is re-introduced into CLAUDE.md / ROADMAP ("f32 secondary 1e-6", "full-649 f32 oracle gate") | mitigate | CLAUDE.md must still contain "Precision: f64 only" + the f32-milestone bullet; ROADMAP #5/G4 must say "MILESTONE-scale follow-up, NOT a Phase-11 gate"; grep-verify both |
| T-11-13-06 | Repudiation | The 6 MGGA f64 parity failures are dropped on the floor (not recorded anywhere) | mitigate | A new ROADMAP Phase 12 entry must name all 6 functionals + root cause; grep-verify "MGGA f64 Parity" + the 6 names |

</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm G-1..G-4 f64 evidence exists + passes, and confirm the Phase 12 placement, before closing Phase 11</name>
  <what-built>11-09 (G-1 tau-clamp + g1 f64 parity), 11-10 (G-3 full-roster f64 sweep -> merged manifest, record count == len(build_roster()) = 280), 11-11 (G-4 idempotency proof SATISFIED), 11-12 (G-2 family-chunked f64 oracle — LDA/GGA pass, 6 MGGA exc attributed + routed) have all executed and produced their f64 evidence artifacts.</what-built>
  <how-to-verify>
1. G-1: src/eval/mgga_dispatch/prepare.rs contains tau_von_weizsacker; the verify-canary g1 parity test PASSES at 1e-12 (read 11-09-SUMMARY.md). NO f32 requirement.
2. G-3: .cache/batched-compile-sweep-manifest.json record count EQUALS len(build_roster()) (currently 280 = LDA 43 + GGA 131 + MGGA 106, incl the 14 mgga_c_tpssloc/mgga_c_revtpss _pK shard crates + 2 facades — NOT 266), zero result=fail, zero pass=-1, at f64 — read 11-10-SUMMARY.md / 11-10-SWEEP-LOG.md. Re-derive the expected count via `python3 tools/batched_compile_sweep.py --dry-run` total_packages; do NOT compare against a hardcoded 266. Confirm NO f32 sweep was run.
3. G-4: 11-11-IDEMPOTENCY-PROOF.md ends with SATISFIED (264 zero-diff + sharded pair dispositioned).
4. G-2: 11-12-SUMMARY.md shows the family-chunked f64 oracle ran to completion — LDA ✓, GGA ✓, MGGA 6/12 routed exc failures ATTRIBUTED + routed to a dedicated MGGA-parity effort (NOT silently passed). NO "full-649 f32 oracle within 1e-3" requirement — that was re-deferred milestone-scale (read 11-12-SUMMARY §Deviations + §"Next Phase Readiness").
5. Phase 12 placement: confirm the planner's default (a NEW dedicated "Phase 12 — MGGA f64 Parity" ROADMAP entry) is acceptable, OR redirect to a backlog note / other home before Task 2/3 write it.
If ANY of the four f64 gaps is missing/red, do NOT proceed to close — surface which gap is outstanding.
  </how-to-verify>
  <acceptance_criteria>All four f64 gap-evidence artifacts exist and report PASS/SATISFIED/attributed-not-passed; the G-3 manifest record count equals len(build_roster()) (currently 280), not 266; NO f32 oracle pass is claimed anywhere; the user confirms Phase 11 is genuinely closeable AND confirms (or redirects) the Phase 12 — MGGA f64 Parity placement.</acceptance_criteria>
  <resume-signal>Type "all f64 gaps closed — proceed; Phase 12 placement OK" or name the outstanding gap(s) / a different Phase 12 home.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 11-08 Task 2 cleanup with f32 wording INVERTED — CLAUDE.md + ROADMAP criteria + new Phase 12 entry + delete 5 tools + remove LIBXC_RS_BYPASS_DEFERRED</name>
  <files>CLAUDE.md, .planning/ROADMAP.md, src/model/mgga_functional.rs, tools/split_oversized_kernel.py, tools/split_oversized_mgga.py, tools/split_mgga_7_kcis.py, tools/rebatch_mgga.py, tools/regen_phase09.py</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-08-PLAN.md Task 2 (the ORIGINAL FROM/TO strings — but note: the f32-wording FROM/TO here is now INVERTED per the context block; use THIS plan's context block, not 11-08's, for the precision + ROADMAP #4/#5 strings)
    - CLAUDE.md "### Constraints" subsection (lines 10-17: keep "Precision: f64 only", add f32-milestone bullet, leave Operation-order bullet)
    - .planning/ROADMAP.md Phase 11 Success Criteria (lines 252-261) + the "## Phases" list (line 23, Phase 11.1) + the end of the Phase 11.1 detail section (line 337) for the new Phase 12 entry
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-12-SUMMARY.md (the 6 MGGA failures + rel_err + root-cause for the Phase 12 entry)
    - src/model/mgga_functional.rs lines 126-141 (the is_deferred_mgga + LIBXC_RS_BYPASS_DEFERRED branch)
    - tools/refactor_helpers_generic.py + tools/refactor_test_fixtures/symbol_class_matrix.rs (RETAIN — verify they exist before/after)
  </read_first>
  <action>
Step 1 — CLAUDE.md precision policy (INVERTED): KEEP `- **Precision**: f64 only; energy relative error
<= 10^-12 vs libxc oracle` verbatim. ADD a new bullet immediately after it:
`- **f32 support**: a documented MILESTONE-scale follow-up (translator re-architecture to emit
float-generic kernels + full ~2491-file regen + FP-order reconciliation), NOT a current target — the
kernels are f64-concrete by design (2491 files &Array<f64>, 0 generic). See memory
project_kernels_f64_concrete_f32_milestone.` LEAVE the "Operation order" and "GPU precision" bullets and
all other constraint bullets verbatim (do NOT apply the old D-05 softening; do NOT touch the 0.9.0
tech-stack bullets — out of scope). Commit with `git commit --only -- CLAUDE.md`.

Step 2 — ROADMAP Phase 11 SC corrections per the context block: #4 -> manifest record count ==
len(build_roster()) = 280 on-disk packages at f64, "cargo build --workspace is NOT a phase gate"; #5 ->
append "at f64 (1e-12); full-649 f32 oracle is a MILESTONE-scale follow-up, NOT a Phase-11 gate"; #7 ->
the per-design fan-out wording (math/src/ <=22). Leave #0/#1/#2/#3/#6/#8 unchanged. Also FLIP the stale
plan-checklist lines: 11-12 `[ ]`->`[x]` (re-word "G4 f32 sweep within 1e-3" -> "family-chunked f64
oracle; MGGA 6/12 routed to Phase 12; f32/G4 re-deferred milestone-scale"); 11-10 `[ ]`->`[x]` (re-word
"all 266 subcrates at f64+f32" -> "280 on-disk packages at f64; f32 dropped"); 11-13 `[ ]`->`[x]`; add
11-15 `[x]`. Commit with `git commit --only -- .planning/ROADMAP.md`.

Step 3 — ADD the new Phase 12 — MGGA f64 Parity ROADMAP entry per the context block (top-level checkbox
after the Phase 11.1 line + a "### Phase 12: MGGA f64 Parity" detail section after the Phase 11.1 detail
section), naming ALL 6 functionals (mgga_x_th 2.0e-1, mgga_x_2d_js17 1.1e-2, mgga_c_cs 9.2e-3,
mgga_x_pkzb 3.7e-3, mgga_x_pbe_gx 1.5e-3, mgga_x_tm 9.2e-4) + the root-cause (per-functional translation
+ residual work_mgga regularization; tau-clamp IS applied @ mgga_dispatch/mod.rs:280-282 and is NOT the
cause). RECORD ONLY — implement no fix. (Honor the user's Task-1 redirect if they chose a different home.)
This may be folded into the same ROADMAP commit as Step 2, or a separate `git commit --only --
.planning/ROADMAP.md`.

Step 4 — Delete exactly the 5 obsolete tools (git rm): split_oversized_kernel.py, split_oversized_mgga.py,
split_mgga_7_kcis.py, rebatch_mgga.py, regen_phase09.py. Before deleting, grep tools/ scripts/ src/
verify/ for each tool name to confirm no surviving caller; if a caller exists, surface it (do not delete
blindly). After deletion, confirm the 3 RETAINED files still exist (test -f batched_compile_sweep.py,
refactor_helpers_generic.py, refactor_test_fixtures/symbol_class_matrix.rs). Commit with `git commit
--only --` on the 5 deleted paths.

Step 5 — Remove the LIBXC_RS_BYPASS_DEFERRED env-gate from src/model/mgga_functional.rs (lines 126-141):
replace the `if is_deferred_mgga(id.raw()) { if std::env::var("LIBXC_RS_BYPASS_DEFERRED")... != Ok("1") {
return Err(...) } }` block with an UNCONDITIONAL `if is_deferred_mgga(id.raw()) { return
Err(...UnsupportedFunctional...) }` (drop the inner env-var `if` + the fall-through comment) so is_deferred
IDs are always rejected (D-11). Grep src/ for LIBXC_RS_BYPASS_DEFERRED -> zero references. Compile-gate
(LIGHT, jobs=1): `cargo check -p libxc_rs --lib --no-default-features` (math+core only, NO kernels — the
no-default-features path proven EXIT-0 by 11-12; do NOT run a default/all-family check which pulls all
280 kernels and OOMs). Commit with `git commit --only -- src/model/mgga_functional.rs`.

BANS: no .cargo/config.toml edits; no --jobs over 1; no cargo build --workspace; no default-features
umbrella check; do NOT delete the 3 retained tools; do NOT re-introduce "f32 secondary 1e-6" or
"full-649 f32 oracle gate" wording; scope every commit with --only.
  </action>
  <acceptance_criteria>CLAUDE.md still contains "Precision" + "f64 only" AND a new bullet containing "MILESTONE-scale follow-up" + "f64-concrete"; CLAUDE.md does NOT contain "f32 is a secondary correctness target"; ROADMAP #4 contains "NOT a phase gate" + "280"; ROADMAP contains "MILESTONE-scale follow-up" for the f32 oracle AND a new "Phase 12: MGGA f64 Parity" entry naming all 6 functionals (mgga_x_th, mgga_x_2d_js17, mgga_c_cs, mgga_x_pkzb, mgga_x_pbe_gx, mgga_x_tm); the 5 obsolete tools are gone (git rm); the 3 retained tools still exist (test -f); `grep -r LIBXC_RS_BYPASS_DEFERRED src/` returns ZERO lines; `cargo check -p libxc_rs --lib --no-default-features` exits 0.</acceptance_criteria>
  <verify><automated>grep -q 'f64 only' CLAUDE.md && grep -q 'MILESTONE-scale follow-up' CLAUDE.md && ! grep -q 'f32 is a secondary correctness target' CLAUDE.md && ! grep -rq 'LIBXC_RS_BYPASS_DEFERRED' src/ && grep -q 'MGGA f64 Parity' .planning/ROADMAP.md</automated></verify>
  <done>CLAUDE.md keeps "f64 only" + adds the f32-milestone bullet (inverted, not "f32 secondary 1e-6"); ROADMAP #4/#5/#7 corrected + checklist flipped + new Phase 12 MGGA f64 Parity entry added; 5 obsolete tools deleted (3 retained survive); LIBXC_RS_BYPASS_DEFERRED removed and D-11 rejection restored; the no-default-features lib check is green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite 11-06-SUMMARY.md COMPLETE (f64 evidence; f32 dispositioned) + finalize 11-FINAL-METRICS.md + close Phase 11 in ROADMAP/STATE</name>
  <files>.planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md, .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md (current PARTIAL; the Tasks-pending / Gate-3-Legs table to flip)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-09-SUMMARY.md, 11-10-SUMMARY.md, 11-11-SUMMARY.md, 11-12-SUMMARY.md (the f64 gap evidence to cite)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md (the DEFERRED F32 rows — re-disposition as milestone-follow-up, NOT "passed")
    - .planning/ROADMAP.md Phase 11 plan checklist + Progress table (after Task 2's edits)
    - .planning/STATE.md status line + Current Position
  </read_first>
  <action>
Step 1 — Rewrite 11-06-SUMMARY.md PARTIAL -> COMPLETE against f64 evidence: flip the Gate-3-Legs table —
Leg 1 (compile) already proven; Leg 2 (parity f64) closed citing 11-09 (G-1 production tau-clamp + g1
parity at 1e-12) + 11-12 (family-chunked f64 oracle: LDA ✓ GGA ✓); Leg 4 (idempotency) closed citing
11-11 (SATISFIED). Leg 3 (parity f32): mark DISPOSITIONED — re-deferred as a milestone-scale follow-up
(kernels f64-concrete; 11-12-SUMMARY §Deviations) — do NOT mark it "passed". Task 8 closed. Change the
top-level Status/Outcome to COMPLETE. PRESERVE the historical session arc / FAILED-iteration history
verbatim (AP-5 — do not rewrite history). Note Leg 2/4 parity is now demonstrated via the production
dispatch path with the G-1 tau-clamp (not the canary host-driver workaround). Also record the 6 MGGA f64
parity residuals routed to Phase 12. Commit with `git commit --only --`.

Step 2 — Update 11-FINAL-METRICS.md: re-disposition the DEFERRED F32 rows (F32 smoke, F32 full-649
sweep) as "milestone-scale follow-up — NOT a Phase-11 gate (kernels f64-concrete)", NOT as passed; flip
the manifest-ingest row to cite 11-10's merged manifest (record count == len(build_roster()) = 280
on-disk packages at f64); update test_idempotency disposition to reference 11-11's SATISFIED proof. Commit
with `git commit --only --`.

Step 3 — Close Phase 11 in ROADMAP.md: confirm 11-09/10/11/12/13/15 are all [x] in the Phase 11 plan
list (Task 2 flipped most; ensure 11-15 is present + [x]), update the Phase 11 status line to COMPLETE,
and add a Progress-table row "11. Splitter v2 5K Cap | <N>/<N> | Complete | 2026-05-XX". Commit with
`git commit --only --`.

Step 4 — Close Phase 11 in STATE.md: update the status / Current Position to record Phase 11 COMPLETE (all
5 original gaps closed at f64 + G-6; G-1 tau-clamp in production; full-roster f64 sweep ALL_OK (280
records); idempotency SATISFIED; family-chunked f64 oracle ran — LDA/GGA pass, 6 MGGA exc routed to
Phase 12; f32/G4 re-deferred milestone-scale). Note phase.complete handler is unimplemented -> manual
ROADMAP/STATE close (cite c4480dd881). Commit with `git commit --only --`.

BANS: no .cargo/config.toml edits; no cargo build; do NOT mark the f32 oracle "passed"; do NOT rewrite
the FAILED-iteration history (AP-5); scope every commit with --only.
  </action>
  <acceptance_criteria>11-06-SUMMARY.md top-level status is COMPLETE and cites 11-09/11-11/11-12 f64 evidence for Legs 2/4 with Leg 3 (f32) DISPOSITIONED as a milestone follow-up (not "passed"); the FAILED-iteration history is still present; 11-FINAL-METRICS.md F32 rows say "milestone-scale follow-up" (not passed) + manifest row cites record count 280; ROADMAP marks 11-09..13 + 11-15 [x] + has a Phase 11 Complete Progress row; STATE.md records Phase 11 COMPLETE with the 6 MGGA residuals routed to Phase 12.</acceptance_criteria>
  <verify><automated>grep -q COMPLETE .planning/phases/11-splitter-v2-unified-5k-cap/11-06-SUMMARY.md</automated></verify>
  <done>11-06-SUMMARY honestly COMPLETE against G-1..G-4 f64 evidence (f32 dispositioned, history preserved); 11-FINAL-METRICS finalized (f32 = milestone, manifest = 280); Phase 11 closed ROADMAP/STATE-effective (manual, handler unimplemented).</done>
</task>

</tasks>

<verification>
- G-1..G-4 f64 evidence confirmed present + passing before any close edit (Task 1 checkpoint); NO f32-oracle pass claimed
- CLAUDE.md keeps "f64 only" + adds f32-milestone bullet (inverted, not "f32 secondary 1e-6"); Operation-order bullet untouched
- ROADMAP #4 ("NOT a phase gate", 280) + #5/G4 ("milestone follow-up") + #7 corrected; checklist flipped; new Phase 12 MGGA f64 Parity entry naming all 6 functionals
- 5 obsolete tools deleted; 3 retained tools survive; LIBXC_RS_BYPASS_DEFERRED removed (zero src/ refs); no-default-features lib check green
- 11-06-SUMMARY rewritten COMPLETE citing real f64 evidence (AP-5 history preserved, f32 dispositioned); 11-FINAL-METRICS finalized
- Phase 11 closed ROADMAP/STATE-effective (manual; handler unimplemented per c4480dd881)
- no .cargo/config.toml edit; no monolithic/umbrella build; all commits --only-scoped
</verification>

<success_criteria>
G-5 closed per 11-VERIFICATION.md Done-when: SUMMARYs reflect reality (f64 evidence; f32 dispositioned as
milestone), the inverted f32 wording is corrected (CLAUDE.md stays "f64 only"; ROADMAP f32 = milestone
follow-up not a gate), the 6 MGGA f64 parity residuals are recorded as a dedicated Phase 12 follow-up,
obsolete tooling/env-gate removed, ROADMAP/STATE mark Phase 11 complete.
</success_criteria>

<output>
After completion, create .planning/phases/11-splitter-v2-unified-5k-cap/11-13-SUMMARY.md (record the f64
gap-evidence confirmation, the inverted f32 wording corrections, the new Phase 12 entry, the cleanup
edits, the bypass removal, and the Phase 11 close disposition).
</output>
