---
phase: 11.1-translator-rule-3-emit-fix-sweep-to-green
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ROADMAP-11.1-SC8   # Phase 11 phase.complete invocation unblocked
  - CONTEXT-D-01       # 11.1 hands BACK to re-opened Phase 11; do NOT close Phase 11 from inside 11.1
user_setup: []

must_haves:
  truths:
    - "11.1-SUMMARY.md exists and captures: all 4 ALL_OK gates verdict (G1 f64 sweep, G2 f32 sweep, G3 mgga_c_b94 1e-12 parity, G4 full-649 f32 oracle with tolerance overrides), D-LOCK-D idempotency proof, translator amendment summary (Plan 01), iteration count consumed (Plan 02 fix loop), tolerance entries added (Plan 03 G4)"
    - "11.1-SUMMARY.md explicitly states the Phase 11 hand-back per CONTEXT.md D-01: the items NOT closed by 11.1 (11-06 Legs 2/3/4 + Task 8, 11-08 Task 2, phase.complete 11 invocation) are documented as ready for the re-opened Phase 11 to pick up"
    - "ROADMAP.md Phase 11.1 entry marked complete (all 8 Success Criteria checked off OR documented HALT for any escalated ceiling-breach)"
    - "STATE.md updated: phase 11.1 status moves from `Planning complete` to `Complete` (or `HALT-escalated` if any of G3/G4 hit a structural-precision escalation); position cursor advances to Phase 11 re-open OR Phase 12"
    - "phase.complete 11.1 invoked via gsd-sdk OR equivalent state machinery; Phase 11.1 closure committed to git"
  artifacts:
    - path: ".planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md"
      provides: "Phase-level summary covering all 4 plans + 4 gates + idempotency + hand-back"
      contains: "VERDICT"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11.1 entry marked complete; success criteria 1-8 status updated"
      contains: "Phase 11.1"
    - path: ".planning/STATE.md"
      provides: "Phase 11.1 status updated; position cursor advanced"
      contains: "11.1"
  key_links:
    - from: ".planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md"
      to: "Plans 01/02/03 SUMMARYs"
      via: "phase-level rollup"
      pattern: "Plan 01|Plan 02|Plan 03"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "phase status sync"
      pattern: "Phase 11.1"
---

<objective>
Close Phase 11.1 by rolling up the four plan-level summaries (01 translator amend, 02 regen + G1+G2 sweep + idempotency, 03 G3 parity + G4 oracle) into a single phase summary, marking the ROADMAP entry complete, updating STATE.md, and explicitly handing back the deferred Phase 11 closure items per CONTEXT.md D-01 to a re-opened Phase 11.

Purpose: keeps the GSD planning ledger consistent — 11.1's narrow translator-fix scope shipped, the broader Phase 11 closure work (11-06 Legs 2/3/4, 11-08 Task 2, phase.complete 11) hands BACK to a re-opened Phase 11 without 11.1 swallowing those deliverables.

Output: 11.1-SUMMARY.md, updated ROADMAP.md, updated STATE.md, phase.complete invocation evidence.
</objective>

<execution_context>
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-CONTEXT.md
@.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-01-PLAN.md
@.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-02-PLAN.md
@.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-03-PLAN.md

<!-- The three plan SUMMARYs are read at execution time, not at planning time;
     they're created by Plans 01/02/03 respectively. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 11.1-SUMMARY.md rolling up Plans 01/02/03 + the 4 ALL_OK gate verdicts + the D-01 Phase 11 hand-back</name>
  <files>.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md</files>
  <read_first>
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-01-SUMMARY.md (Plan 01 translator amend summary)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-02-SUMMARY.md (Plan 02 sweep + idempotency summary)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-03-SUMMARY.md (Plan 03 G3 + G4 summary)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-02-SWEEP-SUMMARY.md (G1 verdict — should be ALL_OK)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-02-SWEEP-SUMMARY-f32.md (G2 verdict — should be ALL_OK)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-02-IDEMPOTENCY-PROOF.md (D-LOCK-D zero-diff proof)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-03-G3-PARITY-LOG.md (G3 verdict)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-03-G4-ORACLE-LOG.md (G4 verdict)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-CONTEXT.md § domain section (the 4 ALL_OK gates definition) + § decisions D-01 (hand-back scope)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md (Phase 11 PARTIAL state — for the "what hands back" listing)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-08-SUMMARY.md (Phase 11 PARTIAL close — the deliverables 11.1 explicitly does NOT touch)
  </read_first>
  <action>
    Write `.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md` using the GSD summary template structure (see @/home/chemtech/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md for canonical shape). Required sections:

    ```markdown
    # Phase 11.1: Translator Rule 3 Emit Fix + Sweep-to-Green — SUMMARY

    **Status:** COMPLETE | HALT-ESCALATED
    **Date completed:** <UTC date>
    **Plans:** 4 (all executed; see per-plan SUMMARYs for detail)
    **Duration:** <plan execution wall-clock total>
    **Closing commit:** <git SHA>

    ## Phase Goal Recap

    [Quote ROADMAP § Phase 11.1 Goal verbatim. ~3 sentences.]

    ## The 4 ALL_OK Gates — Final Verdict Table

    | Gate | Description | Verdict | Evidence |
    |---|---|---|---|
    | G1 | batched_compile_sweep f64 ALL_OK across 266+ subcrates | <PASS|FAIL> | 11.1-02-SWEEP-SUMMARY.md, 11.1-02-SWEEP-MANIFEST.ndjson |
    | G2 | batched_compile_sweep f32 ALL_OK (LIBXC_RS_F32=1 env-gate) | <PASS|FAIL> | 11.1-02-SWEEP-SUMMARY-f32.md, 11.1-02-SWEEP-MANIFEST-f32.ndjson |
    | G3 | mgga_c_b94 f64 parity at 1e-12 relative error | <PASS|FAIL> | 11.1-03-G3-PARITY-LOG.md |
    | G4 | Full-649 f32 oracle at 1e-6 (per-functional overrides bounded by 1e-3 ceiling) | <PASS|HALT-CEILING> | 11.1-03-G4-ORACLE-LOG.md, 11.1-03-G4-TOLERANCE-DIAGNOSES.md |

    ## D-LOCK-D Idempotency Proof

    Second `python3 tools/maple_to_kernels.py translate --family all` produced `git diff --stat -- crates/kernels/` = 0 lines. See 11.1-02-IDEMPOTENCY-PROOF.md.

    ## Plan-Level Rollup

    ### Plan 01 — Translator Amend (D-04..D-10, D-15, D-17)

    [Quote 11.1-01-SUMMARY.md key bullets: files modified, line-delta, allowlist member count, named-const count, pytest pass status, deviations including the D-10 line-367 nuance.]

    ### Plan 02 — Regen + G1 + G2 + D-LOCK-D Idempotency (D-13, D-16, D-18, D-LOCK-D)

    [Quote 11.1-02-SUMMARY.md key bullets: regen ok counts per family, fix-loop iterations consumed (0/1/2/3), G1+G2 sweep stats (pkg count, wall time, peak RSS), two known exemplar status (gga_c_gaploc P1 + lda_c_pk09 P2), idempotency line count = 0.]

    ### Plan 03 — G3 Parity + G4 Oracle (D-02, D-03, D-12, D-20)

    [Quote 11.1-03-SUMMARY.md key bullets: G3 mgga_c_b94 status, count of other phase11_worst_case offenders (documented as Phase 11 hand-back), G4 per-family PASS counts, new tolerance overrides count + rationale breakdown.]

    ### Plan 04 — Phase Close (D-01 hand-back)

    [This plan's actions: SUMMARY rollup, ROADMAP update, STATE update, phase.complete invocation.]

    ## Phase 11 Hand-Back Per D-01 (NOT closed by 11.1)

    Per CONTEXT.md D-01 (locked: 11.1 narrow scope), the following items hand BACK to a re-opened Phase 11. They are READY for execution against the now-compile-green tree from 11.1:

    1. **11-06 Task 6 Legs 2/3/4** — full f64 worst-case parity beyond mgga_c_b94 smoke (Leg 2); full f32 worst-case parity at 1e-6 (Leg 3); idempotency proof at Phase-11 scope (Leg 4 — Plan 02's translator-specific D-LOCK-D proof covers the translator-fix-specific case; Phase 11 Leg 4 is the broader claim across the entire kernel regen pipeline).
    2. **11-06 Task 8** — rewrite 11-06-SUMMARY.md from PARTIAL to COMPLETE.
    3. **11-08 Task 2** — CLAUDE.md edits (D-03a/D-19a wording), ROADMAP success criteria #1/#4/#7 correction, delete 5 obsolete tools (split_oversized_*, rebatch_mgga.py, split_mgga_7_kcis.py — these assume the pre-D-10a numbered-subcrate layout), `LIBXC_RS_BYPASS_DEFERRED` removal once the deferred set has a permanent runtime gate.
    4. **`phase.complete 11` invocation** — depends on items 1-3 above + 11.1's deliverables (delivered here).

    See `.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md` § "Phase 11.1 Follow-Up Scope" for the original scope statement; this hand-back closes that loop.

    ## What Survived from Phase 11 (durable across 11.1's translator changes)

    - `tools/batched_compile_sweep.py` — codifies the per-`-p` compile entry gate
    - D-10a clean-slate restructure (266 per-functional subcrates)
    - D-13 launch budget invariant (1654 routed / 0 unrouted / 22 math/src/)
    - 11-PATTERN.md Rules 1-10 (Rule 10 § "Phase-2 extension" SUPERSEDED annotation added by Plan 01 Task 3)
    - Phase-2 math/ generic conversions (9 files; preserved unchanged across 11.1)
    - f32_tolerance_overrides.toml infrastructure (extended by Plan 03 Task 2 with N new entries)
    - LIBXC_RS_F32 env-gated parity infra

    ## New Phase 11.1 Artifacts

    - `tools/translate_v2/helpers_allowlist.py` (D-09 hardcoded allowlist; 14 generic-helper modules + 3 non-generic exclusions; 22 named-const D-19 name map)
    - `tools/translate_v2/cse.py` extensions (PositionContext, Classification enum, classify_position, collect_named_const_uses, HOIST_PRELUDE_TEMPLATE)
    - `tools/translate_v2/per_functional.py` amendments (_wrap_f64_literals_v2; hoisted-binding prelude emission; D-10 chunk-to-chunk `::<F>` regex; D-08 native helper turbofish)
    - `tools/translate_v2/test_classifier.py` + `tools/translate_v2/test_emit.py` (pytest unit tests; regression-test surface for any future iteration)
    - Family translator simplification (Deviation E/F Block B deleted; Block A reduced to name-remap-only) in translate_lda_v2.py / translate_gga.py / translate_mgga.py
    - 11-PATTERN.md § Rule 10 SUPERSEDED annotation

    ## Lessons Learned (for memory + RETROSPECTIVE.md absorption)

    - **D-04 comprehensive-coverage paid off:** Plan 02's fix loop consumed [N] iterations (target was 0-1; max budget 3). [If 0: ideal — comprehensive AST classifier caught everything pre-empirically.] [If 1-2: residual P-pattern subclasses surfaced; regression tests captured them.] [If 3: AP-8 boundary touched; the bounded iteration discipline prevented runaway.]
    - **D-10 line-367 nuance:** CONTEXT.md described the chunk-to-chunk fix as a single change to per_functional.py:367, but the wrapper context (line 349 is concrete f64 with Array<f64> buffers) meant the actual fix was a DIFFERENT call site — chunk→chunk WITHIN a chunk body, handled via the new `_CHUNK_TO_CHUNK_CALL_RE` regex in `_wrap_f64_literals_v2`. The literal line-367 call stayed `::<f64>` per Rule 9 concrete-caller form. Worth noting for future translator-emit decisions.
    - **D-18 simplification was correct:** the sweep tool's `build_roster` already auto-discovers on-disk carve-outs; no `--include-deferred` flag extension was needed.
    - **D-LOCK-D idempotency:** zero-diff achieved on the first try [if 0 fix-loop iterations] or after [N] iteration commits [otherwise]. The translator's emit pipeline is now a fixed point under `translate --family all`.
    - **G4 tolerance widening:** [N] functionals required overrides; the most common rationale was [Brent / near-zero / structural / long-literal]. [If "long-literal" → flag as a follow-up: tighten the D-06 8-digit threshold or use F::cast_from more aggressively.]

    ## Open Risks / Carry-Forward to Phase 11 (re-open)

    - [Any G3 "other phase11_worst_case offenders" listed in the G3 log]: their f64 numeric correctness is Phase 11's Leg 2 scope.
    - [Any G4 functionals tagged "Long-literal precision loss (DEFECT)"]: these would have been routed through Plan 02's loop in 11.1 if observed; any survivor at Phase 11 re-open should be re-triaged.
    - The `LIBXC_RS_BYPASS_DEFERRED` env-gate is still in use; 11-08 Task 2 (re-opened Phase 11 scope) handles its removal.
    ```

    The bracketed `[...]` markers indicate executor must FILL IN at write time from the actual per-plan SUMMARY contents and gate verdicts. The structure above is the contract.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/libxc_rs && grep -cE "## The 4 ALL_OK Gates|## D-LOCK-D Idempotency Proof|## Plan-Level Rollup|## Phase 11 Hand-Back Per D-01|## What Survived from Phase 11|## New Phase 11.1 Artifacts|## Lessons Learned|## Open Risks" .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md`
    - All 8 required sections present: `grep -cE "^## " .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md` returns ≥ 8
    - 4 gate verdicts present in the gate table: `grep -cE "^\| G[1-4] " .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md` returns 4
    - D-01 hand-back section enumerates 4 explicit items: `grep -cE "^[1-4]\. \*\*11-0|^[1-4]\. \*\*\`phase.complete" .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md` returns 4
    - Phase status line present: `grep -cE "^\*\*Status:\*\* (COMPLETE|HALT-ESCALATED)" .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md` returns 1
    - No unfilled `[...]` markers remain in non-Lessons-Learned sections: `grep -cE "\[N\]|\[If" .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md` returns 0 in the Status / Gate table / Phase Hand-Back sections (Lessons Learned may retain bracketed pattern placeholders if the executor chose to leave them; warn but do not block)
  </acceptance_criteria>
  <done>11.1-SUMMARY.md committed with all 8 required sections, gate verdicts filled, hand-back items enumerated.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md (mark Phase 11.1 entry complete; success criteria checked off) and STATE.md (advance position cursor; note hand-back to Phase 11)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 282-316; the Phase 11.1 entry just inserted at commit 482c288664; success criteria 1-8 listed; plans line "To be scoped via /gsd:discuss-phase 11.1 → /gsd:plan-phase 11.1" — replace with actual 4-plan list)
    - .planning/STATE.md (lines 1-15 frontmatter; line 28 "Current Position"; lines 32-66 the Phase 11 execute-phase run section; the Session Continuity block at end)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md (Task 1 output — source of truth for verdicts to copy into ROADMAP success-criteria checkboxes)
  </read_first>
  <action>
    1. Edit `.planning/ROADMAP.md` § "Phase 11.1: Translator Rule 3 Emit Fix + Sweep-to-Green":

       a) Replace the "Plans: To be scoped via /gsd:discuss-phase 11.1 → /gsd:plan-phase 11.1" line with:
          ```markdown
          **Plans:** 4 plans (all executed)
            - [x] 11.1-01-PLAN.md — Translator amend (D-04..D-10, D-15, D-17): helpers_allowlist.py + cse.py AST classifier + per_functional.py emit fix + Deviation E/F removal
            - [x] 11.1-02-PLAN.md — Full-tree regen + G1 f64 sweep + G2 f32 sweep + D-LOCK-D idempotency proof (D-13, D-16, D-18, D-LOCK-D)
            - [x] 11.1-03-PLAN.md — G3 mgga_c_b94 f64 parity at 1e-12 + G4 full-649 f32 oracle with tolerance overrides (D-02, D-03, D-12, D-20)
            - [x] 11.1-04-PLAN.md — SUMMARY rollup + ROADMAP/STATE update + phase.complete (D-01 hand-back)
          ```

       b) Mark each of Success Criteria 1-8 as `[x] PASS` or `[!] HALT-ESCALATED` (the latter only if Plan 03 G4 hit the 1e-3 ceiling; cross-reference 11.1-SUMMARY.md gate table). Add a one-line evidence-pointer to each (the plan-SUMMARY or specific log).

       c) Append a "Closing note (date)" line: "Phase 11.1 closed COMPLETE on <UTC date>. Deferred Phase 11 closure items hand back per CONTEXT.md D-01; Phase 11 re-opens for: 11-06 Legs 2/3/4 + Task 8, 11-08 Task 2, phase.complete 11 invocation."

    2. Edit `.planning/STATE.md`:

       a) Update frontmatter:
          ```yaml
          status: Phase 11.1 COMPLETE — translator Rule 3 emit fix shipped; Phase 11 ready to re-open for closure items
          stopped_at: Phase 11.1 closed; awaiting /gsd:execute-phase 11 re-open for 11-06 Legs 2/3/4 + Task 8 + 11-08 Task 2 + phase.complete 11
          last_updated: "<ISO 8601 UTC timestamp>"
          last_activity: <UTC date> -- Phase 11.1 closed
          progress:
            total_phases: 12
            completed_phases: 7
            total_plans: 56          # bumped from 52 (added 4 plans for 11.1)
            completed_plans: 48      # bumped from 44 (added 4 completed plans for 11.1)
            percent: 86              # recalculated
          ```

       b) Replace "Current Position" section content with:
          ```markdown
          Phase: 11.1 (translator-rule-3-emit-fix-sweep-to-green) — COMPLETE
          Plan: 11.1-01..04 all executed; 11.1-SUMMARY.md committed
          Previous execution: 11.1-01 ✓; 11.1-02 ✓ (fix-loop iterations: N=<actual>); 11.1-03 ✓ (G3 mgga_c_b94 PASS at 1e-12 f64; G4 PASS at 1e-6 f32 with <count> new tolerance overrides under 1e-3 ceiling); 11.1-04 ✓
          Hand-back to re-opened Phase 11: 11-06 Legs 2/3/4 + Task 8, 11-08 Task 2 (CLAUDE.md / ROADMAP / obsolete-tool cleanup / LIBXC_RS_BYPASS_DEFERRED removal), phase.complete 11.
          ```

       c) Append a new section "## Phase 11.1 execute-phase run — <UTC date>" mirroring the "Phase 11 execute-phase run — 2026-05-18 evening session" structure (status, session arc, deliverables, what hands back). 2 paragraphs max.

       d) Update Session Continuity block at end:
          ```markdown
          Last session: <ISO 8601 UTC timestamp>
          Stopped at: Phase 11.1 closed; Phase 11 ready to re-open
          Resume file: .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md
          Next step: /gsd:execute-phase 11 (re-opens for the deferred items per CONTEXT.md D-01)
          ```

       e) If `.cargo/config.toml` still has the uncommitted `jobs = 10` anomaly noted in STATE.md line 248, RETAIN that anomaly note (user still manages by hand; do NOT touch the file).

    3. Commit ROADMAP + STATE updates atomically:
       ```bash
       git add .planning/ROADMAP.md .planning/STATE.md
       git commit --only -- .planning/ROADMAP.md .planning/STATE.md -m "docs(11.1): mark Phase 11.1 complete in ROADMAP + STATE — hand back closure items to re-opened Phase 11"
       ```
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/libxc_rs && grep -cE "^\- \[x\] 11\.1-0[1-4]-PLAN\.md" .planning/ROADMAP.md && grep -cE "Phase 11\.1.*COMPLETE|11\.1 closed" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\- \[x\] 11\.1-0[1-4]-PLAN\.md" .planning/ROADMAP.md` returns 4 (all 4 plans marked complete in ROADMAP)
    - `grep -cE "11\.1.*(COMPLETE|complete)" .planning/STATE.md` returns ≥ 2 (status + position reflect 11.1 closure)
    - STATE.md frontmatter `completed_plans` advanced by 4 from prior value: `grep -E "^  completed_plans:" .planning/STATE.md` returns a value ≥ 48
    - STATE.md frontmatter `total_plans` advanced by 4: `grep -E "^  total_plans:" .planning/STATE.md` returns a value ≥ 56
    - STATE.md Session Continuity Resume file points to 11.1-SUMMARY.md: `grep -cE "Resume file: .*11\.1-SUMMARY\.md" .planning/STATE.md` returns 1
    - ROADMAP Phase 11.1 success criteria 1-8 each marked: `grep -cE "^  [0-9]+\. \[(x|!)\]" .planning/ROADMAP.md | head -20` returns ≥ 8 (within the Phase 11.1 section's Success Criteria block — sanity-check with explicit section grep if necessary)
    - `.cargo/config.toml` NOT in the commit changeset: `git diff --name-only HEAD~1 HEAD | grep -c "\.cargo/config\.toml"` returns 0
    - Commit message references "11.1" and "Phase 11": `git log --oneline -1 | grep -cE "11\.1.*Phase 11|Phase 11.*11\.1"` returns 1
  </acceptance_criteria>
  <done>ROADMAP Phase 11.1 entry marked complete with all 4 plans + 8 SC ticked; STATE.md frontmatter + Current Position + Session Continuity updated; atomic commit landed without sweeping unrelated files.</done>
</task>

<task type="auto">
  <name>Task 3: Invoke phase.complete 11.1 via gsd-sdk (or equivalent state machinery) — Phase 11.1 closure event</name>
  <files>(state machinery file; varies by GSD harness)</files>
  <read_first>
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md (the closure-event input — must show Status: COMPLETE)
    - Project memory `~/.claude/projects/-home-user-Documents-workspace-libxc-rs/memory/reference_gsd_tooling_path.md` (the gsd-sdk invocation note: "use gsd-tools.cjs <cmd>"; "dotted verbs become spaced args")
  </read_first>
  <action>
    1. Confirm 11.1-SUMMARY.md says COMPLETE (not HALT-ESCALATED):
       ```bash
       grep -E "^\*\*Status:\*\* COMPLETE$" .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-SUMMARY.md || { echo "Status not COMPLETE; cannot invoke phase.complete"; exit 1; }
       ```

    2. Invoke phase.complete via the GSD tooling (per project memory `reference_gsd_tooling_path.md`):
       ```bash
       node .claude/get-shit-done/bin/gsd-tools.cjs phase complete 11.1 2>&1 | tee .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-04-phase-complete.log
       ```
       If the gsd-tools.cjs phase-complete handler does not exist OR errors, fall back to whatever the local equivalent is (sometimes `gsd-sdk query phase.complete 11.1`; the project memory says the dotted form maps to spaced args via gsd-tools.cjs).

       Expected outcome: a state-machinery event is recorded; STATE.md may auto-update (in which case re-run Task 2 acceptance criteria to confirm no regression).

    3. If gsd-tools.cjs phase-complete handler is genuinely absent (the memory note says "doesn't exist — use gsd-tools.cjs"), document that the closure is effective via ROADMAP + STATE update alone (Task 2 already did this); the phase.complete invocation is a no-op in that case. Write a one-line note in 11.1-04-phase-complete.log: "phase.complete handler not exposed via gsd-tools.cjs; Phase 11.1 closure is effective via ROADMAP/STATE update from Task 2."

    4. Commit (if step 2 produced uncommitted changes):
       ```bash
       git status --short
       # If anything changed (likely just the log file):
       git add .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-04-phase-complete.log
       git commit --only -- .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-04-phase-complete.log -m "chore(11.1): phase.complete 11.1 invocation log"
       ```
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/libxc_rs && test -f .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-04-phase-complete.log && echo "phase.complete log exists"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-04-phase-complete.log`
    - Log content is one of: (a) successful gsd-tools.cjs phase-complete invocation output, OR (b) the documented fallback note that closure is ROADMAP/STATE-effective
    - STATE.md remains consistent with Phase 11.1 = COMPLETE (Task 2's acceptance criteria still pass): `grep -cE "11\.1.*COMPLETE" .planning/STATE.md` returns ≥ 1
  </acceptance_criteria>
  <done>phase.complete 11.1 either invoked via gsd-tools.cjs OR documented as ROADMAP/STATE-effective. Phase 11.1 lifecycle closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 11.1 → ROADMAP / STATE | Closure update affects project ledger; over-claim risks losing track of deferred items. |
| 11.1 → Phase 11 hand-back | The D-01 hand-back is the explicit non-closure of Phase 11 from inside 11.1; over-closure swallows the deferred work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T_NEW_5 | Repudiation (closure without evidence) | 11.1-SUMMARY.md gate table | mitigate | Acceptance criteria require 4 gate rows + verdict-from-evidence-file lookup; over-claim is structurally rejected (e.g., if G4 = HALT-CEILING, Status MUST be HALT-ESCALATED not COMPLETE — Task 3 step 1 enforces). |
| T_NEW_6 | Tampering (sweeping unrelated files into the closure commit) | git commit | mitigate | Task 2 step 3 + Task 3 step 4 use `git commit --only -- <paths>` per `feedback_path_scoped_commits.md`; .cargo/config.toml acceptance criterion explicitly excludes it from any 11.1-04 commit (the working tree's `jobs = 10` anomaly stays unstaged). |
| T_NEW_7 | Information disclosure (over-claiming Phase 11 closed) | ROADMAP wording | mitigate | The closing note explicitly states "Phase 11 re-opens for: ..." enumerating the deferred items. Phase 11's own ROADMAP entry status remains PARTIAL (managed by Phase 11, not by 11.1). |
</threat_model>

<verification>
- 11.1-SUMMARY.md has 8 required sections + 4 gate verdicts + D-01 hand-back enumeration
- ROADMAP.md Phase 11.1 marked complete with all 4 plans + 8 SCs ticked
- STATE.md frontmatter + Current Position + Session Continuity updated
- phase.complete invocation logged (either via gsd-tools.cjs or via documented ROADMAP/STATE fallback)
- All commits in Plan 04 use `git commit --only` form (no accidental file sweeping)
- .cargo/config.toml NOT in any Plan 04 commit
</verification>

<success_criteria>
- ROADMAP § 11.1 Success Criterion 8 SATISFIED (Phase 11 phase.complete invocation unblocked — the structural prerequisite is now in place; the actual phase.complete 11 invocation happens in the re-opened Phase 11 per D-01)
- CONTEXT.md D-01 satisfied (Phase 11 closure items explicitly NOT swallowed by 11.1; hand-back documented in SUMMARY)
- ROADMAP.md Phase 11.1 entry shows COMPLETE status with all 4 plans + 8 success criteria ticked
- STATE.md reflects Phase 11.1 closure and points to next step (re-open Phase 11)
- 11.1-SUMMARY.md is the canonical closure document; subsequent Phase 11 re-open reads it as input
</success_criteria>

<output>
Plan 04 IS the phase-close plan; the per-plan SUMMARY for 11.1-04 is the phase-level 11.1-SUMMARY.md itself (Task 1's output). No separate 11.1-04-SUMMARY.md needed — the closing event IS the phase summary.

After completion, the orchestrator's `/gsd:execute-phase 11` re-open is the next step in the project. Per CONTEXT.md D-01, the re-opened Phase 11 picks up:
- 11-06 Task 6 Legs 2/3/4 (full f64/f32 worst-case parity beyond mgga_c_b94 + idempotency)
- 11-06 Task 8 (rewrite 11-06-SUMMARY.md PARTIAL → COMPLETE)
- 11-08 Task 2 (CLAUDE.md / ROADMAP wording / obsolete tool deletion / LIBXC_RS_BYPASS_DEFERRED removal)
- phase.complete 11 invocation
</output>
