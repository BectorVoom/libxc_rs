# Phase 11.1: Translator Rule 3 Emit Fix + Sweep-to-Green - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `11.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-05-19
**Phase:** 11.1-translator-rule-3-emit-fix-sweep-to-green
**Areas discussed:** Scope boundary, P3-preventive taxonomy, Translator native ownership of Rule 9/10 turbofish, Sweep VERDICT: ALL_OK definition

---

## Pre-discussion: ROADMAP entry insertion

Phase 11.1 did not exist in `ROADMAP.md` when `/gsd:discuss-phase 11.1` was invoked. The phase scope is documented in `.planning/phases/11-splitter-v2-unified-5k-cap/11-FINAL-METRICS.md` § "Phase 11.1 Follow-Up Scope (translator fix)" and `11-08-SUMMARY.md`, and the parent phase's `.continue-here.md` records the blocking pattern. Per user disposition, Phase 11.1 was inserted into ROADMAP.md as an INSERTED decimal phase (one-line entry under Phase 11 plus a full "Phase Details" block sourced from the 11-FINAL-METRICS scope) before re-entering this workflow. Committed at `482c288664`.

User selected "Add 11.1 to ROADMAP then discuss" from a 4-option menu; alternatives offered were "Skip discuss → /gsd:plan-phase 11.1 --prd 11-FINAL-METRICS.md", "/gsd:spec-phase 11.1 first", "Discuss inline without ROADMAP entry".

---

## Area selection

User selected ALL 4 surfaced gray areas via multi-select:

| # | Area | Selected |
|---|---|---|
| 1 | Scope boundary (translator+sweep only vs full closure) | ✓ |
| 2 | P3-preventive taxonomy (which f64-literal positions to coerce) | ✓ |
| 3 | Translator native ownership of Rule 9/10 turbofish | ✓ |
| 4 | Sweep VERDICT: ALL_OK definition (gate strictness) | ✓ |

---

## Area 1: Scope boundary

### Q1.1 — Primary deliverable shape

| Option | Selected |
|---|---|
| Translator + sweep ONLY (narrow scope; hand back to Phase 11 for Legs 2/3/4 + Tasks 2/3 + phase.complete) | ✓ |
| 11.1 absorbs full Phase 11 closure | |
| Translator + sweep + cleanup, NOT phase 11 closure (middle ground) | |

**User's choice:** Translator + sweep ONLY. Tight blast radius preferred; deferred 11-06/11-08 items hand back to a re-opened Phase 11.

### Q1.2 — F64 smoke parity gate inclusion

| Option | Selected |
|---|---|
| Yes — include smoke parity on mgga_c_b94 at f64 1e-12 | ✓ |
| No — compile-clean ALL_OK is enough | |
| You decide | |

**User's choice:** Include smoke parity on mgga_c_b94. Rules out "translator fixed compile but broke numerics" regression cheaply.

### Q1.3 — D-24 full-649 f32 oracle sweep inclusion

| Option | Selected |
|---|---|
| No — defer to Phase 12 widening | |
| Yes — run full-649 f32 sweep in 11.1 | ✓ |

**User's choice:** Include the full-649 f32 oracle sweep. Note: This widens 11.1 slightly vs. the "narrow scope" framing in Q1.1, but f32 oracle is interpreted as the strictest empirical test of the translator emit. F32 failures handled per Area 4 (D-12 tolerance overrides, not 11.1-blocking).

---

## Area 2: P3-preventive taxonomy

### Q2.1 — Coverage shape

| Option | Selected |
|---|---|
| Comprehensive — enumerate every position class in one pass | ✓ |
| Minimal — P1 + P2 + observed P3 only | |
| Pattern-driven from regen tree grep | |

**User's choice:** Comprehensive one-pass coverage. AP-8 forbids iterative-extension-as-rescue; the structural answer is to enumerate the position taxonomy comprehensively. Closes P1 (named-const in arithmetic), P2 (bare-literal tuple-return member), P3 (let-binding RHS feeding F), P4 (helper-call arg with bare const), P5 (single-expression body return), P6 (match-arm / conditional / closure body).

### Q2.2 — Named-const emit shape

| Option | Selected |
|---|---|
| Hoist per-fn once: `let pi = F::cast_from(M_PI);` at top of body | ✓ |
| Inline per-use: `F::cast_from(M_PI)` everywhere | |
| Hybrid — hoist if N≥2 uses, inline otherwise | |

**User's choice:** Per-fn hoisted bindings. Matches 11-PATTERN.md Rule 3 example; compile-time win on Brent-method bodies (100+ M_PI uses); cleaner reads.

### Q2.3 — Bare integer-like literal handling

| Option | Selected |
|---|---|
| Wrap as `F::cast_from(1.0)` when in F-typed position | |
| Wrap as `F::new(1.0)` for short-literal integers, `F::cast_from(...)` for everything else | ✓ |
| Always `F::cast_from(...)` for any f64 source in F position | |

**User's choice:** Rule 2 / Rule 3 split exactly as documented in 11-PATTERN.md. Short f32-compatible literals (0.0, 1.0, 2.0, 0.5, etc.) via `F::new(...)`; f64 named consts and precision-sensitive literals via `F::cast_from(...)`.

---

## Area 3: Translator native ownership of Rule 9/10 turbofish

### Q3.1 — Native vs post-emit substitution

| Option | Selected |
|---|---|
| Yes — native emit; retire Deviation E/F substitution | ✓ |
| Keep post-emit substitution; document permanently in PATTERN.md Rule 10 | |
| Hybrid — native for chunk→chunk, post-emit for chunk→math-helper | |

**User's choice:** Native emit; retire Deviation E/F substitution. Idempotency proof simpler (translator output → fixed point); AP-3 wants ALL emit logic in tools/translate_v2/, not post-emit text mutation.

### Q3.2 — Helper genericity lookup mechanism

| Option | Selected |
|---|---|
| Hardcoded allowlist from 11-PATTERN.md Rule 8 exclusion list | ✓ |
| Parse crates/kernels/math/src/*.rs at translator startup | |
| Opt-in marker: helpers add `// xc:generic` doc-comment | |

**User's choice:** Hardcoded allowlist. Explicit, auditable, easy to maintain; reflects the locked Direction A architecture (14 generic helpers from PATTERN.md Rule 8 + the 5 Phase-1 helpers). Refresh policy: manual edit when math/ ships a new generic helper post-11.1.

### Q3.3 — Chunk-to-chunk turbofish form

| Option | Selected |
|---|---|
| `::<F>` — propagate the wrapper's F parameter | ✓ |
| `::<f64>` — keep current behavior, concrete chunks at this layer | |
| No turbofish — let the cube macro infer | |

**User's choice:** `::<F>`. This is what 11-PATTERN.md Rule 9 documents and what makes the generic chain work. Fixes the current `::<f64>` emit at `tools/translate_v2/per_functional.py:367` (inherited from the pre-generic-wrapper era; latent bug surfaced by Deviation F regen).

---

## Area 4: Sweep VERDICT: ALL_OK definition

### Q4.1 — Pass criterion shape

| Option | Selected |
|---|---|
| 266 routed @ f64 compile + 266 routed @ f32 compile + mgga_c_b94 f64 parity 1e-12 + full-649 f32 oracle @ 1e-6 | ✓ |
| Add a 'no compile warnings' rung | |
| Drop the f32 compile sweep — just go straight to f32 oracle | |

**User's choice:** 4 gates in sequence (f64 compile + f32 compile + f64 smoke parity + f32 oracle). Matches Area 1's prior selections. Deferred subcrates (7 D-11) reached via explicit `-p` per D-18 planner's discretion.

### Q4.2 — Iteration discipline

| Option | Selected |
|---|---|
| Bounded N=3 fix cycles; HALT to discuss after 3 | ✓ |
| Strict 1-shot — if first regen+sweep fails, HALT to discuss | |
| Unbounded with monotonic-decrease check | |

**User's choice:** Bounded N=3. Avoids AP-8 unbounded-extension trap; gives slack for unforeseen P-pattern sub-classes; the comprehensive-coverage choice in Area 2 should make 1–2 iterations the realistic case.

### Q4.3 — F32 oracle failure handling

| Option | Selected |
|---|---|
| Inherit existing f32_tolerance_overrides.toml; add Brent-class entries on observed failure | ✓ |
| F32 oracle failures HALT 11.1 — escalate via discuss-phase | |
| Run f32 oracle WITHOUT a numeric gate — collect failure manifest as 11.1 deliverable, defer numeric closure | |

**User's choice:** Inherit f32_tolerance_overrides.toml; add entries on observed failure (with rationale comment); NOT 11.1-blocking unless 1e-3 hard ceiling breached. D-19c-aligned handling.

---

## Wrap-up

### Q5 — Anything still unclear?

| Option | Selected |
|---|---|
| I'm ready for context | ✓ |
| Discuss emit implementation strategy (AST vs regex) | |
| Discuss regen + cleanup mechanism | |
| Explore more gray areas | |

**User's choice:** Ready for context. The AST-vs-regex implementation strategy + full-tree-regen + Deviation E/F cleanup mechanism become Claude's Discretion (planner picks) per CONTEXT.md D-15/D-16/D-17.

---

## Claude's Discretion summary

Deferred to planner per CONTEXT.md `<decisions>` § "Claude's Discretion" (D-15..D-20):

- **D-15** Emit implementation strategy (AST visitor in cse.py vs regex extension vs hybrid) — recommendation leans AST given comprehensive-coverage + Rule 2/3 split + per-fn hoisting requirements
- **D-16** Regen mechanism — recommendation: full clean-slate `python3 tools/maple_to_kernels.py translate --all-families` after each translator change; closing iteration MUST be full-tree per D-LOCK-D
- **D-17** Deviation E/F dead-code removal — delete vs annotate-as-superseded
- **D-18** Sweep tool `--include-deferred` extension — flag vs explicit roster vs separate compile-check
- **D-19** Per-fn hoisted-binding naming scheme — pi/cbrt3/sqrt2/sqrt_eps style vs deterministic snake_case-of-symbol
- **D-20** Sweep ordering within ALL_OK gates — G1→G2→G3→G4 sequencing

## Deferred Ideas

Captured in CONTEXT.md `<deferred>`:
- Phase 11 closure (re-opens after 11.1 lands): 11-06 Legs 2/3/4 + Task 8, 11-08 Task 2 cleanup, `phase.complete 11`
- Phase 12+ widening: 12-entry alphabetical smoke set per FINAL-METRICS recommendation; D-19c tolerance widening; CI gate for D-LOCK-D idempotency check
- Reviewed but not folded: `audit-error-math-placement.md` (Phase 10 prep)
