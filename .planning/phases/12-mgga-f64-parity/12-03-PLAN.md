---
phase: 12-mgga-f64-parity
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - tools/translate_mgga.py
  - tools/translate_v2/emit.py
  - tools/translate_v2/per_functional.py
  - crates/kernels/mgga/mgga_x_th/src/exc_unpol.rs
  - verify-canary/tests/mgga_x_th_parity.rs
autonomous: true
requirements: [SC-1]
user_setup: []

must_haves:
  truths:
    - "The root cause of mgga_x_th's ~20% error is identified (regularization vs translation bug) and documented"
    - "If a translation bug: the fix lives in tools/translate_* and mgga_x_th is regenerated (never hand-edited)"
    - "The mgga_x_th canary passes at 1e-12 vs the libxc C oracle"
    - "The family oracle reports mgga_x_th passing exc at 1e-12"
  artifacts:
    - path: "verify-canary/tests/mgga_x_th_parity.rs"
      provides: "mgga_x_th (id 225) single-kernel canary passing at 1e-12"
    - path: "crates/kernels/mgga/mgga_x_th/src/exc_unpol.rs"
      provides: "regenerated mgga_x_th exc kernel (if a translator fix was needed)"
  key_links:
    - from: "verify-canary/tests/mgga_x_th_parity.rs"
      to: "libxc_kernel_mgga_x_th::exc_unpol::mgga_x_th_exc_unpol"
      via: "direct launch"
      pattern: "mgga_x_th_exc_unpol"
    - from: "tools/translate_mgga.py"
      to: "crates/kernels/mgga/mgga_x_th/src/exc_unpol.rs"
      via: "single-functional regen"
      pattern: "mgga_x_th"
---

<objective>
Drive mgga_x_th (id 225, the worst residual at ~20%) to 1e-12, DECOUPLED from the small-error
cluster. First determine whether the D-01 sigma-down regularization (plan 01) closed it; if not,
the residual is a per-functional translation bug — diff the generated exc_unpol against the
maple2c C source line-by-line, fix the TRANSLATOR (never the generated kernel — AP-3), and regen.

Purpose: D-09 — a 20% energy error is structural, likely NOT the regularization issue. Treat it
as a separate investigation. D-03 — per-functional kernel bugs route through tools/translate_* +
regen, never a hand-edit of crates/kernels/mgga/mgga_x_th/. NOTE (from planning investigation):
the exc_unpol Rust body already matches the C temporaries t3..t37 closely at the top (e.g.
`t30 = pow_1_3::<f64>(1.0 / M_PI)` matches C `t30 = POW_1_3(0.1e1 / M_PI)`), and on the oracle
grid the sigma-down clamp IS active on point i=0 — so D-01 MAY already close mgga_x_th. The first
step is to verify that empirically before assuming a translation bug.

Output: mgga_x_th canary passing 1e-12 (+ a translator fix + regen IF a translation bug is found).
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-mgga-f64-parity/12-CONTEXT.md
@.planning/phases/12-mgga-f64-parity/12-PATTERNS.md
@.planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERN.md

<interfaces>
<!-- Concrete contracts extracted from the codebase 2026-05-25. -->

mgga_x_th exc kernel (crates/kernels/mgga/mgga_x_th/src/exc_unpol.rs:15-49) — the FULL exc body
is short (single tzk0 expression). The Rust closely mirrors the C maple2c source already:
```
C   (maple2c/mgga_exc/mgga_x_th.c func_exc_unpol)   |  Rust (exc_unpol.rs)
t30 = POW_1_3(0.1e1 / M_PI);                          |  t30 = pow_1_3::<f64>(1.0 / M_PI);
t34 = t22*(0.1e1 + 0.7e1/0.216e3*sigma[0]*t23*t19)*t33;  |  t34 = t22 * (1.0 + 7.0/216.0*sigma[ip]*t23*t19) * t33;
t37 = my_piecewise3(t3, 0, -0.27e2/0.16e3*t17*t18*t19*t34); | t37 = piecewise3::<f64>(t3, 0.0, -27.0/160.0*t17*t18*t19*t34);
tzk0 = 0.2e1 * t37;                                   |  tzk0 = 2.0 * t37;
```
IMPORTANT diff candidate: the C coefficient `-0.27e2 / 0.16e3` = -27/160 = -0.16875. The Rust
reads `-27.0 / 160.0` = -0.16875 — CONSISTENT. Re-verify every coefficient in the full body
(there may be a later line beyond :49 if the body continues). The exc body in the kernel ends at
`zk[ip] += tzk0;` — confirm there is no truncated/missing term vs the C `out->zk[...] += tzk0`.

mgga_x_th reads BOTH sigma and tau independently (t19 = 1.0/tau[ip]; t34 uses sigma[ip]*t23*t19)
-> it IS sensitive to the sigma-down clamp. On the oracle grid point i=0 (rho=0.1, sigma=0.01,
tau=0.01) the clamp lowers sigma 0.01 -> 0.008, changing t34 -> a candidate explanation for part
of the residual.

Named constants (crates/kernels/math/src/constants.rs, self-tested to 1e-15):
M_CBRTPI=1.4645918875615232 (:10), M_CBRT2 (:16), M_CBRT3 (:19), M_PI (:4), M_CBRT4. A constant
VALUE bug is unlikely; a constant-WRAP / piecewise / operation-order bug is more likely (11-PATTERN.md Rule 3).

Translator surface (D-03): tools/translate_mgga.py, tools/translate_v2/{cse.py, emit.py,
per_functional.py, helpers_allowlist.py}, tools/maple_to_kernels.py (top-level driver). Single-
functional regen while iterating is allowed (D-04). The full-tree idempotent regen is plan 04.

AP-3: NEVER hand-edit crates/kernels/mgga/mgga_x_th/. Fix the translator, then regen mgga_x_th.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Determine whether D-01 closed mgga_x_th, else root-cause the residual</name>
  <read_first>
    - crates/kernels/mgga/mgga_x_th/src/exc_unpol.rs (FULL file — every temporary t3..tzk0)
    - libxc-master/src/maple2c/mgga_exc/mgga_x_th.c (func_exc_unpol, the translation oracle — diff 1:1)
    - libxc-master/src/mgga_x_th.c (func-level definition + any ext_params / flags)
    - verify-canary/tests/mgga_x_th_parity.rs (the skeleton from plan 02)
    - crates/kernels/math/src/constants.rs (M_CBRTPI, M_CBRT2/3/4, M_PI values)
    - .planning/phases/12-mgga-f64-parity/12-PATTERNS.md § "crates/kernels/mgga/mgga_x_th/src/exc_unpol.rs"
  </read_first>
  <action>
STEP 1 — Empirical check that D-01 didn't already close it. Run the mgga_x_th canary (skeleton
from plan 02, which already applies the D-01 sigma-down regularization):
```
cargo test -p libxc_rs-verify-canary --test mgga_x_th_parity --jobs 1 -- --nocapture
```
If it PASSES at 1e-12 -> D-01 closed mgga_x_th; SKIP to Task 3 (no translator change, no regen).
Record this outcome. (The planning investigation found the exc translation byte-faithful + the
clamp active on the grid, so this is a real possibility — do NOT assume a translation bug exists.)

STEP 2 — If it still FAILS, root-cause via a line-by-line diff of the generated exc_unpol against
`maple2c/mgga_exc/mgga_x_th.c::func_exc_unpol`. Map each Rust temporary `t3, t4, t5, ... tzk0` to
its C counterpart and check for:
  - a constant-WRAP fault (a named const used inline where it should be wrapped, or vice versa — 11-PATTERN.md Rule 3),
  - a coefficient transcription error (e.g. a maple `0.27e2/0.16e3` mis-translated),
  - a piecewise-branch logic difference (piecewise3/piecewise5 argument order or predicate),
  - an operation-order difference (FP-order lock — a reassociated product breaks bit parity),
  - a missing/extra term in the final tzk0 accumulation.
Capture the FIRST divergent temporary (the C value vs the Rust value at a single grid point) to
pinpoint the bug. Use a scratch print or a focused unit check; do NOT commit scratch code.

Document the identified root cause in the SUMMARY (which temporary, which fault class). PER D-07
bounded iteration: cap this at N=3 root-cause -> fix -> verify cycles. If after the 3rd cycle the
residual persists with a NEW unexplained pattern, HALT to `/gsd:discuss-phase 12` and record the
residual + the divergent temporary rather than grinding.
  </action>
  <verify>
    <automated>cargo test -p libxc_rs-verify-canary --test mgga_x_th_parity --jobs 1 -- --nocapture 2>&1 | tee /tmp/12-03-th-baseline.log | grep -qE "verdict=(PASS|FAIL)" && echo "RAN"</automated>
  </verify>
  <acceptance_criteria>
    - The mgga_x_th canary RAN and its verdict (PASS or FAIL) + max_rel_err captured to /tmp/12-03-th-baseline.log
    - IF PASS: the SUMMARY records "D-01 closed mgga_x_th; no translator change required" and Task 2 is a no-op
    - IF FAIL: the SUMMARY records the FIRST divergent temporary (C value vs Rust value at a named grid point) and the fault class (constant-wrap / coefficient / piecewise / op-order / missing-term)
    - No file under `crates/kernels/mgga/mgga_x_th/` was hand-edited in this task (AP-3) — `git status --porcelain crates/kernels/mgga/mgga_x_th/` empty after this task
  </acceptance_criteria>
  <done>The mgga_x_th residual is either confirmed closed by D-01, or root-caused to a specific temporary + fault class with the translator as the fix surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix the translator (if needed) and regenerate mgga_x_th</name>
  <read_first>
    - tools/translate_mgga.py (MGGA family translator)
    - tools/translate_v2/emit.py + tools/translate_v2/per_functional.py (the emit pipeline — the fault surface)
    - tools/maple_to_kernels.py (single-functional regen entry)
    - .planning/phases/11-splitter-v2-unified-5k-cap/11-PATTERN.md (Rule 3 named-const wrap, Rule 9/10 turbofish)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-CONTEXT.md (D-LOCK-D idempotency, AP-3)
  </read_first>
  <action>
SKIP this task entirely if Task 1 found D-01 already closed mgga_x_th (record "no-op — D-01 closed it").

Otherwise, fix the ROOT CAUSE in the translator (NOT the generated kernel — AP-3). Based on the
fault class from Task 1:
  - constant-wrap fault -> fix the named-const / literal wrap rule in tools/translate_v2/emit.py
    (per 11-PATTERN.md Rule 3) so it emits the correct form for mgga_x_th's pattern;
  - coefficient transcription -> if the maple source coefficient is mis-parsed, fix the parse/emit
    in tools/translate_mgga.py or translate_v2/per_functional.py;
  - piecewise / op-order -> fix the corresponding emit path.
The fix MUST be general (driven by the maple AST / emit rule), not a special-case hack for
mgga_x_th. After the fix, regenerate ONLY mgga_x_th (selective single-functional regen is allowed
while iterating — D-04; full-tree idempotent regen is plan 04):
```
python3 tools/maple_to_kernels.py translate --family mgga --functional mgga_x_th
```
(Use the actual single-functional invocation supported by the tool — read tools/maple_to_kernels.py
`--help` / argparse to confirm the exact flag; if only --all-families exists, run the narrowest
supported form and note it.) Re-run the canary after regen. Iterate within the N=3 cap (D-07).

The regenerated crates/kernels/mgga/mgga_x_th/src/exc_unpol.rs is a TRANSLATOR OUTPUT, not a
hand-edit — its diff must be attributable to the translator change (re-running the regen must
reproduce it byte-for-byte; D-LOCK-D).
  </action>
  <verify>
    <automated>cargo test -p libxc_rs-verify-canary --test mgga_x_th_parity --jobs 1 -- --nocapture 2>&1 | grep -q "verdict=PASS" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p libxc_rs-verify-canary --test mgga_x_th_parity --jobs 1` exits 0 and prints `verdict=PASS` (mgga_x_th at 1e-12)
    - IF a translator fix was made: the change lives in `tools/translate_*` (not in `crates/kernels/mgga/mgga_x_th/` as a hand-edit); re-running the single-functional regen reproduces the kernel diff byte-for-byte (D-LOCK-D — verify by running regen twice, second run yields zero git diff)
    - IF Task 1 found D-01 closed it: this task is explicitly a no-op and the SUMMARY says so
    - The translator fix is GENERAL (an emit-rule change), not a mgga_x_th special-case
  </acceptance_criteria>
  <done>mgga_x_th passes 1e-12; any kernel change is a reproducible translator regen output, not a hand-edit (or this task is a documented no-op because D-01 closed it).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Confirm mgga_x_th at the authoritative family-oracle exc tier</name>
  <read_first>
    - verify/tests/mgga_oracle.rs:1-20 (feature cfg, TOL_EXC=1e-12)
    - /tmp/12-02-oracle.log (the family-oracle baseline from plan 02 Task 3, if present)
    - .planning/phases/12-mgga-f64-parity/12-PATTERNS.md § "Authoritative Gate (D-05/D-06)"
  </read_first>
  <action>
Re-run the authoritative family oracle (D-05 — the canary alone is not sufficient; avoids the b94
hollow-gate trap) to confirm mgga_x_th passes exc at 1e-12 in the REAL dispatch path (not just the
single-kernel canary):
```
cargo test -p libxc_rs --features oracle-mgga --test mgga_oracle --jobs 1 -- --nocapture
```
(memory-safe family-chunked build per 11-12; jobs=1). Confirm mgga_x_th now reports exc rel_err
<= 1e-12 AND that the 5 cluster functionals (from plan 02) are still passing AND no
previously-passing MGGA functional regressed. Record the per-functional exc verdict in the SUMMARY.

If the canary passes but the family oracle still fails mgga_x_th, the canary wiring (id/signature/
ext_params) differs from the real dispatch — reconcile the canary to the dispatch path and re-check
(do NOT hand-edit the kernel). Honor the N=3 bound (D-07); HALT to discuss if unresolved.
  </action>
  <verify>
    <automated>cargo test -p libxc_rs --features oracle-mgga --test mgga_oracle --jobs 1 -- --nocapture 2>&1 | tee /tmp/12-03-oracle.log | grep -qiE "test result: ok|mgga_x_th" && echo RAN</automated>
  </verify>
  <acceptance_criteria>
    - The family oracle ran memory-safe (no OOM) via `--features oracle-mgga` and the log is captured
    - The SUMMARY records mgga_x_th's family-oracle exc rel_err <= 1e-12 (PASS)
    - The SUMMARY confirms the 5 cluster functionals still pass and NO previously-passing MGGA functional regressed (D-06)
    - If unresolved within N=3 cycles: a HALT-to-discuss recommendation + residual is recorded (D-07)
  </acceptance_criteria>
  <done>mgga_x_th passes exc at 1e-12 at the authoritative family-oracle tier with no cluster/other-MGGA regression (or a bounded HALT is recorded).</done>
</task>

</tasks>

<threat_model>
Numerical-correctness debug; no network/auth surface. Threat surface is translator-correctness +
regen idempotency + test-gate fidelity.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maple2c C source -> translator -> Rust kernel | the translation pipeline transforms trusted vendored C into Rust; a translation fault is a correctness threat (silent wrong numbers) |
| translator output -> generated kernel file | regenerated code must be reproducible (idempotent), not a hand-edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering (silent miscompute) | translator emit rule | mitigate | Any translator fix is validated against the libxc C oracle at 1e-12 (canary + family oracle). The fix must be a GENERAL emit-rule change (not a mgga_x_th special-case) so it cannot mask the bug for one functional while leaving the rule wrong — the plan-04 full-tree regen + family oracle catches collateral perturbation. |
| T-12-03-02 | Repudiation (non-reproducible kernel) | regenerated mgga_x_th kernel | mitigate | AP-3 forbids hand-edits; the kernel change must be a reproducible translator output (D-LOCK-D: regen twice -> zero diff). AC verifies idempotency. This prevents an un-regeneratable manual patch that the next full regen would silently revert. |
| T-12-03-03 | Spoofing (hollow gate) | single-kernel canary | mitigate | Task 3 re-runs the authoritative family oracle (real dispatch path), not just the canary — the canary id was historically wrong (b94 568 vs 397); the family oracle is the real gate (project_g3_b94_hollow_gate). |
| T-12-03-04 | Tampering (FP-order break) | translator op-order | mitigate | If the fault is op-order, the fix must preserve maple's exact operation order (CLAUDE.md FP-order lock) — verified by 1e-12 bit-level parity, which a reassociated product would fail. |

**Conclusion:** No HIGH-severity security threats. The phase-relevant risks are silent numerical
miscompute and non-reproducible codegen; both mitigated by oracle parity at 1e-12, the general
(not special-case) translator fix, and D-LOCK-D regen idempotency.
</threat_model>

<verification>
- mgga_x_th canary passes 1e-12 (`-p libxc_rs-verify-canary --test mgga_x_th_parity --jobs 1`).
- Any kernel change is a reproducible translator regen (D-LOCK-D), not a hand-edit (AP-3).
- Family oracle confirms mgga_x_th exc 1e-12 with no cluster/other-MGGA regression.
- Root cause documented (D-01-closed, or translator fault class + divergent temporary).
</verification>

<success_criteria>
- mgga_x_th (id 225) passes exc at 1e-12 at both the single-kernel canary and the authoritative
  family-oracle tier (SC-1 — the 6th functional).
- The root cause is documented; any kernel change is a reproducible translator output (not a
  hand-edit), keeping AP-3 + D-LOCK-D intact.
- Bounded iteration (D-07) honored; decoupled from the cluster (D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/12-mgga-f64-parity/12-03-SUMMARY.md`.
Commit path-scoped (include only the files actually changed — if D-01 closed it, only the canary +
SUMMARY): e.g. `git commit --only -- tools/translate_mgga.py tools/translate_v2/emit.py tools/translate_v2/per_functional.py crates/kernels/mgga/mgga_x_th/src/exc_unpol.rs verify-canary/tests/mgga_x_th_parity.rs .planning/phases/12-mgga-f64-parity/12-03-SUMMARY.md`
(NEVER bare `git add -A`; feedback_path_scoped_commits.)
</output>
