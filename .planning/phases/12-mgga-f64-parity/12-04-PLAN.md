---
phase: 12-mgga-f64-parity
plan: 04
type: execute
wave: 4
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - crates/kernels/mgga
  - src/eval/mgga_dispatch/funcs
autonomous: false
requirements: [SC-1, SC-2]
user_setup: []

must_haves:
  truths:
    - "The full-tree regen is byte-idempotent (run twice -> zero git diff) per D-LOCK-D"
    - "Any translator change from plan 03 did not perturb other families (LDA/GGA/other-MGGA kernels unchanged unless intended)"
    - "All 6 target MGGA functionals pass the family oracle exc at 1e-12 (SC-1)"
    - "LDA, GGA, and all other-MGGA functionals show no regression vs the f64 oracle (SC-2)"
    - "mgga_x_2d_js17 is either passing in the gate, or de-routed + documented as inherently 2D (D-13)"
  artifacts:
    - path: "crates/kernels/mgga"
      provides: "regenerated MGGA kernel tree (idempotent full-tree regen output)"
    - path: ".planning/phases/12-mgga-f64-parity/12-04-SUMMARY.md"
      provides: "final regression-gate evidence: per-family oracle verdicts"
  key_links:
    - from: "tools/maple_to_kernels.py --all-families"
      to: "crates/kernels/{lda,gga,mgga}"
      via: "full-tree regen"
      pattern: "translate --all-families"
    - from: "verify/tests/mgga_oracle.rs"
      to: "libxc C oracle"
      via: "family oracle exc comparison at 1e-12"
      pattern: "oracle-mgga"
---

<objective>
Close Phase 12: run the D-04 full-tree idempotent regen (catches any plan-03 translator change
that perturbed other families), then the D-06 final no-regression gate — the family oracle over
ALL routed MGGA (including the previously-passing ones) plus cheap LDA + GGA confirms. Resolve the
D-13 2D escape hatch for mgga_x_2d_js17 (keep in gate if passing; de-route + document if its
residual is inherently 2D-dimensionality-bound). This plan proves BOTH locked success criteria:
SC-1 (all 6 targets pass exc 1e-12) and SC-2 (no LDA/GGA/other-MGGA regression).

Purpose: D-04 — a selective single-functional regen (plan 03) can leave the tree inconsistent or
mask a translator change's collateral effect; the CLOSING regen MUST be full-tree and byte-
idempotent (D-LOCK-D). D-06 — the shared D-01 regularization touched every routed MGGA functional,
so the real no-regression gate is the family oracle over ALL of them, not just the 6 targets.

Output: idempotent regenerated tree + the final per-family oracle verdicts + D-13 resolution.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-mgga-f64-parity/12-CONTEXT.md
@.planning/phases/12-mgga-f64-parity/12-PATTERNS.md
@.planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-CONTEXT.md

<interfaces>
<!-- Concrete contracts extracted from the codebase 2026-05-25. -->

Full-tree regen driver: `python3 tools/maple_to_kernels.py translate --all-families` (confirm the
exact flag via the tool's argparse/--help before running). D-LOCK-D: running it TWICE must produce
zero git diff (byte-idempotent).

Authoritative gate: verify/tests/mgga_oracle.rs (#![cfg(feature = "oracle-mgga")], TOL_EXC=1e-12,
TOL_VXC=1e-10). Memory-safe family-chunked build per 11-12:
  - `cargo test -p libxc_rs --features oracle-mgga --test mgga_oracle --jobs 1`
  - `cargo test -p libxc_rs --features oracle-lda --test lda_oracle --jobs 1`
  - `cargo test -p libxc_rs --features oracle-gga --test gga_oracle --jobs 1`
`cargo tree -p libxc_rs --features oracle-<fam>` resolves only that family's kernels + math (NOT
the all-281 umbrella — proven memory-safe in 11-12). LDA/GGA do NOT route through the MGGA driver,
so the D-01 change should leave them untouched — confirm, don't assume (D-06).

The 6 targets: mgga_x_th(225), mgga_x_2d_js17(609), mgga_c_cs(72), mgga_x_pkzb(213),
mgga_x_pbe_gx(576), mgga_x_tm(540).

D-13 escape hatch: PROJECT.md lists 1D/2D dimensionality as out-of-scope. mgga_x_2d_js17 is a 2D
functional. If after D-01 (+ any plan-03 fix) its residual is closed -> keep it in the gate. If its
residual is inherently a 2D-dimensionality evaluation issue (NOT closed by the shared fix, and the
divergence traces to 2D-specific math) -> de-route it via the per-functional dispatch helper in
`src/eval/mgga_dispatch/funcs/` and document WHY in the SUMMARY (do NOT force a 2D fix into Phase 12).
De-routing means the functional returns UnsupportedFunctional rather than a wrong number.

This plan is autonomous: false — Task 3 is a blocking human-verify checkpoint for the heavy
oracle gate runs (the user runs the per-family oracle gates inline per the RAM-constrained,
jobs=1 workflow).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full-tree regen + D-LOCK-D byte-idempotency proof</name>
  <read_first>
    - tools/maple_to_kernels.py (the --all-families driver — read argparse/--help for exact flags)
    - .planning/phases/11.1-translator-rule-3-emit-fix-sweep-to-green/11.1-CONTEXT.md (D-LOCK-D idempotency mandate)
    - .planning/phases/12-mgga-f64-parity/12-03-SUMMARY.md (whether plan 03 made a translator change)
  </read_first>
  <action>
Run the CLOSING full-tree regen (D-04). This is mandatory regardless of whether plan 03 changed
the translator — it proves the tree is consistent and catches any collateral perturbation:
```
python3 tools/maple_to_kernels.py translate --all-families
```
(use the exact flag the tool supports). Then prove byte-idempotency (D-LOCK-D): run it a SECOND
time and confirm the second run produces ZERO git diff:
```
git status --porcelain crates/kernels/ > /tmp/12-04-regen-diff-1.txt   # after 1st run
python3 tools/maple_to_kernels.py translate --all-families
git status --porcelain crates/kernels/ > /tmp/12-04-regen-diff-2.txt   # after 2nd run
diff /tmp/12-04-regen-diff-1.txt /tmp/12-04-regen-diff-2.txt           # MUST be empty
```
If plan 03 made NO translator change, the regen should leave crates/kernels/ unchanged from the
committed state (the tree was already idempotent). If plan 03 DID change the translator, the
expected diff is confined to mgga_x_th (and any other functional sharing the fixed emit rule —
inspect any unexpected diffs; an unexpected diff in another family is a RED FLAG that the
translator fix was too broad — investigate before proceeding). Do NOT hand-edit any kernel (AP-3).

Record in the SUMMARY: the set of files the regen changed (ideally empty or mgga_x_th-only), and
the idempotency result (2nd run zero-diff).
  </action>
  <verify>
    <automated>diff /tmp/12-04-regen-diff-1.txt /tmp/12-04-regen-diff-2.txt && echo "IDEMPOTENT_OK"</automated>
  </verify>
  <acceptance_criteria>
    - The full-tree regen ran via the tool's `--all-families` (or equivalent) entry
    - The SECOND regen run produces zero additional git diff vs the first (D-LOCK-D byte-idempotency) — `diff /tmp/12-04-regen-diff-1.txt /tmp/12-04-regen-diff-2.txt` is empty
    - The SUMMARY lists exactly which kernel files the regen changed; any change outside mgga_x_th (and intended emit-rule siblings) is investigated and explained
    - No hand-edits to `crates/kernels/` (AP-3) — every change is regen output
  </acceptance_criteria>
  <done>The MGGA/LDA/GGA kernel tree is regenerated full-tree and byte-idempotent; collateral changes (if any) are scoped and explained.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Resolve the D-13 mgga_x_2d_js17 escape hatch</name>
  <read_first>
    - .planning/phases/12-mgga-f64-parity/12-02-SUMMARY.md (mgga_x_2d_js17 canary + oracle result)
    - src/eval/mgga_dispatch/funcs/ (per-functional dispatch helpers — the de-route surface if needed)
    - .planning/PROJECT.md § "Out of Scope" (1D/2D dimensionality)
    - libxc-master/src/maple2c/mgga_exc/mgga_x_2d_js17.c (if a residual investigation is needed)
  </read_first>
  <action>
Decide mgga_x_2d_js17's disposition from the plan-02 evidence:

CASE A — its canary + family oracle PASS at 1e-12 (closed by D-01): KEEP it in the gate. No code
change. Record "mgga_x_2d_js17 closed by D-01; remains routed" in the SUMMARY. (This is the
preferred outcome.)

CASE B — its residual is NOT closed by D-01/D-03 AND tracing the divergence shows it is inherently
a 2D-dimensionality evaluation issue (out-of-scope per PROJECT.md): DE-ROUTE it. In the
per-functional dispatch helper under `src/eval/mgga_dispatch/funcs/`, make mgga_x_2d_js17 return
`LibxcRsError::UnsupportedFunctional { id: 609, reason: "mgga_x_2d_js17 is a 2D functional; 2D
dimensionality is out of scope per PROJECT.md — de-routed pending a dedicated 2D evaluation path" }`
rather than producing a wrong number. Update the mgga_x_2d_js17 canary to assert the de-route
(or mark it #[ignore] with a documented reason) so the test suite reflects the decision. Document
the 2D-specific divergence evidence in the SUMMARY. Do NOT force a 2D-dimensionality fix into
Phase 12 (D-13).

PER D-07: only reach CASE B after the bounded N=3 attempt under the shared fix has failed AND the
residual is specifically traced to 2D math. If it fails for a non-2D reason, that is a HALT-to-
discuss, not an automatic de-route.
  </action>
  <verify>
    <automated>grep -rq "mgga_x_2d_js17" src/eval/mgga_dispatch/funcs/ && echo "ROUTED_OR_DEROUTED_PRESENT" || echo "CHECK_DISPATCH"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY states the disposition explicitly: CASE A (closed by D-01, remains routed) or CASE B (de-routed as inherently 2D, with the 2D-specific divergence evidence)
    - IF CASE B: `src/eval/mgga_dispatch/funcs/` returns `UnsupportedFunctional` for id 609 with the documented 2D reason, and the mgga_x_2d_js17 canary asserts the de-route (or is #[ignore]'d with reason) — the functional never returns a wrong number
    - IF CASE A: no code change; mgga_x_2d_js17 passes the family oracle exc at 1e-12 (verified in Task 3)
    - The decision honored the N=3 bound and the "trace to 2D before de-routing" rule (D-07/D-13)
  </acceptance_criteria>
  <done>mgga_x_2d_js17 is either passing in the gate (CASE A) or cleanly de-routed + documented as inherently 2D (CASE B) — never silently wrong.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final per-family oracle regression gate (SC-1 + SC-2)</name>
  <what-built>
    Plans 01-03 + this plan's regen are complete. The MGGA dispatch now uses libxc's sigma-down
    regularization, 6 permanent canaries exist, mgga_x_th is fixed/closed, and the kernel tree is
    byte-idempotent. This checkpoint runs the AUTHORITATIVE per-family f64 oracle gate that proves
    both locked success criteria — it is a heavy build run, so it is USER-RUN inline (jobs=1,
    memory-safe family-chunked per 11-12).
  </what-built>
  <read_first>
    - .planning/phases/12-mgga-f64-parity/12-PATTERNS.md § "Authoritative Gate (D-05/D-06)"
    - /tmp/12-03-oracle.log (the plan-03 MGGA oracle baseline, if present)
  </read_first>
  <how-to-verify>
    Run each gate inline, jobs=1 (the assistant pre-confirms `cargo tree` resolves family-only;
    the USER runs the actual compiles per feedback_ram_constraints). NEVER edit .cargo/config.toml.

    1. MGGA gate (SC-1 + SC-2 MGGA): all 6 targets pass exc 1e-12, no other-MGGA regression:
       `cargo test -p libxc_rs --features oracle-mgga --test mgga_oracle --jobs 1 -- --nocapture`
       EXPECT: test result ok; all 6 of {mgga_x_th, mgga_x_2d_js17 (unless de-routed CASE B),
       mgga_c_cs, mgga_x_pkzb, mgga_x_pbe_gx, mgga_x_tm} report exc rel_err <= 1e-12; every
       previously-passing MGGA functional still passes.

    2. LDA confirm (SC-2): `cargo test -p libxc_rs --features oracle-lda --test lda_oracle --jobs 1`
       EXPECT: test result ok (unchanged from the Phase-11 baseline — LDA does not route through
       the MGGA driver).

    3. GGA confirm (SC-2): `cargo test -p libxc_rs --features oracle-gga --test gga_oracle --jobs 1`
       EXPECT: test result ok (unchanged — GGA does not route through the MGGA driver).

    If any gate OOMs: it should not (family-chunked); if it does, STOP and report — do NOT switch
    to an all-281 umbrella build. If any target still fails exc at 1e-12 after N=3 bounded cycles
    across the phase, HALT to `/gsd:discuss-phase 12` (D-07) rather than declaring the phase complete.
  </how-to-verify>
  <acceptance_criteria>
    - MGGA oracle: all 6 targets (or 5 + a documented CASE-B de-route for mgga_x_2d_js17) pass exc at 1e-12; the log shows `test result: ok`; no previously-passing MGGA functional regressed
    - LDA oracle: `test result: ok` (no regression)
    - GGA oracle: `test result: ok` (no regression)
    - vxc-unpol did not regress (oracle TOL_VXC=1e-10 still passes) — D-08
    - The SUMMARY pastes the per-family verdict lines as SC-1/SC-2 evidence
  </acceptance_criteria>
  <resume-signal>Type "approved" with the three per-family oracle verdicts (or report the failing functional + rel_err to trigger D-07 HALT-to-discuss).</resume-signal>
</task>

</tasks>

<threat_model>
Numerical-correctness debug closing gate; no network/auth surface. Threat surface is regen
idempotency, regression detection, and gate fidelity (memory-safe build path).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator -> full kernel tree | the closing regen must be reproducible and must not silently perturb unrelated families |
| family oracle -> libxc C | raw f64 inputs cross the FFI boundary; the oracle is the trusted reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering (collateral codegen drift) | full-tree regen | mitigate | The closing regen is full-tree (not selective) so any plan-03 translator change that perturbed another family surfaces as a git diff; an unexpected cross-family diff is a RED FLAG investigated before proceeding (Task 1 AC). D-LOCK-D idempotency (2nd run zero-diff) proves the tree is stable. |
| T-12-04-02 | Spoofing (silent regression) | family oracle scope | mitigate | The no-regression gate (D-06) runs the family oracle over ALL routed MGGA + LDA + GGA — not just the 6 targets — so a regularization change that fixed the 6 but broke a 7th MGGA functional is caught. LDA/GGA confirmed, not assumed. |
| T-12-04-03 | Denial of Service (OOM) | per-family oracle build | mitigate | Each gate uses the `oracle-<fam>` Cargo feature (family-chunked, proven memory-safe in 11-12), jobs=1, USER-RUN inline. `cargo tree` pre-check confirms family-only resolution. The all-281 umbrella build is explicitly forbidden. |
| T-12-04-04 | Tampering (wrong-number on de-route) | mgga_x_2d_js17 D-13 disposition | mitigate | A CASE-B de-route returns `UnsupportedFunctional` (a typed error), NEVER a silently-wrong 2D number — the gate would otherwise pass a functional producing incorrect energies. The de-route is documented with 2D-specific evidence. |

**Conclusion:** No HIGH-severity security threats. The closing risks are codegen drift, missed
regression, and OOM on the gate build; all mitigated by full-tree idempotent regen, an all-family
oracle gate, the memory-safe family-chunked build path, and a typed-error (not wrong-number)
de-route for the D-13 escape hatch.
</threat_model>

<verification>
- Full-tree regen byte-idempotent (D-LOCK-D: 2nd run zero diff); collateral diffs scoped/explained.
- mgga_x_2d_js17 disposition resolved (CASE A passing, or CASE B de-routed + documented).
- MGGA family oracle: all 6 targets (or 5 + documented de-route) pass exc 1e-12; no other-MGGA regression (SC-1 + SC-2).
- LDA + GGA oracle: no regression (SC-2).
- vxc-unpol did not regress (D-08).
</verification>

<success_criteria>
- SC-1: all 6 target MGGA functionals pass the family oracle exc at 1e-12 (or 5 pass + a documented
  CASE-B de-route for mgga_x_2d_js17 per the D-13 out-of-scope escape hatch).
- SC-2: no regression in LDA, GGA, or other-MGGA f64 oracle results.
- The kernel tree is byte-idempotent (D-LOCK-D) and free of hand-edits (AP-3).
- Bounded iteration honored throughout (D-07); HALT-to-discuss if a target is unresolved.
</success_criteria>

<output>
After completion, create `.planning/phases/12-mgga-f64-parity/12-04-SUMMARY.md` with the per-family
oracle verdicts as SC-1/SC-2 evidence.
Commit path-scoped (the regen-changed kernel files + any funcs/ de-route + SUMMARY): e.g.
`git commit --only -- crates/kernels/mgga/mgga_x_th/src/exc_unpol.rs src/eval/mgga_dispatch/funcs .planning/phases/12-mgga-f64-parity/12-04-SUMMARY.md`
(list the ACTUAL regen-changed paths; NEVER bare `git add -A`; feedback_path_scoped_commits.)
</output>
