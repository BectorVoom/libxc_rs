---
quick_id: 260508-q01
slug: update-cubecl-010-workspace
description: Update cubecl version from 0.9.0 to 0.10.0 in all workspace
date: 2026-05-08
status: complete
---

# Quick Task 260508-q01 Summary

## What Was Done

Updated cubecl dependency from 0.9.0 to 0.10.0 across all 172 Cargo.toml files
in the workspace and regenerated Cargo.lock.

## Tasks Completed

### Task 1: Mass-replace cubecl version
- Used `sed -i` to replace `cubecl = { version = "0.9.0"` → `cubecl = { version = "0.10.0"` in all matching Cargo.toml files
- Files updated: 172 total
  - 1 root Cargo.toml
  - 4 top-level crate Cargo.toml files (kernel-math, kernel-lda, kernel-gga, kernel-mgga)
  - ~163 sub-crate Cargo.toml files (kernel-gga-*, kernel-mgga-*)
  - 1 verify/Cargo.toml
- Verified: 0 remaining `cubecl.*0.9.0` references (grep returns empty)

### Task 2: Regenerate Cargo.lock
- Ran `cargo update -p cubecl` which locked all 11 cubecl sub-packages at 0.10.0:
  - cubecl, cubecl-common, cubecl-core, cubecl-cpu, cubecl-ir,
    cubecl-macros, cubecl-macros-internal, cubecl-opt, cubecl-runtime,
    cubecl-std, cubecl-zspace
- Also pulled in new dependencies required by cubecl 0.10.0 (darling, cubecl-zspace, etc.)

## Result

All workspace crates now declare `cubecl = { version = "0.10.0", ... }`.
Cargo.lock is fully consistent with 0.10.0.
