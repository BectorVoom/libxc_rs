---
quick_id: 260509-q07
slug: kernels-parent-dir
status: complete
date: 2026-05-09
---

# Quick Task 260509-q07 — Summary

## Headline

All 17 kernel sub-crates relocated under `crates/kernels/` parent directory:

```
Before                         After
crates/kernel-lda          →   crates/kernels/lda
crates/kernel-lda-1        →   crates/kernels/lda-1
crates/kernel-gga          →   crates/kernels/gga
crates/kernel-gga-1..5     →   crates/kernels/gga-1..5
crates/kernel-mgga         →   crates/kernels/mgga
crates/kernel-mgga-1..8    →   crates/kernels/mgga-1..8
crates/kernel-math         →   crates/kernels/math
```

The `kernel-` prefix in directory names is dropped (parent already says
`kernels/`). Cargo PACKAGE names are preserved unchanged
(`libxc-kernel-lda`, `libxc-kernel-gga-1`, `libxc-kernel-math`, etc.) so
every `use libxc_kernel_*::*;` import in `src/` keeps working without
modification.

## Compilation profile

Unchanged. Each sub-crate is still its own rustc compile unit. No
OOM-mitigation regression from q06's "Mild" reduction package.

## Commits

- `24535856` — `refactor(quick-260509-q07): move kernel-* sub-crates under crates/kernels/`
  - `git mv crates/kernel-X crates/kernels/X` for all 17 dirs (git
    detected as renames: 4076 files / 75 insertions / 75 deletions).
  - Workspace `Cargo.toml`: 17 `[workspace.members]` paths and 17
    `[workspace.dependencies]` paths mass-replaced
    (`crates/kernel-` → `crates/kernels/`).
  - Each sub-crate `Cargo.toml`: sibling path-deps `path = "../kernel-X"`
    → `"../X"`.

- `d4fd678a` — `tools(quick-260509-q07): update splitter tools for crates/kernels/ layout`
  - 7 splitter/repack tools updated:
    - `split_lda_subcrates.py`
    - `resplit_gga.py`
    - `rebatch_mgga.py`
    - `split_oversized_kernel.py`
    - `split_oversized_mgga.py`
    - `split_mgga_7_kcis.py`
    - `batch_translate_mgga.py`
  - Mechanical changes per tool: `CRATES_DIR` now points at
    `crates/kernels/`; `SUBCRATE_RE` regex patterns drop the `kernel-`
    prefix; aggregator path constants (`LDA_AGG`, `GGA_SRC`) point at the
    new locations; generated Cargo.toml templates use the new paths.
  - Smoke-tested the 3 primary tools with `--dry-run`; output matches the
    pre-move bin counts (idempotent at current threshold).

## Verification

| Check | Result |
|-------|--------|
| All 17 sub-crates under `crates/kernels/` | ✅ |
| No `crates/kernel-*` directories left | ✅ |
| Workspace `Cargo.toml` paths resolve | ✅ (manual grep — 0 stale path strings) |
| Sub-crate `Cargo.toml` sibling deps resolve | ✅ |
| Cargo package names unchanged (`libxc-kernel-X`) | ✅ |
| 3 splitter tools dry-run successfully | ✅ |
| `cargo check` | NOT RUN (RAM-constrained per memory; deferred to user) |

## Known follow-ups (NOT in q07)

1. **Doc-comment staleness** — these still mention `crates/kernel-mgga-*`:
   - `src/eval/mgga_dispatch/mod.rs`
   - `src/model/mgga_functional.rs` (multiple lines)
   - `src/eval/gga_dispatch/mod.rs` (less referenced)
   - Various splitter-tool docstrings and print labels
   - Comment-only; no compilation impact.

2. **Inherited from q05/q06**:
   - `src/eval/{gga,mgga}_dispatch/` references `batch1a`/`batch3d`/etc.
     by literal name — these aliases were already stale before q07.
     Regenerate via `tools/generate_gga_dispatch.py` for GGA;
     hand-update for MGGA (no auto-generator).

3. **Non-splitter tools** (translators, generators) MAY have hardcoded
   `crates/kernel-X` paths. Spot-check before invoking. q07 only
   touched the 7 splitter/repack tools the user explicitly named.
