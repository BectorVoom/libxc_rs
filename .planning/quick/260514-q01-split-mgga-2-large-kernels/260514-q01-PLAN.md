---
quick_id: 260514-q01
slug: split-mgga-2-large-kernels
status: complete
created: 2026-05-14
---

# Quick Task 260514-q01: split mgga-2 and near large kernel

## Goal

Reduce the largest generated MGGA kernel files by re-emitting targeted MGGA functionals with the existing translator at `--split-threshold 5000`, preserving CubeCL split-helper fanout rules from `docs/manual/Cubecl/cubecl_macro_fanout_manual.md`.

## Tasks

1. Re-emit all functionals currently housed in `crates/kernels/mgga-2/src/` from `libxc-master/src/maple2c/mgga_exc/*.c` into their existing module directories with `tools/translate_mgga.py --split-threshold 5000`, lowering four remaining edge cases to `--split-threshold 4500` to stay under the 5K file cap.

2. Re-emit the nearest top remaining MGGA large-kernel offenders identified by `tools/audit_kernel_size.py`, limited to mechanical translator regeneration and without changing dispatch or crate membership.

3. Verify with a before/after kernel-size audit and `cargo check -p libxc-kernel-mgga-2`, saving all Cargo output under `log/` before analysis.

## Notes

- This quick task does not attempt the full Phase 11 subcrate collapse.
- Generated split parts should remain plain `#[cube]` helpers unless they are true entry kernels, matching the routing-aware translator behavior already in `tools/translate_mgga.py`.
