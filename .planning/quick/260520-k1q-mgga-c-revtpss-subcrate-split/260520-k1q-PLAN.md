---
quick_id: 260520-k1q
slug: mgga-c-revtpss-subcrate-split
type: quick
created: 2026-05-20
mode: inline-sequential
predecessor: 260520-eem
autonomous: false
files_modified:
  - .planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/run_regen_revtpss.py   # NEW — adapted regen driver (func+c_file differ from tpssloc)
  - crates/kernels/mgga/mgga_c_revtpss/                 # facade: regen (hier) + lxc_pol/mod.rs re-source + Cargo.toml shard deps
  - crates/kernels/mgga/mgga_c_revtpss_*/               # NEW — shard sub-crates (generated by the splitter)
  - Cargo.toml                                          # + root [dependencies] path-deps for shards (revtpss STAYS in default-members)
  - .planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/   # logs + SUMMARY

must_haves:
  truths:
    - "A facade crate named libxc-kernel-mgga_c_revtpss still exists at crates/kernels/mgga/mgga_c_revtpss with that exact package name (D-10 public-interface invariant preserved)."
    - "The 113 lxc_pol parts are physically distributed across N shard sub-crates (N>1); no single shard holds the whole lxc_pol part set."
    - "The facade's lxc_pol/mod.rs keeps its #[cube] pub fn mgga_c_revtpss_lxc_pol(...) body with all 113 call statements unchanged in order; only the import sources change from partN::fn to <shard_crate>::fn."
    - "The largest shard sub-crate compiles under jobs=1 without OOM (peak RSS materially under the 30 GB box)."
    - "The facade crate itself compiles under jobs=1 without OOM — its 9 remaining outputs (incl. kxc_pol's 20 parts) fit, and it LINKS against shard expand fns rather than re-expanding lxc_pol part bodies."
    - "revtpss REMAINS in workspace default-members; bare `cargo build` still includes it (the recipe keeps it compiling)."
    - "Re-running the splitter on the same regen output produces a byte-identical crate layout (D-LOCK-D determinism)."
  artifacts:
    - path: ".planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/run_regen_revtpss.py"
      provides: "Regen driver for revtpss — reuses translate_mgga.emit_per_functional (NOT a new entry point); only func name + c_file path differ from the tpssloc run_regen.py."
      contains: "mgga_c_revtpss"
    - path: "crates/kernels/mgga/mgga_c_revtpss/src/lxc_pol/mod.rs"
      provides: "Facade lxc_pol output wrapper with shard-sourced use imports and the unchanged 113-statement #[cube] body."
      contains: "pub fn mgga_c_revtpss_lxc_pol"
    - path: "crates/kernels/mgga/mgga_c_revtpss/Cargo.toml"
      provides: "Facade manifest with a [dependencies] entry per shard sub-crate (plus existing cubecl + libxc-kernel-math)."
      contains: "libxc-kernel-mgga_c_revtpss"
    - path: "Cargo.toml"
      provides: "Root workspace manifest with one [dependencies] path-dep per shard sub-crate (makes them workspace members); shards intentionally absent from default-members; revtpss itself stays in default-members."
      contains: "mgga_c_revtpss_p"
  key_links:
    - from: "crates/kernels/mgga/mgga_c_revtpss/src/lxc_pol/mod.rs"
      to: "shard sub-crate part fns"
      via: "use libxc_kernel_mgga_c_revtpss_pK::mgga_c_revtpss_lxc_pol_partN_<outputs>; then the existing call in the #[cube] body"
      pattern: "use libxc_kernel_mgga_c_revtpss_p"
    - from: "crates/kernels/mgga/mgga_c_revtpss_pK/src/lib.rs"
      to: "the moved partN module(s)"
      via: "pub mod lxc_pol; pub use lxc_pol::partN::<fn>; re-export at a stable crate-root path"
      pattern: "pub use"
    - from: "Cargo.toml [dependencies]"
      to: "each shard crate dir"
      via: "path-dep entry — makes the shard a workspace member without adding it to default-members"
      pattern: "path = .crates/kernels/mgga/mgga_c_revtpss_p"
---

<objective>
Apply the PROVEN mgga_c_tpssloc OOM-fix recipe (hierarchical CSE + facade/`_pK`
shard sub-crate split) to **mgga_c_revtpss** so it keeps compiling under jobs=1 on
the 30 GB box without the per-`#[cube]-fn` proc-macro OOM in its 11 flat-oversized
lxc_pol parts.

This is a **DIRECT REPLAY of quick task 260520-eem** (which fixed tpssloc). The
hard design work is done and proven: the splitter tool ALREADY EXISTS
(`tools/split_per_functional_subcrate.py`, committed 769912377c), the hierarchical
CSE infra exists (260520-c91), and the recipe is validated end-to-end. This plan is
deliberately SHORT — it leans on the eem precedent rather than re-deriving anything.

Cross-references (the recipe; do NOT re-document it here):
- @.planning/quick/260520-eem-mgga-c-tpssloc-subcrate-split/260520-eem-SUMMARY.md
  — proven recipe, per-build RSS table, KEY FINDING (RSS scales with PART COUNT not
  file count; budget ≲70 parts/shard).
- @.planning/quick/260520-eem-mgga-c-tpssloc-subcrate-split/260520-eem-PLAN.md
  — the template. This plan mirrors its Task 2 (regen+split+sanity) and Task 3
  (compile sweep). It SKIPS eem Task 1 (build the splitter) — the tool exists.
- @.planning/quick/260520-c91-mgga-c-tpssloc-hierarchical-sub-wrapper/260520-c91-SUMMARY.md
  — the hierarchical CSE layer (env vars LIBXC_RS_HIERARCHICAL_CSE +
  LIBXC_RS_ACCEPT_OVERSIZED_WRAPPER).

Purpose: keep revtpss compiling under jobs=1 and bound the per-fn OOM risk in its 11
dense lxc_pol parts (worst = part22 @ 12,649 lines — bigger than tpssloc's 9698L
worst case). Numeric correctness / oracle parity is OUT OF SCOPE (G3/G4 covers it).

Output:
- An adapted regen driver `run_regen_revtpss.py` (reuses the existing translator).
- A regenerated-then-sharded `mgga_c_revtpss` facade + N `_pK` shard sub-crates
  (lxc_pol ONLY; kxc_pol stays in the facade — see Task 2 contingency).
- An empirical per-shard budget validated by a canary compile + a facade compile.

LOCKED design decisions (from the eem precedent + orchestrator; do NOT revisit):
- Hierarchical layout (`LIBXC_RS_HIERARCHICAL_CSE=1`) lives INSIDE the shard sub-crates.
- A facade re-export crate keeps the public name `libxc-kernel-mgga_c_revtpss`.
- Shard lxc_pol ONLY. revtpss STAYS in default-members. Reuse the existing splitter
  + the existing translator (via run_regen_revtpss.py) — author NOTHING new beyond
  the 1-line-different regen driver.
</objective>

<execution_context>
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/libxc_rs/.claude/get-shit-done/templates/summary.md

RAM-constrained machine, jobs=1 (memory `feedback_ram_constraints`):
- Run inline-sequential. NO worktree isolation.
- Do NOT edit `.cargo/config.toml` (user manages jobs by hand).
- Each `cargo build -p` is minutes-long. Single-pass compile sweep; NO retry loop.
- Largest shard = canary, compiled FIRST.

Path-scoped commits (memory `feedback_path_scoped_commits`): the working tree has
many pre-staged unrelated files (deleted .planning/spikes/, .cargo/config.toml,
.cache/, BUILD_ERROR_*.md, untracked partN/ dirs). EVERY commit MUST use
`git commit -m "msg" -- <explicit paths>` (or `git commit --only -- <paths>`).
NEVER `git add -A` / `git add .`. NEW untracked files (run_regen_revtpss.py, shard
dirs, logs, SUMMARY) must be `git add`ed by EXPLICIT path FIRST, then committed with
`-- <those same paths>` — the eem executor hit this exact ordering gotcha.

Reuse-only constraints:
- Reuse `tools/split_per_functional_subcrate.py` AS-IS. Do NOT rewrite it. A single
  `--selftest` sanity run is the only "test" needed (it is already proven on tpssloc).
- Reuse `translate_mgga.emit_per_functional` via `run_regen_revtpss.py`. Do NOT add a
  new translate entry point.
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260520-eem-mgga-c-tpssloc-subcrate-split/260520-eem-SUMMARY.md
@.planning/quick/260520-eem-mgga-c-tpssloc-subcrate-split/260520-eem-PLAN.md
@.planning/quick/260520-c91-mgga-c-tpssloc-hierarchical-sub-wrapper/260520-c91-SUMMARY.md
@.planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/run_regen_revtpss.py
@tools/split_per_functional_subcrate.py

<verified_structural_facts>
Established by orchestrator exploration of the CURRENT (HEAD) revtpss tree — build on
these, do NOT re-litigate:

1. STRUCTURE MIRRORS tpssloc. revtpss has the same per-functional crate layout
   (`crates/kernels/mgga/mgga_c_revtpss/`, package `libxc-kernel-mgga_c_revtpss`,
   edition 2024, deps cubecl 0.10.0 cpu + `libxc-kernel-math = { path = "../../math" }`).
   10 output modules: exc/vxc/fxc/kxc/lxc × {unpol, pol}.

2. lxc_pol IS THE OOM DRIVER: 113 parts (113 `mod partN;`, 113 `use partN::` lines,
   113 call statements in the #[cube] body), 17,080 `.rs` files at HEAD. 11 dense flat
   parts are in `tools/kernel_size_exceptions.txt` lines 71-81: part20-24, 28-33, up
   to **12,649 lines** (part22). These flat single-`#[cube] fn` bodies have the per-fn
   proc-macro OOM (12,649L > tpssloc's 9698L worst case → larger per-fn RAM estimate).
   The hier CSE regen (Task 2 Step A) bounds this by chunking those parts into
   meta/chunk subtrees, exactly as it did for tpssloc.

3. SECONDARY OUTPUT kxc_pol: 20 parts, 2,871 `.rs` files at HEAD. NO kxc_pol entries
   in kernel_size_exceptions.txt → its parts are NOT flat-oversized (no per-fn OOM;
   small or already CSE-chunked). 20 parts is well under the ≲70-parts/shard RSS
   budget. PLAN: shard lxc_pol ONLY; leave kxc_pol in the facade. The facade compile
   (kxc_pol's ~20 parts + the small exc/vxc/fxc + unpol outputs) is expected to fit
   (~5-9 GB by the eem part-count finding; eem's facade was 9.0 GB with similar
   residual outputs). See Task 2 FALLBACK if the facade OOMs.

4. revtpss IS currently in workspace default-members (Cargo.toml line 500) AND has a
   root path-dep (line 211), UNLIKE tpssloc (excluded). The recipe keeps revtpss
   compiling, so it STAYS in default-members and KEEPS its path-dep. The facade is the
   default-member; its shard deps build transitively. Do NOT add shards to
   default-members (matches the splitter's existing behavior + the eem outcome). Do
   NOT remove revtpss from default-members.

5. SPLITTER IS GENERIC + PROVEN. CLI (confirmed):
   `python3 tools/split_per_functional_subcrate.py <family> <func> <output> --budget <N> [--dry-run] [--selftest]`.
   It scans an output's part units (file OR dir), bin-packs CONTIGUOUS ranges into
   `<func>_pK` shards under a file budget (a dense single part exceeding budget → solo
   shard), MOVES units, generates shard Cargo.toml+lib.rs, rewrites the facade
   output-wrapper imports (keeping the #[cube] body VERBATIM), patches the facade +
   root Cargo.toml. Idempotent + deterministic (D-LOCK-D) + selftested.

6. BUDGET GUIDANCE from eem: budget 10000 files/shard gave tpssloc 7 shards, worst RSS
   16.5 GB (the 68-part shard p6), facade 9.0 GB — all well under 30 GB. revtpss
   lxc_pol is SMALLER at HEAD (17,080 files vs tpssloc's ~30,795; after hier regen it
   roughly doubles, ~34K vs tpssloc's 63,360) and has 113 parts vs tpssloc's 122. So
   budget 10000 should give ~4-5 shards. Use budget 10000 unless the dry-run suggests
   otherwise. ALSO respect the part-count ceiling (≲70 parts/shard — eem's 68-part
   shard = 16.5 GB, still safe). With 113 parts, a budget-only pack could put >70 small
   tail parts in one shard; if the dry-run shows a shard with substantially >70 parts,
   FLAG IT and lower the budget so the tail shard splits.

7. C SOURCE present: `libxc-master/src/maple2c/mgga_exc/mgga_c_revtpss.c` (6.3 MB).
   `run_regen_revtpss.py` (already authored in this quick dir) points at it.
</verified_structural_facts>

<interfaces>
Facade lxc_pol/mod.rs CURRENT (HEAD) shape — the splitter's rewrite target:

```rust
//! MGGA_C_REVTPSS lxc pol kernel — lxc_pol (nested-by-output, 113 parts).
#![allow(unused_imports, unused_variables, non_snake_case, clippy::excessive_precision, clippy::too_many_arguments, clippy::needless_return)]

mod part0;
// ... mod part1 .. mod part112;

use part0::mgga_c_revtpss_lxc_pol_part0_zk_vrho_vsigma_vlapl_vtau;
use part1::mgga_c_revtpss_lxc_pol_part1_v2rho2;
// ... 113 `use partN::<fn>;` lines total

#[allow(unused_variables, non_snake_case)]
#[cube]
pub fn mgga_c_revtpss_lxc_pol(rho: &Array<f64>, /* ... */ dens_threshold: f64, zeta_threshold: f64) {
    mgga_c_revtpss_lxc_pol_part0_...(rho, sigma, lapl, tau, /* outputs */, ..., dens_threshold, zeta_threshold);
    // ... 113 call statements total, in order
}
```

After the split (proven on tpssloc), the facade mod.rs has ZERO `mod partN;` lines;
each `use part{i}::{fn};` becomes `use libxc_kernel_mgga_c_revtpss_pK::{fn};` (the
symbol name `mgga_c_revtpss_lxc_pol_partN_...` is unchanged — so a grep for that
substring still matches both the shard-sourced `use` lines AND the call statements);
the #[cube] header + all 113 call statements + the math/cubecl `use` lines are
VERBATIM. Parts are concrete-f64 (`rho: &Array<f64>`), NOT generic `<F: Float>`, so
the facade calls need NO turbofish (proven on tpssloc; PATTERN.md Rules 9/10 apply
only to the generic meta-fns INSIDE each part, unchanged by the split).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sanity-run the splitter selftest, regen revtpss (hier ON), split lxc_pol, sanity-check layout — CHECKPOINT after</name>
  <files>tools/split_per_functional_subcrate.py, crates/kernels/mgga/mgga_c_revtpss/, crates/kernels/mgga/mgga_c_revtpss_*/, Cargo.toml, .planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/run_regen_revtpss.py</files>
  <action>
This task mirrors eem Task 2 (the splitter ALREADY exists, so no eem Task 1 needed).

Step 0 — TOOL SANITY (cheap, no compile). Confirm the existing splitter still passes:
`python3 tools/split_per_functional_subcrate.py --selftest`  → must exit 0.
This is the ONLY validation of the tool — it is proven on tpssloc; do NOT re-audit it.

Step A — REGEN. Reuse the adapted driver (already written to this quick dir):
`LIBXC_RS_HIERARCHICAL_CSE=1 LIBXC_RS_ACCEPT_OVERSIZED_WRAPPER=1 python3 .planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/run_regen_revtpss.py`
(run_regen_revtpss.py asserts ACCEPT_OVERSIZED; HIERARCHICAL_CSE triggers the hier
branch landed at fde9608e00. It regenerates mgga_c_revtpss into the real crates/kernels
path. EXPECT: the 11 problem parts (20-24, 28-33) get meta dirs; largest single `.rs`
shrinks well under 5K (tpssloc's hit 4487L); lxc_pol file count roughly doubles from
17,080 to ~34K. The 10 output modules — incl. the unchanged kxc_pol — are re-emitted.)
Capture stdout+stderr to `260520-k1q-regen.log` in this quick dir.

Step B — PICK THE BUDGET. Default = the value approved at the checkpoint, else 10000
files/shard (the eem-validated value). Justification: revtpss lxc_pol post-regen
(~34K files, 113 parts) is SMALLER than tpssloc's (63,360 files, 122 parts) which
sharded into 7 at budget 10000 with worst RSS 16.5 GB. So budget 10000 should give
~4-5 shards, comfortably under 30 GB. Watch the part-count ceiling (≲70/shard) — see
Step D check 8.

Step C — SPLIT lxc_pol ONLY:
`python3 tools/split_per_functional_subcrate.py mgga mgga_c_revtpss lxc_pol --budget <N>`
Capture stdout+stderr to `260520-k1q-split.log`. (Optionally precede with a `--dry-run`
of the same command to preview the shard plan into the log.)

Step D — SANITY-CHECK the layout (NO compile yet — that is Task 2). Assert ALL of:
1. N shard dirs exist: `ls -d crates/kernels/mgga/mgga_c_revtpss_p*/`. N>1.
2. Each shard has Cargo.toml + src/lib.rs + src/lxc_pol/mod.rs.
3. Files-per-shard <= budget (recursive `*.rs` count per shard dir). RECORD the LARGEST
   shard's file count AND its part count (the Task 2 canary).
4. The facade `crates/kernels/mgga/mgga_c_revtpss/src/lxc_pol/mod.rs` has ZERO
   `mod partN;` lines: `grep -c '^mod part' .../lxc_pol/mod.rs` == 0.
5. The facade now references shard idents: it has `use libxc_kernel_mgga_c_revtpss_p*`
   lines (one per part, 113 total across all shards).
6. The facade #[cube] body still has 113 `mgga_c_revtpss_lxc_pol_partN_` CALL statements
   (indented calls). Note: a bare `grep -c 'mgga_c_revtpss_lxc_pol_part'` on the
   post-split facade returns ~226 (113 shard-sourced `use` lines + 113 calls), because
   the symbol name is unchanged; the call-only count (indented `^\s*mgga_c_revtpss_lxc_pol_part`)
   must be 113.
7. The facade Cargo.toml has a [dependencies] line per shard; root Cargo.toml has a
   path-dep per shard near line ~211 (revtpss's existing path-dep region); NO shard
   appears in `default-members`; revtpss ITSELF is STILL in default-members + still has
   its own path-dep (the splitter must not have touched those).
8. PART-COUNT CEILING: report each shard's part count. If any shard holds substantially
   MORE than 70 parts (eem's 68-part shard was 16.5 GB — the safe ceiling), FLAG it in
   the log and re-run the split with a LOWER budget so the tail shard splits, THEN
   re-run checks 1-7. (Do NOT compile a layout with a >70-part shard.)
9. Determinism: re-run the splitter once → it no-ops (idempotency guard) or reproduces
   byte-identical (D-LOCK-D). `git status --short` shows no NEW churn on the second run.

If sanity-check FAILS (facade still has `mod partN;`, a part unit duplicated, imports
don't reference shards, or a shard exceeds budget): STOP — do NOT proceed to Task 2.
Document the failure in 260520-k1q-split.log + the SUMMARY. Per `feedback_ram_constraints`,
do NOT burn a compile cycle on a known-bad layout.

Then COMMIT the sharded layout + regen artifacts (path-scoped — `git add` each NEW path
EXPLICITLY first, then commit with `-- <those paths>`; enumerate each shard dir; do NOT
`git add -A`):
`git add .planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/run_regen_revtpss.py crates/kernels/mgga/mgga_c_revtpss crates/kernels/mgga/mgga_c_revtpss_p0 <...each shard...> Cargo.toml .planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/260520-k1q-PLAN.md 260520-k1q-regen.log 260520-k1q-split.log`
(adjust the log paths to be absolute under the quick dir), then:
`git commit -m "feat(mgga_c_revtpss): regen hier + shard lxc_pol into sub-crates (260520-k1q)" -- .planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split crates/kernels/mgga/mgga_c_revtpss crates/kernels/mgga/mgga_c_revtpss_p0 <...each shard...> Cargo.toml`

This task ends at the CHECKPOINT below — Task 2's RAM-heavy compile sweep does not run
until the user approves the layout.
  </action>
  <verify>
    <automated>python3 tools/split_per_functional_subcrate.py --selftest && test "$(grep -c '^mod part' crates/kernels/mgga/mgga_c_revtpss/src/lxc_pol/mod.rs)" = "0" && ls -d crates/kernels/mgga/mgga_c_revtpss_p*/ >/dev/null 2>&1 && test "$(grep -cE '^[[:space:]]*mgga_c_revtpss_lxc_pol_part[0-9]+' crates/kernels/mgga/mgga_c_revtpss/src/lxc_pol/mod.rs)" -ge 113 && grep -q 'use libxc_kernel_mgga_c_revtpss_p' crates/kernels/mgga/mgga_c_revtpss/src/lxc_pol/mod.rs && grep -q 'crates/kernels/mgga/mgga_c_revtpss"' Cargo.toml && echo SANITY_OK</automated>
  </verify>
  <done>
Splitter --selftest exits 0. Regen produced the hier layout (11 problem parts get meta
dirs, largest single `.rs` < 5K). Splitter sharded lxc_pol into N>1 contiguous-range
`_pK` shards; all 9 sanity checks pass; largest shard's file count + part count recorded
(Task 2 canary). Facade keeps name `libxc-kernel-mgga_c_revtpss`, its #[cube] body
verbatim (113 calls), zero `mod partN;`, shard-sourced `use` lines. revtpss STILL in
default-members; shards NOT in default-members. Splitter idempotent (D-LOCK-D). NO
compile attempted. Layout + regen artifacts committed path-scoped.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
revtpss was regenerated with hierarchical CSE ON (bounding the per-fn macro RAM in its
11 dense lxc_pol parts, worst part22 @ 12,649L) and its lxc_pol 113 parts were sharded
into N `_pK` sub-crates behind a thin facade — using the EXISTING, proven splitter
(`tools/split_per_functional_subcrate.py`). The facade keeps the public name
`libxc-kernel-mgga_c_revtpss` and its #[cube] body verbatim. kxc_pol (20 parts, not
flat-oversized) was deliberately LEFT in the facade. Sanity checks pass; revtpss stays
in default-members; shards are NOT in default-members. NO compile has run yet.
  </what-built>
  <how-to-verify>
Before the RAM-heavy Task 2 compile sweep, review the layout:
1. Read `crates/kernels/mgga/mgga_c_revtpss/src/lxc_pol/mod.rs` — confirm 0 `mod partN;`
   lines, shard-sourced `use libxc_kernel_mgga_c_revtpss_p*` imports, and the unchanged
   113-statement #[cube] body. Facade Cargo.toml + root Cargo.toml have one shard dep
   each; revtpss itself is still a default-member.
2. Review `260520-k1q-split.log` for the shard plan: N shards, per-shard file counts AND
   part counts. Confirm the largest shard is materially under budget and (per the eem
   KEY FINDING) holds ≲70 parts. The largest shard is the Task 2 canary.
3. Sanity-check N looks reasonable (eem precedent: tpssloc's larger lxc_pol → 7 shards
   at budget 10000; revtpss is smaller → expect ~4-5).
Approve to proceed to the compile sweep (canary shard → remaining shards → facade), or
request a budget change / re-shard.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with a budget override, e.g. "approved, budget 8000") or describe changes.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Compile sweep — largest shard (canary) first, then remaining shards, then facade (kxc_pol-in-facade is the watched risk)</name>
  <files>crates/kernels/mgga/mgga_c_revtpss/, crates/kernels/mgga/mgga_c_revtpss_*/</files>
  <action>
Mirrors eem Task 3. Single-pass compile budget (jobs=1, minutes each, NO retry loop).
Run each build under `/usr/bin/time -v` and capture peak RSS to `260520-k1q-compile.log`.

Step 1 — CANARY: compile the LARGEST shard first (most files / most parts, recorded in
Task 1). It is the worst case; if it fits, the other shards fit.
`/usr/bin/time -v cargo build -p libxc-kernel-mgga_c_revtpss_<largest_shard>`
Disposition:
- PASS (peak RSS materially under 30 GB): per-shard budget validated → Step 2.
- OOM (exit 137): the per-shard budget is STILL too high. STOP the sweep. Do NOT retry
  in-session. Document the failing shard's file+part count + the budget in the SUMMARY
  and recommend a SMALLER budget for a follow-up re-shard (re-run the splitter with
  `--budget <smaller>` next session — the regen is already committed, so re-sharding is
  cheap). Per the eem KEY FINDING, prefer a budget that caps part count ≲70/shard.

Step 2 — REMAINING SHARDS: compile each remaining shard, in order, under jobs=1.
`for s in <remaining shard pkgs>; do /usr/bin/time -v cargo build -p $s; done`
Any OOM here (unexpected, since the canary was the largest) → STOP, document, same
re-shard recommendation.

Step 3 — FACADE: compile the facade LAST.
`/usr/bin/time -v cargo build -p libxc-kernel-mgga_c_revtpss`
This is the WATCHED RISK for revtpss specifically: the facade still owns kxc_pol's 20
parts + the 8 other small outputs IN-CRATE, plus it type-checks 113 cross-crate #[cube]
lxc_pol calls (which LINK against shard expand fns — cheap, proven on tpssloc; eem's
facade was 9.0 GB). kxc_pol is NOT flat-oversized (no size-exception entries) and 20
parts is well under the ≲70 ceiling, so the facade is EXPECTED to fit (~5-9 GB).
Disposition:
- PASS: the whole approach is validated. revtpss compiles under jobs=1 via facade +
  shards, and (since it stays in default-members) bare `cargo build` still includes it.
- OOM — kxc_pol-in-facade is the bottleneck (the CONTINGENCY from the orchestrator):
  the splitter is GENERIC, so run it a SECOND time on kxc_pol —
  `python3 tools/split_per_functional_subcrate.py mgga mgga_c_revtpss kxc_pol --budget <N>`
  — then re-run Step 3 ONCE. If you are out of compile budget for the session, instead
  STOP and document the kxc_pol-split contingency as the recommended next step (do NOT
  loop). Either way, record peak RSS + the decision in the SUMMARY. This is the
  load-bearing finding if it occurs.

Do NOT run the verify/ crate or any oracle tests (out of scope; would re-trigger the
verify OOM per memory `feedback_verify_crate_oom`). Compile-only.

Post-compile, IF all PASS: note (do NOT necessarily execute — separate decision per the
constraints) that the 11 revtpss lxc_pol entries in `tools/kernel_size_exceptions.txt`
(lines 71-81) could be revisited ONLY if a size audit passes post-split. Leave it as a
follow-up. revtpss is already in default-members, so (unlike tpssloc) there is no
default-members re-add follow-up.

Then COMMIT the logs (path-scoped):
`git add .planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/260520-k1q-compile.log`
`git commit -m "docs(260520-k1q): compile sweep logs + disposition" -- .planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split`
(If a kxc_pol contingency split ran, also enumerate the new kxc shard dirs + the touched
facade/root Cargo.toml in the add+commit, path-scoped.)
  </action>
  <verify>
    <automated>cargo build -p libxc-kernel-mgga_c_revtpss 2>&1 | tail -5 | grep -qiE 'Finished|Compiling libxc-kernel-mgga_c_revtpss' && echo FACADE_BUILD_OK</automated>
  </verify>
  <done>
Compile sweep ran single-pass: largest shard (canary) → remaining shards → facade. Peak
RSS per build captured in 260520-k1q-compile.log. Disposition documented per outcome. If
all PASS: `cargo build -p libxc-kernel-mgga_c_revtpss` succeeds under jobs=1 and revtpss
stays a working default-member — the recipe replay is validated. If a shard OOMs: the
failing budget + file/part count are recorded with a smaller-budget re-shard
recommendation (no in-session retry). If the FACADE OOMs: the kxc_pol-split contingency
was either executed (one re-try) or recorded as the next step. Logs committed path-scoped.
  </done>
</task>

</tasks>

<verification>
Overall checks (compile-only; correctness is out of scope, G3/G4 covers it):
- `python3 tools/split_per_functional_subcrate.py --selftest` exits 0 (Task 1 Step 0).
- Facade `lxc_pol/mod.rs` has zero `mod partN;` lines, 113 part-call statements, and
  shard-sourced `use libxc_kernel_mgga_c_revtpss_p*` imports (Task 1 sanity).
- N>1 `_pK` shard sub-crates exist; each <= budget files and ≲70 parts; largest recorded
  as canary (Task 1).
- Splitter is idempotent (second run byte-identical / no-op) — D-LOCK-D (Task 1).
- revtpss STILL in default-members + still has its own root path-dep; shards NOT in
  default-members (Task 1).
- Largest shard compiles under jobs=1 without OOM (Task 2 canary).
- Facade compiles under jobs=1 without OOM — `cargo build -p libxc-kernel-mgga_c_revtpss`
  succeeds (Task 2 final; kxc_pol-in-facade is the watched risk + has a documented
  one-shot contingency).
</verification>

<success_criteria>
- `cargo build -p libxc-kernel-mgga_c_revtpss` succeeds under jobs=1 on the 30 GB box
  via a facade + N `_pK` shard sub-crates (PRIMARY).
- The facade crate retains the exact package name `libxc-kernel-mgga_c_revtpss` (D-10
  public-interface invariant) and its #[cube] lxc_pol body verbatim (113 calls).
- lxc_pol's 113 parts are distributed across N>1 shard sub-crates, each compiling in its
  own rustc process under budget + ≲70 parts.
- revtpss REMAINS in workspace default-members (bare `cargo build` still includes it);
  shards are workspace members via root path-deps but absent from default-members.
- The existing splitter + the existing translator were REUSED (only run_regen_revtpss.py
  is new — a 1-line-different invocation, not a new entry point).
- All commits are path-scoped (no sweep of pre-staged unrelated working-tree files).

OR, if a shard OOMs: a documented disposition with observed file/part counts, the
failing budget, and a concrete smaller-budget re-shard recommendation — NO in-session
retry loop.

OR, if the FACADE OOMs (kxc_pol-in-facade): the kxc_pol-split contingency was executed
once (`splitter ... kxc_pol`) and the facade re-compiled, OR recorded as the documented
next step if out of compile budget — NO loop.
</success_criteria>

<output>
After completion, create
`.planning/quick/260520-k1q-mgga-c-revtpss-subcrate-split/260520-k1q-SUMMARY.md`
recording: the chosen budget + justification, the shard count + files/parts-per-shard,
the canary + facade compile dispositions (with peak RSS), whether
`cargo build -p libxc-kernel-mgga_c_revtpss` now succeeds, whether the kxc_pol
contingency was needed, and the follow-up state (kernel_size_exceptions.txt lines 71-81
revisit candidate; revtpss is already a default-member so no re-add follow-up). Confirm
the recipe replay outcome vs the eem precedent. Update STATE.md Quick Tasks Completed
table + Session Continuity.
</output>
