//! Dispatch for `lda_c_pk09` over the rayon kernels.
//!
//! GENERATED by tools/translate_rayon/gen_eval.py -- do not hand-edit.
//! Parameter defaults are libxc's `ext_params` values, extracted by
//! tools/translate_rayon/extract_params.py.

use libxc_core::error::LibxcRsError;
use libxc_core::input::LdaInput;
use libxc_core::model::{DerivativeOrder, Spin, Thresholds};
use libxc_core::output::LdaOutput;

use libxc_rkernel_lda_c_pk09 as k;

/// libxc's raw integer id for this functional.
pub const ID: u16 = 554;




/// The derivative orders libxc implements for this functional (its
/// `XC_FLAGS_HAVE_*`). libxc ships a maple2c function for exactly these, so
/// this kernel tree has fewer than the usual ten modules; any other order is
/// refused, as libxc refuses it.
pub const CLAIMED: &[DerivativeOrder] = &[DerivativeOrder::Exc, DerivativeOrder::Vxc, DerivativeOrder::Fxc, DerivativeOrder::Kxc];

/// `XC_FLAGS_HAVE_EXC`. Clear for a potential-only functional (`XC_NO_EXC`):
/// its kernels take no `zk`, so `zk` is neither demanded nor written.
pub const HAVE_EXC: bool = true;

/// `crate::ten_arm_dispatch_rlda` for a tree with only the [`CLAIMED`]
/// modules. Same invocation; the slot list is for the reader, and each arm
/// names its kernel directly.
macro_rules! partial_dispatch {
    (
        $input:expr, $output:expr, $order:expr, $spin:expr, $thresholds:expr,
        needs_tau = $needs_tau:expr, zero_tau = $zero_tau:expr,
        $( [ $($slot:tt)* ], )*
        params = ( $( $scalar:expr ),* $(,)? )
    ) => {{
        use crate::sweep_lda::{min_chunk, par_sweep};
        use libxc_core::dims::Dimensions;

        // Refused before `prepare` takes the caller's buffers.
        if !CLAIMED.contains(&$order) {
            return Err(LibxcRsError::UnsupportedDerivativeOrder {
                id: libxc_core::model::FunctionalId(ID),
                order: $order,
                max: CLAIMED[CLAIMED.len() - 1],
            });
        }
        let d = Dimensions::lda($spin);
        let dt = $thresholds.density;
        let zt = $thresholds.zeta;
        // libxc screens and clamps every point before the maple2c body sees
        // it (`work_lda_inc.c`), potential-only functionals included.
        let sc = crate::screen::Screen::new($thresholds, $needs_tau, $zero_tau);
        let chunk = crate::lda::prepare_for($input, $output, $order, &d, HAVE_EXC)?;

        match ($order, $spin) {
            (DerivativeOrder::Exc, Spin::Unpolarized) => par_sweep(chunk, &d, min_chunk(), &sc, &|c: &mut crate::sweep_lda::LdaChunk<'_, '_>| {
                k::exc_unpol::lda_c_pk09_exc_unpol(
                        c.rho,
                        c.zk.as_deref_mut().expect("prepare guarantees this buffer"),
                        $( $scalar, )*
                        dt, zt,
                    );
            }),
            (DerivativeOrder::Vxc, Spin::Unpolarized) => par_sweep(chunk, &d, min_chunk(), &sc, &|c: &mut crate::sweep_lda::LdaChunk<'_, '_>| {
                k::vxc_unpol::lda_c_pk09_vxc_unpol(
                        c.rho,
                        c.zk.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.vrho.as_deref_mut().expect("prepare guarantees this buffer"),
                        $( $scalar, )*
                        dt, zt,
                    );
            }),
            (DerivativeOrder::Fxc, Spin::Unpolarized) => par_sweep(chunk, &d, min_chunk(), &sc, &|c: &mut crate::sweep_lda::LdaChunk<'_, '_>| {
                k::fxc_unpol::lda_c_pk09_fxc_unpol(
                        c.rho,
                        c.zk.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.vrho.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.v2rho2.as_deref_mut().expect("prepare guarantees this buffer"),
                        $( $scalar, )*
                        dt, zt,
                    );
            }),
            (DerivativeOrder::Kxc, Spin::Unpolarized) => par_sweep(chunk, &d, min_chunk(), &sc, &|c: &mut crate::sweep_lda::LdaChunk<'_, '_>| {
                k::kxc_unpol::lda_c_pk09_kxc_unpol(
                        c.rho,
                        c.zk.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.vrho.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.v2rho2.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.v3rho3.as_deref_mut().expect("prepare guarantees this buffer"),
                        $( $scalar, )*
                        dt, zt,
                    );
            }),
            (DerivativeOrder::Exc, Spin::Polarized) => par_sweep(chunk, &d, min_chunk(), &sc, &|c: &mut crate::sweep_lda::LdaChunk<'_, '_>| {
                k::exc_pol::lda_c_pk09_exc_pol(
                        c.rho,
                        c.zk.as_deref_mut().expect("prepare guarantees this buffer"),
                        $( $scalar, )*
                        dt, zt,
                    );
            }),
            (DerivativeOrder::Vxc, Spin::Polarized) => par_sweep(chunk, &d, min_chunk(), &sc, &|c: &mut crate::sweep_lda::LdaChunk<'_, '_>| {
                k::vxc_pol::lda_c_pk09_vxc_pol(
                        c.rho,
                        c.zk.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.vrho.as_deref_mut().expect("prepare guarantees this buffer"),
                        $( $scalar, )*
                        dt, zt,
                    );
            }),
            (DerivativeOrder::Fxc, Spin::Polarized) => par_sweep(chunk, &d, min_chunk(), &sc, &|c: &mut crate::sweep_lda::LdaChunk<'_, '_>| {
                k::fxc_pol::lda_c_pk09_fxc_pol(
                        c.rho,
                        c.zk.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.vrho.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.v2rho2.as_deref_mut().expect("prepare guarantees this buffer"),
                        $( $scalar, )*
                        dt, zt,
                    );
            }),
            (DerivativeOrder::Kxc, Spin::Polarized) => par_sweep(chunk, &d, min_chunk(), &sc, &|c: &mut crate::sweep_lda::LdaChunk<'_, '_>| {
                k::kxc_pol::lda_c_pk09_kxc_pol(
                        c.rho,
                        c.zk.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.vrho.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.v2rho2.as_deref_mut().expect("prepare guarantees this buffer"),
                        c.v3rho3.as_deref_mut().expect("prepare guarantees this buffer"),
                        $( $scalar, )*
                        dt, zt,
                    );
            }),
            _ => unreachable!("CLAIMED is exactly the set of arms above"),
        }
        Ok(())
    }};
}


/// Number of libxc `ext_params` this dispatch accepts at runtime: none,
/// because this functional has no ext_params.
pub const N_EXT_PARAMS: usize = 0;

/// Number of kernel arguments (compiled-in constants).
pub const N_PARAMS: usize = 0;

/// Compiled-in libxc defaults, in kernel argument order.
pub const DEFAULTS: [f64; 0] = [];

/// The kernel's parameters for a caller-supplied `ext_params` array: always
/// [`DEFAULTS`] here, and a non-empty `ext` is rejected rather than guessed
/// at (this functional has no ext_params). The fused composite dispatch (`crate::fused`) uses this to
/// hand an auxiliary's constants to a kernel that evaluates several
/// auxiliaries in one loop.
pub fn kernel_params(ext: Option<&[f64]>) -> Result<[f64; N_PARAMS], LibxcRsError> {
    if let Some(e) = ext
        && !e.is_empty()
    {
        return Err(LibxcRsError::ExtParamCountMismatch {
            id: libxc_core::model::FunctionalId(ID),
            expected: 0,
            actual: e.len(),
        });
    }
    Ok(DEFAULTS)
}

/// Same as [`dispatch`], with an optional caller-supplied `ext_params` array
/// in libxc's own order.
///
/// This functional does not accept runtime ext_params (this functional has no ext_params), so a non-empty
/// `ext` is rejected rather than guessed at.
pub fn dispatch_with(
    input: &LdaInput<'_>,
    output: &mut LdaOutput<'_>,
    order: DerivativeOrder,
    spin: Spin,
    thresholds: &Thresholds,
    ext: Option<&[f64]>,
) -> Result<(), LibxcRsError> {
    kernel_params(ext)?;
    dispatch(input, output, order, spin, thresholds)
}

pub fn dispatch(
    input: &LdaInput<'_>,
    output: &mut LdaOutput<'_>,
    order: DerivativeOrder,
    spin: Spin,
    thresholds: &Thresholds,
) -> Result<(), LibxcRsError> {
    partial_dispatch!(
        input, output, order, spin, thresholds,
        needs_tau = false, zero_tau = false,
        [k::exc_unpol::lda_c_pk09_exc_unpol],
        [k::vxc_unpol::lda_c_pk09_vxc_unpol],
        [k::fxc_unpol::lda_c_pk09_fxc_unpol],
        [k::kxc_unpol::lda_c_pk09_kxc_unpol],
        [k::exc_pol::lda_c_pk09_exc_pol],
        [k::vxc_pol::lda_c_pk09_vxc_pol],
        [k::fxc_pol::lda_c_pk09_fxc_pol],
        [k::kxc_pol::lda_c_pk09_kxc_pol],
        params = ()
    )
}
