diff --git a/.devcontainer/Dockerfile b/.devcontainer/Dockerfile
index 2a0a2488..2cca69ad 100644
--- a/.devcontainer/Dockerfile
+++ b/.devcontainer/Dockerfile
@@ -85,8 +85,16 @@ RUN set -eux; \
     fi; \
     fi; \
     \
+    # sudo グループ追加 + パスワード不要 sudo
+    usermod -aG sudo "${USERNAME}"; \
+    echo "${USERNAME} ALL=(ALL) NOPASSWD:ALL" > "/etc/sudoers.d/${USERNAME}"; \
+    chmod 0440 "/etc/sudoers.d/${USERNAME}"; \
+    \
     install -d -m 0755 -o "${USER_UID}" -g "${USER_GID}" \
     /workspace \
+    /workspace/src \
+    /workspace/verify \
+    /workspace/verify/tests \
     "/home/${USERNAME}/.cargo" \
     "/home/${USERNAME}/.cargo/registry" \
     "/home/${USERNAME}/.cargo/git" \
@@ -97,10 +105,11 @@ RUN set -eux; \
     "/home/${USERNAME}/.local/bin" \
     "/home/${USERNAME}/.bun" \
     "/home/${USERNAME}/.bun/install" \
-    "/home/${USERNAME}/.bun/install/cache" \
-    /etc/sudoers.d; \
-    echo "${USERNAME} ALL=(ALL) NOPASSWD:ALL" > "/etc/sudoers.d/${USERNAME}"; \
-    chmod 0440 "/etc/sudoers.d/${USERNAME}"
+    "/home/${USERNAME}/.bun/install/cache"; \
+    \
+    chown -R "${USER_UID}:${USER_GID}" \
+    /workspace \
+    "/home/${USERNAME}"
 
 ENV TZ=Asia/Tokyo \
     LANG=C.UTF-8 \
diff --git a/Cargo.toml b/Cargo.toml
index c69d0c09..0761e8ab 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -130,7 +130,6 @@ libxc-kernel-mgga-37b = { path = "crates/kernel-mgga-37b" }
 members = [
     "xtask",
     "verify",
-    "libxc-sys",
     "crates/kernel-math",
     "crates/kernel-lda",
     "crates/kernel-gga",
diff --git a/libxc-sys/Cargo.toml b/libxc-sys/Cargo.toml
deleted file mode 100644
index 1f2efbe7..00000000
--- a/libxc-sys/Cargo.toml
+++ /dev/null
@@ -1,11 +0,0 @@
-[package]
-name = "libxc-sys"
-version = "0.1.0"
-edition = "2024"
-publish = false
-
-[dependencies]
-
-[build-dependencies]
-bindgen = "0.72.1"
-cmake = "0.1.58"
diff --git a/libxc-sys/build.rs b/libxc-sys/build.rs
deleted file mode 100644
index d7d168c5..00000000
--- a/libxc-sys/build.rs
+++ /dev/null
@@ -1,40 +0,0 @@
-use std::env;
-use std::path::PathBuf;
-
-fn main() {
-    // Build vendored libxc 7.0.0 via cmake
-    let dst = cmake::Config::new("../libxc-master")
-        .define("BUILD_SHARED_LIBS", "OFF")
-        .define("BUILD_TESTING", "OFF")
-        .define("ENABLE_FORTRAN", "OFF")
-        .define("ENABLE_PYTHON", "OFF")
-        .define("DISABLE_VXC", "OFF")
-        .define("DISABLE_FXC", "OFF")
-        .define("DISABLE_KXC", "OFF")
-        .define("DISABLE_LXC", "OFF")
-        .define("CMAKE_POLICY_VERSION_MINIMUM", "3.5")
-        .build();
-
-    // Link against the built static library
-    println!("cargo:rustc-link-search=native={}/lib", dst.display());
-    println!("cargo:rustc-link-lib=static=xc");
-
-    // Also check lib64 (some cmake builds put it there)
-    println!("cargo:rustc-link-search=native={}/lib64", dst.display());
-
-    // Generate Rust FFI bindings from xc.h
-    let header_path = dst.join("include").join("xc.h");
-    let bindings = bindgen::Builder::default()
-        .header(header_path.to_string_lossy())
-        .allowlist_function("xc_.*")
-        .allowlist_type("xc_.*")
-        .allowlist_var("XC_.*")
-        .derive_default(true)
-        .generate()
-        .expect("Failed to generate bindings for libxc");
-
-    let out_path = PathBuf::from(env::var("OUT_DIR").unwrap());
-    bindings
-        .write_to_file(out_path.join("libxc_bindings.rs"))
-        .expect("Failed to write bindings");
-}
diff --git a/libxc-sys/src/lib.rs b/libxc-sys/src/lib.rs
deleted file mode 100644
index ad1c6efb..00000000
--- a/libxc-sys/src/lib.rs
+++ /dev/null
@@ -1,11 +0,0 @@
-//! Raw FFI bindings to C libxc 7.0.0, generated by bindgen at build time.
-//! Build-tier only: main `libxc_rs` crate MUST NOT depend on this.
-#![allow(
-    non_upper_case_globals,
-    non_camel_case_types,
-    non_snake_case,
-    dead_code,
-    clippy::all
-)]
-
-include!(concat!(env!("OUT_DIR"), "/libxc_bindings.rs"));
