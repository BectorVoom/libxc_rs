[package]
name = "libxc_rs-verify"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
libxc-sys = { path = "../libxc-sys" }
libxc_rs = { path = ".." }

[dev-dependencies]
approx = "0.5.1"
cubecl = { version = "0.10.0", default-features = false, features = ["cpu"] }
# The `libxc-kernel-{lda,mgga}` family-crate dev-deps were removed by Phase 11
# plan 11-03: the D-10a restructure deleted those family crates, so the dead
# path-deps left the whole workspace unloadable. Plan 11-04 (D-05) re-adds the
# narrowed per-functional `libxc-kernel-<func>` dev-deps that parity_phase11.rs
# actually exercises — the structural fix for the verify/ OOM.

